---
id: doc-3
title: Backlog campaign tracker — Codex review follow-ups (round 2)
type: other
created_date: '2026-07-21 22:27'
updated_date: '2026-07-23 03:28'
---
Round 2 of the Codex-review follow-up campaign (see [[Backlog campaign tracker]] / doc-1 for
round 1, LORE-69..95, which closed all 20 high-severity findings from doc-2). Re-initialised on
2026-07-22 for the **wave-parallel** `/backlog-handover` driver (Opus orchestrator + parallel
Sonnet workers + mandatory Fable review), replacing the original one-issue-per-session cursor
model this doc was first written under.

Protocol: restore → compute the ready/conflict graph → mark the wave Dispatched → dispatch
(parallel Sonnet implement + Fable review) → serialize the merge → update this tracker once more
at settlement → loop until the queue is empty or blocked → write handover.

## Source

All 78 original queue items are LORE-96..173, created 2026-07-21 from a re-audit of doc-2 ("Codex
second-opinion review — lore codebase (2026-07-20)"). The re-audit (25 parallel agents, one per
review cluster) re-verified every one of doc-2's 201 confirmed findings against the live source
tree and the resolved task list as of 2026-07-21. Result: all 20 high-severity findings were
already resolved by round 1; 78 medium-severity findings were still open. Those 78 are this
round's queue (plus follow-ups filed mid-campaign, e.g. LORE-174 from the wave-1 integration
review). The 91 still-open low-severity findings were deliberately left out of scope (user chose
"medium only" on 2026-07-21, re-affirmed "re-arm the 78 medium" on 2026-07-22) — they are not
tracked as tasks yet; revisit via a fresh `/backlog-handover init` pass over doc-2's low-severity
section if/when this round completes.

## Scope / order confirmation

- **Scope**: medium-severity only (LORE-96..173, 78 tasks, plus mid-campaign follow-ups).
  Confirmed by the user 2026-07-21 ("medium only") and re-affirmed 2026-07-22 ("re-arm the 78
  medium"). The 91 low-severity findings and the 4 deferred v2/library tasks (LORE-42..45) are
  out of scope — see "Not queued".
- **Order**: the queue below is grouped by review cluster (alphabetical), matching doc-2's own
  structure for context-locality (same-file fixes stay adjacent). This is an implementation
  default, **not** a strict user-mandated sequence — under the wave-parallel driver it is only a
  tie-break priority for wave inclusion, never a guarantee any item lands in any specific wave.
  Reorder freely (via `backlog doc update`) if a different priority makes sense. Do not re-ask.
- **Formal dependencies**: none. All original 78 task files were YAML-parsed on 2026-07-22 —
  every `dependencies` list is empty. Readiness is therefore gated **only** by the file-conflict
  graph (recomputed live each wave), never by a topological dep order.

## Frontier

The "ready now" set is **always recomputed live** from `backlog/tasks/*.md` + this table at the
start of every restore/wave — never trust a persisted "next wave" plan; it can go stale the
moment a conflict changes. Informational hint only: as of 2026-07-22 after **waves 1-12 merged**, **71 tasks are Done** — wave 1 (LORE-96, 98, 102, 107, 110, 114), wave 2 (LORE-97, 99, 103, 108, 111, 115), wave 3 (LORE-109, 112, 116, 122, 126), wave 4 (LORE-176 + the previously-held LORE-100 — docker-e2e CI gate green-at-birth), wave 5 (LORE-101, 104, 113, 117, 121, 123), wave 6 (LORE-105, 118, 124, 128, 132, 133), wave 7 (LORE-106, 175, 174, 177, 119, 125), wave 8 (LORE-120, 129, 134, 135, 139, 141), wave 9 (LORE-130, 136, 142, 145, 148, 154), wave 10 (LORE-137, 143, 146, 149, 155, 158), wave 11 (LORE-138, 144, 147, 150, 151, 156) + the wave-11 integration follow-up LORE-190, wave 12 (LORE-140, 152, 157, 159, 162, 164), plus 3 resolved-by-merge duplicates (LORE-127, 131 by LORE-107; LORE-173 by LORE-115). **0 held / 0 in-flight.** **28 tasks remain To Do**: the original medium queue minus the above, plus mid-campaign follow-ups still open (LORE-178, 179, LORE-180 [medium] / 181 / 182 filed from wave 6, LORE-183 [medium] filed from wave 7, LORE-184 [medium] / 185 [low] filed from the wave-8 integration review, and LORE-186 [medium] / 187 [low] filed from the wave-9 integration review, and LORE-188 [low] / 189 [low] filed from the wave-10 integration review, and LORE-191 [low] / 192 [medium] filed from the wave-11 integration review — LORE-190 [medium] from that same review was fixed same-session, and LORE-193 [medium] / 194 [low] filed from the wave-12 integration review). Every To Do item has **zero unmet formal deps** (re-verified live 2026-07-22 via YAML parse each restore: the only 4 dep-bearing non-terminal tasks are the deferred LORE-42..45 in "Not queued"). Readiness is gated purely by the live pairwise file-conflict graph (same-cluster items serialize; note the new follow-ups' conflict hints in the Queue table); wave membership is bounded by the 6-worker cap. Recompute the ready set live at next restore — do NOT hardcode a next-wave list.

**Cross-cluster duplicate (RESOLVED):** LORE-107 (cli-entry-state) merged in wave 1,
fixing the `lore <command> --help` bug in src/cli.ts + test/help.test.ts and adding a `lore <cmd>
--help` == `lore help <cmd>` regression assertion. LORE-127 (cmd-meta-c, "`lore <command> --help`
shows top-level help") and LORE-131 (cmd-meta-c, "Add regression test asserting `lore <command>
--help` matches `lore help <command>`") describe the SAME bug/test — they are very likely now
the SAME bug/test — **both verified resolved-by-merge by LORE-107 and marked Done 2026-07-22** (help.test.ts:271
asserts `lore query --help` byte-identical to `lore help query`). No further action.

**Second cross-cluster duplicate (RESOLVED):** LORE-115 (cmd-crud-a, dispatched wave 2) and LORE-173
(errors-output-git, still To Do) both target `renderTaskSummaryRows` in `src/output.ts` (control-char
/ line normalization of Backlog id/status/title). They must never share a wave; once LORE-115 merges,
**LORE-173 verified resolved-by-merge by LORE-115 and marked Done 2026-07-22** — output.test.ts covers the newline/control-char row. No further action.

## Queue (confirmed order)

| # | Issue | Cluster | Formal deps | Status | Wave | Note |
|---|---|---|---|---|---|---|
| 1 | LORE-96 | adapter-backlog | — | Done | 1 | Validate/escape argv values passed to backlog CLI to prevent flag injection |
| 2 | LORE-97 | adapter-backlog | — | Done | 2 | createTask discards the new task id when the 'Created task <ID>' line fails to parse |
| 3 | LORE-98 | build-ci-config | — | Done | 1 | Pin third-party GitHub Actions to commit SHAs instead of mutable tags |
| 4 | LORE-99 | build-ci-config | — | Done | 2 | verify-versions job doesn't check os/cpu fields or binary filenames; only linux-x64 build is executed |
| 5 | LORE-100 | build-ci-config | LORE-176 | Done | 3→4 | MERGED wave 4 (PR #110, dev @ `995a02b`) after LORE-176 unblocked it. Rebased onto dev+LORE-176 (clean; only a task-file metadata conflict, resolved to Done). docker-e2e CI gate is green-at-birth: harness ran 299 passed / 0 failed on the exact merged bytes; actionlint clean. |
| 6 | LORE-101 | build-ci-config | — | Done | 5 | MERGED wave 5 (PR #111, dev @ `60c1b2d`). publishConfig.access:public on 6 manifests + release-publishing.md note. |
| 7 | LORE-102 | build-runtime | — | Done | 1 | Harden e2e Dockerfile: digest-pin base image, avoid root curl\|bash, pin mkdocs |
| 8 | LORE-103 | build-runtime | — | Done | 2 | Surface report-write failures and fixed-UID bind-mount permission risk in e2e run |
| 9 | LORE-104 | build-runtime | — | Done | 5 | MERGED wave 5 (PR #112, dev @ `c98471a`). `--exit-code-from e2e` added to both documented docker-compose usage invocations (comment-only). |
| 10 | LORE-105 | build-runtime | — | Done | 6 | MERGED wave 6 (PR #117, dev @ `c2dfd8c`). `jq -nc` compact JSONL in record()/check(). |
| 11 | LORE-106 | build-runtime | — | Done | 7 | MERGED wave 7 (PR #123, dev @ `1de9ee7`). Two write-no-golden guards in record-backlog-goldens.ts: commit-pin check vs Dockerfile BACKLOG_COMMIT + task-view specimen-shape check, both before any write; main() gated behind import.meta.main for unit-testability. Fable approve, 0 fix. See Resolved 35. |
| 12 | LORE-107 | cli-entry-state | — | Done | 1 | lore <command> --help shows generic help instead of the command's own help |
| 13 | LORE-108 | cli-entry-state | — | Done | 2 | readConfigText maps EACCES/EPERM config read failures to 'validation' not 'denied' |
| 14 | LORE-109 | cli-entry-state | — | Done | 3 | commitBacklogFiles discards LoreError.hint (real git/hook stderr) on commit failure |
| 15 | LORE-110 | cmd-check | — | Done | 1 | Cap probeLiveness's total URL count and wall-clock time, not just per-URL concurrency |
| 16 | LORE-111 | cmd-check | — | Done | 2 | Bound resolveTaskDetails's per-task adapter.viewTask fan-out with a concurrency limit |
| 17 | LORE-112 | cmd-check | — | Done | 3 | check's JSON report doesn't mark itself incomplete when reconciliation errors mid-run |
| 18 | LORE-113 | cmd-check | — | Done | 5 | MERGED wave 5 (PR #113, dev @ `6e8bfc2`). Shared normalizeBundleLabel() for docPath + isDocsRoot; divergence structurally impossible. |
| 19 | LORE-114 | cmd-crud-a | — | Done | 1 | lore new --out bypasses reserved index/log stem policy |
| 20 | LORE-115 | cmd-crud-a | — | Done | 2 | orphans table rows skip control-character sanitization on task fields (renderTaskSummaryRows in src/output.ts) |
| 21 | LORE-116 | cmd-crud-a | — | Done | 3 | lore replace commit phase has no atomic write or rollback on partial failure |
| 79 | LORE-174 | cmd-crud-a | — | Done | 7 | MERGED wave 7 (PR #125, dev @ `25b6c43`). resolveDocPath() now applies assertNotReservedStem on the default title-slug path (parity with LORE-114's --out path); docs/index.md keeps RESERVED_ROOT_INDEX message. Fable approve, 0 fix. See Resolved 37. |
| 80 | LORE-175 | cli-entry-state | — | Done | 7 | MERGED wave 7 (PR #124, dev @ `829cf9d`). readConfigText denied LoreError now carries errno `code` in input (matches errors.ts ioError/readFileIfPresent); loadConfig docstring updated. Fable approve, 0 fix. See Resolved 36. |
| 81 | LORE-176 | build-runtime | — | Done | 4 | MERGED wave 4 (PR #109, dev @ `0fd3a98`). Rewrote the stale run-e2e.sh AC4 assertion as a `step_fail … 6 '.error_type=="validation"' -- lore check --json` encoding LORE-89's profile-aware fail-loud contract; harness 299/0. Unblocked LORE-100. |
| 82 | LORE-177 | cmd-link | — | Done | 7 | MERGED wave 7 (PR #126, dev @ `92971d4`). verifiedViewTask() (mirrors LORE-122) wired into 3 link.ts viewTask consumers; mismatched id refused. Fable approve, 0 fix. Low nit: moveBackRefs (link.ts:~432) is a 4th consumer still unverified — see Resolved 38 / wave-7 log. |
| 83 | LORE-178 | build-ci-config | — | To Do | — | Runbook docs/runbooks/docker-e2e-testing-environment.md doesn't mention the harness now runs as the docker-e2e CI gate (post-LORE-100); filed from the wave-4 integration review. Docs-only via lore — touches docs/runbooks/, no code conflict. |
| 84 | LORE-179 | cmd-link | — | To Do | — | lore unlink/rename retry after a failed backlog commit silently no-ops on the leftover dirty file (same-class sibling of LORE-121; wave-5 review finding). Touches src/commands/link.ts (runUnlink/moveBackRefs) + test/link.test.ts — conflicts with LORE-121-area / LORE-177. |
| 85 | LORE-180 | core-rewrite-engine | — | Dispatched | 13 | rewrite.ts newDestPathFor ignores leading-slash link targets, diverging from resolvePath — lore rename mis-derives /-absolute links (rot/hijack). Pre-existing; surfaced by LORE-133 (wave-6). Touches src/core/rewrite.ts — conflicts with LORE-164/165. **Medium.** |
| 86 | LORE-181 | cmd-crud-b | — | To Do | — | Export one shared stripAnsiAndControls seam + de-dup query.ts's local copy (wave-6 LORE-118 finding). Touches src/commands/query.ts + src/output.ts — conflicts with LORE-118-area / output.ts tasks. Low. |
| 87 | LORE-182 | cmd-meta-a | — | To Do | — | schema confineOutDir: retain isAbsolute(rel) belt-and-suspenders for win32 cross-drive --out (wave-6 LORE-124 finding). Touches src/commands/schema.ts — conflicts with LORE-124-area schema tasks. Low. |
| 88 | LORE-183 | cmd-link | — | To Do | — | **Medium.** Guard moveBackRefs (link.ts:~432) — a genuine 4th, unguarded viewTask consumer (write-path gap in `lore rename`) — + de-duplicate the id-mismatch check now triplicated across link.ts/tasks.ts/reconcile-shared.ts + fix the stale verifiedViewTask doc comment. Wave-7 integration finding. Touches src/commands/link.ts + src/commands/tasks.ts (+ rename path) — conflicts with LORE-177-area / LORE-125 / LORE-179. |
| 89 | LORE-184 | core-bundle-check | — | To Do | — | **Medium.** resolveRef path-first precedence (from LORE-134) lets a mirroring directory shadow lore's OWN canonical bare-id refs — silent ref-text corruption on `lore rename`, missed supersede dedup, wrong graph edges. Wave-8 integration finding; needs ref-shape disambiguation (design decision), not a revert. Touches src/core/bundle.ts + src/core/rewrite.ts + src/commands/supersede.ts — conflicts with core-bundle-check + core-rewrite-engine (LORE-164/165/180) + LORE-183-area. |
| 90 | LORE-185 | core-bundle-check | — | To Do | — | Low. Consolidate the two template-path confinement guards (LORE-139's profile.ts assertTemplateConfined vs pre-existing new.ts assertTemplateNameConfined) into one shared helper + fix the stale readTemplateFile comment + profile-path symlink asymmetry. Wave-8 integration finding. Touches src/core/profile.ts + src/commands/new.ts — conflicts with core-bundle-check profile/new tasks (LORE-140). |
| 91 | LORE-186 | core-index-context | — | Dispatched | 13 | **Medium.** linkText (indexes.ts) inserts bracket escapes without doubling pre-existing backslashes first — a title like `a\[b` yields a live `[` that can break a generated index entry (exact class LORE-154 fixed in cell()). Wave-9 integration finding; pre-existing latent bug, now documented drift. Touches src/core/indexes.ts (+ cross-ref managed-block.ts) — conflicts with core-index-context (LORE-149/150/162-area) + core-managed-template. |
| 92 | LORE-187 | cmd-meta-c | — | To Do | — | Low. Refresh two stale O_NOFOLLOW comments left by LORE-130 (schema.ts:115 + fswrite.ts:419 ioError docstring) — mechanism is now lstat+rename; guarantee intact, comment-only. Wave-9 integration finding. Touches src/commands/schema.ts + src/commands/fswrite.ts — conflicts with fswrite.ts / schema.ts tasks (LORE-144/167/168/182). |
| 93 | LORE-188 | core-engine-a | — | To Do | — | Low. `:(literal)`-quote the raw `-- <root>` git-log pathspec in realGitAdapter.history (LORE-49 convention); production only passes the constant "docs" today so no live bug, but the root seam is exported (profile-configurable docs root is a plausible future). Wave-10 integration finding. Touches src/adapters/git.ts — conflicts with LORE-143-area / LORE-169/170 (git.ts). |
| 94 | LORE-189 | cmd-crud-b | — | To Do | — | Low. Fix stale src/commands/sync.ts:30-31 module doc comment claiming link/unlink/rename don't self-commit — now contradicts LORE-146's corrected `linking` instructions prose (regression hazard: a future agent could re-corrupt instructions.ts from the stale header). Wave-10 integration finding. Touches src/commands/sync.ts — conflicts with sync.ts tasks (LORE-150). |
| 95 | LORE-190 | core-engine-b | — | Done | 11 | Wave-11 integration fix: correct check/sync `validation`-cause + collapsed same-line marker-shape drift (LORE-147/156) + document LORE-150 orphanedIndexes. Docs/comment/test only. MERGED wave 11 (PR #153, dev @ `c11ef0e`). |
| 96 | LORE-191 | cmd-check | — | To Do | — | Low. Discovery advisories are lost when checkBundles throws in the scan phase (post-LORE-138 re-throw): flush advisories right after collectBundles so they survive a scan/reconcile throw. Wave-11 integration finding. Touches src/commands/check.ts — conflicts with cmd-check / core check tasks (LORE-138-area). |
| 97 | LORE-192 | core-bundle-check | — | To Do | — | **Medium.** loadBundle profile asymmetry (sibling of LORE-144): a fresh scaffold under a custom Reference-required profile still fails `lore graph`/query/etc. because loadBundle (src/core/bundle.ts) validates the reserved root index against the active profile; apply the reserved-root exemption at loadBundle's bundle-relative root path. Wave-11 (LORE-144) review finding. Touches src/core/bundle.ts — conflicts with core-bundle-check (LORE-138/184) + core-engine-a. |
| 98 | LORE-193 | core-bundle-check | — | To Do | — | **Medium.** parseItems accepts a list field's `items = { enum = [] }` -> z.array(z.enum([])) with no error (LORE-140 sibling, one seam over); reuse assertNonEmptyEnum in parseItems. Wave-12 finding. Touches src/core/profile.ts — conflicts with core-bundle-check (LORE-140[done]/184/192) + LORE-185-area (profile.ts). |
| 99 | LORE-194 | core-replace | — | To Do | — | Low. Document/pin the throw-on-duplicate locator contract behind assertNoInjectedMarker (MANAGED_REGION_LOCATORS). Wave-12 integration finding. Touches src/core/replace.ts — conflicts with core-replace (LORE-162[done]/163). |
| 22 | LORE-117 | cmd-crud-b | — | Done | 5 | MERGED wave 5 (PR #114, dev @ `c819458`). writeFileAtomic preserves dest mode (masked 0o7777) + best-effort chown; first-write falls back cleanly. |
| 23 | LORE-118 | cmd-crud-b | — | Done | 6 | MERGED wave 6 (PR #118, dev @ `ca0f112`). renderText sanitizes 4 fields via stripAnsiAndControls(singleLine). |
| 24 | LORE-119 | cmd-crud-b | — | Done | 7 | MERGED wave 7 (PR #127, dev @ `f3b87a0`). sync status-change write now derives body from freshly re-read on-disk bytes, not the stale pre-round-trip concept → concurrent on-disk edit survives. Fable approve, 0 fix. See Resolved 39. |
| 25 | LORE-120 | cmd-crud-b | — | Done | 8 | sync's multi-file write loop has no cross-file rollback on mid-loop failure |
| 26 | LORE-121 | cmd-link | — | Done | 5 | MERGED wave 5 (PR #115, dev @ `2e2ac8c`). link.ts already-present branch includes task file as commit candidate → retry recommits drift; clean tree still no-ops. |
| 27 | LORE-122 | cmd-meta-a | — | Done | 3 | resolveTaskDetails doesn't verify viewTask's returned id matches the requested id |
| 28 | LORE-123 | cmd-meta-a | — | Done | 5 | MERGED wave 5 (PR #116, dev @ `52e3368`). schema.ts write loop uses existing writeFileNoFollow → leaf symlink refused with conflict LoreError (exit 5). |
| 29 | LORE-124 | cmd-meta-a | — | Done | 6 | MERGED wave 6 (PR #119, dev @ `a9fd33c`). confineOutDir rejects absolute --out (isAbsolute(out)) → clean usage LoreError. |
| 30 | LORE-125 | cmd-meta-a | — | Done | 7 | MERGED wave 7 (PR #128, dev @ `723d458`). resolveRollup throws not_found on viewTask id mismatch (mirrors LORE-122/reconcile-shared). Fable approve, 0 fix. See Resolved 40. |
| 31 | LORE-126 | cmd-meta-b | — | Done | 3 | Collapse embedded newlines in graph node id/title before rendering |
| 32 | LORE-127 | cmd-meta-c | — | Done | 1 | RESOLVED-BY-MERGE by LORE-107 (PR #95): cli.ts routes `lore <cmd> --help` to runHelp; help.test.ts:271 asserts byte-identical to `lore help <cmd>` |
| 33 | LORE-128 | cmd-meta-c | — | Done | 6 | MERGED wave 6 (PR #120, dev @ `12f5b7c`). detectDiskStyle/reapplyDiskStyle preserve BOM + EOL (agents.ts only). |
| 34 | LORE-129 | cmd-meta-c | — | Done | 8 | `lore agents --check --force` mislabels a stale hand-edited SKILL.md and prints a remedy that won't fix it |
| 35 | LORE-130 | cmd-meta-c | — | Done | 9 | writeAllOrRollback's --force overwrite is not crash-safe against a mid-write kill |
| 36 | LORE-131 | cmd-meta-c | — | Done | 1 | RESOLVED-BY-MERGE by LORE-107 (PR #95): help.test.ts:271 is the byte-identical `lore <cmd> --help` == `lore help <cmd>` regression test |
| 37 | LORE-132 | cmd-rename-supersede | — | Done | 6 | MERGED wave 6 (PR #121, dev @ `f5c8230`). moveFile re-stats dest (dev/ino) before rename → conflict on raced-in file; case-only safe. |
| 38 | LORE-133 | core-bundle-check | — | Done | 6 | MERGED wave 6 (PR #122, dev @ `b67b273`). resolvePath special-cases leading-/ (matches check.ts). Follow-up LORE-180 (rewrite.ts). |
| 39 | LORE-134 | core-bundle-check | — | Done | 8 | resolveRef tries frontmatter ref as a root id before trying it as a relative path |
| 40 | LORE-135 | core-bundle-check | — | Done | 8 | Anchor-link check lower-cases fragments, masking case-mismatched broken anchors |
| 41 | LORE-136 | core-bundle-check | — | Done | 9 | Heading slug computation ignores image alt text in headings |
| 42 | LORE-137 | core-bundle-check | — | Done | 10 | reconcileDriftFindings ignores its own newStatus:null contract for managed-block drift |
| 43 | LORE-138 | core-bundle-check | — | Done | 11 | bodyText's catch-all swallows any gray-matter exception, not just YAML parse errors |
| 44 | LORE-139 | core-bundle-check | — | Done | 8 | Profile-declared type `template` path allows reading files outside .lore/templates/ via traversal |
| 45 | LORE-140 | core-bundle-check | — | Done | 12 | parseFieldSpec accepts an empty `enum = []`, making the field impossible to satisfy |
| 46 | LORE-141 | core-concept-manifest | — | Done | 8 | Malformed closing frontmatter fence bleeds bytes into concept body |
| 47 | LORE-142 | core-engine-a | — | Done | 9 | Add missing `help` entry to LORE_COMMANDS in agent-bridge.ts |
| 48 | LORE-143 | core-engine-a | — | Done | 10 | Scope `git log` in GitAdapter.history to the docs root instead of the whole repo |
| 49 | LORE-144 | core-engine-a | — | Done | 11 | serializeStructuralConcept's fixed default-profile write breaks `lore validate` under a custom Reference profile |
| 50 | LORE-145 | core-engine-b | — | Done | 9 | Fix DOT quote() to not double-escape backslashes; escape newlines |
| 51 | LORE-146 | core-engine-b | — | Done | 10 | Fix `linking` instructions: link/unlink now commit backlog/tasks themselves |
| 52 | LORE-147 | core-engine-b | — | Done | 11 | Fix `check` instructions: expandRoot/reconciliation throws besides usage/not_found |
| 53 | LORE-148 | core-index-context | — | Done | 9 | context export tokenEstimate ignores title field and JSON overhead |
| 54 | LORE-149 | core-index-context | — | Done | 10 | linkText re-escapes already-escaped brackets, enabling injected markdown links |
| 55 | LORE-150 | core-index-context | — | Done | 11 | generateIndexes never detects or removes an orphaned sub-index directory |
| 56 | LORE-151 | core-links-resolution | — | Done | 11 | decodeTarget whole-path decode lets %2F forge a structural slash in link targets |
| 57 | LORE-152 | core-links-resolution | — | Done | 12 | Dotted extensionless links (e.g. orders.v2) skip both portability lint and broken-link check |
| 58 | LORE-153 | core-links-resolution | — | To Do | — | LinkFinding.message interpolates raw link target unescaped into terminal-rendered text |
| 59 | LORE-154 | core-managed-template | — | Done | 9 | cell() escapes pipes without escaping pre-existing backslashes first |
| 60 | LORE-155 | core-managed-template | — | Done | 10 | upsertManagedBlock's update path skips the post-splice validation the insert path has |
| 61 | LORE-156 | core-managed-template | — | Done | 11 | Same-line marker pair collapses into one mdast node and is invisible to locateLabeledMarkers |
| 62 | LORE-157 | core-managed-template | — | Done | 12 | PLACEHOLDER regex silently passes through malformed {{...}} tokens instead of flagging them unresolved |
| 63 | LORE-158 | core-query-validate | — | Done | 10 | Strip ANSI/control characters from query text output for id, type, and query text |
| 64 | LORE-159 | core-query-validate | — | Done | 12 | h2Headings() counts nested headings (inside blockquotes/list items) as top-level sections |
| 65 | LORE-160 | core-query-validate | — | Dispatched | 13 | Quote-safety check omits leading colon `:` from INDICATOR_CHARS despite ADR-0007 |
| 66 | LORE-161 | core-query-validate | — | To Do | — | Resource-drift finding message embeds raw frontmatter value unsanitized in CLI output |
| 67 | LORE-162 | core-replace | — | Done | 12 | replace: validate expanded output, not just matched span, against managed ranges |
| 68 | LORE-163 | core-replace | — | Dispatched | 13 | replace: $<name> should stay literal when the regex has no named groups, not expand to "" |
| 69 | LORE-164 | core-rewrite-engine | — | Done | 12 | Fix rewriteInbound: excluded move source yields rename with no matching write |
| 70 | LORE-165 | core-rewrite-engine | — | To Do | — | Add regression test for rewriteInbound's move + excluded-source-id combination |
| 71 | LORE-166 | core-scaffold-consumer | — | Dispatched | 13 | buildObsidianScaffold never emits the .gitignore entry the docs promise |
| 72 | LORE-167 | core-scaffold-consumer | — | To Do | — | validateFrontmatter misclassifies differently-cased known types as unknown |
| 73 | LORE-168 | core-scaffold-consumer | — | To Do | — | okf_version extra-key warning is exempted on every file, not just the root index |
| 74 | LORE-169 | errors-output-git | — | Dispatched | 13 | Harden realGitAdapter.history against quoted non-ASCII paths and sentinel collision |
| 75 | LORE-170 | errors-output-git | — | To Do | — | resolveHeadSha can't tell an unborn branch from a corrupted-but-present .git |
| 76 | LORE-171 | errors-output-git | — | To Do | — | asText can return runtime undefined for Symbol/function input despite its string type |
| 77 | LORE-172 | errors-output-git | — | To Do | — | WarningCollector.flush writes raw multi-line/control-char warnings to stderr unnormalized |
| 78 | LORE-173 | errors-output-git | — | Done | 2 | RESOLVED-BY-MERGE by LORE-115 (PR #103): renderTaskSummaryRows now singleLine+stripAnsiAndControls id/status/title; output.test.ts covers newline/control-char rows |

## Resolved

| # | Issue | Status/date/wave | Evidence summary |
|---|---|---|---|
| 1 | LORE-96 | Done 2026-07-22 / wave 1 | PR #92, merged into dev @ `fa9387d`. Fable verdict **approve** (0 fix rounds). Added `rejectFlagLike()` in src/adapters/backlog.ts rejecting any caller-controlled argv value beginning with `-` before spawn, across listTasks/viewTask/searchTasks/searchByLabel/createTask/editTask (folded into commaJoin); 9-case regression block in test/backlog-adapter.test.ts. Reviewer re-ran: adapter 39/0, full suite 1707/0, typecheck clean, no unguarded caller-controlled argv left. |
| 2 | LORE-98 | Done 2026-07-22 / wave 1 | PR #93, merged into dev @ `b2f1019`. Fable verdict **approve** (0 fix). Pinned all 7 third-party `uses:` refs (setup-bun/action.yml, ci.yml, release.yml) to 40-char commit SHAs with `# vX.Y.Z` comments. Reviewer independently re-resolved every SHA via git ls-remote + gh api (confirmed commit objects, not tag objects; byte-identical to current tag targets → zero behavior change), actionlint clean. |
| 3 | LORE-102 | Done 2026-07-22 / wave 1 | PR #94, merged into dev @ `02a485c`. Fable verdict **approve** (0 fix). Hardened docker/e2e/Dockerfile: digest-pinned `oven/bun:1.2.23@sha256:6ebf30…`, replaced curl\|bash NodeSource bootstrap with download→sha256-verify→execute, pinned mkdocs==1.6.1/mkdocs-material==9.7.7. Reviewer verified digest + script sha256 resolve. |
| 4 | LORE-107 | Done 2026-07-22 / wave 1 | PR #95, merged into dev @ `4df15fc`. Fable verdict **approve** (0 fix). src/cli.ts run() now renders a command's own help for `lore <cmd> --help`/`-h`; `lore --help`/`lore help` unchanged; 3 regression tests in test/help.test.ts incl. `lore <cmd> --help` == `lore help <cmd>`. Reviewer re-ran full suite 1710/0. **Note:** likely also resolves LORE-127 and LORE-131 (see queue re-check note). |
| 5 | LORE-110 | Done 2026-07-22 / wave 1 | PR #96, merged into dev @ `92e5f56`. Fable verdict **approve** (0 fix). Added `LIVENESS_MAX_URLS=500` ceiling on probeLiveness's distinct-URL worklist (src/commands/check.ts); excess URLs skipped (never fetched) and surfaced as an advisory external-link finding; new bounding test in test/check.test.ts. Reviewer re-ran full suite 1711/0. |
| 6 | LORE-114 | Done 2026-07-22 / wave 1 | PR #97, merged into dev @ `5476b15`. Fable verdict **approve** (0 fix). resolveOutPath() in src/commands/new.ts now calls shared assertNotReservedStem() on the extension-stripped `--out` path; nested index/log basenames throw the usage error rename/supersede/link already produce; root index keeps its own message. Reviewer re-ran full suite 1712/0. **Integration review found a sibling gap → filed LORE-174** (default title-slug path still bypasses the policy). |
| 7 | LORE-97 | Done 2026-07-22 / wave 2 | PR #98, merged into dev @ `cfa82a4`. Fable **approve** (0 fix). createTask now passes {title, stdout} as the drift LoreError's `input` when the `Created task <ID>` line fails to parse, so a caller can recover an orphaned Backlog task; regression test in test/backlog-adapter.test.ts. Reviewer re-ran full suite 1713/0. |
| 8 | LORE-99 | Done 2026-07-22 / wave 2 | PR #99, merged into dev @ `c044788`. Fable **approve** (0 fix). verify-versions in release.yml now validates each platform's os/cpu fields + binary filename (derived from platform name); build Verify step asserts the compiled-binary path exists for every platform, not just linux-x64. Reviewer executed the extracted node/shell steps against real + mutated fixtures. |
| 9 | LORE-103 | Done 2026-07-22 / wave 2 | PR #100, merged into dev @ `ff9e760`. Fable **approve** (0 fix). run-e2e.sh record()/check() now detect a failed $REPORT append (REPORT_WRITE_FAILURES counter → nonzero exit); Dockerfile/docker-compose.yml gained PUID/PGID build args to rebind the `bun` user to the host uid/gid. Reviewer bash -n + logic-traced exit propagation. |
| 10 | LORE-108 | Done 2026-07-22 / wave 2 | PR #101, merged into dev @ `2ee01ae`. Fable **approve** (0 fix). readConfigText throws `denied` on EACCES/EPERM (was `validation`), matching the shared contract; test in test/config.test.ts. Reviewer re-ran full suite 1714/0. **Integration review noted the denied `input` omits errno `code` → filed LORE-175.** |
| 11 | LORE-111 | Done 2026-07-22 / wave 2 | PR #102, merged into dev @ `a8f7554`. Fable **approve** (0 fix). Moved private mapWithConcurrency helper from check.ts into reconcile-shared.ts (exported) and capped resolveTaskDetails's viewTask fan-out at TASK_DETAILS_CONCURRENCY=8; test in test/reconcile-shared.test.ts. Integration review verified single definition, all callers updated, no import cycle. Full suite 1716/0. |
| 12 | LORE-115 | Done 2026-07-22 / wave 2 | PR #103, merged into dev @ `aff6f95`. Fable **approve** (1 fix round). renderTaskSummaryRows (src/output.ts, shared by lore tasks/orphans) now singleLine+stripAnsiAndControls id/status/title before formatting; test in test/output.test.ts. Reviewer re-ran full suite 1718/0. **Also resolves LORE-173.** |
| 13 | LORE-127 | Done 2026-07-22 / resolved-by-merge (wave 1) | Resolved-by-merge by LORE-107 (PR #95). Verified: cli.ts routes `lore <cmd> --help`/`-h` to runHelp; test/help.test.ts:271 asserts `lore query --help` byte-identical to `lore help query`; no-command top-level catalog preserved. Marked Done with evidence note (no re-fix). |
| 14 | LORE-131 | Done 2026-07-22 / resolved-by-merge (wave 1) | Resolved-by-merge by LORE-107 (PR #95). Verified: test/help.test.ts:271 is exactly the byte-identical `lore <cmd> --help` == `lore help <cmd>` regression test for `query`, and fails against the pre-LORE-107 short-circuit. Marked Done with evidence note (no re-fix). |
| 15 | LORE-173 | Done 2026-07-22 / resolved-by-merge (wave 2) | Resolved-by-merge by LORE-115 (PR #103). Verified: renderTaskSummaryRows applies stripAnsiAndControls(singleLine(asText(...))) to id/status/title; test/output.test.ts covers a newline/control-char title → single sanitized line. Marked Done with evidence note (no re-fix). |
| 16 | LORE-109 | Done 2026-07-22 / wave 3 | PR #104, merged into dev @ `cae05e8`. Fable **approve** (0 fix). commitBacklogFiles captures LoreError.hint into a new BacklogCommitResult.hint field; renderBacklogCommitLine appends it after the failure message; regression test in test/state.test.ts. Reviewer re-verified typecheck clean, full suite 1719/0. |
| 17 | LORE-112 | Done 2026-07-22 / wave 3 | PR #105, merged @ `15e30fa`. Fable **approve** (0 fix). Added required CheckReport.complete:boolean (src/core/check.ts); runCheck's two driftPromise branches emit complete=(error===null) before rethrowing; test in test/check.test.ts. Suite 1721/0. |
| 18 | LORE-116 | Done 2026-07-22 / wave 3 | PR #106, merged @ `936e429`. Fable **approve** (1 fix — test strengthened to be reversion-proof, mutation-checked). runReplace Phase-2 loop now writes each file via writeFileAtomic (temp+rename) instead of writeFileOverwriting; fswrite.ts docstrings updated; test in test/replace.test.ts. Suite 1722/0. |
| 19 | LORE-122 | Done 2026-07-22 / wave 3 | PR #107, merged @ `852f6a1`. Fable **approve** (0 fix). resolveTaskDetails rejects an adapter.viewTask result whose id != requested id (case-insensitive) as ok:false / not_found LoreError; test in test/reconcile-shared.test.ts. Suite 1725/0. Integration review confirmed clean composition with LORE-112's complete flag. |
| 20 | LORE-126 | Done 2026-07-22 / wave 3 | PR #108, merged @ `cec7d4e`. Fable **approve** (2 fix — edge endpoints + node.type also guarded, regression tests). renderText (graph.ts) + quote (core/graph.ts) run node id/title/type + edge endpoints through singleLine(); test/graph.test.ts. Final integrated suite 1729/0. |
| 21 | LORE-176 | Done 2026-07-22 / wave 4 | PR #109, merged into dev @ `0fd3a98`. Fable verdict **approve** (0 fix). Rewrote the stale docker/e2e/run-e2e.sh AC4 assertion (old: "lore check is NOT profile-bearing", byte-identical baseline diff) as `step_fail "…" 6 '.error_type=="validation"' -- lore check --json`, encoding LORE-89's profile-aware fail-loud contract (check.ts:47,142 loadProfile → validation LoreError → exit 6). Removed the dead baseline-capture lines; rewrote the now-false comment. Orchestrator docker gate (AC#2): full harness **299 passed / 0 failed, exit 0** (was 298/1). Fable independently confirmed non-tautology via step_fail's 3 conditions (exit==6 ∧ empty stdout ∧ stderr `.error_type=="validation"`); scope/collateral clean. |
| 22 | LORE-100 | Done 2026-07-22 / wave 3 dispatch → wave 4 merge | PR #110, merged into dev @ `995a02b`. Held from wave 3 (Fable-approved impl; merge escalated because the gate was red-at-birth). Unblocked by LORE-176. Rebased onto dev+LORE-176 (clean — only a lore-100 task-file metadata conflict, resolved to Done); ci.yml byte-identical to the wave-3 approved diff, so that approval carried. Decisive docker gate on the exact merged bytes: **299 passed / 0 failed, exit 0** — green-at-birth; actionlint clean. Added a `docker-e2e` job (`compose up --build --exit-code-from e2e`, PUID/PGID, 45m timeout, uploads report.jsonl). Branch protection can now mark it a required check (repo setting, out of the diff's scope). |
| 23 | LORE-101 | Done 2026-07-22 / wave 5 | PR #111, merged into dev @ `60c1b2d`. Fable **approve** (0 fix). Added `publishConfig: { access: "public" }` to root package.json + all 5 npm/<platform>/package.json manifests, and a "Scoped-package public access" prose paragraph to docs/runbooks/release-publishing.md (non-managed region). Reviewer independently JSON-parsed all 6 manifests; `npm publish --dry-run` on a leaf scoped package → exit 0, public access, no access error; `lore check` 38/0. Config+docs only — no code/exit-code/permission surface. |
| 24 | LORE-104 | Done 2026-07-22 / wave 5 | PR #112, merged into dev @ `c98471a`. Fable **approve** (0 fix). Both documented `docker compose … up --build` invocations in docker/e2e/docker-compose.yml's usage comment now carry `--exit-code-from e2e` (comment-only; matches ci.yml:146). Reviewer re-proved AC#2 with a throwaway `--exit-code-from` compose over a failing service: plain `up` → `$?=0` (bug reproduced), with-flag → `$?=1` (matches run-e2e.sh); `compose config -q` clean. |
| 25 | LORE-113 | Done 2026-07-22 / wave 5 | PR #113, merged into dev @ `6e8bfc2`. Fable **approve** (0 fix). Extracted isDocsRoot's label normalization into a shared `normalizeBundleLabel()` (backslash→/, posix.normalize, trailing-slash strip, lowercase) and built docPath from it — isDocsRoot semantics character-identical to pre-change; divergence now structurally impossible. Regression test in test/check.test.ts proven discriminating (revert fails 2/2). Full suite 1731/0, typecheck clean. 2 behavior-neutral nits (docPath lowercased for all bundles — traced harmless; pre-existing prefixFinding raw-label out of scope). |
| 26 | LORE-117 | Done 2026-07-22 / wave 5 | PR #114, merged into dev @ `c819458`. Fable **approve** (0 fix). writeFileAtomic now statSyncs the destination and, if present, chmods the temp to the dest mode (masked 0o7777) + best-effort chowns uid/gid (swallowing EPERM) before rename; missing dest falls back to default-umask without erroring. Signature + temp/rename atomicity unchanged. New test/fswrite.test.ts (0o640/0o600 survive; discriminating vs old 0644; first-write no-throw). Full suite 1736/0. **Low (non-blocking):** chmod-then-chown can clear setuid/setgid on a successful chown — exotic for a docs bundle (ACs cover permission bits only); recorded for any future hardening task. |
| 27 | LORE-121 | Done 2026-07-22 / wave 5 | PR #115, merged into dev @ `2e2ac8c`. Fable **approve** (0 fix). runLink's already-present/unchanged branch now pushes the task's file path as a git-status commit candidate, so a retry after a prior failed backlog/ commit discovers + commits leftover drift instead of a false no-op success; genuinely clean tree still no-ops (empty commit, exit 0). Shared src/state.ts guard untouched. All 3 ACs confirmed in test/link.test.ts; full suite 1737/0. **Low → filed [[LORE-179]]:** the same-class no-op-on-dirty gap remains in runUnlink (removeBackRefs already-absent) and moveBackRefs (already-current) — out of LORE-121's stated scope; same fix pattern applies. |
| 28 | LORE-123 | Done 2026-07-22 / wave 5 | PR #116, merged into dev @ `52e3368`. Fable **approve** (0 fix). schema.ts's per-file write loop now calls the EXISTING leaf-symlink-safe `writeFileNoFollow` (fswrite.ts) instead of `writeFileOverwriting`; a symlink planted at a schema output path (e.g. .lore/schemas/story.schema.json) pointing outside the repo is refused with a `conflict` LoreError (**exit 5**, not the AC parenthetical's "denied" — reviewer judged exit 5 the codebase-consistent choice, matching LORE-93 symlink refusals) instead of writing through. Change confined to schema.ts (LORE-117 owned fswrite.ts this wave). Real-CLI repro + regression test in test/schema-export.test.ts (proven non-vacuous); schema suites 61/0. **Nit → folded into wave-5 housekeeping:** `writeFileNoFollow`'s docstring still named writeAllOrRollback as its only caller (now false). |
| 29 | LORE-105 | Done 2026-07-22 / wave 6 | PR #117, merged into dev @ `c2dfd8c`. Fable **approve** (0 fix). record() and check() in docker/e2e/run-e2e.sh now use `jq -nc` (compact single-line) so every appended report.jsonl entry is standalone JSON. Reviewer re-proved AC#2 via the isolated method: 6 hostile-fixture calls → 6 standalone-parseable lines (per-line `jq -e`), REPORT_WRITE_FAILURES=0, stdout round-trips byte-identically; discriminating control (jq -n) → 36 lines/FAIL. bash -n clean; failure-capture + exit-code chain untouched. |
| 30 | LORE-118 | Done 2026-07-22 / wave 6 | PR #118, merged into dev @ `ca0f112`. Fable **approve** (0 fix). renderText (query.ts) passes data.query + hit.id/type/snippet through sanitizeField() = stripAnsiAndControls(singleLine(...)), byte-identical to output.ts's LORE-115 seam. 2 regression tests (ANSI in --query, ANSI in frontmatter) mutation-verified. Full suite 1740/0. **Low → filed [[LORE-181]]:** stripAnsiAndControls duplicated locally (output.ts's copy is module-private, out of scope) — export a shared seam to prevent divergence. |
| 31 | LORE-124 | Done 2026-07-22 / wave 6 | PR #119, merged into dev @ `a9fd33c`. Fable **approve** (0 fix). confineOutDir (schema.ts) now checks isAbsolute(out) on the raw --out → clean usage LoreError (exit 2) before any I/O, fixing the double-prefix/unhandled-ENOENT crash. Live-CLI verified (absolute inside+outside repo → clean error, zero files; relative → exit 0); regression test in test/schema-export.test.ts mutation-verified. Full suite 1741/0. **Low → filed [[LORE-182]]:** the removed isAbsolute(rel) clause was live on win32 cross-drive drive-relative --out (contained: no traversal/deletion, just a less-clean IO error). |
| 32 | LORE-128 | Done 2026-07-22 / wave 6 | PR #120, merged into dev @ `12f5b7c`. Fable **approve** (0 fix). detectDiskStyle + reapplyDiskStyle (src/commands/agents.ts) detect the raw file's BOM + dominant EOL and re-apply that convention to the planned CLAUDE.md before writeFileAtomic, so content outside the managed block is byte-preserved; fit entirely in agents.ts (core/agent-bridge.ts untouched). End-to-end real-CLI verify (CRLF+BOM → preserved, idempotent) + 2 regression tests. Full suite 1743/0. Low nit: detectDiskStyle is presence-priority (mixed-EOL edge only; uniform files round-trip exactly). |
| 33 | LORE-132 | Done 2026-07-22 / wave 6 | PR #121, merged into dev @ `f5c8230`. Fable **approve** (0 fix). moveFile (src/commands/fswrite.ts) gained assertMoveTargetSafe: re-stat the destination immediately before renameSync and compare dev/ino vs the source, throwing the same `conflict` error assertTargetFree raises for any raced-in *different* file, while a same-inode case-only rename still proceeds (AC#3). Regression test (spyOn-injected race) mutation-verified; case-only tests exercise the same-inode branch on APFS. Full suite 1744/0. Integration review confirmed `lore supersede` (another moveFile caller) unaffected. |
| 34 | LORE-133 | Done 2026-07-22 / wave 6 | PR #122, merged into dev @ `b67b273`. Fable **approve** (0 fix). resolvePath (src/core/bundle.ts) special-cases a leading `/` (strip → root-relative), aligning with check.ts's linkFindings so resolveRef + the graph builder agree with the link-check gate on `/`-absolute inputs. 4 regression tests incl. a cross-module agreement test (mutation-verified); relative targets unchanged. Full suite 1748/0. **Medium → filed [[LORE-180]]:** src/core/rewrite.ts newDestPathFor still lacks the leading-slash case (pre-existing; lore rename mis-derives /-absolute link targets — rot/hijack). |
| 35 | LORE-106 | Done 2026-07-22 / wave 7 | PR #123, merged into dev @ `1de9ee7`. Fable **approve** (0 fix). Two write-no-golden guards added to test/support/record-backlog-goldens.ts: resolveGitCommit vs docker/e2e/Dockerfile's pinned BACKLOG_COMMIT (both a missing and a mismatched upstream abort exit-1, zero writes) and assertTaskViewSpecimenShape validating all 7 documented properties before trimSample/writeFileSync; main() gated behind import.meta.main so guards are importable/unit-testable. Reviewer independently repro'd both abort paths + 10 unit tests on mutated golden copies (full E2E impossible w/o the genuine pinned upstream — AC#1's guard blocks substitutes). Suite 1765/0, typecheck clean; 3 mutations each broke exactly the matching tests. |
| 36 | LORE-175 | Done 2026-07-22 / wave 7 | PR #124, merged into dev @ `829cf9d`. Fable **approve** (0 fix). readConfigText's denied LoreError now attaches { path, code: errnoCode(cause) }, matching errors.ts readFileIfPresent/ioError denied shape; loadConfig docstring documents the denied path. Reviewer independently repro'd via the real toErrorEnvelope seam ({error_type:denied, input:{path:.lore/config.toml, code:EACCES}, exit 4}); mutation 36/37 reverted → 37/37 restored. Suite 1765/0, typecheck clean. Scope config.ts + config.test.ts. |
| 37 | LORE-174 | Done 2026-07-22 / wave 7 | PR #125, merged into dev @ `25b6c43`. Fable **approve** (0 fix). resolveDocPath() applies assertNotReservedStem(idFromPath(docPath),"create") on the default title-slug path (parity with LORE-114's --out fix): `lore new reference "Index"/"Log"` → exit 2 usage error, no file; docs/index.md keeps its RESERVED_ROOT_INDEX message; 'Index Strategy' still scaffolds. Reviewer CLI-repro'd all 3 ACs; mutation 51/1 reverted → 52/0 restored. Suite 1766/0, typecheck clean. |
| 38 | LORE-177 | Done 2026-07-22 / wave 7 | PR #126, merged into dev @ `92971d4`. Fable **approve** (0 fix). verifiedViewTask() helper (mirrors LORE-122 resolveTaskDetails) wired into runLink's pre-write existence check, fresh back-ref re-read, and unlink's removeBackRefs; a mismatched viewTask id is refused. Reviewer: stub-adapter mismatch → refuses write; suite 1769/0, typecheck + biome clean, mutation 60/3. **Low (recorded, non-blocking):** verifiedViewTask doc comment claims to cover "every viewTask consumer" but moveBackRefs (link.ts:~432) is a 4th consumer still unverified — same-class gap, candidate follow-up (see wave-7 log). |
| 39 | LORE-119 | Done 2026-07-22 / wave 7 | PR #127, merged into dev @ `f3b87a0`. Fable **approve** (0 fix). sync's status-changing write path (sync.ts) derives the new body from freshly re-read on-disk `original` bytes, not the stale pre-round-trip in-memory `concept`, so a concurrent on-disk edit during gatherReconciliation's async Backlog round-trip survives. Reviewer confirmed both ACs; regression simulates a concurrent edit between load and status-change write and asserts survival. Suite 1770/0, typecheck clean. **Low (non-blocking):** statusChanged gate still compares vs the stale snapshot status — matches pre-existing no-change-branch behavior; not a regression. |
| 40 | LORE-125 | Done 2026-07-22 / wave 7 | PR #128, merged into dev @ `723d458`. Fable **approve** (0 fix). resolveRollup (tasks.ts) throws a not_found LoreError on a case-insensitive viewTask id mismatch instead of pushing the mismatched detail (mirrors reconcile-shared resolveTaskDetails / LORE-122). Reviewer confirmed both ACs via a stub-adapter mismatch → thrown error. Suite 1771/0, typecheck clean. **Low (non-blocking):** impl note's CLI repro only exercised the zero-linked path (no bundle concept has a real tasks: list; local backlog binary not --json-capable) — stub-adapter unit tests are the operative coverage per AC/LORE-122 precedent. |
| 41 | LORE-120 | Done 2026-07-22 / wave 8 | PR #129, merged into dev @ `354878e`. Fable **approve** (0 fix). Added writeManyAtomicOrRollback (src/commands/fswrite.ts) — LIFO best-effort rollback of every already-applied write in the run when a later write throws (before-bytes captured at diff time, so rollback restores true pre-run state with no re-read race); sync.ts's write phase routed through it. 5 new tests (4 unit incl. call-order + freshly-created-file removal + swallowed-restore-failure; 1 real-EACCES chmod integration proving the concept doc stays byte-identical + index/log never created). Reviewer re-ran 1776/0, typecheck clean, confirmed the integration test hit its full assertion path (not the perms-skip path); 3 low/info findings only (JSDoc blank line, notes off-by-one, in-process-only rollback matches existing writeAllOrRollback discipline). |
| 42 | LORE-129 | Done 2026-07-22 / wave 8 | PR #130, merged into dev @ `8ee8617`. Fable **approve** (0 fix). Threaded --check into the agents plan (src/core/agent-bridge.ts + src/commands/agents.ts) so a differing (hand-edited) SKILL.md is reported 'protected' under --check, never mislabeled 'updated' with a remedy that wouldn't fix it. Reviewer re-ran 1778/0, typecheck clean; 1 low doc-comment-ambiguity finding. |
| 43 | LORE-134 | Done 2026-07-22 / wave 8 | PR #131, merged into dev @ `494aba0`. Fable **approve** (0 fix). resolveRef (src/core/bundle.ts) now tries the dir-relative path form before the bundle-root id form, so a relative/.md-suffixed frontmatter ref that dir-joins to a real concept wins over a coincidental root id; regression tests cover the fix + the id-form fallback. Reviewer re-ran 1780/0, typecheck clean. **Medium (wave-8 integration finding) → filed [[LORE-184]]:** the precedence flip regresses the mirror direction — lore's own canonical bare-id refs (written by rewrite.ts/supersede.ts) become shadowable by a mirroring dir. Not a defect in LORE-134's own tests; a latent tension exposed. |
| 44 | LORE-135 | Done 2026-07-22 / wave 8 | PR #132, merged into dev @ `1a19f15`. Fable **approve** (0 fix, 0 findings). anchorFindings (src/core/check.ts) drops an errant .toLowerCase() so case-mismatched anchors are flagged broken against GitHub-style heading slugs; regression test added. Reviewer re-ran 1781/0, typecheck clean; confirmed no second anchor comparator exists (slugify lower-casing is generation-side only). |
| 45 | LORE-139 | Done 2026-07-22 / wave 8 | PR #133, merged into dev @ `4a9c0cc`. Fable **approve** (0 fix). Validates every profile-declared [[types]].template value at profile parse time (src/core/profile.ts) — rejects absolute paths + ..-escapes before commands/new.ts reads the file (single choke point). Reviewer re-ran 1788/0, typecheck clean; 1 low win32-drive-relative edge note. **Low (wave-8 integration) → filed [[LORE-185]]:** duplicate confinement guard vs pre-existing new.ts + stale comment + profile-path symlink asymmetry. |
| 46 | LORE-141 | Done 2026-07-22 / wave 8 | PR #134, merged into dev @ `e34d9c2`. Fable **approve** (0 fix). splitFrontmatter (src/core/concept.ts) rejects a malformed closing frontmatter fence (extra trailing dashes/junk) with a validation LoreError instead of letting gray-matter's loose substring search leak stray bytes into the body. Reviewer re-ran 1794/0, typecheck clean; 1 low finding (deliberate strictness widening, documented). Integration review confirmed no contract mismatch with check.ts/validate.ts consumers. |
| 47 | LORE-130 | Done 2026-07-22 / wave 9 | PR #135, merged into dev @ `cda7196`. Fable **approve** (0 fix). writeFileNoFollow's --force overwrite now commits via write-to-temp + renameSync instead of in-place O_TRUNC — a kill mid-write leaves the complete original or complete new file, never a partial truncation; the LORE-92 symlink refusal is preserved (up-front lstat + rename-never-follows) + mode/ownership preserved. Reviewer independently repro'd a real SIGKILL mid-write (original bytes byte-intact); suite 1800/0, typecheck clean. **3 low (comment-accuracy):** two stale O_NOFOLLOW comments (schema.ts:115 / fswrite.ts:419, guarantee intact) → filed [[LORE-187]]; + a minor unlink-failure discipline nit. |
| 48 | LORE-136 | Done 2026-07-22 / wave 9 | PR #136, merged into dev @ `d25f4e6`. Fable **approve** (1 fix round). **Task premise was inverted:** the review determined GitHub renders `<img>` with empty textContent, so it EXCLUDES image alt text from heading slugs — lore already matched. A first attempt to INCLUDE alt text was reverted; the net change is a nodeText (bundle.ts) docstring documenting the deliberate exclusion (verified empirically vs GitHub; mdast-util-to-string's includeImageAlt:true default diverges and does not apply) + 23 lines of regression tests locking exclusion in. Reviewer re-ran 1803/0, typecheck clean; 1 low residual (pre-existing slugify .trim() divergence). |
| 49 | LORE-142 | Done 2026-07-22 / wave 9 | PR #137, merged into dev @ `4c3b5b6`. Fable **approve** (0 fix, 0 findings). Added the missing `help` entry to LORE_COMMANDS (agent-bridge.ts, summary byte-identical to manifest.ts), regenerated the checked-in .claude/skills/lore/SKILL.md, and added a reverse-direction lockstep test so LORE_COMMANDS ↔ manifest can't drift. Reviewer re-ran 1804/0, typecheck + `lore check` 38/0/0 clean. |
| 50 | LORE-145 | Done 2026-07-22 / wave 9 | PR #138, merged into dev @ `9a12ad0`. Fable **approve** (1 fix round). **Task premise partly inverted:** DOT's lexer (scan.l) requires literal backslashes to be DOUBLED to survive, so the pre-existing doubling was correct; the real bug was only the missing newline escape. A first attempt to remove backslash-doubling was reverted (it introduced a worse bug — a trailing `\` escapes the closing quote and `dot` rejects the file). Final quote() (graph.ts): keeps `\`→`\\`, adds `\r\n|\r|\n`→`\n`, keeps `"`→`\"`; verified against real graphviz 15.1.0 `dot -Tcanon`. Reviewer re-ran 1807/0, typecheck clean, 0 findings. |
| 51 | LORE-148 | Done 2026-07-22 / wave 9 | PR #139, merged into dev @ `43cf732`. Fable **approve** (0 fix). neighborOf's tokenEstimate (context.ts) now charges the neighbor's title (when present) alongside id/type/summary — matches the --json neighbor object — so a long-titled/short-summarized neighbor is no longer undercounted against --max-tokens. Reviewer re-ran 1808/0, typecheck clean; 2 low (self-report test-count off-by-one; JSON-overhead uncounted but not required by any AC — informational). |
| 52 | LORE-154 | Done 2026-07-22 / wave 9 | PR #140, merged into dev @ `a1fd570`. Fable **approve** (0 fix, 0 findings). cell() (managed-block.ts) escapes literal backslashes before escaping pipes, so a pre-existing backslash-pipe sequence (e.g. `x\|y`) can't produce a spurious live table delimiter; regression test added. Reviewer re-ran 1809/0, typecheck clean. **Medium (wave-9 integration) → filed [[LORE-186]]:** the sibling escaper linkText (indexes.ts) still lacks the same backslash-first doubling. |
| 53 | LORE-137 | Done 2026-07-22 / wave 10 | PR #141, merged into dev @ `b770b9c`. Fable **approve** (0 fix). reconcileDriftFindings (src/core/check.ts) early-returns [] when input.newStatus === null, gating BOTH status-drift and managed-block-drift on the documented "never drift either way" contract (previously regenerateTaskBlock ran unconditionally). Regression test in test/check.test.ts discriminates (fails pre-fix). Reviewer re-ran 1810/0, typecheck clean. Integration review: the null path is unreachable via production callers (gatherReconciliation requires ≥1 linked task; reconcileStatus returns null only on an empty status list) — defensive hardening of the exported pure seam, zero live-behavior change. |
| 54 | LORE-143 | Done 2026-07-22 / wave 10 | PR #142, merged into dev @ `fac4966`. Fable **approve** (0 fix). GitAdapter.history (src/core/log.ts) + realGitAdapter (src/adapters/git.ts) now take a root/pathspec so `git log` is pruned to `-- docs` at the git level, not merely post-filtered after buffering the whole repo; a shared resolveRoot() keeps the adapter pathspec and generateLog's post-filter in agreement. Tests at both layers (spy-based args assertion + real-adapter out-of-docs-commit exclusion) discriminate (5 failures pre-fix). Reviewer re-ran 1814/0, typecheck clean. **Low (integration) → filed [[LORE-188]]:** the `-- <root>` pathspec is passed raw, not `:(literal)`-quoted (LORE-49 convention); production only ever passes the constant "docs" so no live bug. |
| 55 | LORE-146 | Done 2026-07-22 / wave 10 | PR #143, merged into dev @ `c26c7b2`. Fable **approve** (0 fix). The `linking` instructions topic (src/core/instructions.ts) no longer claims lore link/unlink leave backlog/tasks/*.md uncommitted for `lore sync`; it now describes that link/unlink commit their own touched files via commitBacklogFiles. test/instructions.test.ts asserts the stale "do not commit" claim is gone. Reviewer re-ran 1815/0, typecheck clean. **Low (integration) → filed [[LORE-189]]:** a PRE-EXISTING stale module doc at src/commands/sync.ts:30-31 now states the opposite of this corrected prose — regression hazard. |
| 56 | LORE-149 | Done 2026-07-22 / wave 10 | PR #144, merged into dev @ `e973267`. Fable **approve** (0 fix). linkText (src/core/indexes.ts) now doubles pre-existing backslashes before the bracket-escape, so a title like `Plan \]B\[` can't shift text into an injected markdown link — the exact class LORE-154 fixed in cell(). Existing plain-bracket test preserved; already-escaped-bracket case added. Reviewer re-ran 1816/0, typecheck clean. Integration review confirmed linkText and the sibling cell() escaper now agree in discipline (double-first, then escape). |
| 57 | LORE-155 | Done 2026-07-22 / wave 10 | PR #145, merged into dev @ `71a23c6`. Fable **approve** (0 fix). upsertManagedBlock's update branch (src/core/managed-block.ts) now re-locates the labeled markers in its spliced result and throws the same labeledMarkerError the insert branch does when the result no longer parses as a clean top-level pair — fail-loud parity instead of silently returning corrupted content. Regression test added. Reviewer re-ran 1817/0, typecheck clean. Integration review: sole production caller (agent-bridge buildNudgeBody, static template) can't trip it; regenerateTaskBlock splices via findMarkers, not upsertManagedBlock, so no check/sync interaction. |
| 58 | LORE-158 | Done 2026-07-22 / wave 10 | PR #146, merged into dev @ `36604a9`. Fable **approve** (0 fix). renderText's ANSI/control sanitization already landed via LORE-118 (sanitizeField/stripAnsiAndControls on data.query + hit id/type); the remaining untested gap was the id-from-path path (a concept id derived from a file path containing ANSI, unreachable via frontmatter). Added the regression test in test/query.test.ts — test-only diff, mutation-verified (fails when sanitizeField(hit.id) removed). Reviewer re-ran 1818/0, typecheck clean. Same pattern as wave-9 LORE-136 (premise already satisfied by an earlier merge). |
| 59 | LORE-138 | Done 2026-07-22 / wave 11 | PR #147, merged into dev @ `de5e5b7`. Fable **approve** (0 fix). bodyText (src/core/check.ts) catch now falls back to the normalized text ONLY for gray-matter's YAMLException (duck-typed via error.name; gray-matter vendors its own js-yaml instance) and re-throws every other exception; 2 regression tests via an unregistered `---toml` fence (checkBundle + collectExternalLinks). Double-mutation verified (bare-catch fails the 2 new tests; always-rethrow fails the LORE-27 malformed-YAML test). Suite 1820/0. Integration review noted pre-existing bare-matter() JS-eval-engine reachability + missing YAML-bomb guard on the check path (out of scope, unchanged by this diff). |
| 60 | LORE-144 | Done 2026-07-22 / wave 11 | PR #148, merged into dev @ `02e80b0`. Fable **approve** (0 fix). New effectiveProfileFor() in src/core/validate.ts judges the reserved root docs/index.md (ROOT_INDEX_PATH, exported from scaffold.ts) against defaultProfile() — matching scaffold's deliberate default-profile write — so `lore init` then `lore validate` succeeds even when the active profile adds a required Reference field; scaffold write path unchanged; 3 regression tests (CLI + core), mutation-verified. Suite 1823/0. Per-task review medium residual → filed [[LORE-192]]: the same asymmetry survives at the loadBundle seam (`lore graph`/query/etc. still exit 6 on a fresh scaffold under such a profile; loadBundle path space is bundle-relative). |
| 61 | LORE-147 | Done 2026-07-22 / wave 11 | PR #149, merged into dev @ `f85d1ff`. Fable **approve** (0 fix). Corrected the `check` instructions topic (src/core/instructions.ts) that falsely claimed only usage/not_found throw — now enumerates usage(2)/not_found(3)/denied(4)/validation(6), consistent with check.ts's docstring + reconcile-shared.ts @throws; discriminating test in test/instructions.test.ts. Suite 1824/0. **Wave-11 integration review filed [[LORE-190]] (medium):** this prose mis-attributed the `validation` cause to reconcile-config only (the marker-corruption path, widened by LORE-156 same wave, was omitted) — fixed same session via LORE-190 (Resolved row 65). |
| 62 | LORE-150 | Done 2026-07-22 / wave 11 | PR #150, merged into dev @ `f79e49d`. Fable **approve** (0 fix). New pure orphanedIndexPaths() in src/core/indexes.ts (shares a factored liveIndexDirs helper with generateIndexes so the live-set derivation can't drift; generateIndexes signature byte-unchanged → rename.ts unaffected) diffs on-disk index.md keys vs live dirs; sync surfaces them as SyncReport.orphanedIndexes + a distinct "orphaned index ..." render line (file left on disk, NOT auto-deleted — AC#1 report branch). 12 new tests, mutation-verified. Suite 1836/0. Info: `lore check`'s drift gate remains orphan-blind (out of scope). |
| 63 | LORE-151 | Done 2026-07-22 / wave 11 | PR #151, merged into dev @ `649eae9`. Fable **approve** (0 fix). decodeTarget (src/core/links.ts, the single exported decode) now splits on literal '/', decodes each segment, and folds any decode-produced '/' back to %2F before rejoining — so `orders%2Fv2.md` no longer forges the two-segment `orders/v2`; bundle.ts/check.ts/validate.ts inherit the fix with no edits (151 stayed disjoint from 138's check.ts). 5 discriminating tests; same-class vectors (%5C, double-decode, %252F) probed clean; validate.ts:301 decode-tolerant compare traced safe. Suite 1841/0. |
| 64 | LORE-156 | Done 2026-07-22 / wave 11 | PR #152, merged into dev @ `048470c`. Fable **approve** (0 fix). collectMarkerSpans (src/core/managed-block.ts) now detects a top-level html node carrying marker text that isn't a clean standalone sentinel; findMarkers/locateTaskBlock/locateLabeledMarkers throw a validation LoreError naming the label instead of returning null, so upsertManagedBlock no longer appends a duplicate block after a collapsed same-line marker pair (also fixes `lore replace`/`lore agents` same-class paths). 3 tests, mutation-verified. Suite 1844/0. Low nit: loosenMarkerPattern drops regex flags (latent; all marker patterns flag-free today). |
| 65 | LORE-190 | Done 2026-07-22 / wave 11 (integration follow-up) | PR #153, merged into dev @ `c11ef0e`. Fable **approve** (1 fix round — the first pass over-corrected into a falsely-exhaustive "two distinct causes" claim; re-review caught it → non-exhaustive, code-verified 4-cause enumeration). Fixes the wave-11 integration finding on LORE-147's check-topic `validation`-cause prose + adds the collapsed same-line marker shape to the SYNC topic + check.ts:454 docstring + documents LORE-150's orphanedIndexes in SYNC --json prose. Docs/comment/test only; discriminating test. Suite 1845/0. |
| 66 | LORE-140 | Done 2026-07-22 / wave 12 | PR #154, merged into dev @ `f435cc6`. Fable **approve** (0 fix). parseFieldSpec (src/core/profile.ts) now calls a new assertNonEmptyEnum immediately after asStringArray, so a field spec with `enum = []` fails at profile-parse with a validation LoreError naming the field (e2e via loadProfile: '.lore/profile.toml: types[0].fields.status.enum must not be empty'). Regression test in test/profile.test.ts, mutation-verified (base 58/1 -> 59/0). Suite 1846/0, typecheck clean. **Per-task medium residual -> filed [[LORE-193]]:** parseItems/itemToZod still accepts a list field's `items = { enum = [] }` (same class, one seam over). |
| 67 | LORE-152 | Done 2026-07-22 / wave 12 | PR #155, merged into dev @ `90f589d`. Fable **approve** (0 fix). lacksMarkdownSuffix (src/core/links.ts) now flags a dotted extensionless concept link (e.g. orders.v2 for a dropped orders.v2.md) as a missing-extension **portability warning** via a new closed KNOWN_ASSET_EXTENSIONS allowlist (replaced the old "any dot = asset" rule); the false doc-comments claiming the existence check catches it are corrected. 3 ACs, mutation-verified (base 80/1 -> 81/0); `lore check docs` 38/0/0 on the real bundle. Suite 1847/0. Info (documented tradeoff): unlisted real-asset extensions (.html/.mkv/...) now warn. |
| 68 | LORE-157 | Done 2026-07-22 / wave 12 | PR #156, merged into dev @ `f39ceb3`. Fable **approve** (0 fix). PLACEHOLDER regex (src/core/template.ts) broadened to any {{...}} shape + a PLACEHOLDER_NAME gate routes malformed brace tokens ({{owner name}}, {{}}, {{ owner/name }}) through the existing unresolved fail-loud path -> renderBody throws a validation LoreError (exit 6), no verbatim write. 6 tests, mutation-verified (base 36/6 -> 42/0); e2e `lore new` exits 6, writes nothing. Suite 1853/0. Low residual (out of AC scope, not filed): inner-brace tokens like `{{ {k} }}` still pass through. |
| 69 | LORE-159 | Done 2026-07-22 / wave 12 | PR #157, merged into dev @ `e9833d7`. Fable **approve** (0 fix). h2Headings (src/core/validate.ts) now iterates fromMarkdown(body).children (was full-tree walkMdast DFS), so a `> ## Consequences` (blockquote) or list-item-nested `##` no longer satisfies requiredSectionFindings; single caller. 2 tests, mutation-verified (base 56/2 -> 58/0). Suite 1855/0. The cross-worktree git-stash collision with the LORE-140 worker was independently verified fully resolved (both branches clean, stash list empty). check.ts anchor-slug traversal intentionally stays full-tree (correct divergence — GitHub anchors nested headings). |
| 70 | LORE-162 | Done 2026-07-22 / wave 12 | PR #158, merged into dev @ `a95aa98`. Fable **approve** (0 fix). applyReplacement (src/core/replace.ts) re-validates the fully-assembled output via a new assertNoInjectedMarker (reusing managedRanges/MANAGED_REGION_LOCATORS), so a regex $`/$' expansion (or literal paste) that would duplicate a managed-block marker is rejected with a usage LoreError, never returned. 3 tests + adversarial probes, mutation-verified (base 77/2 -> 79/0); commands/replace.ts's plan-then-write two-phase design aborts in phase 1 with zero bytes written. Suite 1858/0. **Integration low -> filed [[LORE-194]]:** document/pin the throw-on-duplicate locator contract the guard relies on. |
| 71 | LORE-164 | Done 2026-07-22 / wave 12 | PR #159, merged into dev @ `bd27700`. Fable **approve** (0 fix). rewriteInbound (src/core/rewrite.ts) sets plan.rename only when `move && !exclude.has(from)` — the same normalized key the writes-skip uses — so rename and writes stay mutually consistent for any exclude set containing the move source. Regression test in test/rename.test.ts, mutation-verified (base 98/1 -> 99/0). Both callers unaffected (rename move:true/no-exclude; supersede move:false). Suite 1859/0. Info: the fixed branch is engine-API-only today (no production caller hits it) — contract hardening. |

## Not queued — needs a human / blocked

- LORE-42: `lore mcp server` — deferred v2 roadmap item, product/scope decision, not agent-resolvable now.
- LORE-43: Confluence one-way publish adapter — deferred, needs a live Confluence target + product decision.
- LORE-44: Confluence production mirror — deferred, same as LORE-43.
- LORE-45: Typed importable library build (.d.ts + subpath exports) — deferred, gated on the ECK-alignment decision to keep lore a standalone CLI (importable library deferred).
- **docker-e2e branch protection (repo setting — needs repo admin; wave-4 integration finding, low):** LORE-100 wired the docker-e2e job into ci.yml (it fails the workflow run on any red scenario), but marking it a *required status check* is a GitHub branch-protection setting on `dev`/`main` that no workflow diff can deliver and that an agent should not change autonomously (it's outward-facing config affecting how everyone's PRs merge). Until it's set, the gate does not block GitHub-side merges. Narrow in practice — the campaign's `--admin` squash-merge flow bypasses required checks anyway. **User action:** either enable `docker-e2e` as a required check in repo branch-protection settings, or record a decision that it's moot.

(All 78 medium findings were assessed as agent-resolvable at creation time; if one turns out not
to be, Fable escalates it and the orchestrator moves it here per the lifecycle's mid-flight
fallback.)

## Wave log

- 2026-07-22 — re-init (wave 0, no code): migrated this tracker from the round-1 cursor format to
  the wave-parallel skeleton (Frontier + Status/Wave columns + Wave log). No tasks worked; queue
  unchanged (78 To Do, LORE-96..173). Verified zero formal deps across all 78 via YAML parse.
- 2026-07-22 — **wave 1 COMPLETE** (issues: LORE-96, 98, 102, 107, 110, 114; workers: Sonnet 5,
  reviewer: Fable 5). One task per distinct cluster, file-disjoint (adapter-backlog, build-ci-config,
  build-runtime, cli-entry-state, cmd-check, cmd-crud-a). Worktrees created @ base `d6abe3b` under
  lore.worktrees/; dispatch marked at `d0c5857`. **All 6 approved by Fable on the first pass (0 fix
  rounds, 0 escalations)** — see Resolved table for per-task evidence, PRs, and merged SHAs. Merged
  serially (rebase onto moving dev, mandatory re-verify each: typecheck + full `bun test`, all
  clean) as PRs #92–#97; final integrated dev suite **1712 pass / 0 fail**, typecheck clean. Base
  `d6abe3b` → dev `5476b15`.
  - **Wave-level integration review (Fable):** overall SAFE, no blocking findings. Three findings:
    (1) *minor* — LORE-114 fixed only the `--out` path; the default title-slug path in
    resolveDocPath still creates reserved index/log stems (`lore new reference "Index"` → exit 0).
    Outside LORE-114's `--out` charter → **filed as LORE-174** (cmd-crud-a). (2) *nit* — LORE-107
    shifts two machine help contracts (unknown-cmd `--help` now exit 3; `--help --json` emits
    `help.manifest`); verified NO in-repo consumer (ci.yml, Dockerfile, run-e2e.sh) breaks. (3)
    *nit* — LORE-98/LORE-102 pin mkdocs at different strictness (Dockerfile exact vs ci.yml range);
    documented/intentional, flagged for awareness only. Per-task nits (probe-before-validate in
    LORE-96; URL-cap-vs-deadline tradeoff in LORE-110) recorded in PR bodies, none actioned.
- 2026-07-22 — wave 2 dispatched (issues: LORE-97, 99, 103, 108, 111, 115; workers: Sonnet 5,
  reviewer: Fable 5). One per distinct cluster, file-disjoint (adapter-backlog, build-ci-config,
  build-runtime, cli-entry-state, cmd-check, cmd-crud-a). Worktrees created @ base `19a3705` under
  lore.worktrees/. Settlement entry to follow.
- 2026-07-22 — **wave 2 COMPLETE** (issues: LORE-97, 99, 103, 108, 111, 115; workers: Sonnet 5,
  reviewer: Fable 5). One per distinct cluster, file-disjoint. Worktrees @ base `19a3705`; dispatch
  marked at `f78bb32`. **All 6 approved by Fable** (LORE-115 after 1 fix round; the rest 0). Merged
  serially as PRs #98–#103 (rebase onto moving dev, mandatory re-verify each: typecheck + full
  `bun test`, all clean; bash -n for LORE-103's shell). Base `19a3705` → dev `aff6f95`; final
  integrated suite **1718 pass / 0 fail**, typecheck clean.
  - **Resolved-by-merge duplicates closed this session** (orchestrator verified against merged dev,
    marked Done with evidence, no re-fix): LORE-127 + LORE-131 (by LORE-107, wave 1) and LORE-173
    (by LORE-115, wave 2). See Resolved rows 13–15.
  - **Wave-level integration review (Fable):** overall SAFE, no blocking/major cross-task defects;
    the LORE-111 mapWithConcurrency relocation verified clean (one definition, all callers updated,
    no import cycle). Findings: (1) *minor* — LORE-108's denied error omits errno `code` from its
    structured `input` (diverges from errors.ts's denied contract) + stale loadConfig docstring →
    **filed LORE-175** (cli-entry-state, low). (2) *nit* — LORE-99 left a dead `names` setup output
    in release.yml (zero consumers, harmless); not tasked — recorded here for a future cleanup.
- 2026-07-22 — wave 3 dispatched (issues: LORE-100, 109, 112, 116, 122, 126; workers: Sonnet 5,
  reviewer: Fable 5). One per distinct cluster, file-disjoint (build-ci-config, cli-entry-state,
  cmd-check, cmd-crud-a, cmd-meta-a, cmd-meta-b). Deferred this wave on file-conflict: LORE-104 (↔100,
  docker/e2e exit-code story), LORE-117 (↔116, src/commands/fswrite.ts), LORE-121 (↔109, src/state.ts).
  Worktrees created @ base `dda01bb` under lore.worktrees/. Settlement entry to follow.
- 2026-07-22 — **wave 3 COMPLETE** (issues dispatched: LORE-100, 109, 112, 116, 122, 126; workers:
  Sonnet 5, reviewer: Fable 5). One per distinct cluster, file-disjoint (build-ci-config, cli-entry-state,
  cmd-check, cmd-crud-a, cmd-meta-a, cmd-meta-b). Worktrees @ base `dda01bb`; dispatch marked at `cbf5d33`.
  **5 of 6 merged, all Fable-approved** (LORE-109/112/122 first pass; LORE-116 after 1 fix; LORE-126 after
  2 fix): PRs #104-#108, merged serially (rebase onto moving dev + mandatory re-verify each: typecheck +
  full `bun test`, all clean). Base `dda01bb` → dev `cec7d4e`; final integrated suite **1729 pass / 0 fail**,
  typecheck clean. Deferred this wave on file-conflict (to a later wave): LORE-104 (↔100, docker/e2e
  exit-code), LORE-117 (↔116, src/commands/fswrite.ts), LORE-121 (↔109, src/state.ts).
  - **LORE-100 ESCALATED (Fable, human_needed) — HELD, NOT merged.** The diff is approval-quality (all 3
    ACs independently confirmed; PUID/PGID + timeout fixes verified end-to-end via a real local Docker run;
    scope exact) but merging installs a *required* docker-e2e CI gate that is **red on every run**: two
    independent agents reproduced 298 pass / 1 fail because `docker/e2e/run-e2e.sh:1298` asserts 'lore check
    is NOT profile-bearing', contradicting LORE-89's intentional profile-aware check.ts
    (`src/commands/check.ts:47,142` loadProfile). The fix lives in run-e2e.sh, which LORE-100 is scoped not
    to touch. Disposition: branch `feature/LORE-100` @ `89f8133` kept pushed + worktree preserved; LORE-100
    set In Progress, dep LORE-176; **filed LORE-176** (fix the stale run-e2e.sh assertion). Merging LORE-100
    additionally needs a **user decision**: whether to wire a Docker-based CI gate at all (adds a ~15-45 min
    Docker job to every PR). Session STOPS after this wave per the escalation rule (R4j).
  - **Wave-level integration review (Fable): SAFE**, independently re-ran 1729/0, no blocking cross-task
    defects. Traced LORE-112↔LORE-122: a viewTask id-mismatch is never swallowed, correctly marks the check
    report `complete:false`, and throws through LORE-112's already-handled path. Findings: (1) *minor* —
    LORE-122's id-guard covers only resolveTaskDetails; sibling viewTask consumers stay unguarded:
    resolveRollup (already queued as LORE-125) and lore link (link.ts:180/212/346) → **filed LORE-177** for
    the link.ts half. (2) *info* — LORE-116's fswrite.ts hunks are docstring-only (benign; no conflict for
    deferred LORE-117).
  - **USER DECISION (2026-07-22): WIRE THE GATE.** The user approved making docker/e2e a CI gate. Plan for the next restore: (1) land LORE-176 (fix the run-e2e.sh:1298 stale assertion), (2) verify the full harness green via a real `docker compose -f docker/e2e/docker-compose.yml up --build --exit-code-from e2e` run (0 failed scenarios), (3) rebase + re-verify + merge the held `feature/LORE-100`; then branch protection can mark docker-e2e a required check. Do NOT re-ask this decision.
- 2026-07-22 — **wave 4 COMPLETE** (issues: LORE-176, then the held LORE-100; worker: Sonnet 5, reviewer: Fable 5). This wave executed the wave-3 USER DECISION (wire the docker-e2e gate): land LORE-176 to fix the stale run-e2e.sh assertion, then merge the held LORE-100. Base `d90fa80` → dev `995a02b`.
  - **LORE-176** (build-runtime) — Sonnet rewrote the stale run-e2e.sh AC4 assertion as a `step_fail … 6 '.error_type=="validation"' -- lore check --json` (encoding LORE-89's profile-aware fail-loud contract); removed the dead baseline-capture lines + false comment. Fable **approve** (0 fix; independently verified the loadProfile→validation→exit-6→stderr-envelope chain and non-tautology via step_fail's 3 conditions). **Orchestrator docker gate** (the AC#2 harness run, kept orchestrator-side to avoid `e2e-e2e` image/container collision with a concurrent run): `docker compose … up --build --exit-code-from e2e` → **299 passed / 0 failed, exit 0** (was 298/1 with the stale assertion). Merged PR #109 @ `0fd3a98`. AC#2 ticked post-gate.
  - **LORE-100** (build-ci-config, HELD from wave 3) — NOT re-implemented. Rebased `feature/LORE-100` onto dev+LORE-176: ci.yml applied clean (byte-identical to the wave-3 Fable-approved diff → that approval carried; the only merge-blocker, the red gate, was cleared by LORE-176), one lore-100 task-file metadata conflict resolved to Done. Mandatory post-rebase re-verify = **decisive docker gate on the exact merged bytes: 299 passed / 0 failed, exit 0** (gate green-at-birth, no longer red-at-birth); actionlint clean. Merged PR #110 @ `995a02b`. Worktree + local/remote branches cleaned up (the known `gh pr merge --delete-branch` remote no-op handled via explicit `git push origin --delete`).
  - **Wave-level integration review (Fable): findings_present (2 low; 0 blocking/high/medium).** All deep seams verified clean: `--exit-code-from e2e` matches the sole compose service; the report.jsonl path chain (run-e2e.sh `$REPORT=/results/report.jsonl` ↔ compose `./results:/results` ↔ ci.yml `docker-e2e-report` artifact) is consistent; the PUID/PGID chain (ci.yml env → compose build args → Dockerfile ARG + bun-user remap) has no missing declaration; no other stale profile-blindness premise among the ~20 `lore check` sites in run-e2e.sh (the malformed-profile window holds exactly the four new/validate/sync/check step_fail probes); task hygiene consistent. Findings: (1) *low* — docker-e2e **branch protection** never applied (repo admin setting, agent-out-of-scope) → moved to "Not queued — needs a human"; narrow because `--admin` merges bypass it. (2) *low* — the **runbook** docs/runbooks/docker-e2e-testing-environment.md still reads local-only → **filed LORE-178** (docs-only via lore).
  - **Session STOPS after wave 4** (clean between-wave context checkpoint per R4j — not an escalation). The restore's primary objective — execute the user's wire-the-gate decision and merge the held LORE-100 — is complete, and the gate is proven green-at-birth. The 60 remaining medium To-Do items are a fresh normal-wave undertaking best dispatched with a clean context budget; the next restore recomputes the ready set live and drains them.
- 2026-07-22 — **wave 5 COMPLETE** (issues: LORE-101, 104, 113, 117, 121, 123; workers: Sonnet 5, reviewer: Fable 5). One per distinct cluster, file-disjoint (build-ci-config, build-runtime, cmd-check, cmd-crud-b, cmd-link, cmd-meta-a). Worktrees @ base `befb34e`; dispatch marked at `882ae84`. **All 6 approved by Fable on the FIRST pass (0 fix rounds, 0 escalations).** Merged serially in queue order (rebase onto moving dev + mandatory post-rebase re-verify each: typecheck + full `bun test`, all clean — 1731→1738 as tests accreted; LORE-101 via lore check 38/0 + npm dry-run; LORE-104 via `compose config -q` + throwaway `--exit-code-from` proof) as PRs #111–#116. Base `befb34e` → dev `52e3368`. See Resolved rows 23–28 for per-task evidence + merged SHAs.
  - **R4b file-citation note:** LORE-117 (fswrite.ts writeFileAtomic) and LORE-123 (schema.ts) were kept disjoint by scoping LORE-123 to the EXISTING `writeFileNoFollow` seam (a call-site swap in schema.ts, not an fswrite.ts edit). Confirmed correct at integration review. LORE-121's citation budgeted src/state.ts but the merged fix never touched it — link.ts reused the existing commitBacklogFiles seam (the better outcome).
  - **Wave-level integration review (Fable): SAFE**, independently re-ran dev HEAD **1738 pass / 0 fail**, typecheck + biome clean (4 pre-existing info diagnostics in untouched files). The fswrite.ts↔schema.ts seam **composes cleanly** — writeFileNoFollow byte-identical to base (LORE-117 confined to writeFileAtomic), the schema.ts swap a true drop-in, error contracts (EACCES→denied exit 4; ELOOP→conflict exit 5) intact. Cross-task hazard sweep clean (no stale caller of a renamed path; normalizeBundleLabel the sole label normalizer; link.ts detail.file push type-safe with the LORE-49 `:(literal)` invariant). Scope confirmed — exactly the six file-sets + campaign bookkeeping, no drive-bys. Findings: (1) *low, doc-only* — writeFileNoFollow's docstring still named writeAllOrRollback its only caller → **fixed directly** as wave-5 housekeeping (dev `41cf7d8`, comment-only). (2) *nit* — check.ts test-exports driftFindingsForBundle (matches the existing isDocsRoot test-export pattern; acceptable).
  - **Per-task low findings recorded (non-blocking):** LORE-117 chmod-then-chown can clear setuid/setgid on a successful chown (exotic for a docs bundle; ACs cover permission bits only). LORE-123 leaf-symlink refusal is **exit 5 (conflict)**, not the AC parenthetical's "denied" — reviewer judged exit 5 the codebase-consistent choice (matches LORE-93). LORE-121's same-class no-op-on-dirty gap in runUnlink/moveBackRefs → **filed LORE-179** (cmd-link).
  - **Session continues** past wave 5 (clean between-wave checkpoint per R4j — no escalations, no human_needed).

- 2026-07-22 — **wave 6 COMPLETE** (issues: LORE-105, 118, 124, 128, 132, 133; workers: Sonnet 5, reviewer: Fable 5). One per distinct cluster, file-disjoint (build-runtime, cmd-crud-b, cmd-meta-a, cmd-meta-c, cmd-rename-supersede, core-bundle-check). Worktrees @ base `811c513`; dispatch marked at `56ef42b`. **All 6 approved by Fable on the FIRST pass (0 fix rounds, 0 escalations).** Merged serially in queue order (rebase onto moving dev + mandatory post-rebase re-verify each: typecheck + full `bun test`, all clean — 1740→1748 as tests accreted; LORE-105 via `bash -n` + isolated JSONL proof) as PRs #117–#122. Base `811c513` → dev `b67b273`. See Resolved rows 29–34 for per-task evidence + merged SHAs.
  - **R4b file-citation note:** LORE-132's fix landed in fswrite.ts `moveFile` (as anticipated) + rename.ts; LORE-128 fit entirely in agents.ts (core/agent-bridge.ts ended up untouched — better outcome). No conflict materialised.
  - **Wave-level integration review (Fable): SAFE**, independently re-ran dev HEAD **1748 pass / 0 fail**, typecheck + biome clean (same 4 pre-existing info diagnostics in untouched files). Seams verified: (a) `moveFile` has EXACTLY ONE caller (rename.ts:314) — `lore supersede` imports only writeFileOverwriting (no-move by design), so LORE-132's `assertMoveTargetSafe` guard cannot regress any command outside rename; same-inode case-only renames pass. (b) The rewrite.ts↔bundle.ts `/`-absolute divergence is **strictly pre-existing** (git diff empty on rewrite.ts); LORE-133 aligned bundle.ts with check.ts leaving rewrite.ts the sole divergent resolver; rename's `/`-absolute link-rewrite output is byte-identical pre/post wave; no destructive interaction with LORE-132. (c) query.ts's duplicated stripAnsiAndControls is byte-identical to output.ts's. Scope confirmed — exactly the six file-sets + campaign bookkeeping, no drive-bys, no stale cross-file callers. Only finding: 1 *nit* (the acknowledged sanitizer duplication — already filed LORE-181).
  - **Follow-ups filed this wave (orchestrator, sequential mint):** [[LORE-180]] (core-rewrite-engine, **medium** — rewrite.ts newDestPathFor leading-slash divergence; conflicts w/ LORE-164/165), [[LORE-181]] (cmd-crud-b, low — shared stripAnsiAndControls seam / de-dup), [[LORE-182]] (cmd-meta-a, low — retain isAbsolute(rel) for win32 cross-drive --out).
  - **Session STOPS after wave 6** (clean between-wave context checkpoint per R4j — no escalations, no human_needed). Two full waves (12 tasks) drained this session. The 53 remaining To-Do items are recomputed live at the next restore.
- 2026-07-22 — **wave 7 DISPATCHED** (issues: LORE-106, 175, 174, 177, 119, 125; workers: Sonnet 5, reviewer: Fable 5). One per distinct cluster, file-disjoint (build-runtime, cli-entry-state, cmd-crud-a, cmd-link, cmd-crud-b, cmd-meta-a). Live YAML re-parse: all 53 To-Do carry zero formal deps (only deferred LORE-42..45 in "Not queued" carry deps); ready set = all 53, gated purely by the file-conflict graph. R4b file-citation — edit targets are `test/support/record-backlog-goldens.ts` (106), `src/config.ts` (175), `src/commands/new.ts` (174), `src/commands/link.ts` (177), `src/commands/sync.ts` (119), `src/commands/tasks.ts` (125): six disjoint files; `reconcile-shared.ts` + `args.ts` referenced read-only by several, edited by none. Worktrees @ base `a899109` under lore.worktrees/. Settlement entry to follow.
- 2026-07-22 — **wave 7 COMPLETE** (issues: LORE-106, 175, 174, 177, 119, 125; workers: Sonnet 5, reviewer: Fable 5). One per distinct cluster, file-disjoint (build-runtime, cli-entry-state, cmd-crud-a, cmd-link, cmd-crud-b, cmd-meta-a). Worktrees @ base `2daf649` (dev incl. the wave-7 dispatch-mark; pinned so all 6 fork identically); dispatch marked at `2daf649`. **All 6 approved by Fable on the FIRST pass (0 fix rounds, 0 escalations).** Merged serially in queue order (rebase onto moving dev + mandatory post-rebase re-verify each: typecheck + full `bun test`, all clean — 1765→1771 as tests accreted) as PRs #123–#128. Base `2daf649` → dev `723d458`. See Resolved rows 35–40 for per-task evidence + merged SHAs.
  - **R4b file-citation note:** all six edit-targets landed exactly as cited (record-backlog-goldens.ts, config.ts, new.ts, link.ts, sync.ts, tasks.ts) — integration review confirmed 18 changed files, each appearing once, no shared-module collision; args.ts + reconcile-shared.ts + errors.ts had zero diff (read-only references as planned). LORE-177 and LORE-125 independently re-implemented LORE-122's id-check (see integration finding).
  - **Wave-level integration review (Fable): FINDINGS_PRESENT (no blocking, no high; 1 medium follow-up filed, rest info).** Independently re-ran dev HEAD **1771 pass / 0 fail**, typecheck + biome clean (same 4 pre-existing info diagnostics in untouched files — managed-block.ts/test, supersede.test.ts). Verified: (a) the THREE id-mismatch guards (reconcile-shared.ts:245 resolveTaskDetails / link.ts:511 verifiedViewTask / tasks.ts:170 inline) are **byte-consistent today** — identical comparison, LoreError('not_found'), message, hint, {taskId,resolvedId} input; exit-code differences (tasks.ts direct exit 3; link.ts allSettled rethrows as-is; reconcile-shared stores ok:false per its batch contract) are deliberate/pre-existing, not a wave defect. (b) LORE-175's denied `code` is guaranteed defined inside the EACCES/EPERM gate → matches errors.ts ioError; errors.ts untouched. (c) No renames/stale callers; new symbols are module-private or test-support-only (nothing in src imports test/support). (d) No scope creep — exactly the 6 file-sets + their 6 task files.
  - **Medium finding → filed [[LORE-183]] (cmd-link):** integration review upgraded the recorded LORE-177 low ("doc comment overstates coverage") to medium after confirming **moveBackRefs (link.ts:~432) is a genuine 4th UNGUARDED viewTask consumer** — a write-path gap: it uses a returned detail's labels/documentation to compute an editTask write under the requested id during `lore rename`, exactly the hazard class LORE-177/122 guard against; outside LORE-177's cited scope (180/212/346). LORE-183 covers wiring moveBackRefs through the guard + hoisting ONE shared id-mismatch helper (killing the 3-copy triplication) + fixing the stale "every viewTask consumer" doc comment + a rename-path mismatch test. Not a wave-7 regression (pre-existing gap).
  - **Session STOPS after wave 7** (clean between-wave context checkpoint per R4j — no escalations, no human_needed). One full wave (6 tasks) drained this session. The 48 remaining To-Do items are recomputed live at the next restore.
- 2026-07-22 — **wave 8 DISPATCHED** (issues: LORE-120, 129, 134, 135, 139, 141; workers: Sonnet 5, reviewer: Fable 5). File-disjoint across 6 clusters (cmd-crud-b, cmd-meta-c, core-bundle-check ×3, core-concept-manifest). Live YAML re-parse: all 48 To-Do carry zero formal deps (only deferred LORE-42..45 in "Not queued"); ready set gated purely by the file-conflict graph. R4b file-citation edit targets: sync.ts+fswrite.ts (120), agents.ts+agent-bridge.ts (129), bundle.ts (134), check.ts (135), new.ts+profile.ts (139), concept.ts (141) — six disjoint file-sets. Worktrees @ base `08846c0` under lore.worktrees/; dispatch marked at `08846c0` (pushed to origin/dev so all 6 fork identically).
- 2026-07-22 — **wave 8 COMPLETE** (issues: LORE-120, 129, 134, 135, 139, 141). **All 6 approved by Fable on the FIRST pass (0 fix rounds, 0 escalations).** Merged serially in queue order (rebase onto moving dev + mandatory post-rebase re-verify each: typecheck + full `bun test`, all clean — 1776→1794 as tests accreted) as PRs #129–#134. Base `08846c0` → dev `e34d9c2`. See Resolved rows 41–46 for per-task evidence + merged SHAs.
  - **R4b file-citation note:** all six edit-targets landed exactly as cited; cumulative wave diff = 22 files (+886/-72), each source file appearing under exactly one task's cluster, no shared-module collision.
  - **Wave-level integration review (Fable): FINDINGS_PRESENT (no blocking, no high; 1 medium + 1 low follow-up filed, 1 info).** Independently re-ran dev HEAD **1794 pass / 0 fail**, typecheck clean, `lore check` 38/0/0. Explicitly traced the directed cross-consumer checks and cleared them: LORE-141's stricter frontmatter throw is absorbed by check.ts's per-file try/catch + core/validate.ts's documented throw→finding contract (no leak into the link/anchor pass); LORE-135's anchor comparator is unique (no drift); LORE-129's widened planBridge input has exactly one caller (typecheck-proven); LORE-120's captured before-bytes are exact raw disk bytes (rollback truly restores).
  - **Medium finding → filed [[LORE-184]] (core-bundle-check):** LORE-134's path-first flip makes lore's OWN canonical bare-id ref form (written by rewrite.ts remapRefItem + supersede.ts, both untouched by the task) shadowable when a mirroring directory exists — reviewer repro'd resolveRef('adr/old','notes',{…,notes/adr/old}) → shadowed; real consequences at rename (silent ref-text corruption of the referring file), supersede dedup, and graph edges. Needs ref-shape disambiguation (design), not a revert (the flip is a legit fix; dev green). NOT a regression in the merged code's own tests — a latent tension the flip exposed.
  - **Low finding → filed [[LORE-185]] (core-bundle-check):** LORE-139 added a 2nd template-confinement guard (profile.ts) beside the pre-existing new.ts one — edge-divergent duplication + stale readTemplateFile comment + profile-path symlink asymmetry. Extract one shared guard.
  - **Info (not filed):** LORE-120 added a 2nd multi-file rollback primitive (writeManyAtomicOrRollback) beside scaffold's writeAllOrRollback in fswrite.ts — disciplines genuinely differ (before-bytes+atomic-rename vs never-clobber+writeFileNoFollow), both defensible; rename.ts/replace.ts docstrings still call cross-file rollback a single "deferred shared concern" — cosmetic doc drift for a future consolidation, noted only.
- 2026-07-22 — **wave 9 DISPATCHED** (issues: LORE-130, 136, 142, 145, 148, 154; workers: Sonnet 5, reviewer: Fable 5). File-disjoint across 6 clusters (cmd-meta-c, core-bundle-check, core-engine-a, core-engine-b, core-index-context, core-managed-template). Live YAML re-parse: all 44 To-Do carry zero formal deps (only deferred LORE-42..45 carry deps). R4b file-citation edit targets: fswrite.ts (130), check.ts+bundle.ts (136), agent-bridge.ts+cli.ts (142), graph.ts (145), context.ts (148), managed-block.ts (154) — six disjoint file-sets. Worktrees @ base `cde75d1` under lore.worktrees/; dispatch marked + pushed at `cde75d1`.
- 2026-07-22 — **wave 9 COMPLETE** (issues: LORE-130, 136, 142, 145, 148, 154). **All 6 approved by Fable (LORE-136 + LORE-145 each took 1 fix round; the other 4 first-pass; 0 escalations).** Merged serially in queue order (rebase onto moving dev + mandatory post-rebase re-verify each: typecheck + full `bun test`, all clean — 1800→1809) as PRs #135–#140. Base `cde75d1` → dev `a1fd570`. See Resolved rows 47–52 for per-task evidence + merged SHAs.
  - **Two mis-framed task premises corrected by the review loop (the adversarial gate working as designed):** LORE-136 — the premise "heading slug ignores image alt text" was wrong; GitHub itself excludes alt text (empty

- 2026-07-22 — **wave 11 COMPLETE** (issues: LORE-138, 144, 147, 150, 151, 156). **All 6 approved by Fable on the FIRST pass (0 fix rounds, 0 escalations).** Merged serially in queue order (rebase onto moving dev + mandatory post-rebase re-verify each: typecheck + full `bun test`, all clean — 1820→1844 as tests accreted) as PRs #147–#152. Base `43c9415` → dev `048470c`. See Resolved rows 59–64 for per-task evidence + merged SHAs.
  - **R4b file-citation note:** cumulative wave diff = 21 files (8 src + 7 test + 6 backlog task files), +752/-73; each src file (check.ts / scaffold.ts+validate.ts / instructions.ts / indexes.ts+sync.ts / links.ts / managed-block.ts) under exactly one cluster, no shared-module collision. decodeTarget confirmed single-definition in links.ts (callers only import) so LORE-151 stayed disjoint from LORE-138's check.ts.
  - **Wave-level integration review (Fable): FINDINGS_PRESENT (no blocking/high; 1 medium + 2 low + 2 info).** Independently re-ran dev HEAD 1844/0, typecheck clean, `lore check` 38/0/0. Behavior composes cleanly (traced: 138's re-throw surfaces as an exit-1 `uncaught` envelope that 147's prose already reserves; 151's per-segment decode vs 138/144 check.ts-path changes are disjoint; 156's new throw already existed for same-line pairs via findMarkers on the check/sync paths → no new failure class there, only clearer messages; liveIndexDirs provably equivalent to the old inline derivation; no import cycle; no stale callers).
    - **Medium → filed [[LORE-190]] and FIXED same session (PR #153, dev @ `c11ef0e`, Resolved row 65):** LORE-147's new check-topic prose mis-attributed the `validation` throw to reconcile-config only, while LORE-156 (same wave) widened the marker-corruption `validation` path reachable from `lore check`; also the collapsed same-line shape was missing from the SYNC topic + check.ts docstring, and SYNC --json omitted LORE-150's orphanedIndexes. Fable re-review caught the fix's own first-pass over-correction (a falsely-exhaustive claim) → 1 fix round → a non-exhaustive, code-verified 4-cause enumeration.
    - **Low → filed [[LORE-191]]:** LORE-138's scan-phase re-throw can lose discovery advisories flushed after checkBundles (commands/check.ts) — reorder the flush after collectBundles + add a test.
    - **Info (not filed):** new.ts RESERVED_ROOT_INDEX duplicates the now-exported ROOT_INDEX_PATH (pre-existing; overlaps LORE-185's consolidation area).
  - **Per-task review medium residual → filed [[LORE-192]]:** LORE-144's profile-asymmetry fix covers `lore validate` only; the same hole survives at the loadBundle seam (`lore graph` empirically exits 6 on a fresh scaffold under a custom Reference-required profile; loadBundle's path space is bundle-relative "index.md", not repo-relative "docs/index.md").
  - **Session STOPS after wave 11 + its integration follow-up** (clean between-wave context checkpoint per R4j — no escalations, no human_needed). One full wave (6 tasks) + 1 integration-fix (LORE-190) drained this session; 3 follow-ups filed (LORE-190 fixed same-session, LORE-191/192 queued). The 32 remaining To-Do items are recomputed live at the next restore.

- 2026-07-22 — **wave 12 COMPLETE** (issues: LORE-140, 152, 157, 159, 162, 164; workers: Sonnet 5, reviewer: Fable 5). File-disjoint across 6 clusters (core-bundle-check, core-links-resolution, core-managed-template, core-query-validate, core-replace, core-rewrite-engine). Live YAML re-parse: all 32 in-scope To-Do carry zero formal deps (only deferred LORE-42..45 in "Not queued"). R4b file-citation edit targets: profile.ts (140), links.ts (152), template.ts (157), validate.ts (159), **core/replace.ts** (162), rewrite.ts (164) — six disjoint file-sets. Citation-drift caught pre-dispatch: LORE-162's applyReplacement is src/core/replace.ts (NOT commands/replace.ts's runReplace); LORE-164's rewriteInbound is defined in rewrite.ts (only referenced from links.ts/supersede.ts/rename.ts). Worktrees @ base `6e55e67`; dispatch marked + pushed at `6e55e67`. **All 6 approved by Fable on the FIRST pass (0 fix rounds, 0 escalations).** Merged serially in queue order (rebase onto moving dev + mandatory post-rebase re-verify each: typecheck + full `bun test`, all clean — 1846->1859 as tests accreted) as PRs #154–#159. Base `6e55e67` -> dev `bd27700`. See Resolved rows 66–71.
  - **Cross-worktree git-stash hazard (process finding, no code impact):** the LORE-140 and LORE-159 workers each used `git stash` for a mutation-check revert; `refs/stash` is repo-wide (shared across all worktrees of one repo), so their uncommitted diffs transiently swapped between worktrees. Both self-recovered losslessly; the orchestrator independently verified `git stash list` empty + each branch's `BASE...HEAD` diff touches only its own 3 files before merging. Now in auto-memory: forbid `git stash` in worker prompts; use `git diff > patch` + `git apply -R`/`apply` (or a file-copy revert) instead.
  - **Wave-level integration review (Fable): FINDINGS_PRESENT (no blocking/high/medium; 1 low + 5 info).** Independently re-ran dev HEAD **1859 pass / 0 fail**, typecheck clean, `lore check docs` 38/0/0. Semantic file-disjointness held: no shared-helper conflict, no contradictory sibling predicate, no caller/callee contract mismatch. Traced clean: the two heading predicates (validate.ts h2Headings root-only for required sections vs check.ts extractHeadingSlugs full-tree for anchors) intentionally differ and must NOT be unified; LORE-162's newly-reachable mid-batch throw is absorbed by commands/replace.ts's plan-then-write two-phase design (zero bytes on error); all 4 new symbols (assertNonEmptyEnum/KNOWN_ASSET_EXTENSIONS/PLACEHOLDER_NAME/assertNoInjectedMarker) module-local, no export drift; KNOWN_ASSET_EXTENSIONS is the tree's only asset allowlist.
    - **Low -> filed [[LORE-194]]:** assertNoInjectedMarker's guarantee rests on an undocumented "locator throws on duplicate" contract (MANAGED_REGION_LOCATORS) — document + pin it so a future first-span-returning locator can't silently reopen LORE-162.
  - **Per-task medium residual -> filed [[LORE-193]]:** LORE-140's assertNonEmptyEnum covers scalar fields (parseFieldSpec) only; the same empty-enum hole survives at parseItems/itemToZod for list fields (`items = { enum = [] }` -> z.array(z.enum([]))) — confirmed real+unfixed by the integration review. One-call fix (reuse assertNonEmptyEnum in parseItems).
  - **Note — tracker truncation incident (recovered):** the wave-12 dispatch-mark write (commit `6e55e67`) accidentally truncated this doc 361->340 lines (the wave-8..11 log tail), because `backlog doc view | tail` truncated the ~89KB read on the read side. Recovered at settlement by rebuilding the body from `git show 6d8a076:<tracker>` (complete). Lesson: build tracker updates from a git-extracted or size-verified full body, never a piped `backlog doc view` that may truncate; verify the on-disk line count after every write.
  - **Session STOPS after wave 12** (clean between-wave context checkpoint per R4j — no escalations, no human_needed). One full wave (6 tasks) drained + 2 follow-ups filed (LORE-193 medium, LORE-194 low). The remaining To-Do items are recomputed live at the next restore.
