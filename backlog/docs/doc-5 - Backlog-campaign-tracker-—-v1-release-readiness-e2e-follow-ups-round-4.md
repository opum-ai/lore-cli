---
id: doc-5
title: Backlog campaign tracker — v1 release readiness & e2e follow-ups (round 4)
type: other
created_date: '2026-07-25 02:15'
updated_date: '2026-07-25 20:20'
---
# Backlog campaign tracker — v1 release readiness & e2e follow-ups

Round 4 of the lore backlog campaign. Round 1 (doc-1, LORE-69..95, 20 high), round 2 (doc-3, LORE-96..194, 78 medium), round 3 (doc-4, LORE-198..250, 55 low) closed the entire Codex second-opinion review (doc-2) and are all Done. This round covers the **v1-release-readiness follow-ups + e2e-testing findings** filed 2026-07-24 (LORE-253..263) — the residue after the review campaign, surfaced by shipping-readiness work, LORE-252 (Windows CI green), and a 56-concept/40-task Meridian e2e stress test.

Protocol: restore → compute the ready/conflict graph → mark the wave Dispatched → dispatch (parallel Sonnet implement + reviewer gate) → serialize the merge → update this tracker once more at settlement → loop until the queue is empty or blocked → write handover.

## Scope / order confirmation

- **Scope**: the 9 agent-resolvable follow-ups from LORE-253..263 (bug/enhancement polish + CI/release + the onboarding feature), plus LORE-264 filed mid-campaign. The 6 non-agent-resolvable items are in "Not queued" below.
- **Order**: user confirmed **"Lowest-risk first"** on 2026-07-24 — small bug/polish fixes → CI/release → the large onboarding feature (LORE-260) last. This is the wave-builder's **tie-break only**, NOT a strict execution order; the ready set is recomputed live each restore.
- All queued tasks carry **zero formal Backlog dependencies** (re-verified live 2026-07-25 by YAML parse); readiness is gated purely by the live pairwise file-conflict graph + the 6-worker wave cap.

## Frontier

**CAMPAIGN COMPLETE — 2026-07-25.** Every queued item is Done. `dev` @ `346d3c1`, clean, no worktrees, no feature branches, no open PRs. Suite 2126 → **2176 pass / 0 fail**; `lore check` 40 files 0/0; docker e2e **302 / 0**. Nothing remains in the queue; the 6 "Not queued" items below are blocked-upstream, needs-human, or deferred-v2, and three new tasks (LORE-265/266/267) were filed from review findings.

> **✅ BLOCKER 1 CLEARED (2026-07-25).** GitHub Actions runs again. The prior session's diagnosis was exactly right: run `30157878759` attempt 1 had its two longest jobs cancelled mid-flight while every short job succeeded, and attempts 2–3 started **zero** jobs with a generic "workflow file issue" — the signature of an Actions spending limit, not a code defect. A plain full `gh run rerun 30157878759` (attempt 4) started all 6 jobs and went green. **Lesson: a billing cutoff masquerades as a CI failure; read the per-attempt job list before blaming the diff, and note `gh run rerun --failed` is a no-op on *cancelled* jobs.**
>
> **⛔ BLOCKER 2 STILL OPEN — needs a human.** **Fable 5 remains over its monthly spend limit** (re-probed 2026-07-25 with a trivial agent; failed immediately). Waves 2 and 3, all their fix cycles, and all three integration reviews ran on **Opus** by the user's explicit choice. That preserved the property that matters — an independent adversarial reviewer that is not the implementer — but at materially higher cost than the campaign design assumes. **Resolve before opening a round 5.**

## Queue (confirmed order)

**Empty — every queued item is Done.** See Resolved below.

## Resolved
| # | Issue | Status/date/wave | Evidence summary |
|---|---|---|---|
| 1 | LORE-258 | Done · 2026-07-25 · wave 1 | PR [#243](https://github.com/jeremy-newhouse/lore/pull/243), merged `2f44ada`. Suppresses the `no frontmatter mapping` advisory for `RESERVED_STEMS` (index/log) at `loadBundle`'s single choke point in `src/core/bundle.ts`; `check`/`validate` needed no source change and are pinned by new tests. Fable **approve**, 0 fix cycles — all 3 ACs independently confirmed incl. a reverse-apply experiment proving the new tests bite. `bun test` 2072/0; live repro went 6 spurious warning lines → 0. |
| 3 | LORE-263 | Done · 2026-07-25 · wave 1 | PR [#244](https://github.com/jeremy-newhouse/lore/pull/244), merged `42ce1bc`. `lore scaffold <target>` idempotent when the on-disk generated config is byte-identical (exit 0 no-op); only a user-MODIFIED file conflicts (exit 5 + `--force` hint). New lstat-based, conservative `classifyExistingFile` in `fswrite.ts`. Fable **approve** after **2 fix cycles**. ⚠️ **Failed the required docker-e2e gate on its first PR run (298/1)** — the harness still asserted the old always-conflicts contract. Fix rewrote Phase 18 strictly *stronger* (299→302 assertions). |
| 5 | LORE-262 | Done · 2026-07-25 · wave 1 | PR [#245](https://github.com/jeremy-newhouse/lore/pull/245), merged `06890dc`. `rewriteInbound` now surfaces any inbound link whose display TEXT still names the OLD id after retargeting, via a new pure `RewritePlan.textMismatches` rendered as a stderr warning by `supersede` and `rename`. Decision: **report, don't skip** (skipping is unsafe for `rename`, which deletes the old file). Fable **approve**, 0 fix cycles; 11 new regression tests. |
| 7 | LORE-254 | Done · 2026-07-25 · wave 1 | PR [#246](https://github.com/jeremy-newhouse/lore/pull/246), merged `aec6fa5`. Daily `.github/workflows/upstream-backlog-watch.yml` drives `src/scripts/upstream-backlog-watch.ts`, which polls the upstream Releases API and **ancestor-checks** each candidate tag against commit `22a091b` (`identical`/`ahead`), distinguishing a genuinely `--json`-capable tag from a merely-newer one. Fable **approve** after **2 fix cycles** (pass 2 caught a blocking workflow-permissions defect). 13 new tests. **Unblocks LORE-253** once upstream tags. |
| — | wave-1 docs drift | Done · 2026-07-25 · wave 1 tail | PR [#247](https://github.com/jeremy-newhouse/lore/pull/247), merged `c20e696`. Fixed `docs/reference/cli-surface.md`'s stale pre-LORE-263 scaffold exit contract, swept the repo's prose for the same drift, and corrected a pre-existing README error (`lore scaffold agent` does not exist; `lore agents` emits SKILL.md). Fable **approve** after 1 fix cycle. Its prose sweep found the CHANGELOG gap → filed as LORE-264. |
| 2 | LORE-259 | Done · 2026-07-25 · wave 2 | PR [#248](https://github.com/jeremy-newhouse/lore/pull/248), merged `388fb42`. Harmonized error/usage/success phrasing: `link`/`unlink` `--plain` per-task line → `<task>: tasks: <status>, back-ref: <backRef>`; the shared `conceptNotInBundle` hint repointed from `lore check` (which only prints a pass/fail count) to `lore query`/`lore graph`; `tasks`/`context` missing-arg text aligned to `link`'s existing template. Exit codes and `--json` envelope shape unchanged. **Opus approve, 0 fix cycles.** Reviewer re-ran the docker e2e harness locally: **302 passed / 0 failed**; a mutation check reverting all 5 changed strings produced 9 failures, so every string is pinned. Deviated from the task description's own suggested `(doc)` wording — `link.ts`'s JSDoc showed the description had it backwards; recorded in the task. |
| 6 | LORE-256 | Done · 2026-07-25 · wave 2 | PR [#249](https://github.com/jeremy-newhouse/lore/pull/249), merged `0670e27`. New `renameOverDestination` helper wraps the commit `renameSync` in both `writeFileAtomic` and `writeFileNoFollow`: bounded 4 attempts, 20/40/80 ms `Bun.sleepSync` backoff, retrying **only** on errno `EPERM`/`EBUSY`/`EACCES` — gated on **errno, not `process.platform`**. LORE-231/117/130/92 invariants untouched. **Opus approve, 1 fix cycle** (pass 1 replaced a `Date.now()` busy-spin with `Bun.sleepSync`). Mutation check: reverting both call sites fails 4 of 6 new tests. **AC#4's windows-latest half was closed post-merge by PR #249's own green CI run** (`53628e9`) — it was unverifiable from a macOS worktree. |
| 8 | LORE-255 | Done · 2026-07-25 · wave 2 | PR [#250](https://github.com/jeremy-newhouse/lore/pull/250), merged `9a6b519`. First-release rehearsal (`npm publish --dry-run` across all 6 artifacts) + first-release checklist, **and** the real publish job: `workflow_dispatch`-only, `publish` input defaulting false, `if: inputs.publish == true`, `id-token: write` scoped to that job while workflow-level stays `contents: read`, npm `>= 11.5.1` floor asserted fail-closed, platform packages published before the root launcher, resumable, hard refusal to publish `0.0.0`. **Opus approve, 2 fix cycles.** ⚠️ **Residual risk recorded, needs a human decision** — see "Not queued" below. |
| 10 | LORE-264 | Done · 2026-07-25 · wave 2 | PR [#251](https://github.com/jeremy-newhouse/lore/pull/251), merged `650e1e7`. Backfilled the four wave-1 `[Unreleased]` CHANGELOG entries, each verified against the **real merged diffs on `origin/dev`** rather than the task text. **Opus approve, 1 fix cycle.** |
| — | wave-2 CHANGELOG + docstring | Done · 2026-07-25 · wave 2 tail | PR [#252](https://github.com/jeremy-newhouse/lore/pull/252), merged `d702ff6`. The wave-2 integration review's two findings. See the wave log below — this one is worth reading, because pass 1 caught four *factually false* CHANGELOG claims. |
| 4 | LORE-261 | Done · 2026-07-25 · wave 3 | PR [#253](https://github.com/jeremy-newhouse/lore/pull/253), merged `cac03fa`. `lore orphans` no longer flags a subtask whose parent task is linked. **AC#2 decision: orphans-side hierarchy awareness** (walk `parentTaskId`) over a link-side cascade — `parentTaskId` already rides the single `listTasks()` snapshot, so no extra subprocess call, and a cascade would go stale when a subtask is added later. New `hasOwnedAncestor`, visited-set guarded; every failure mode falls toward *still reported*. **Opus approve, 0 fix cycles** — an exceptional pass: the reviewer built its OWN 3-level fixture (grandchild included), ran **13 self-constructed adversarial cases** (3-cycles, rho-shapes, absent/self/empty `parentTaskId`, sibling and descendant exemption leaks), and proved contract stability on the real binary via a **10-invocation matrix diffed against `dev`** — identical exit codes, identical JSON key names *and order*. The key property holds structurally: the exemption predicate is a strict subset of the parent's own not-an-orphan predicate, so nothing itself reported as an orphan can exempt a descendant. Two doc-citation findings folded in pre-merge; the ADR-0009 drift behind them became **LORE-265**. |
| 9 | LORE-260 | Done · 2026-07-25 · wave 3 | PR [#254](https://github.com/jeremy-newhouse/lore/pull/254), merged `fd92b29`. **The round's largest task.** A bare `lore init` runs a TTY-gated wizard folding in the agent bridge, scaffolds, and a backlog capability check. `agents.ts`/`scaffold.ts` logic extracted into `applyAgentsBridge`/`applyScaffold`; new **ADR-0017**. **Opus approve after 2 review passes + 2 fix cycles — pass 1 found TWO BLOCKING defects, both reproduced live:** (1) the wizard gated on **stdin only** while all its UI goes to **stderr**, so `lore init >/dev/null 2>&1` — the exact idiom at `lore-setup.sh:93`, the prior art this task cites — **hung forever, invisibly**; (2) **Ctrl-D mid-wizard exited 0 with an empty stdout envelope** and a half-applied run, because `readline/promises`' `question()` never settles on EOF, so `process.exitCode` was never set and `finally { prompter.close() }` never ran. Fixed by gating on `stdinIsTTY && stderrIsTTY && !jsonRequested && !anyFlagGiven` (all defaulting **false**, fail-closed) and racing each question against the interface's `close` event, failing closed with exit 2. Pass 2 reproduced each original failure with its own pty/expect harness and confirmed the fixes end them — the EOF mutation makes the test **hang** rather than fail. It also verified the extraction is a true pure refactor (`agents.ts` differs from `dev` by exactly one `export`; `scaffold.ts` untouched), that LORE-93/76/263's symlink and idempotent-skip guards survived, and that bare-`init` stdout is **byte-identical to `dev`**. Worker ran the real docker e2e: **302 / 0**. Five low findings folded in without a third round. |
| — | wave-3 doc-gate propagation | Done · 2026-07-25 · wave 3 tail | PR [#255](https://github.com/jeremy-newhouse/lore/pull/255), merged `346d3c1`. LORE-260's round-2 gate widening reached `cli-surface.md`, ADR-0017's *body*, and `CHANGELOG.md` — but **three sites still said stdin-only**, leaving ADR-0017 **self-contradictory** (frontmatter `summary` vs. its own §2). Since that summary is the machine-readable digest lore surfaces via `query`/`context`/`graph`, it propagated further than ordinary prose — and it is precisely the mental model that produced the shipped hang. Fixed README, `agent-onboarding.md`, the ADR summary (tightened to **193 chars** to stay inside `lore sync`'s verified 200-char soft limit), plus two completeness gaps in `architecture.md`/`cli-contract.md`. **Opus approve.** Reviewer ran **three pty proofs including a positive control** (bare `lore init` on a full pty *does* wizard — ruling out a vacuously-off gate) and re-derived `docs/log.md` with the production `buildLog` path, proving it **byte-identical** with zero hand-authored lines. |

## Not queued — needs a human / blocked
- **LORE-253** (migrate adapter to the released --json backlog): BLOCKED on an external event — MrLesk/Backlog.md must tag a release >v1.48.0 containing commit `22a091b` (PR #790). Latest tag v1.48.0 (2026-07-12) predates it by 10 commits. **LORE-254 (Done) is now its live watch** — a daily job opens an `upstream-watch` issue when the qualifying tag appears.
- **LORE-257** (make windows-latest a required check): NEEDS-HUMAN (repo-admin) — toggling a branch-protection ruleset is a human action an autonomous agent must not self-authorize (same boundary as LORE-196). Agent role is prep/verify only.
- **LORE-255 residual security risk** (the task itself is Done; this is a follow-on decision): npm Trusted Publishing pins **repo + workflow filename, not a ref**. Because the publish job is reachable via `workflow_dispatch` on *any* ref, an actor with write access could push a branch carrying a modified `release.yml` (guards stripped) and dispatch it with `publish: true`. Documented, not fixed — mitigating it is a governance decision (restrict `workflow_dispatch` refs, or require an environment with reviewers).
- **LORE-42** (lore mcp server), **LORE-43** (Confluence publish adapter), **LORE-44** (Confluence prod mirror), **LORE-45** (typed importable library): DEFERRED-v2 — post-v1 roadmap requiring product/scope decisions + a release milestone.

### Filed during this campaign, not worked (candidates for a round 5)
- **LORE-265** (bug, low) — ADR-0009 §2 misdescribes how `lore orphans` finds unowned tasks: it claims a `backlog search --json` call that `orphans.ts` has never made, and omits the parent-chain clause LORE-261 added. Deliberately out of LORE-261's scope because fixing it properly also corrects pre-existing drift.
- **LORE-266** (bug, low) — the agent bridge's pre-write symlink sweep (`assertNoSymlinkInAnyPath`, the LORE-93 AC#5 invariant) has **zero test coverage**: deleting it fails no test, on `dev` as well. `ensureDir`'s reactive per-call guard masks it in the single-target case. Pre-existing; surfaced because LORE-260 refactored that path.
- **LORE-267** (bug, low) — `lore agents` renders a `protected` bridge file **green** while `lore init` now renders the same action **yellow**. `protected` means the bridge is stale and needs `--force`, so green reads as success. The divergence lives in `agents.ts`.

## Wave log
- 2026-07-24 — init: round-4 tracker created from LORE-253..263. 9 queued (lowest-risk-first, user-confirmed), 6 not queued.
- 2026-07-25 — **wave 1 COMPLETE** (LORE-258, 263, 262, 254; workers sonnet, reviewer fable; base `be730be`). All four approved and merged; 14 agents, 4 fix cycles total. No escalations.
  - **Gate lesson:** LORE-263 passed `bun test`/typecheck/lint/`lore check` *and* its unit review, then failed the **required docker-e2e gate**. That harness is a separate contract-test suite `bun test` does **not** cover. **Any task changing a user-visible CLI contract must run it locally before review.**
  - **Integration review** (Fable, `be730be..42ce1bc`): composition **sound**. One minor finding — `docs/reference/cli-surface.md` documented the pre-LORE-263 contract → fixed on `feature/wave1-docs-drift` (PR #247), whose prose sweep found the CHANGELOG gap → LORE-264.
- 2026-07-25 — **wave 2 DISPATCHED** (LORE-259, 256, 255, 264; base `42d0723`). 261 and 260 excluded on the (later-proven over-broad) 259-conflict estimate. LORE-264 given explicit ownership of `CHANGELOG.md` for the wave.
- 2026-07-25 — **wave 2 implemented + reviewed, then BLOCKED AT THE MERGE GATE.** Two simultaneous external outages: **Fable 5 hit its monthly spend limit** mid-wave (all four reviewers failed; the user chose **Opus** substitutes — approve verdicts with 0/1/2/1 fix cycles, nits only), and then **GitHub Actions stopped running** entirely. All four branches ended implemented, Opus-approved, rebased, pushed, green locally — and **0 of 4 merged**.
- 2026-07-25 — **wave 2 MERGE QUEUE DRAINED + SETTLED.**
  - **Actions blocker cleared first.** A plain full `gh run rerun 30157878759` started all 6 jobs (attempts 2–3 had started zero) and went green — confirming the spending-limit diagnosis and that the cap had been lifted. **`gh run rerun --failed` is a no-op when jobs were *cancelled* rather than failed.**
  - **Merge queue, strictly serial**, each branch re-fetched, rebased onto the moving `dev`, **re-verified in its own worktree** (`bun test` / typecheck / lint / `lore check` all green every time), force-pushed, PR'd, CI-gated on all six checks including the required docker e2e, then merged: **LORE-259 → `388fb42`**, **LORE-256 → `0670e27`**, **LORE-255 → `9a6b519`**, **LORE-264 → `650e1e7`**. Every rebase was clean; no merge conflicts, no escalations, no `human_needed`.
  - **LORE-256 AC#4 closed post-merge** (`53628e9`) — the windows-latest leg it required is structurally unverifiable from a macOS worktree, so PR #249's own green run is the objective evidence.
  - **Housekeeping:** six stale `origin/feature/*` refs from waves where `gh pr merge --delete-branch` had failed on a held worktree were pruned, so future R2 drift checks stop reporting phantom leftovers. All worktrees removed; repo down to a single checkout.
  - **WAVE-2 INTEGRATION REVIEW** (Opus, cumulative `42d0723...650e1e7`): code composition **sound**. Evidence on the merged tree: `bun test` **2126/0** across 49 files, typecheck/lint clean, `lore check` 39 files 0/0, `lore agents --check` up-to-date, plus live composed-CLI runs of the new phrasing. **`shared_test_files`: NONE** — 18 changed non-backlog files, all 18 unique, so the collateral-test-file collision vector did not fire. Rename/callsite skew sweep clean: zero live callers or asserters of any pre-LORE-259 string anywhere in `src/`, `test/`, `docs/`, `README.md`, `.claude/skills/`, `docker/`. Test arithmetic reconciles exactly (2116 + 1 + 9 = 2126), so no test was lost in the merge sequence. **It also corrected this tracker's own conflict model** — LORE-259 never touched `args.ts` or the 18 `usage()` call sites.
  - **Two findings, both narrow → direct follow-up** (PR #252, merged `d702ff6`), not a new task:
    1. *minor* — wave 2 shipped **three undocumented user-visible changes**. `docs/reference/cli-contract.md` §1.3/§7.2 classify substantial `--plain` reformatting as a **contract-level (breaking)** change, and LORE-259 did exactly that. lore has not cut a release, so `[Unreleased]` *is* the first release's notes.
    2. *nit* — LORE-256's own review-fix commit left a stray blank line detaching `writeFileAtomic`'s docstring, the exact defect it set out to fix.
  - **The follow-up's review is the most instructive artifact of this wave.** Pass 1 returned **`request_changes`** having caught **four factually false CHANGELOG claims**: (a) a universal claim that a persistent rename failure "still surfaces the identical `denied` `LoreError`" — false for `EBUSY`, since `ioError` maps only `EACCES`/`EPERM` to `denied` and the repo's own tests deliberately assert no type for persistent EBUSY; (b) the wrong envelope field named (`hint` where it was `message`), self-contradicting the entry's own earlier bullet; (c) `sync`'s `scopeConcepts` listed as a consumer of the shared `conceptNotInBundle` helper when it holds a standalone copy; (d) OIDC Trusted Publishing credited to `actions/setup-node` when the token exchange is the npm CLI's — which is *why* the `npm >= 11.5.1` floor exists. Pass 2 **approved**, re-deriving all five fixes from source and confirming no over-correction; its two remaining one-clause precision notes were folded in. **Lesson: a confidently-worded but wrong CHANGELOG entry is worse than a missing one.**
  - **PROCESS FIX for wave 3 — the CHANGELOG gap has now recurred in BOTH waves.** The repo already encodes the rule (`.github/PULL_REQUEST_TEMPLATE.md` checkbox, `docs/runbooks/dev-kickoff.md` step 6), but campaign workers are dispatched into bare worktrees with task-scoped prompts and never read either file. **Every wave-3 worker prompt must carry the `[Unreleased]` CHANGELOG requirement explicitly**, including for phrasing-only tasks, since §1.3 makes those contract changes too.
  - **Open for the user: the wave-3 reviewer model.** Fable is still capped (re-probed and confirmed). Opus works and has now carried four task reviews plus an integration review and two follow-up passes, but at materially higher cost than the design assumes.
- 2026-07-25 — **wave 3 DISPATCHED** (LORE-261, LORE-260; workers sonnet, reviewer **opus** — user's explicit choice on 2026-07-25 after Fable was re-probed and still capped). Wave base `0b42088`. Worktrees at `/Volumes/external/repos/lore.worktrees/<KEY>`. This is the **whole remaining queue** — the two are file-disjoint (261: `orphans.ts`/`reconcile-shared.ts`/`adapters/backlog.ts`; 260: `init.ts`/`agents.ts`/`scaffold.ts`), and every 259-edge retired when 259 merged. User chose to run both in parallel despite LORE-260's size, since its key design decision is already locked in the task notes.
  - **docker e2e allocation** (the harness serializes on one `e2e-e2e` container, so exactly one worker may run it): **LORE-260's worker runs it**, because it changes `lore init` — which every e2e phase depends on — and AC#2's non-TTY/CI-safe path is precisely what the harness exercises. **LORE-261's worker must NOT run the container**, but must grep `docker/e2e/run-e2e.sh` for assertions on the orphans surface and update them in its own branch if its change invalidates a contract assertion (the LORE-263 lesson); its PR's CI run is the verification.
  - **Both worker prompts carry the `[Unreleased]` CHANGELOG requirement explicitly** — the process fix for the gap that recurred in waves 1 and 2.

- 2026-07-25 — **wave 3 COMPLETE — CAMPAIGN CLOSED.** Both members merged: **LORE-261 → `cac03fa`**, **LORE-260 → `fd92b29`**, plus the doc-gate follow-up **`346d3c1`**. LORE-261 was merged ahead of the wave barrier (it is first in queue order regardless, and landing it meant LORE-260 rebased onto the true integrated state, surfacing any shared-file conflict during its own re-verify rather than in a post-hoc scramble). Every branch rebased, re-verified in its worktree, force-pushed, and CI-gated on all six checks before merge.
  - **The `CHANGELOG.md` collision the conflict model missed.** Requiring *both* wave members to add an `[Unreleased]` entry created a shared-file edge between two otherwise file-disjoint tasks — the collateral-file vector, self-inflicted by the process fix. It resolved cleanly (261 under `### Fixed`, 260 under `### Added`, ~170 lines apart) exactly as the LORE-261 reviewer predicted. **The serial merge queue is what made this safe; keep it.**
  - **WAVE-3 INTEGRATION REVIEW** (Opus, cumulative `b97ab87...fd92b29`): composition **sound**, no cross-task defect. Evidence on the merged tree: `bun test` **2176/0**, typecheck/lint clean, `lore check` 40 files 0/0, `lore agents --check` up-to-date, plus live composed runs proving `lore init`'s wizard/flags/vetoes and `lore orphans`' hierarchy awareness coexist in one bundle. **Test arithmetic reconciles exactly** (2126 → +10 → +40 → 2176; an orchestrator estimate of ~2186 was wrong, double-counting LORE-261's +10 which was already inside LORE-260's post-rebase baseline). **Both structural hazards this repo has been bitten by came up clean**, verified by file-list intersection and a full symbol sweep rather than inference: the collateral test-file vector did **not** fire (LORE-261 → `orphans.test.ts`; LORE-260 → `init.test.ts` + `cli.test.ts`; zero overlap), and no caller anywhere still uses a pre-extraction private shape.
  - **One finding → the doc-gate follow-up above.** Notably it was a *per-task review miss*, not a cross-task interaction defect — the class a wave-level review is not even designed to catch, found anyway.
  - **Three tasks filed from wave-3 review findings**, none in scope for the task that surfaced them: **LORE-265** (ADR-0009 §2 misdescribes the orphans rule — one claim was already wrong pre-wave), **LORE-266** (the agent bridge's pre-write symlink sweep has **zero test coverage**; deleting it fails no test **on `dev` too** — a security-relevant guard one refactor from vanishing), **LORE-267** (`lore agents` paints `protected` green while `lore init` paints it yellow; green reads as success when the bridge is actually stale).
  - **What the review gate bought, across the whole campaign.** Reviews were not a formality: they caught a CHANGELOG entry with **four claims false against source** (wave 2), and **two blocking runtime defects** on the default path of the command every new user runs first (wave 3). Both would have shipped. The recurring lesson is now explicit: **a confidently-worded but wrong doc is worse than a missing one**, and every fix must be re-reviewed, not just the original implementation.
