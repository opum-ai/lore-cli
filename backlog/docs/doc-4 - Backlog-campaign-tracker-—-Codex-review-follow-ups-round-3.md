---
id: doc-4
title: Backlog campaign tracker — Codex review follow-ups (round 3)
type: other
created_date: '2026-07-23 16:05'
updated_date: '2026-07-23 21:19'
---
Round 3 of the Codex-review follow-up campaign (see [[Backlog campaign tracker — Codex review follow-ups (round 2)]] / doc-3 for round 2, LORE-96..194, which closed all 78 medium findings; doc-1 for round 1, LORE-69..95, all 20 high). This round covers the **low-severity** findings from doc-2 ("Codex second-opinion review — lore codebase (2026-07-20)").

Protocol: restore → compute the ready/conflict graph → mark the wave Dispatched → dispatch (parallel Sonnet implement + Fable review) → serialize the merge → update this tracker once more at settlement → loop until the queue is empty or blocked → write handover.

## Source

doc-2 “Low-severity findings” section held **97 findings across 25 clusters**. On 2026-07-23 a **50-agent re-audit** (one general-purpose reauditor + one adversarial verifier per cluster, in full wave-parallel mode) re-verified every one against the LIVE `dev` tree AFTER rounds 1–2 had merged (LORE-69..194 all Done). Result:

- **53 open + agent-resolvable** → this round’s queue (new tasks LORE-198..250), plus 2 already-filed follow-ups folded in (LORE-195 lint-baseline chore, LORE-197 check.ts multi-root advisory bug) = **55 agent-resolvable**.
- **25 resolved-by-merge** by the round-1/2 campaign — dropped (evidence captured in the re-audit output, .repro-scratch/round3-resolved-dismissed.json at init time).
- **12 dismissed** (non-issue / duplicate of an existing task).
- **7 needs-human** → "Not queued" below.

The adversarial verify pass corrected 4 classifications. The 3 follow-ups filed 2026-07-23 after round 2 completed: LORE-195 (lint) + LORE-197 (check.ts) fold into this queue; LORE-196 (docker-e2e required check) is repo-admin → "Not queued".

## Scope / order confirmation

- **Scope**: low-severity only (doc-2’s low section, 97 findings → 53 open-agent + the 2 folded follow-ups). User chose **"Full round 3 (re-audit)"** on 2026-07-23.
- **Order**: user confirmed **docs-first** on 2026-07-23 — lowest-risk-first (docs → chore → task → enhancement → bug, cluster-mates adjacent). This is the wave-builder’s **tie-break only**, NOT a strict execution order (item #7 will not necessarily run in the 7th slot); the ready set is recomputed live each restore.
- All 55 queued tasks carry **zero formal dependencies** (re-verify live via YAML parse each restore); readiness is gated purely by the live pairwise file-conflict graph (same-cluster items serialize).

## Frontier

The "ready now" set is **always recomputed live** from `backlog/tasks/*.md` + this table at the start of every restore/wave — never trust a persisted "next wave" plan. Informational hint only: as of 2026-07-23 after **waves 19-26 merged**, **6 tasks are ready** (of the original 55; 49 resolved: waves 19-21 resolved 18 + LORE-206 dup + waves 22-26 resolved 30), **0 in-flight**, **49 resolved**. Every item has zero unmet formal deps; wave membership is bounded by the live file-conflict graph + the 6-worker cap. Recompute live at next restore — do NOT hardcode a next-wave list.

## Queue (confirmed order)
| # | Issue | Cluster | Formal deps | Status | Wave | Note |
|---|---|---|---|---|---|---|
| 1 | LORE-198 | build-runtime | — | Done | 19 | docs. Update test/fixtures/README.md backlog-json section to match the upstream recorder. [test/fixtures/README.md] |
| 2 | LORE-199 | cmd-check | — | Done | 19 | docs. Correct check's cli-surface docs: it does not surface token estimates. [docs/reference/cli-surface.md] |
| 3 | LORE-200 | core-engine-b | — | Done | 19 | docs. Correct GraphNode.title JSDoc to reflect frontmatterScalar's number/boolean coercion. [src/core/graph.ts] |
| 4 | LORE-201 | core-engine-b | — | Done | 20 | docs. Fix the `validation` instructions topic's overstated colon quote-safety claim and add a regression test. [src/core/instructions.ts] |
| 5 | LORE-202 | core-engine-b | — | Done | 21 | docs. Correct order.ts module-doc rationale: default Array.prototype.sort is code-unit-ordered and stable, not locale/engine-dependent. [src/core/order.ts] |
| 6 | LORE-203 | core-index-context | — | Done | 20 | docs. Clarify `lore context` --max-tokens docs: omitting it applies no token cap (bounded only by --depth). [src/core/context.ts] |
| 7 | LORE-204 | build-ci-config | — | Done | 19 | chore. release.yml: assert the compiled binary --version matches package.json exactly, not just non-empty (mirror ci.yml). [.github/workflows/release.yml] |
| 8 | LORE-205 | build-ci-config | — | Done | 20 | chore. Test fakes dirtyGitSpawn/failingCommitGitSpawn: dispatch the dirty status on the git subcommand, not on call index. [test/helpers.ts] |
| 9 | LORE-206 | build-runtime | — | Done | 20 | chore. Make scripted git-spawn test fakes dispatch by observed command, not call index. [test/helpers.ts] |
| 10 | LORE-207 | cmd-check | — | Done | 20 | chore. Release the response body in check's --external liveness fetch. [src/commands/check.ts] |
| 11 | LORE-208 | cmd-meta-d | — | Done | 19 | chore. Export InstructionsData and drop the duplicated test-side declaration. [src/commands/instructions.ts] |
| 12 | LORE-209 | cmd-rename-supersede | — | Done | 19 | chore. Correct the inaccurate comment and strengthen rename's "never constructs a Backlog adapter" test. [test/rename.test.ts] |
| 13 | LORE-210 | core-index-context | — | Done | 21 | chore. Remove indexes.ts's duplicate encodePathSegments; import the canonical encoder from links.ts (LORE-28 landed). [src/core/indexes.ts] |
| 14 | LORE-195 | cmd-crud-b/tooling | — | Done | 20 | chore. Restore biome lint baseline to green (biome check . -> 0 errors/0 infos). Filed 2026-07-23. [test/context.test.ts, test/replace.test.ts, test/validate.test.ts, src/core/managed-block.ts] |
| 15 | LORE-211 | cmd-meta-a | — | Done | 20 | task. Strengthen the tasks drift test: mix a dangling id with a failing read and assert empty streams. [test/tasks.test.ts] |
| 16 | LORE-212 | cmd-meta-d | — | Done | 21 | task. Strengthen validate's realpath de-dup test with a genuine symlink alias. [test/validate.test.ts] |
| 17 | LORE-213 | core-concept-manifest | — | Done | 21 | task. Guard manifest `kind` against drift from each command's emitted `kind:`. [test/help.test.ts] |
| 18 | LORE-214 | core-managed-template | — | Done | 21 | task. Cover edge-case resource bases (and the new-path section boundary) in template.test.ts. [test/template.test.ts] |
| 19 | LORE-215 | core-replace | — | Done | 21 | task. Scope replace.test.ts temp-dir hooks to the command suites and guard their cleanup. [test/replace.test.ts] |
| 20 | LORE-216 | errors-output-git | — | Done | 22 | task. Replace tautological tasks/orphans byte-identity test with one exercising both command render paths. [test/output.test.ts] |
| 21 | LORE-217 | adapter-backlog | — | Done | 22 | enhancement. Bound the real backlog subprocess spawn (bunBacklogSpawn) with a timeout. [src/adapters/backlog.ts] |
| 22 | LORE-218 | adapter-backlog | — | Done | 23 | enhancement. Remove the lone `this` binding in the backlog adapter's searchByLabel. [src/adapters/backlog.ts] |
| 23 | LORE-219 | core-concept-manifest | — | Done | 22 | enhancement. Enforce the single-line modeline contract in serializeConceptWithModeline. [src/core/concept.ts] |
| 24 | LORE-220 | core-concept-manifest | — | Done | 23 | enhancement. Freeze the manifest singletons returned by buildManifest(). [src/core/manifest.ts] |
| 25 | LORE-221 | errors-output-git | — | Done | 23 | enhancement. Align id/status columns by terminal display width, not UTF-16 length. [src/output.ts] |
| 26 | LORE-222 | adapter-backlog | — | Done | 24 | bug. Map spawn-rejections on all backlog calls (not just the probe's --version) to typed LoreErrors. [src/adapters/backlog.ts] |
| 27 | LORE-223 | cli-entry-state | — | Done | 22 | bug. cli.ts: rejectStrayCommandFlags/rejectCommandArgs re-scan post-`--` tokens as flags. [src/cli.ts] |
| 28 | LORE-224 | cli-entry-state | — | Done | 23 | bug. state.ts: `.trim()` on git show-prefix corrupts a whitespace-leading bundle prefix. [src/state.ts] |
| 29 | LORE-225 | cmd-check | — | Done | 22 | bug. De-duplicate check's bundle roots by canonical filesystem identity. [src/commands/check.ts] |
| 30 | LORE-226 | cmd-check | — | Done | 23 | bug. Sanitize control characters in check's finding output. [src/commands/check.ts] |
| 31 | LORE-197 | cmd-check | — | Done | 24 | bug. Discovery advisories from an earlier bundle root lost when a later root throws inside collectBundles (LORE-191 residual). [src/commands/check.ts] |
| 32 | LORE-227 | cmd-crud-a | — | Done | 22 | bug. new.ts: parse arguments before loading the profile so a malformed profile.toml can't mask a usage error. [src/commands/new.ts] |
| 33 | LORE-228 | cmd-crud-a | — | Done | 23 | bug. replace.ts / validate.ts: reject an inline =value on boolean flags (--regex, --dry-run, --strict). [src/commands/replace.ts] |
| 34 | LORE-229 | cmd-crud-a | — | Done | 24 | bug. replace.ts: sanitize discovered file paths in the report (strip ANSI/control chars) to prevent output forging. [src/commands/replace.ts] |
| 35 | LORE-230 | cmd-crud-b | — | Done | 24 | bug. existingIsRegularFile masks non-ENOENT lstat failures as a benign 'already exists' skip. [src/commands/fswrite.ts] |
| 36 | LORE-231 | cmd-crud-b | — | Done | 25 | bug. writeFileAtomic leaks an uncleaned temp file when writeFileSync fails mid-write. [src/commands/fswrite.ts] |
| 37 | LORE-232 | cmd-crud-b | — | Done | 26 | bug. lore query --type/--status/--tag values are not trimmed, inconsistent with --field. [src/commands/query.ts] |
| 38 | LORE-233 | cmd-link | — | Done | 24 | bug. Bound runLink's up-front viewTask existence-check fan-out with a concurrency limit. [src/commands/link.ts] |
| 39 | LORE-234 | cmd-link | — | Done | 25 | bug. runLink's doc-membership check is exact-case while unlink's is case-insensitive — a casing-variant documentation entry duplicates instead of dedups. [src/commands/link.ts] |
| 40 | LORE-235 | cmd-meta-a | — | Done | 25 | bug. Bound resolveRollup's viewTask fan-out with the shared concurrency cap. [src/commands/reconcile-shared.ts] |
| 41 | LORE-236 | cmd-meta-a | — | Done | 26 | bug. Strip ANSI/control/OSC escapes on the stderr warning path (WarningCollector.flush). [src/commands/tasks.ts] |
| 42 | LORE-237 | cmd-meta-b | — | Done | 25 | bug. Harden `validate` arg parser: reject `--strict=<value>`, repeated `--strict`, and repeated `--type`. [src/commands/validate.ts] |
| 43 | LORE-238 | cmd-meta-c | — | Done | 24 | bug. scaffold: differentiate the conflict hint for structural directory blockers (--force cannot replace a file with a directory). [src/commands/scaffold.ts] |
| 44 | LORE-239 | core-bundle-check | — | Done | 25 | bug. callout portability detector false-positives on inline formatting before [!type] in ordinary prose. [src/core/check.ts] |
| 45 | LORE-240 | core-bundle-check | — | Done | 26 | bug. check portability lint mis-parses a leading indented code block in frontmatter-free files. [src/core/check.ts] |
| 46 | LORE-241 | core-bundle-check | — | To Do | — | bug. parseJson rewrites a valid-but-non-object profile.json into a misleading 'is not valid JSON' error. [src/core/profile.ts] |
| 47 | LORE-242 | core-bundle-check | — | To Do | — | bug. profile does not validate a field's `default` against its declared kind/enum. [src/core/profile.ts] |
| 48 | LORE-243 | core-engine-a | — | Done | 25 | bug. Harden log.ts resolveRoot against equivalent-but-differently-spelled bundle roots. [src/core/log.ts] |
| 49 | LORE-244 | core-index-context | — | Done | 26 | bug. index.md conceptTitle: coerce numeric/boolean titles via frontmatterScalar (match graph/query/context). [src/core/indexes.ts] |
| 50 | LORE-245 | core-links-resolution | — | Done | 26 | bug. validateLink: flag bare '.'/'..' navigation destinations instead of exempting them as dotfiles. [src/core/links.ts] |
| 51 | LORE-246 | core-query-validate | — | To Do | — | bug. matchesField: resolve case-insensitive field key across ALL case-variant spellings, not just the first. [src/core/query.ts] |
| 52 | LORE-247 | core-rewrite-engine | — | Done | 26 | bug. Preserve above-repo-root outbound links during rename instead of silently clamp-retargeting them. [src/core/rewrite.ts] |
| 53 | LORE-248 | core-scaffold-consumer | — | To Do | — | bug. warnSummary counts UTF-16 code units but reports "chars" — non-BMP summaries warn prematurely. [src/core/schema.ts] |
| 54 | LORE-249 | errors-output-git | — | To Do | — | bug. Harden stderrHint: strip terminal control sequences and cap length. [src/errors.ts] |
| 55 | LORE-250 | errors-output-git | — | To Do | — | bug. Suppress ANSI color on stderr diagnostics when stderr is not a TTY. [src/output.ts] |

## Resolved
| # | Issue | Status/date/wave | Evidence summary |
|---|---|---|---|
| 1 | LORE-198 | Done / 2026-07-23 / w19 | Fable approve (0 fix). test/fixtures/README.md realigned to upstream recorder (MrLesk PR#790, LORE_BACKLOG_UPSTREAM_CLI, hyphenated kinds, no-redaction). bun test 1913 pass/0 fail; tsc clean. Merged 7e8c20d (PR#187). |
| 2 | LORE-199 | Done / 2026-07-23 / w19 | Fable approve (0 fix). cli-surface.md check section corrected (no token estimates) via `lore replace`. bun test 1913 pass; tsc clean; lore check 38/0/0. Merged 03faf9e (PR#188). |
| 3 | LORE-200 | Done / 2026-07-23 / w19 | Fable approve (0 fix). GraphNode.title JSDoc corrected for number/boolean coercion (no code change). bun test 1913 pass incl. numeric-title '2024' assertion; tsc clean. Merged e3cc835 (PR#189). |
| 7 | LORE-204 | Done / 2026-07-23 / w19 | Fable approve (0 fix). release.yml build + install-sanity now assert binary --version == package.json (mirrors ci.yml:56-64); verify-versions untouched. YAML-valid; bun test 1913 pass; tsc clean. Merged 282798d (PR#190). |
| 11 | LORE-208 | Done / 2026-07-23 / w19 | Fable approve (0 fix). InstructionsData exported; test/instructions.test.ts imports it (dropped local dup). tsc clean (key gate); bun test 1913 pass. Merged 8781404 (PR#191). |
| 12 | LORE-209 | Done / 2026-07-23 / w19 | Fable approve (0 fix). rename.test.ts comment corrected + no-adapter-construction enforced via a spy (zero src change). bun test test/rename.test.ts 105 pass; full 1913 pass; tsc clean. Merged 81d9469 (PR#192). |
| 4 | LORE-201 | Done / 2026-07-23 / w20 | Fable approve (0 fix). VALIDATION instructions topic reworded to colon-plus-space hazard (matches validate.ts:429); new content-assertion test in test/instructions.test.ts, proven to fail if old wording restored. bun test 1914 pass; tsc clean. Merged e001430 (PR#193). |
| 6 | LORE-203 | Done / 2026-07-23 / w20 | Fable approve (0 fix). cli-surface.md + mcp-tools.md context --max-tokens phrasing fixed (no default cap; bounded by --depth); cli-contract.md needed none; no src/ change. bun test 1914 pass; tsc clean; lore check 38/0/0. Merged 8da5fee (PR#194). |
| 8 | LORE-205 | Done / 2026-07-23 / w20 | Fable approve (0 fix). dirtyGitSpawn/failingCommitGitSpawn dispatch dirty status on args[0]==="status" not call index; public shape preserved. Dirty-path consumers (link/sync/rename) 200 pass; full 1914 pass; tsc clean. Merged 1cc7275 (PR#195). |
| 10 | LORE-207 | Done / 2026-07-23 / w20 | Fable approve (0 fix). check --external defaultFetch now cancels (not reads) response.body on both 3xx + terminal paths; SSRF invariant preserved; new body-cancel test. check.test.ts 207 pass; full 1915 pass; tsc clean. Merged 6a897a0 (PR#196). |
| 14 | LORE-195 | Done / 2026-07-23 / w20 | Fable approve (0 fix). biome lint baseline restored to green (exit 0, 0 errors/0 infos): safe reflow+import-sort + byte-identical template-literal rewrites; no biome.json rule relaxed. bun test 1915 pass incl. managed-block fixpoint; tsc clean. Merged 870abc4 (PR#197). |
| 15 | LORE-211 | Done / 2026-07-23 / w20 | Fable approve (0 fix). tasks drift test strengthened: dangling+failing id, both orderings, empty stdout/stderr asserted. test/tasks.test.ts 26 pass; full 1917 pass; tsc clean; lint stays green. Merged 041d487 (PR#198). |
| 5 | LORE-202 | Done / 2026-07-23 / w21 | Fable approve (0 fix). order.ts module-doc corrected: default Array.prototype.sort is UTF-16 code-unit ordered + stable since ES2019 (not locale/engine-dependent); comparator body byte-unchanged (SHA-256 match). bun test 1917 pass; tsc clean. Merged 1039b0a (PR#199). |
| 9 | LORE-206 | Done / 2026-07-23 / w20-dup | Reconciled DUPLICATE of LORE-205 (same test/helpers.ts fakes; build-runtime finding [2] == build-ci-config finding). LORE-205 (PR#195, 1cc7275, wave 20) already dispatches dirtyGitSpawn/failingCommitGitSpawn on args[0]==="status"/"commit" not call index; cleanGitSpawn unchanged; suite green. All 4 ACs verified satisfied on dev. No separate change. |
| 13 | LORE-210 | Done / 2026-07-23 / w21 | Fable approve (0 fix). indexes.ts private encodePathSegments/escapePercent + stale TODO(LORE-28) deleted; imports canonical encoder from ./links (reviewer proved byte-identical across char codes 1-0x24F). tsc clean; bun test 1917 pass; lore check 38/0/0. Merged 44d4a8f (PR#200). |
| 16 | LORE-212 | Done / 2026-07-23 / w21 | Fable approve (0 fix). New POSIX-only symlink-alias test in validate.test.ts; genuine mutation-killer (reviewer re-ran revert on discover.ts → fails; discover.ts unmodified in diff). validate.test.ts 62 pass; full 1918 pass; tsc clean. Merged f5c85c8 (PR#201). |
| 17 | LORE-213 | Done / 2026-07-23 / w21 | Fable approve (0 fix). New help.test.ts golden pins each manifest command's kind to its handler-emitted kind, keyed to manifestCommandNames(); mutation-killer (reviewer re-ran revert on manifest.ts → fails; manifest.ts unmodified). full 1919 pass; tsc clean. Merged b81b1cf (PR#202). |
| 18 | LORE-214 | Done / 2026-07-23 / w21 | Fable approve (0 fix). template.test.ts gains resourceFor opaque-join tests (query/fragment/non-hierarchical scheme bases) + new-path section-boundary test; src/core/template.ts untouched. template.test.ts 46 pass; full 1923 pass; tsc clean. Merged b1821f4 (PR#203). |
| 19 | LORE-215 | Done / 2026-07-23 / w21 | Fable approve (0 fix). replace.test.ts temp-dir root hooks scoped to command-level suites only (pure-engine + fswrite suites no longer run them); afterEach cleanup guarded on unset root. replace.test.ts 85 pass; full 1923 pass; tsc clean. Merged d6f7bc2 (PR#204). |
| 20 | LORE-216 | Done / 2026-07-23 / w22 | Fable approve (0 fix). test/output.test.ts tautology (f(x)===f(x) at :528-531) replaced with a test driving the REAL runTasks (renderTable) + runOrphans --tasks-only (renderReport) paths on equivalent {id,status,title} data, asserting byte-identical rows pinned to renderTaskSummaryRows. AC#3 mutation-proven (inlining a divergent layout in either command fails exactly the new test; both reverted). src/commands/tasks.ts + orphans.ts UNCHANGED (test-only). bun test 1923 pass; tsc clean. Merged 25014c3 (PR#205). |
| 21 | LORE-217 | Done / 2026-07-23 / w22 | Fable approve (0 fix). Real bunBacklogSpawn (src/adapters/backlog.ts) bounded with an operator-overridable wall-clock timeout (LORE_BACKLOG_TIMEOUT_MS, default 30000ms): on expiry SIGKILL + typed LoreError(validation, exit 6); injectable BacklogSpawn seam + test fakes unchanged. New non-terminating-process test (real-binary pattern) proves termination-within-bound, not a hang. bun test 1924 pass; tsc clean. Merged 7287f06 (PR#206). |
| 23 | LORE-219 | Done / 2026-07-23 / w22 | Fable approve (0 fix). serializeConceptWithModeline (src/core/concept.ts) now throws LoreError(validation, exit 6) on a newline-containing modeline BEFORE splicing; single-line splice byte-identical (existing test/concept.test.ts L281-311 cases green unchanged); new multi-line-rejection regression test. No caller changed (defensive contract-enforcement). bun test 1925 pass; tsc clean. Merged b9d4eb0 (PR#207). |
| 27 | LORE-223 | Done / 2026-07-23 / w22 | Fable approve (0 fix). rejectStrayCommandFlags/rejectCommandArgs (src/cli.ts) honor the `--` terminator: post-`--` dash tokens are positionals (`--version tasks -- -x` prints version exit 0; `--help tasks -- -x` renders help); init's post-`--` token classified `unexpected` not `unknown option` (human message + --json input key); pre-`--` stray flags still rejected. New cli.test.ts cases. bun test 1932 pass; tsc clean; AC#1/#4 re-verified live post-rebase. Merged ea9ecde (PR#208). |
| 29 | LORE-225 | Done / 2026-07-23 / w22 | Fable approve (0 fix). collectBundles (src/commands/check.ts) de-dups bundle roots by canonicalIdentity (realpathSync.native) not a raw join() string, so symlink/case-variant aliases scan once (fileCount/finding counts not doubled, no double URL-probe under --external); a nonexistent root still surfaces expandRoot's typed not_found (exit 3)/denied (realpath ENOENT guarded, not swallowed). Case-variant assertion skipIf-guarded for case-sensitive CI; added missing multi-root dedup coverage (doc-2 finding [7]). check.test.ts 210 pass; full 1935 pass; tsc clean. Merged 6fad768 (PR#209). |
| 32 | LORE-227 | Done / 2026-07-23 / w22 | Fable approve (0 fix). runNew (src/commands/new.ts) calls parseNewArgs before loadProfile so a malformed .lore/profile.toml no longer masks the usage error — `lore new` with no args + broken profile.toml exits 2 with "`lore new` needs a type" (not the TOML-parse error); profile still threaded to canonicalType/resolveTemplate/buildNewConcept unchanged (matches link/rename/supersede ordering). new.test.ts 56 pass; full 1936 pass; tsc clean. Merged f2b78b3 (PR#210). |
| 22 | LORE-218 | Done / 2026-07-23 / w23 | Fable approve (0 fix). searchByLabel's lone `this.listTasks` binding removed: listTasks is now a closed-over local (mirroring read/ensureProbed) that both the returned method and searchByLabel call directly — no `this` inside createBacklogAdapter (grep-clean), works when destructured. Mutation-proven (reverting backlog.ts fails exactly the new destructured-call test). Behavior byte-identical (same --labels AND-match). bun test 1937 pass; tsc clean. Merged 7b5226a (PR#211). |
| 24 | LORE-220 | Done / 2026-07-23 / w23 | Fable approve (0 fix). buildManifest() return deep-frozen via new deepFreeze() (envelope + commands array + each command + its flags + globalFlags), mirroring concept.ts's Object.freeze convention; manifest data-shape unchanged (LORE-213 kind-golden still green). New nested-mutation-rejection tests in help.test.ts. bun test 1943 pass; tsc clean. (Task note cosmetically says "7 tests"; 6 added — no code impact.) Merged 334bf7d (PR#212). |
| 25 | LORE-221 | Done / 2026-07-23 / w23 | Fable approve (0 fix). renderTaskSummaryRows (src/output.ts) now measures + pads id/status columns by TERMINAL DISPLAY WIDTH (full-width CJK=2, zero-width/combining=0) via a compact self-contained width helper — no external runtime dep (AC#5, package.json untouched). Pretty-mode only; ASCII-only output byte-identical (LORE-216's byte-identity test still green — AC#4); CJK + combining-mark rows align title column. bun test 1950 pass; output.test.ts 68 pass; tsc clean. Merged afbcb5a (PR#213). |
| 28 | LORE-224 | Done / 2026-07-23 / w23 | Fable approve (0 fix). porcelainPaths (src/state.ts) strips ONLY the trailing newline from `git rev-parse --show-prefix` (was `.trim()`, which also stripped LEADING whitespace and corrupted a whitespace-leading bundle prefix → spurious drift error blocking the commit). Empty-prefix + normal-nested cases unchanged; backlog.ts:733 emptiness-only trim untouched. New scripted-GitSpawn regression (` proj/\n` reaches git add/commit not drift). bun test 1951 pass; state.test.ts 45 pass; tsc clean. Merged 5c84f3e (PR#214). |
| 30 | LORE-226 | Done / 2026-07-23 / w23 | Fable approve (0 fix). findingLine (src/commands/check.ts) runs finding.file + finding.message through the shared stripAnsiAndControls (src/errors.ts, AC#2 — not a re-copied regex) before emission, so a crafted link target/filename can no longer inject ANSI escapes or forge a phantom finding row via an embedded newline, in both plain + pretty modes. Ordinary output unchanged. Mutation-proven (reverting the src fix fails exactly the 3 new injection tests). Sits cleanly alongside LORE-225's dedup (different function). check.test.ts 214 pass; full 1955 pass; tsc clean. Merged 2d1ef7c (PR#215). |
| 33 | LORE-228 | Done / 2026-07-23 / w23 | Fable approve (0 fix). parseReplaceArgs (src/commands/replace.ts) + parseValidateArgs (src/commands/validate.ts) now reject an inline value on boolean flags (`--regex=false`/`--dry-run=false`/`--strict=false`) with a usage LoreError (exit 2), mirroring orphans.ts/graph.ts — closing a data-write footgun where `--regex=false` silently enabled regex. Bare boolean forms unchanged; orphans.ts/graph.ts/args.ts untouched (AC#4). AC live-verified post-rebase (all three inline forms exit 2). bun test 1959 pass; tsc clean. Merged e36c546 (PR#216). |
| 26 | LORE-222 | Done / 2026-07-23 / w24 | Fable approve (0 fix). A shared `spawnOrThrow` wrapper (src/adapters/backlog.ts) now routes ALL 6 spawn sites (probe --version, probe task-list --json, read helper, viewTask, createTask, editTask) through typed-LoreError mapping: ENOENT→not_found(3), EACCES/EPERM→denied(4), other coded errno→validation(6), uncoded rejections (incl. LORE-217's timeout LoreError, which carries no .code) pass through unchanged — the only remaining bare `await spawn(` is inside the wrapper. New AC#2 test lets the probe PASS then rejects a later viewTask with ENOENT → typed not_found; mutation-proven (base-revision backlog.ts fails it). backlog-adapter+backlog-probe 61 pass; full 1960 pass; tsc clean. Merged 0aa33a5 (PR#217). |
| 31 | LORE-197 | Done / 2026-07-23 / w24 | Fable approve (0 fix). runCheck's collectBundles call + its single NON-draining advisories.flush are now wrapped in try/finally (src/commands/check.ts:160-181), so an EARLIER bundle root's discovery advisories survive a LATER root's throw (surfaced on stderr) while the throw still propagates with its unchanged exit code — WITHOUT a second flush site (grep-confirmed exactly one advisories.flush; closes the LORE-191 residual one grain earlier, inside discovery). New multi-root regression test (earlier skipped-symlink advisory + later not_found throw asserts both). full 1961 pass; tsc clean. Merged 0adacac (PR#218). |
| 34 | LORE-229 | Done / 2026-07-23 / w24 | Fable approve (0 fix). `lore replace`'s text report now routes each discovered file path through a local sanitizeField calling the shared stripAnsiAndControls(singleLine(...)) imported from errors.ts (no copied regex) before interpolation — both pretty and plain modes (single render()), closing an output-forging vector via a crafted POSIX filename (ANSI/ESC/newline). Normal repo-relative paths byte-identical; --json envelope unchanged. New ESC+newline strip test, mutation-proven non-vacuous. replace.test.ts 90 pass; full 1963 pass; tsc clean. Merged e9b182d (PR#219). |
| 35 | LORE-230 | Done / 2026-07-23 / w24 | Fable approve (0 fix). existingIsRegularFile's catch (src/commands/fswrite.ts:424-435) narrowed to degrade-to-benign-skip ONLY on ENOENT (keyed on errnoCode); any other lstat failure (EACCES/EIO) now propagates and createIfAbsent (:164-171) classifies it via the shared ioError helper (EACCES/EPERM→denied) instead of masking a real I/O fault as 'a regular file already exists, skipped'. Documented raced-away ENOENT case unchanged. 4 new createIfAbsent tests. fswrite.test.ts 19 pass; full 1967 pass; tsc clean. Merged 2cd4739 (PR#220). |
| 38 | LORE-233 | Done / 2026-07-23 / w24 | Fable approve (0 fix). runLink's up-front viewTask existence-check fan-out (src/commands/link.ts:182) is now bounded by mapWithConcurrency capped at TASK_DETAILS_CONCURRENCY=8. To avoid a link→reconcile-shared→link import cycle, both symbols were relocated verbatim (byte-identical body) out of reconcile-shared.ts into a NEW dependency-free src/commands/concurrency.ts; link.ts + reconcile-shared.ts now import from it (whole-repo `tsc --noEmit` clean confirms no dangling import / no cycle). First-invalid-id-in-argument-order reporting preserved (order-preserving pool); instrumented peak-in-flight cap test saturates AND caps. link.test.ts 69 pass; full 1969 pass. Merged 1b1ce24 (PR#221). **NOTE for LORE-235 (future):** mapWithConcurrency + TASK_DETAILS_CONCURRENCY now live in src/commands/concurrency.ts, NOT reconcile-shared.ts — import from the new location. |
| 43 | LORE-238 | Done / 2026-07-23 / w24 | Fable approve (0 fix). `lore scaffold`'s preflight conflict hint is now differentiated by collision kind via a new conflictHint(hasDirBlocker, hasFileCollision) helper (src/commands/scaffold.ts): dir-blocker-only drops the misleading `--force` claim and directs remove/rename; files-only keeps the `--force` hint verbatim; mixed conveys BOTH remedies. Justified by a new test proving `--force` against a dir-blocker still fails with `conflict` (fswrite.ts UNTOUCHED — AC#4 exercises its existing ensureDir→EEXIST path). The two previously-misleading consumer-scaffold assertions (~:452, ~:599) updated to the corrected hint. consumer-scaffold.test.ts 56 pass; full 1972 pass; tsc clean. Merged 0a7a2b3 (PR#222). |
| 36 | LORE-231 | Done / 2026-07-23 / w25 | Fable approve (0 fix). writeFileAtomic now creates its temp via `O_WRONLY|O_CREAT|O_EXCL` openSync with the `tmpFileExists` cleanup guard set the instant the open returns (mirroring writeFileNoFollow's discipline), then writes bytes through the existing writeAllBytes/writeSync loop — so a mid-write failure (temp already created: ENOSPC/EDQUOT/EIO) hits the catch-block unlinkSync cleanup instead of leaking a stray `.lore-sync-tmp-*`, while a before-create failure (EACCES on a read-only dir) still skips cleanup and never claims a stray temp remains (AC#2 preserved). New AC#3 test spies writeSync to throw AFTER the real openSync created the temp, asserts zero `.lore-sync-tmp-*` litter; mutation-proven (reverting the guard to pre-fix post-write timing fails it). The LORE-116 commit-phase atomicity test was migrated from a writeFileSync spy to openSync+writeSync spies (writeFileAtomic no longer calls writeFileSync) with all discriminating per-file-atomicity assertions preserved. O_EXCL fail-loud on a pre-existing temp path is strictly safer (pid+random suffix makes collision unreachable); no fd leak on any path. Scope: fswrite.ts + replace.test.ts. bun test 1973 pass; tsc clean; biome clean. Merged 90a1a95 (PR#223). |
| 39 | LORE-234 | Done / 2026-07-23 / w25 | Fable approve (0 fix). runLink's `docChanged` (src/commands/link.ts:251) and `addDoc` (:766) now use the shared `containsCaseInsensitive` predicate (:751) — no inline copy — matching hasLabel/hadDoc/removeDoc, so a Backlog `documentation:` entry differing only by case is recognized instead of appended as a casing-variant duplicate. Two new tests cover BOTH the `doc:`-label-present and label-absent branches → exactly one doc entry, runLink still succeeds and writes all back-refs; a genuinely-new (no case-variant) doc path is still appended. moveBackRefs (rename-path exact-case family) intentionally left untouched (explicitly task-permitted). Scope: link.ts + link.test.ts. Re-verified post-rebase: bun test 1975 pass; tsc clean; test/link.test.ts 71 pass. Merged b128692 (PR#224). |
| 40 | LORE-235 | Done / 2026-07-23 / w25 | Fable approve (0 fix). resolveRollup's per-linked-id `verifiedViewTask` subprocess fan-out (src/commands/tasks.ts) is now bounded to `TASK_DETAILS_CONCURRENCY` (=8) via `mapWithConcurrency`, both imported from the canonical dependency-free `src/commands/concurrency.ts` (NOT reconcile-shared.ts, no hardcoded copy — the correct post-LORE-233 home; whole-repo `tsc --noEmit` clean confirms no import cycle). Semantics preserved EXACTLY: linked-order, the first read that FAILS in `tasks:` order rethrown BEFORE any stdout/stderr output, dangling (null) reads dropped with the existing stderr advisory, `--status` filter applied last. New peak-in-flight test saturates AND caps at 8 (mirrors reconcile-shared.test.ts); mutation-proven. Scope: tasks.ts + tasks.test.ts. Re-verified post-rebase: bun test 1976 pass; whole-repo tsc clean; test/tasks.test.ts 27 pass. Merged d1fbeb6 (PR#225). |
| 42 | LORE-237 | Done / 2026-07-23 / w25 | Fable approve (0 fix). parseValidateArgs (src/commands/validate.ts) gains exactly two duplicate-flag guards — repeated `--strict` and repeated `--type` each now raise a `usage` LoreError (exit 2) — mirroring graph.ts/context.ts's "--X given more than once" wording and ordering (+6 source lines). AC#1 (`--strict=<value>` rejection) was ALREADY shipped by merged LORE-228; this task closed the repeated-flag residual only. Preserved accepted behavior: single `--strict`, single `--type ADR`, inline `--type=ADR`, empty `--type=` usage error, and `--` ending option parsing (trailing `--strict`/`--type` becomes a positional). 3 new tests assert LoreError TYPE `usage`. Scope: validate.ts + validate.test.ts. Re-verified post-rebase: bun test 1979 pass; tsc clean; test/validate.test.ts 66 pass. Merged cbc9bf3 (PR#226). |
| 44 | LORE-239 | Done / 2026-07-23 / w25 | Fable approve (0 fix). The per-text-node callout regex was removed from DETECTORS and replaced by a STRUCTURAL `calloutFinding()` (src/core/check.ts:795-819) that fires only when a blockquote's first-child paragraph's first inline is `[!type]` — so `ordinary **bold** [!note] prose` (inline formatting splitting an ordinary paragraph) no longer false-positives (also under `--strict`), while a genuine `> [!note]` callout, including a first-inline `[!type]` followed by content, is still flagged (mirrors the sibling blockReferenceFinding's structural approach). 5 new tests (negative false-positive case + positive still-flagged cases). Scope: check.ts + check.test.ts. Re-verified post-rebase: bun test 1984 pass; tsc clean; test/check.test.ts 220 pass. Merged 3f8316d (PR#227). |
| 48 | LORE-243 | Done / 2026-07-23 / w25 | Fable approve (0 fix). resolveRoot (src/core/log.ts:183-185) now runs `posix.normalize()` before stripping trailing slashes, canonicalizing all equivalent bundle-root spellings (`./docs`, `docs/.`, `./docs/`, `docs//adr`, `docs/./adr`) in the SINGLE seam shared by generateLog's post-filter (isUnderRoot) AND buildLog's `adapter.history` pathspec — so those spellings no longer produce a silently-empty log. Empty/all-slashes root still falls back to DOCS_DIR; existing assertions at test/log.test.ts:158,192-193 stay green; the LORE-143 post-filter↔adapter-walk agreement assertion was extended to prove both use the same canonical root. New equivalent-spelling tests. Scope: log.ts + log.test.ts. Re-verified post-rebase: bun test 1987 pass; tsc clean; test/log.test.ts 23 pass. Merged 8258491 (PR#228). |
| 37 | LORE-232 | Done / 2026-07-23 / w26 | Fable approve (0 fix). `readValue`'s --type/--status/--tag call sites now route through a new `readTrimmedValue` wrapper (src/commands/query.ts:250-262) that trims and rejects a whitespace-only value as a `usage` error — matching `--field`'s existing empty-after-trim rejection — so `--type " Story "` / `--tag "  archive  "` now match the same concepts as the untrimmed forms and `--status "  "`/`--tag "  "` are usage errors. CRITICAL preserved: `readValue` itself is byte-unchanged and --limit still feeds untrimmed input to parseCount, so `--limit " 2 "` is STILL rejected (the trap). 4 new/updated tests via expectError("usage", …) confirm the error TYPE, not just the message; revert experiment proves exactly the 4 new assertions fail on base code (non-vacuous). Scope: query.ts + query.test.ts; src/core/query.ts untouched (sibling LORE-246). bun test 1992/0; tsc clean; biome clean. Merged 102abdf (PR#229). |
| 41 | LORE-236 | Done / 2026-07-23 / w26 | Fable approve (0 fix). `WarningCollector.flush` (src/errors.ts:661) now routes each warning body through the SHARED `stripAnsiAndControls` (errors.ts:168) after singleLine/asText — no new regex copy — closing the ANSI/OSC/control-byte escape-forgery gap on the stderr warning path (e.g. a dangling `tasks:` id carrying `\x1b[2J`), while the painted `warning:` prefix is built before the loop and never sanitized so its color survives (asserted byte-exact). Docstring updated to match. 3 new tests (direct flush with CSI+OSC+bare BEL; colored-prefix-survives; end-to-end `lore tasks` dangling-id with a YAML \e-escaped CSI in frontmatter exercising warnDangling→flush) — all fail on the reverted fix line (mutation-proven). Scope: errors.ts + errors.test.ts + tasks.test.ts; output.ts/tasks.ts source untouched (LORE-249/250 not in wave). Re-verified post-rebase: bun test 1995/0; tsc clean. Merged e19e7f1 (PR#230). |
| 45 | LORE-240 | Done / 2026-07-23 / w26 | Fable approve (0 fix). `bodyText` (src/core/check.ts:939) detects "no frontmatter fence" via gray-matter's own `matter.test` on the normalized string and, ONLY on that path, applies BOM-strip + CRLF/CR normalization while skipping normalizeInput's leading-`\s+` strip — so a frontmatter-free file whose first content is an indented (4-space/tab) code block is parsed as `code`, not prose, and no longer false-positives portability hazards (`{`, `[[wikilink]]`) incl. under `--strict`. Every fence-opening file (padded/malformed/unclosed) falls through to the byte-identical pre-existing path; concept.ts/normalizeInput have a ZERO-line diff (frontmatter path unchanged, AC#3 guard). Reviewer edge probes: tab-indented block also fixed; 1-3-space prose + blank-lines-then-prose hazards still flagged (no real hazard missed). Mutation-proven (revert fails exactly the 4 behavior-dependent new tests). Scope: check.ts + check.test.ts. Re-verified post-rebase: bun test 2003/0; tsc clean; check.test.ts 228/0. Merged e4746e8 (PR#231). |
| 49 | LORE-244 | Done / 2026-07-23 / w26 | Fable approve (0 fix). `conceptTitle` (src/core/indexes.ts:265) now derives its title via the SHARED `frontmatterScalar` imported from ./bundle (bundle.ts untouched — confirmed via merge-base name-only diff), so an unquoted numeric (`title: 2024`) or boolean title is coerced to its string form matching graph.ts/query.ts/context.ts, instead of dropping to the filename. No re-introduced `.trim()` (frontmatterScalar's verbatim-string contract preserved; rendered output for padded titles stays byte-identical because linkText's singleLine already trims, and listing sort keys on link not title → fixpoint/byte-stability structurally unaffected). Fallback to posix.basename preserved for absent/whitespace-only/non-scalar. 2 new coercion tests; revert of only indexes.ts fails exactly those 2 (non-vacuous). Scope: indexes.ts + indexes.test.ts. Re-verified post-rebase: bun test 2006/0; tsc clean; indexes.test.ts 32/0. Merged c63c00f (PR#232). |
| 50 | LORE-245 | Done / 2026-07-23 / w26 | Fable approve (0 fix). `isDirectoryLink` (src/core/links.ts:520-530) now treats a decoded final path segment of exactly `.` or `..` as a directory link; because that check runs in validateLink's if-branch BEFORE `lacksMarkdownSuffix`'s else-if (links.ts:313-325), bare navigation destinations (`.`, `..`, `../..`, `foo/..`, `foo/.`) are flagged and never reach the dotfile exemption — which is byte-for-byte UNCHANGED, so genuine dotfiles (`.gitignore`) stay exempt (AC#4 anti-regression). Edge probes both directions: no over-flagging (`./orders.md`, `a/../b.md`, `../.github/dependabot.yml` still `[]`); percent-encoded `%2E%2E` correctly flagged (operates on decoded path). 5 new tests; revert of only the src fix fails exactly those 5 (mutation-proven). Scope: links.ts + links.test.ts. Re-verified post-rebase: bun test 2013/0; tsc clean; links.test.ts 91/0. Merged fbd5ab9 (PR#233). |
| 52 | LORE-247 | Done / 2026-07-23 / w26 | Fable approve (0 fix). `newDestPathFor`'s `ctx.isMoved` branch (src/core/rewrite.ts:528-530) now checks the normalized repo-relative `targetPath` (`=== '..' || startsWith('../')`) and returns `null` BEFORE the `normalizeLink` call at :531, so `computeBodyEdits` skips the candidate and an above-repo-root outbound link's authored bytes survive VERBATIM instead of being silently clamp-retargeted. AC#3 non-regression traced: a legitimate in-repo cross-subtree link (`../../backlog/tasks/foo.md` from docs/stories/ → repo-relative `backlog/tasks/foo.md`, no `..` prefix) does NOT fire the guard and is still correctly recomputed to the 4×-`../` link for the deeper destination (discriminating toContain). links.ts NOT edited (sibling LORE-245 owns it). Mutation-proven (revert fails exactly the new AC#2 test). One non-blocking comment-accuracy nit (the `decoded.startsWith("/")` "can never trigger" parenthetical slightly overclaims — behavior is correct; guard sits after the ternary covering both sub-branches). Scope: rewrite.ts + rename.test.ts. Re-verified post-rebase: bun test 2015/0; tsc clean; rename.test.ts 107/0. Merged e622292 (PR#234). |

## Not queued — needs a human / blocked
- **(finding) build-ci-config** .github/workflows/ci.yml:77: The mkdocs/docusaurus scaffold smoke jobs install floating dependency ranges (`pip install "mkdocs>=1.6" "mkdocs-material>=9"`, and a lockfile-free `npm install` for the freshly scaffolded website/ project). — STILL PRESENT: ci.yml:77 `pip install "mkdocs>=1.6" "mkdocs-material>=9"` (floating) and ci.yml:114 `npm install` (no lockfile). But this is a deliberate CI-design tradeoff, not a mechanical fix an agent should make unil
- **(finding) build-ci-config** biome.json:10: biome's `includes` covers only `src/**/*.ts`, `test/**/*.ts`, and root `*.json`, leaving `bin/lore.cjs`, the five `npm/*/package.json` manifests, workflow YAML, the Dockerfile, and shell scripts outside any lint/format g — STILL PRESENT: biome.json:10 `"includes": ["src/**/*.ts", "test/**/*.ts", "*.json"]` — bin/lore.cjs and npm/*/package.json are outside the gate. But this needs a maintainer/tooling decision, not an autonomous fix: (1) Bi
- **(finding) build-ci-config** bunfig.toml:10: Coverage threshold is pinned at 0.0 with a comment claiming it will ratchet up 'as the core/ library lands', and coverage is not run in CI at all. — STILL PRESENT: bunfig.toml:10 `coverageThreshold = 0.0`; the comment (lines 8-9) says the floor 'is raised as the core/ library lands so coverage can only ratchet upward'; and ci.yml runs `bun test` (lines 30,33) with no
- **(finding) build-runtime** test/support/backlog-golden.ts:34: The golden test's 'contract mirror' schema is a direct re-export of the production adapter's own Zod schema, so loosening the runtime parser also loosens its own regression test. — Structurally STILL TRUE. test/support/backlog-golden.ts:30-39 re-exports `EnvelopeKind`, `EnvelopeSchema`, `SearchHitSchema`, `TaskSchema`, `TaskSummarySchema` straight from src/adapters/backlog.ts, where those schemas a
- **(finding) cmd-crud-b** src/commands/fswrite.ts:272: writeAllOrRollback's rollback restores/deletes paths without verifying they still hold this transaction's own written version. — Still live. `writeAllOrRollback`'s rollback (catch block, src/commands/fswrite.ts:566-575) replays each undo step: created files are deleted (`rmSync` pushed at :553/:563) and force-overwritten files are restored to thei
- **(finding) core-bundle-check** src/core/bundle.ts:286: The walk collects all file paths (checking `entry.isSymbolicLink()`/`isFile()` via `Dirent`) before any file is opened, so a TOCTOU window exists where a regular file could be replaced by a symlink between the walk and t — Still present in live code, exactly as described. `loadBundle` (src/core/bundle.ts:174) collects paths via `walkMarkdown`→`walkFiles`, whose per-entry checks use `Dirent` (`entry.isSymbolicLink()` at bundle.ts:362 skips 
- **(finding) core-managed-template** src/core/template.ts:66: resourceFor joins `resource_base` as an opaque trimmed string with no URL validation, so a base carrying a query string, fragment, or non-hierarchical scheme joins in a semantically odd way (e.g. `https://x/base?lang=en` — The defect still exists verbatim. resourceFor is now at src/core/template.ts:62-68; the cited behavior is line 66 `const base = resourceBase.trim().replace(/\/+$/, "");` followed by `return `${base}/${encodePathSegments(
- **LORE-196** [needs-human, repo-admin]: make the docker-e2e CI job a required status check on dev/main — a GitHub branch-protection/ruleset toggle an autonomous agent must not perform (repo currently has no protection/ruleset; required-check context string captured in the task).
- **LORE-42 / 43 / 44 / 45**: deferred v2 roadmap (lore mcp server; Confluence one-way publish adapter; Confluence production mirror; typed importable library build) — product deferrals, not agent-resolvable cleanup.

## Wave log
- 2026-07-23 — wave 19 (issues: LORE-198, LORE-199, LORE-200, LORE-204, LORE-208, LORE-209; workers: sonnet, reviewer: fable): docs-first opening wave of round 3. Six file-disjoint tasks across distinct clusters (build-runtime / cmd-check / core-engine-b / build-ci-config / cmd-meta-d / cmd-rename-supersede). At scheduling time, caught and avoided a real cross-cluster same-file collision — LORE-199 (cmd-check) and LORE-203 (core-index-context) both edit `docs/reference/cli-surface.md`, so LORE-203 was held back to a later wave (the file-citation read, not the cluster label, is what caught it). All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after its serial rebase onto the moving dev base: full `bun test` 1913 pass / 0 fail + `tsc --noEmit` clean; LORE-199 additionally `lore check` 38 files/0 errors/0 warnings; LORE-204 release.yml YAML-valid + exact-match mirror of ci.yml:56-64; LORE-209 test/rename.test.ts 105 pass (enforced via a test-side spy, zero src change). Merged SHAs: LORE-198=7e8c20d (PR#187), LORE-199=03faf9e (#188), LORE-200=e3cc835 (#189), LORE-204=282798d (#190), LORE-208=8781404 (#191), LORE-209=81d9469 (#192). Wave-level Fable integration review over 4a31575...dev: **clean** — files genuinely disjoint, typecheck clean, full suite 1913/0, zero cross-task findings (verified the lone symbol-visibility change — InstructionsData export — has a single declaration with its sole consumer importing it, plus every embedded cross-file claim). Process note: the dispatch-mark commit was made on local dev but not pushed before basing worktrees on it, so GitHub's rebase-merge replayed it onto origin/dev under a new SHA (52dd1d0); reconciled by resetting local dev to origin/dev — content identical, no loss. Fix for next wave: push the dispatch mark to origin/dev before creating worktrees, or base worktrees on origin/dev.
- 2026-07-23 — wave 20 (issues: LORE-201, LORE-203, LORE-205, LORE-207, LORE-195, LORE-211; workers: sonnet, reviewer: fable): six file-disjoint tasks across distinct clusters (core-engine-b / core-index-context / build-ci-config / cmd-check / cmd-crud-b-tooling / cmd-meta-a) — a mix of docs, test-infra, one real code fix, and the lint-baseline restoration. All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after its serial rebase onto the moving dev base: full `bun test` 0 failures (suite grew 1913→1917 as tasks added tests) + `tsc --noEmit` clean; LORE-203 also `lore check` 38/0/0 with no src/ change; LORE-207 (real fix: cancel undrained response.body on check --external, SSRF invariant preserved) check.test.ts 207 pass incl. new body-cancel test; LORE-205 dirty-path consumers (link/sync/rename) 200 pass; LORE-195 restored `bun run lint` to GREEN (exit 0, 0 errors/0 infos) with behavior-preserving edits, no biome.json rule relaxed. Merged SHAs: LORE-201=e001430 (PR#193), LORE-203=8da5fee (#194), LORE-205=1cc7275 (#195), LORE-207=6a897a0 (#196), LORE-195=870abc4 (#197), LORE-211=041d487 (#198). Wave-level Fable integration review over 470ea33...dev: **clean** — files genuinely disjoint; the highest-risk seam (LORE-205 rewriting the dispatch mechanism of git-spawn test fakes shared by three UNTOUCHED consumer suites) is safe because production issues exactly one `git status` per commit sequence, so subcommand dispatch is behaviorally identical for every current caller; LORE-195's managed-block template-literal rewrites confirmed byte-identical (fixpoint tests green); final dev typecheck clean, full suite 1917/0, and lint STAYS green (no sibling — incl. LORE-211's new test — re-reddened it). Process note: dispatch mark was pushed to origin/dev BEFORE basing worktrees this wave (the wave-19 divergence fix), so every rebase-merge was clean with no dup-commit skip and no local/origin divergence.
- 2026-07-23 — wave 21 (issues: LORE-202, LORE-210, LORE-212, LORE-213, LORE-214, LORE-215; workers: sonnet, reviewer: fable): six file-disjoint tasks across distinct clusters (core-engine-b / core-index-context / cmd-meta-d / core-concept-manifest / core-managed-template / core-replace) — one module-doc correction, one real encoder-dedup, and four test-strengthening tasks (two of them mutation-killers). All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after serial rebase: full `bun test` 0 failures (suite grew 1917→1923 as tasks added tests) + `tsc --noEmit` clean; LORE-210 also `lore check` 38/0/0 (byte-stable index.md destinations). The two mutation-killer tasks (LORE-212 symlink realpath-dedup, LORE-213 manifest kind-vs-emitted drift-guard) each PROVED their test fails when the code is degraded via a throwaway revert experiment (on discover.ts / manifest.ts respectively), then reverted it — final diffs are test-only. Merged SHAs: LORE-202=1039b0a (PR#199), LORE-210=44d4a8f (#200), LORE-212=f5c85c8 (#201), LORE-213=b81b1cf (#202), LORE-214=b1821f4 (#203), LORE-215=d6f7bc2 (#204). ALSO reconciled LORE-206 (build-runtime finding [2]) as a confirmed DUPLICATE of LORE-205 (build-ci-config finding, merged wave 20 PR#195 1cc7275) — the two findings describe the identical dirtyGitSpawn/failingCommitGitSpawn change in test/helpers.ts; verified all 4 of LORE-206's ACs already satisfied on dev, marked Done (no separate code change). Wave-level Fable integration review over 311ff02...dev: **clean** — files genuinely disjoint; the highest-risk seam (LORE-210 deleting a local encoder and re-pointing to links.ts encodePathSegments) is a PROVABLE no-op (deleted copy was a self-documented faithful clone; regex/escape-casing/join semantics match exactly across char codes 1–0x24F; no sibling touched links.ts or the indexes call sites); LORE-215's 500-line re-indent has a byte-identical test inventory (no test dropped/reordered/moved; pure-engine + fswrite suites no longer run the root hooks; cleanup guarded); both mutation-proof edits confirmed absent from the cumulative diff. Final dev: typecheck clean, full suite 1923/0, lint STAYS green (wave-20 baseline preserved).
- 2026-07-23 — wave 22 (issues: LORE-216, LORE-217, LORE-219, LORE-223, LORE-225, LORE-227; workers: sonnet, reviewer: fable): six file-disjoint tasks across distinct clusters (errors-output-git / adapter-backlog / core-concept-manifest / cli-entry-state / cmd-check / cmd-crud-a) — one test-only tautology-replacement (216), two real hardening enhancements (217 subprocess timeout, 219 modeline contract), and three real bug fixes (223 `--` terminator, 225 canonical-identity root dedup, 227 arg-parse-before-profile). This wave opened the round-3 bug/enhancement front (docs/chore/task front was cleared in waves 19-21). All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after its serial rebase onto the moving dev base: full `bun test` 0 failures (suite grew 1923→1936 as tasks added tests) + `tsc --noEmit` clean; LORE-223 AC#1/#4 additionally re-checked live post-rebase (`--version tasks -- -x` prints version exit 0; `--version tasks --bogus` unknown-option exit 2); LORE-225 test/check.test.ts 210 pass; LORE-227 test/new.test.ts 56 pass. LORE-216's AC#3 was mutation-proven by the reviewer (inlining a divergent row layout in tasks.ts renderTable OR orphans.ts renderReport each fails exactly the new byte-identity test; both experiments reverted). LORE-217's timeout maps to a typed LoreError(validation, exit 6) on SIGKILL, is env-overridable (LORE_BACKLOG_TIMEOUT_MS, default 30000ms), and leaves the injectable seam + fakes untouched. Merged SHAs: LORE-216=25014c3 (PR#205), LORE-217=7287f06 (#206), LORE-219=b9d4eb0 (#207), LORE-223=ea9ecde (#208), LORE-225=6fad768 (#209), LORE-227=f2b78b3 (#210). Wave-level Fable integration review over e16a4de...f2b78b3: **clean** — no cross-task defects across all four seams checked: (1) no duplicate/rival implementations — 217's spawn-timeout is the only spawn-bounding mechanism (check.ts's pre-existing HTTP LIVENESS_TIMEOUT_MS is a different domain, untouched), and 225 ADOPTED the existing canonicalIdentity rather than inventing a rival dedup helper; (2) the two new exit-6 validation errors (217 in backlog.ts, 219 in concept.ts) live in disjoint modules, both use the codebase-standard `new LoreError("validation", …)` (no shared factory bypassed), and every BacklogSpawn consumer was traced to correctly propagate 217's new timeout-rejection mode (probeBacklog rethrows non-ENOENT; the bare catch blocks wrap only JSON/YAML parses, never a spawn await); (3) no shared-helper drift — no sibling touched src/output.ts/tasks.ts/orphans.ts/discover.ts/errors.ts/test-helpers.ts, so 216's byte-identity pin and 225's canonicalIdentity dependency both rest on unchanged code; (4) no test-level interference — all six changed test files are self-contained (per-test mkdtemp roots torn down in finally/afterEach, backlog-probe's env mutation is save/restore, resolveBacklogTimeoutMs re-reads per call so 216's new transitive import can't be poisoned). Final dev (f2b78b3): typecheck clean, full suite 1936/0, lint GREEN (108 files). Process note: dispatch mark pushed to origin/dev (e16a4de) BEFORE basing worktrees, so every rebase-merge was clean with zero local/origin divergence (the wave-19 fix, held since).
- 2026-07-23 — wave 23 (issues: LORE-218, LORE-220, LORE-221, LORE-224, LORE-226, LORE-228; workers: sonnet, reviewer: fable): six file-disjoint tasks across distinct clusters (adapter-backlog / core-concept-manifest / errors-output-git / cli-entry-state / cmd-check / cmd-crud-a) — two hardening enhancements (218 drop the lone `this`-binding; 220 deep-freeze buildManifest singletons), one net-new display-width column-alignment enhancement (221), and three real bug fixes (224 state.ts show-prefix `.trim()` leading-whitespace corruption; 226 check finding-output control-char sanitization; 228 boolean-flag inline-value rejection in replace.ts+validate.ts). All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after its serial rebase onto the moving dev base: full `bun test` 0 failures (suite grew 1937→1959 as tasks added tests) + `tsc --noEmit` clean; LORE-221 additionally re-ran test/output.test.ts (68 pass — LORE-216's byte-identity test still green, AC#4) with package.json untouched (AC#5, no new dep); LORE-224 state.test.ts 45 pass; LORE-226 check.test.ts 214 pass (incl. LORE-225's dedup tests) with a mutation-proof (reverting the src fix fails exactly the 3 new injection tests); LORE-228 AC live-verified post-rebase (`replace --regex=false`, `replace --dry-run=false`, `validate --strict=false` each exit 2; orphans.ts/graph.ts/args.ts untouched). LORE-218's new destructured-call test is a mutation-killer (reverting backlog.ts fails exactly it). One cosmetic note: LORE-220's task final-summary says "7 new tests" but 6 were added — a doc inaccuracy in the task notes, no code impact (reviewer approved). Merged SHAs: LORE-218=7b5226a (PR#211), LORE-220=334bf7d (#212), LORE-221=afbcb5a (#213), LORE-224=5c84f3e (#214), LORE-226=2d1ef7c (#215), LORE-228=e36c546 (#216). Wave-level Fable integration review over ba056b0...e36c546: **clean** — no cross-task defects across all four seams: (1) no rival helpers — displayWidth/padEndDisplay (output.ts) are the only width helpers and stripAnsiAndControls stays single-homed in errors.ts (check.ts is merely a new importer); their character domains are disjoint (strip = C0/C1+ANSI; width zero-set = Cf/Mn), so they never disagree on a byte; (2) no shared-helper drift — nothing monkey-patches listTasks/searchByLabel on a createBacklogAdapter instance (grep-clean; fakeAdapter builds its own object), and the only buildManifest consumer (commands/help.ts) is strictly read-only (spread-copies before use), so the deep-freeze breaks no caller; (3) contract interactions compose correctly — LORE-216's byte-identity test uses ASCII fixtures where displayWidth===.length so output is byte-preserved; check's findingLine output is never column-padded (renderReport joins raw lines, doesn't route through renderTaskSummaryRows), and where strip+pad DO meet (renderTaskSummaryRows) the order is strip→singleLine→measure→pad (widths measured on exactly the printed text — correct); LORE-228 converges with the shared args.ts tokenizer's existing takesValue:false rejection; (4) no test-level interference — the LORE-220 freeze tests throw before mutating so the process-shared frozen singletons can't pollute LORE-213's read-only golden in the same file; output.test.ts's LORE-221/LORE-216 additions use local literals / per-test mkdtemp dirs. Final dev (e36c546): typecheck clean, full suite 1959/0, lint GREEN (108 files). Non-defect noted out-of-scope (no task filed): 3 pre-existing `.length`-based padEnd sites remain (help.ts:97/100/130, instructions.ts:176, orphans.ts:420), all ASCII-by-construction — the residual sites if display-width alignment is ever wanted repo-wide. Process note: dispatch mark pushed to origin/dev (ba056b0) BEFORE basing worktrees, so every rebase-merge was clean with zero divergence.
- 2026-07-23 — wave 24 (issues: LORE-222, LORE-197, LORE-229, LORE-230, LORE-233, LORE-238; workers: sonnet, reviewer: fable): six file-disjoint tasks across distinct clusters (adapter-backlog / cmd-check / cmd-crud-a / cmd-crud-b / cmd-link / cmd-meta-c) — all real low-severity bug fixes. Scheduling caught two conflicts the cluster labels alone would miss: (a) LORE-233 (cmd-link) relocates the shared `mapWithConcurrency`/`TASK_DETAILS_CONCURRENCY` out of reconcile-shared.ts, and LORE-235 (cmd-meta-a) imports those same symbols from reconcile-shared.ts → held 235 to a later wave (over-approximated as conflicting); (b) LORE-236's true edit target is the shared src/errors.ts (WarningCollector.flush), NOT tasks.ts as the tracker Note column implied — swapped it out for the fully-isolated LORE-238 (scaffold.ts) so all six source files stay disjoint. All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after its serial rebase onto the moving dev base: full `bun test` 0 failures (suite grew 1959→1972 as tasks added tests) + `tsc --noEmit` clean; LORE-233 additionally whole-repo typecheck (catches any dangling import from the helper relocation) + instrumented peak-in-flight cap test; LORE-222's new later-spawn-ENOENT test is a mutation-killer (base backlog.ts fails it); LORE-229's ESC+newline strip + LORE-197's multi-root advisory-survives-throw tests likewise mutation-proven. Merged SHAs: LORE-222=0aa33a5 (PR#217), LORE-197=0adacac (#218), LORE-229=e9b182d (#219), LORE-230=2cd4739 (#220), LORE-233=1b1ce24 (#221), LORE-238=0a7a2b3 (#222). Wave-level Fable integration review over be7c289...0a7a2b3: **clean** — no cross-task defects across all four seams: (1) the LORE-233 relocation is internally consistent — `mapWithConcurrency`/`TASK_DETAILS_CONCURRENCY` are defined exactly once (in the new dependency-free src/commands/concurrency.ts, byte-identical to the deleted reconcile-shared.ts block), link.ts imports from ./concurrency, reconcile-shared.ts imports+re-exports them (a LIVE, documented re-export — its own resolveTaskDetails and check.ts's probeLiveness are the two surviving consumers), no dangling import, no duplicate, acyclic (check → {link, reconcile-shared} → concurrency), tree ready for LORE-235; (2) check.ts's single-flush invariant holds — the two `.flush()` sites are on DIFFERENT WarningCollectors (LORE-197's `advisories.flush` at :180 inside the finally, executed once per runCheck; a fresh `late` collector's flush at :261 for the --external liveness-abort advisory, deliberately separate to avoid non-draining re-emit), and LORE-197 composes cleanly with the already-merged LORE-226 sanitization + LORE-225 dedup in the same file; (3) no shared-helper drift — src/errors.ts is UNTOUCHED by the wave (diff --stat empty), LORE-229's replace.ts sanitizeField mirrors query.ts's shared-primitive composition exactly, and LORE-222's spawnOrThrow pass-through of already-typed errors is correct because LoreError carries no top-level `.code` (the LORE-217 timeout LoreError stores timeoutMs inside `input`, so errnoCode returns undefined and it falls through unwrapped as its docstring claims); (4) the real composition seam LORE-222 × LORE-233 is safe — runLink's newly-bounded fan-out calls verifiedViewTask → adapter.viewTask → spawnOrThrow (all 6 raw spawn sites now routed), and link.ts catches the now-typed rejections PER-INDEX inside the pool callback (:194-206) so the worker pool never aborts and first-invalid-id-in-argument-order reporting is preserved; LORE-230 × LORE-238 have no interaction (existingIsRegularFile is fswrite-private, callers init/new not in wave; scaffold.ts doesn't import createIfAbsent). Final dev (0a7a2b3): typecheck clean, full suite 1972/0. Two low residual nits noted, NO task filed: check.ts reaches mapWithConcurrency via the reconcile-shared re-export rather than the canonical ./concurrency (migration candidate once LORE-235 lands, would let the re-export be dropped); `sanitizeField` now exists as two identical documented one-line local helpers (query.ts, replace.ts) over the shared errors.ts primitives (future hoist candidate, not a divergent copy). Process note: dispatch mark pushed to origin/dev (be7c289) BEFORE basing worktrees, so every rebase-merge was clean with zero local/origin divergence (the wave-19 fix, held since).
- 2026-07-23 — wave 25 (issues: LORE-231, LORE-234, LORE-235, LORE-237, LORE-239, LORE-243; workers: sonnet, reviewer: fable): six file-disjoint tasks across distinct clusters (cmd-crud-b / cmd-link / cmd-meta-a / cmd-meta-b / core-bundle-check / core-engine-a) — all real low-severity bug fixes. Live conflict-graph scheduling (all 18 remaining tasks have zero formal deps, so the file+test conflict graph is the whole scheduler): greedy one-per-cluster in ordinal order gave exactly these six, fully disjoint. Two stale-guidance traps caught from task bodies (written pre-wave-24) and corrected in the worker prompts, verified live on dev first: (a) LORE-235's AC#1 says import the concurrency helper "from reconcile-shared.ts", but LORE-233 (wave 24) RELOCATED mapWithConcurrency + TASK_DETAILS_CONCURRENCY to the canonical dependency-free src/commands/concurrency.ts — worker directed to import from ./concurrency; (b) LORE-237's AC#1 (--strict=<value> rejection) was ALREADY shipped by merged LORE-228, so the worker was scoped to the repeated-flag residual (repeated --strict / repeated --type) only. Also confirmed two collateral test-file conflicts that the source-file graph alone would miss and kept OUT of this wave (LORE-232 & LORE-246 both add to test/query.test.ts; 236/249/250 all touch src/errors.ts). All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after its serial rebase onto the moving dev base (mandatory even when the rebase was clean/no-op): full `bun test` 0 failures (suite grew 1973→1987 as tasks added 14 tests) + `tsc --noEmit` clean; LORE-235 additionally whole-repo typecheck (no import cycle from the ./concurrency import) + a peak-in-flight cap test saturating AND capping at 8; LORE-231's new mid-write-leaves-temp test + LORE-235's cap test + LORE-243's equivalent-spelling tests are mutation-proven (base-revision fails them). Merged SHAs (gh pr merge --rebase --admin): LORE-231=90a1a95 (PR#223), LORE-234=b128692 (#224), LORE-235=d1fbeb6 (#225), LORE-237=cbc9bf3 (#226), LORE-239=3f8316d (#227), LORE-243=8258491 (#228). Wave-level Fable integration review over 04f6d00...8258491: **clean** — the six edited source files are fully disjoint (fswrite.ts / link.ts / tasks.ts / validate.ts / core/check.ts / core/log.ts) and all 10 cross-task seams verified with evidence: (1) LORE-231's writeFileAtomic O_EXCL restructure has no wave-internal callers (replace.ts/agents.ts/sync's path only; link.ts imports the UNTOUCHED writeFileOverwriting); (2) the spy-based tests OUTSIDE the wave that hook writeFileSync (rename.test.ts, fswrite.test.ts/LORE-230) still target writeFileSync-based functions so they remain live, not vacuous, and the one suite depending on writeFileAtomic internals (replace.test.ts LORE-116) was migrated in-wave to openSync/writeSync spies with its discriminating assertions intact; (3) mapWithConcurrency/TASK_DETAILS_CONCURRENCY have exactly one definition (concurrency.ts), imported directly by link.ts + tasks.ts and re-exported by reconcile-shared.ts for check.ts — no duplicate, no cycle (tasks→link→concurrency is a DAG); (4) LORE-235's call-shape matches mapWithConcurrency's contract and the never-rejecting callback preserves the pre-fix first-failure-in-tasks:-order rethrow; (5) LORE-234 changed only runLink-private docChanged/addDoc, no exported link.ts symbol shape changed for its tasks.ts importer; (6) LORE-234's case-insensitive dedup coexists correctly with moveBackRefs' deliberately EXACT-case rename-path matching (no contradiction); (7) LORE-237's new usage messages byte-match the established sibling-parser pattern, no overlap with tasks.ts's pre-existing --status repeat-guard; (8) LORE-239's structural calloutFinding fires only on blockquote nodes (no double-flag with the per-text-node detectors, blockReferenceFinding/mdxHazard untouched); (9) LORE-243's resolveRoot is module-private with only generateLog+buildLog consumers, no competing root normalization elsewhere; (10) integrated tree typecheck clean, full suite 1987/0, lint green (109 files). Three record-only residual nits, NO task filed: `lore check`'s parser still silently accepts repeated --strict/--external (pre-existing, outside LORE-237's scope, both idempotent booleans — cross-command harden candidate); tasks.ts:180's `settled[i] as PromiseSettledResult` density is now a callback invariant rather than type-guaranteed (same pattern LORE-233/LORE-111 already use); LORE-234's casing-variant "already covered" is the reviewed design (mirrors hadDoc/removeDoc). Process note: dispatch mark pushed to origin/dev (04f6d00) BEFORE basing worktrees, so every rebase-merge was clean with zero local/origin divergence (the wave-19 fix, held since wave 20).
- 2026-07-23 — wave 26 (issues: LORE-232, LORE-236, LORE-240, LORE-244, LORE-245, LORE-247; workers: sonnet, reviewer: fable): six file-disjoint tasks across distinct clusters (cmd-crud-b / cmd-meta-a / core-bundle-check / core-index-context / core-links-resolution / core-rewrite-engine) — all real low-severity bug fixes. Live conflict-graph scheduling (all 12 remaining tasks have zero formal deps, so the file+test conflict graph is the whole scheduler): ordinal-greedy one-per-cluster gave exactly these six, fully source+test disjoint. Three cross-cluster/borderline edges were verified against the TASK BODIES (not the tracker Note column) and correctly handled: (a) LORE-247 edits ONLY src/core/rewrite.ts (the ctx.isMoved null-guard, before the normalizeLink call) and merely REFERENCES links.ts, so it does NOT conflict with LORE-245 (links.ts) — the two co-waved safely; (b) LORE-232 edits src/commands/query.ts (readTrimmedValue wrapper) not core/query.ts, and shares test/query.test.ts with LORE-246 → LORE-246 kept OUT of this wave; (c) the errors.ts trio LORE-236/249/250 — only LORE-236 (WarningCollector.flush) taken this wave, 249/250 deferred. All 6 approved by Fable on the FIRST pass (0 fix rounds). Each re-verified after its serial rebase onto the moving dev base (mandatory even when clean): full `bun test` 0 failures (suite grew 1992→2015 as tasks added 23 tests) + `tsc --noEmit` clean. Mutation-proven new tests (revert-of-fix fails exactly the new assertions): LORE-232 (4 assertions incl. preserved --limit strictness), LORE-236 (3 tests incl. dangling-id warnDangling→flush path), LORE-240 (4 behavior-dependent tests), LORE-244 (2 numeric/boolean coercion tests), LORE-245 (5 tests), LORE-247 (AC#2 above-repo byte-preservation test). Merged SHAs (gh pr merge --rebase --admin): LORE-232=102abdf (PR#229), LORE-236=e19e7f1 (#230), LORE-240=e4746e8 (#231), LORE-244=c63c00f (#232), LORE-245=fbd5ab9 (#233), LORE-247=e622292 (#234). Wave-level Fable integration review over 0ce63d2...e622292: **clean** — all six edited source files disjoint (commands/query.ts / errors.ts / core/check.ts / core/indexes.ts / core/links.ts / core/rewrite.ts) and every cross-file seam verified in LIVE code + a combined-behavior probe: (a) LORE-245's isDirectoryLink/lacksMarkdownSuffix changes are on functions DISJOINT from normalizeLink (the only links.ts symbol rewrite.ts imports; normalizeLink has zero wave-diff hunks) — LORE-247's rename path is unaffected, and its new null return flows into the pre-existing `if (newPath === null) continue` (same no-edit contract); (b) check.ts is validateLink's only src consumer and builds its mdast via bodyText — a live probe (frontmatter-free file with a leading indented code-block link `[x](..)` + a prose link `[y](..)`) yielded EXACTLY ONE portability finding (prose flagged by LORE-245, code excluded by LORE-240), confirming the two changes compose with no double-report and no lost finding, and the new bodyText branch replicates normalizeInput's BOM + CRLF/CR regexes byte-for-byte, skipping only the leading-\s+ strip (no normalization drift); (c) frontmatterScalar has a single definition (bundle.ts:633, unchanged, no cycle) and indexes.ts's new importer uses the same `?? fallback` contract as graph/query/context; (d) stripAnsiAndControls has a single definition (errors.ts:168) used in-module by flush with no import change/cycle, and no paint() feeds WarningCollector.add so intentional color is never stripped; (e) per-commit --stat confirms all 6 commits touch fully disjoint source AND test files (the collateral-test-file collision vector from prior waves is absent here); (f) LORE-247's above-root preserve-don't-clamp guard is semantically parallel to check.ts's pre-existing above-root out-of-scope guard (preserve + warn is coherent, not contradictory). Integrated gates: bun test 2015/0, tsc --noEmit clean, biome clean (109 files). Three record-only notes, NO task filed: LORE-244 changed conceptTitle from trimmed to verbatim strings (neutral today — index entries sort by link and linkText's singleLine trims before render, so padded-title index bytes are identical; a future conceptTitle consumer that skips linkText would surface untrimmed padding); bodyText is newly exported from check.ts for unit tests (no symbol collision); LORE-232's readTrimmedValue intentionally re-throws readValue's own usage-error text (same-file string duplication, not a cross-task concern). Also non-blocking: LORE-247's comment parenthetical ("the decoded.startsWith('/') sub-branch can never trigger this") slightly overclaims — the guard sits after the ternary so it correctly covers both sub-branches and the behavior is right; worth a one-word soften on a future touch of rewrite.ts. Process note: dispatch mark pushed to origin/dev (0ce63d2) BEFORE basing worktrees, so every rebase-merge was clean with zero local/origin divergence (the wave-19 fix, held since wave 20).
