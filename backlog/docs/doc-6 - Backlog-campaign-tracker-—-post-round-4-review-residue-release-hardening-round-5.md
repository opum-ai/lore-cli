---
id: doc-6
title: >-
  Backlog campaign tracker — post-round-4 review residue & release hardening
  (round 5)
type: other
created_date: '2026-07-25 23:20'
updated_date: '2026-07-26 15:26'
---
# Backlog campaign tracker — post-round-4 review residue & release hardening

Round 5 of the lore backlog campaign. Rounds 1–3 (doc-1 / doc-3 / doc-4, LORE-69..250) closed the entire Codex second-opinion review (doc-2). Round 4 (doc-5, LORE-253..264) closed the v1-release-readiness and e2e follow-ups — **complete, queue empty**.

This round is the **residue of round 4's own review gate**: three defects its reviewers found in code they were reviewing but which were deliberately out of scope for the task that surfaced them, plus the one security exposure round 4 documented and never fixed. It is a **small round** — 4 items, 2 waves at most.

Protocol: restore → compute the ready/conflict graph → mark the wave Dispatched → dispatch (parallel Sonnet implement + reviewer gate) → serialize the merge → update this tracker once more at settlement → loop until the queue is empty or blocked → write handover.

## Scope / order confirmation

- **Scope**: 4 agent-resolvable items — LORE-265, LORE-267, LORE-268, LORE-266. The 6 non-agent-resolvable items are in "Not queued" below.
- **Order**: user confirmed **"265 → 267 → 266"** on 2026-07-25 (lowest-risk-first: docs-only → one-line colour fix → the security-relevant coverage gap last, since its AC#3 sweep has the widest possible footprint). **LORE-268 was filed after that confirmation** at the user's explicit instruction ("file it as a task, queue it") and is slotted third — it is disjoint from everything, so its position only affects which wave it rides in, not correctness. Effective order: **265 → 267 → 268 → 266**.
- This is the wave-builder's **tie-break only**, NOT a strict execution order; the ready set is recomputed live each restore.
- All 4 queued tasks carry **zero formal Backlog dependencies** (verified live 2026-07-25); readiness is gated purely by the live pairwise file-conflict graph.

## Frontier

The "ready now" set is **always recomputed live** from `backlog/tasks/*.md` + this table at the start of every restore/wave — never trust a persisted "next wave" plan. Informational hint only: as of **wave 2 dispatch (2026-07-26)**, **4 queued** (3 Dispatched in wave 2, 1 held for wave 3), **3 resolved + integration residue**, **6 not queued**.

Baseline on merged `dev` @ `421f7bb`, verified not assumed: `bun test` **2181 pass / 0 fail** · `lore check` **40 files, 0 errors, 0 warnings** · `biome` clean (112 files) · `tsc --noEmit` clean · **docker e2e 302 passed / 0 failed** (run on integrated `dev`, since all four PRs were merged with `--admin`, bypassing the required check).

> **Reviewer model: OPUS**, confirmed by the user at the wave-1 restore after a live re-probe showed **Fable 5 still over its monthly spend limit**. Re-probe cheaply at each restore; if Fable is back, prefer it. Do not silently spend Opus without asking.

Live conflict edges as of end of wave 1 — **recompute, do not trust**:
- **LORE-266 <-> LORE-271 CONFLICT.** Both touch `src/commands/agents.ts`. LORE-271 also touches `docs/reference/cli-contract.md`; LORE-266 also touches `test/agents.test.ts`. They cannot share a wave.
- **LORE-269** (`docker/e2e/run-e2e.sh`) and **LORE-270** (`docs/reference/backlog-cli-contract.md`, `docs/reference/architecture.md`) are disjoint from everything.
- LORE-266's **AC#3** may pull in `src/commands/rename.ts` / `src/commands/sync.ts` (both call `assertNoSymlinkInAnyPath`) plus their tests.
- **`CHANGELOG.md` remains a known, accepted shared edge.** Wave 1 confirmed the cost is real but bounded: LORE-267 and LORE-268 produced a guaranteed add/add conflict at the `### Fixed` anchor, resolved keep-both by the serial merge queue. Do **not** drop the `[Unreleased]` requirement to avoid it.

Recomputed live at the wave-2 restore (2026-07-26): the predicted shape **held**. **Wave 2 = {LORE-266, LORE-269, LORE-270}** (pairwise file-disjoint), **wave 3 = {LORE-271}** (blocked out of wave 2 by the `src/commands/agents.ts` + `test/agents.test.ts` edge with LORE-266). Fable 5 re-probed at this restore and is **still over its monthly spend limit** — reviewer stays **Opus**, per the user's standing wave-1 authorisation for this round.

## Queue (confirmed order)
| # | Issue | Cluster | Formal deps | Status | Wave | Note |
|---|---|---|---|---|---|---|
| 1 | LORE-266 | security / test-coverage | — | Dispatched | 2 | bug (security-relevant). The pre-write symlink sweep `assertNoSymlinkInAnyPath` (the LORE-93 AC#5 invariant) has **zero test coverage** — deleting it fails no test, on `dev` as well as on any branch. `ensureDir`'s reactive per-call guard masks it in the single-target case. AC#3 sweeps the other call sites (`rename.ts`, `sync.ts`). [src/commands/agents.ts, src/commands/fswrite.ts, test/agents.test.ts] |
| 2 | LORE-269 | build-ci-config / dx | — | Dispatched | 2 | bug (HIGH). `docker/e2e/run-e2e.sh` is `set -uo pipefail` with **no `-e`** (line 17) and an unguarded `cd /workspace` (line 163), so run on a host the `cd` fails silently and its destructive phases execute against the caller's cwd. **Hit for real in wave 1** — overwrote `backlog/config.yml` and created 3 spurious Backlog tasks in a worktree before being caught and reverted. `set -e` cannot simply be added (the harness relies on continuing past failed assertions), so the guard must be local to the `cd`. [docker/e2e/run-e2e.sh]
| 3 | LORE-271 | cli-ux / docs-drift / cmd-crud-a | — | To Do | — | bug (medium). Found by wave 1's **integration review** — invisible to every single-task pass. LORE-267's colour fix made `lore agents --check` the first command where ANSI colour is the *sole* carrier of a per-file status: `actionLabel` collapses `protected` and `updated` to the same literal `out of date`. Falsifies `cli-contract.md` §6's "colour is never load-bearing" for piped/`NO_COLOR` consumers. **Conflicts with LORE-266** (both `src/commands/agents.ts`). [src/commands/agents.ts, docs/reference/cli-contract.md]
| 4 | LORE-270 | docs-drift / adapter-backlog / cmd-meta-a | — | Dispatched | 2 | bug (medium). `backlog-cli-contract.md` §2.4 calls `--labels`/`--label`/`--add-label`/`--remove-label` single-value last-wins. True at the doc's pinned v1.47.1 floor; v1.48.0 gave all four `createMultiValueAccumulator()`, and lore runs a v1.48.0-based binary. The same file also says lore consumes a build "at or past PR #790", so it contradicts itself on which version governs. [docs/reference/backlog-cli-contract.md, docs/reference/architecture.md]

## Resolved
| # | Issue | Status/date/wave | Evidence summary |
|---|---|---|---|
| 1 | LORE-265 | Done · 2026-07-26 · wave 1 | PR #256 -> `97f5cfe`. ADR-0009 §2 no longer claims `lore orphans` calls `backlog search --json`; now states the full three-part ownership rule incl. LORE-261's `parentTaskId` ancestor walk. Same drift class also corrected in `backlog-cli-contract.md`, `backlog-json-schema.md` §3/§4, ADR-0009's Context, and a `--label`->`--add-label` error in ADR-0009 + ADR-0002. Zero `src/` changes. **2 Opus passes.** Pass 1 `request_changes`: the AC#3 sweep was keyed on the literal string "orphan" and so missed `backlog-json-schema.md` §4, leaving the branch's own new text contradicting a peer doc. Pass 2 `approve` after a drift-*class* re-sweep; the reviewer re-derived all six clauses of the rewrite from source and checked the upstream flag surface against MrLesk/Backlog.md tag `v1.48.0` rather than the patched PATH binary. No CHANGELOG entry (deliberate; verified against LORE-67/LORE-52 precedent). `bun test` 2176/0. |
| 2 | LORE-267 | Done · 2026-07-26 · wave 1 | PR #257 -> `90f3942`. `lore agents` painted a `protected` bridge file green; now yellow, via a shared total `BRIDGE_ACTION_COLOR: Record<BridgeAction, string>` called by both `agents.ts` and `init.ts`. +4 pinning tests. **3 Opus passes.** Pass 1 `request_changes`: the diff shipped a documented safety property that did not exist — the reviewer added a 5th `BridgeAction` variant and both `tsc` and all 2180 tests stayed green while it silently painted success-green, contradicting the CHANGELOG *and* a code comment. Pass 2 `request_changes`: the guard was made real (TS2741, reproduced under probe) but the fix added a CHANGELOG claim that the `--check` two-colour split was pre-existing — disproved by rendering the same fixture through both trees. Pass 3 `approve`. Reviewer also traced the compile-time-for-runtime-totality trade and judged it unreachable and sound. `bun test` 2180/0, **docker e2e 302/0 re-run post-rebase**. |
| 3 | LORE-268 | Done · 2026-07-26 · wave 1 | PR #258 -> `697a331`. `environment: release` on the publish job as an out-of-file gate against the `workflow_dispatch`-any-ref vector, pinned by a test, plus a runbook section naming the repo-admin steps and the residual risk. **3 Opus passes.** Pass 1 `request_changes`: the CHANGELOG's bolded headline claimed the exposure was *closed* (false as shipped, contradicted by its own body 11 lines later); the runbook offered a deployment branch policy as a standalone alternative, which does not hold — `main` has **no** protection here. Pass 2 `request_changes`: the body was fixed but the First-release **checklist item** fronting it still offered the options as interchangeable. Pass 3 `approve` — reviewer independently reproduced the `bypass_actors` entry, `main`'s zero effective rules, blob-SHA identity of `release.yml`/its test across the whole branch, and a `--word-diff` proving the CHANGELOG reflow changed zero words. All 6 LORE-255 guarantees verified preserved. Repo-admin boundary respected (0 environments, no workflow runs). `bun test` 2177/0 -> 2181/0 on merged `dev`. |
| — | wave-1 integration residue | Done · 2026-07-26 · wave 1 | PR #259 -> `421f7bb`. Closed the two `fix-now-narrow` findings from the wave-level integration review: three `release-publishing.md` prose enumerations left stale by LORE-268, and a test that inlined `loadWorkflow()`'s body. **2 Opus passes.** Pass 1 `request_changes` — the first fix replaced a true narrow statement with "inert until **two** one-time setups", which is **false in the safety-reducing direction**: only the npm setup causes inertness; without the Environment the job *publishes, unprotected*. Also the approval-pause note never reached Rollback's re-dispatch — the same fixed-here/stale-there pattern recurring inside the fix. Pass 2 `approve`. |

## Not queued — needs a human / blocked
- **LORE-253** (migrate adapter to the released `--json` backlog): BLOCKED on an external event — MrLesk/Backlog.md must tag a release containing commit `22a091b` (PR #790). **Re-verified at init 2026-07-25**: latest upstream tag is still `v1.48.0` (2026-07-12), and LORE-254's daily watcher ran clean at 07:04Z that day and correctly opened no issue. **LORE-254 is its live trigger** — when the qualifying tag appears, a one-time `upstream-watch` GitHub issue naming LORE-253 is opened automatically. Check `gh issue list` at each restore.
- **LORE-257** (make windows-latest a required check): NEEDS-HUMAN (repo-admin) — toggling a branch-protection ruleset is a human action an autonomous agent must not self-authorize (same boundary as LORE-196). Agent role is prep/verify only. **Note**: LORE-268 has a related repo-admin half; if the user is doing repo-settings work anyway, these two pair naturally.
- **LORE-42** (lore mcp server), **LORE-43** (Confluence publish adapter), **LORE-44** (Confluence prod mirror), **LORE-45** (typed importable library): DEFERRED-v2 — post-v1 roadmap requiring product/scope decisions + a release milestone.

## Wave log
- 2026-07-25 — **init**: round-5 tracker created. Inventory found 10 non-terminal tasks: 4 agent-resolvable (queued), 6 not queued. Three of the four queued items (LORE-265/266/267) were **filed by round 4's own review gate** — each a real defect a reviewer found in code it was reviewing, deliberately kept out of the reviewed diff's scope. The fourth (LORE-268) was filed at init from round 4's documented-but-unfixed LORE-255 publish exposure, on the user's explicit instruction to queue rather than merely carry it forward.
  - **Baseline verified at init, not assumed**: `dev` @ `7df940c` clean, no worktrees, no `feature/*` branches, no open PRs; `bun test` 2176/0; `lore check` 40 files 0/0.
  - **Carried-over open blocker**: Fable 5 over its monthly spend limit — the reviewer model must be confirmed at the first restore.
  - **Carried-over lessons from round 4, load-bearing for this round:**
    1. **Every worker prompt must carry the `[Unreleased]` CHANGELOG requirement explicitly** for any user-visible change. The repo encodes the rule (`.github/PULL_REQUEST_TEMPLATE.md` checkbox, `docs/runbooks/dev-kickoff.md` step 6) but workers dispatched into bare worktrees never read either file. The gap recurred in **both** of round 4's waves and cost a follow-up branch each time. Note `docs/reference/cli-contract.md` §1.3/§7.2 make *substantial `--plain` reformatting* a contract-level change, so phrasing-only tasks need an entry too.
    2. **Re-review the fixes, not just the implementation.** Round 4's most valuable catches were in *fix* passes: a CHANGELOG entry whose four claims were false against source, and two blocking runtime defects (an invisible infinite hang, and an exit-0-with-empty-envelope partial success) that only pass 1 of a two-pass review found.
    3. **A confidently-worded but wrong doc is worse than a missing one.** Multiple round-4 findings were docs asserting things the source contradicted. Every quoted literal output string must be re-verified against the real binary, not reconstructed from a diff.
    4. **The docker e2e harness is a separate contract-test suite `bun test` does not cover, and it is a required CI check.** It serializes on a single `e2e-e2e` container — designate exactly **one** worker per wave to run it. Baseline **302 passed / 0 failed**.
    5. **`git stash` is repo-wide, not per-worktree** — it has previously swapped diffs between sibling worktrees in this campaign. Use `git diff > patch` + `git apply -R`/`git apply` for mutation checks.
    6. **A billing cutoff masquerades as a CI failure**: long jobs cancelled mid-flight, later attempts starting *zero* jobs with a generic "workflow file issue". `gh run rerun --failed` is a no-op on *cancelled* jobs — use a full `gh run rerun`.
- 2026-07-26 — **wave 1** (issues: LORE-265, LORE-267, LORE-268; workers: sonnet; reviewer: **opus**, by user decision after Fable re-probe failed): **all three merged, plus one integration-residue branch.** Merge order 265 -> 267 -> 268, serial, each rebased + re-verified + force-pushed before merge.
  - **Every one of the three drew `request_changes` on its first review pass, and two needed a second.** That is the gate working, not the wave failing — the implementations were sound throughout. **Every single blocking finding was a claim that had been written rather than tested**, and in each case the reviewer disproved it in under a minute by actually running something:
    1. **LORE-267** documented an exhaustiveness guard that did not exist. The reviewer added a 5th `BridgeAction` variant; `tsc` and all 2180 tests stayed green while the new action silently painted success-green. The false claim was also in a **code comment**, which would have misled the next maintainer into reintroducing the same bug class.
    2. **LORE-268**'s CHANGELOG headline said a security exposure was "closed" when it was fully open — and contradicted its own body 11 lines later. 19 of the entry's 20 claims audited true; the headline was the one that was not.
    3. **LORE-267's fix** then claimed the `--check` two-colour divergence was pre-existing; the reviewer rendered the same fixture through both trees and showed the fix *creates* it.
    4. **The integration-residue fix** wrote "inert until two one-time setups" — false in the **safety-reducing** direction (missing the Environment does not make the job inert, it makes it publish *unprotected*).
  - **Second recurring class: fixed in one place, left standing in another.** It appeared in FOUR separate branches — LORE-265 (ADR fixed, peer reference doc left contradicting it), LORE-268 (runbook body fixed, the checklist fronting it left stale), the integration branch (pause note added at Step 3 only, not Rollback), and inside that branch's own first fix. **Make a consistency sweep an explicit deliverable of every fix prompt**, not a hope: grep the whole file for the claim being corrected, not just the cited line.
  - **Wave-level integration review found the one defect no single-task pass could see** (filed as LORE-271): LORE-267 made `lore agents --check` the first command where ANSI colour is the sole carrier of a per-file status, because `actionLabel` collapses `protected` and `updated` to the same literal `out of date`. That falsifies `cli-contract.md` §6 — a doc neither branch touched. **This is the strongest argument for keeping the integration step.**
  - **One integration finding was dismissed on evidence, not deferred**: the reviewer argued LORE-265 should have had a CHANGELOG entry, citing LORE-60 as a docs-only precedent. `b8a4667` also touched `docker/e2e/run-e2e.sh`, so it is not docs-only. Two reviewers disagreed and the earlier one was right — **check disputed precedents directly rather than trusting the more recent opinion.**
  - **Incident (contained, verified):** a worker ran `docker/e2e/run-e2e.sh` directly on the host instead of via compose. It overwrote `backlog/config.yml` and created 3 spurious Backlog tasks in its worktree. Fully reverted; the orchestrator independently confirmed no trace in any commit, in the primary checkout, or on the branch. Root cause filed as **LORE-269**.
  - **Orchestrator process error worth not repeating:** the R4c dispatch-marking commit was made on `dev` and **not pushed**, so it rode into origin as part of LORE-265's PR. GitHub's `--rebase` merge then replayed it under a new SHA (`650de91`), diverging local `dev` and making `git pull --ff-only` fail. Harmless once diagnosed (content verified byte-identical before `reset --hard`), but **push the dispatch-marking commit immediately**.
  - New tasks filed at settlement: **LORE-269** (high), **LORE-270** (medium), **LORE-271** (medium) — committed and pushed as `c16a506` rather than left uncommitted in the working tree.
  - Merged SHAs: `97f5cfe` (LORE-265, PR #256) · `90f3942` (LORE-267, PR #257) · `697a331` (LORE-268, PR #258) · `421f7bb` (integration residue, PR #259).
