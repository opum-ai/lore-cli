---
id: LCLI-283.1.4
title: Establish LadybugDB performance packaging and scale gates
status: In Progress
assignee:
  - '@codex'
created_date: '2026-07-30 13:33'
updated_date: '2026-08-01 05:21'
labels:
  - ladybugdb
  - benchmark
  - packaging
  - performance
milestone: m-13
dependencies:
  - LCLI-283.1.3
  - LCLI-283.1.5
documentation:
  - docs/specs/local-graph-platform-roadmap.md
modified_files:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - benchmark/ladybug/accounting.ts
  - benchmark/ladybug/concurrency-evidence.ts
  - benchmark/ladybug/concurrency-protocol.ts
  - benchmark/ladybug/concurrency-worker.ts
  - benchmark/ladybug/fixture.ts
  - benchmark/ladybug/fixtures/v1/large.json
  - benchmark/ladybug/fixtures/v1/schema.json
  - benchmark/ladybug/fixtures/v1/small.json
  - benchmark/ladybug/gates.ts
  - benchmark/ladybug/gates/v1.json
  - benchmark/ladybug/native-probe.ts
  - benchmark/ladybug/orchestrator.ts
  - benchmark/ladybug/package-qualification.ts
  - benchmark/ladybug/protocol.ts
  - benchmark/ladybug/qualification-evidence.ts
  - benchmark/ladybug/report.ts
  - benchmark/ladybug/run.ts
  - benchmark/ladybug/statistics.ts
  - benchmark/ladybug/worker.ts
  - biome.json
  - docs/log.md
  - docs/reference/ladybugdb-benchmark-and-scale-acceptance-strategy.md
  - docs/runbooks/release-publishing.md
  - docs/specs/local-graph-platform-roadmap.md
  - src/core/ladybug-lifecycle.ts
  - test/ci-workflow.test.ts
  - test/ladybug-benchmark-fixture.test.ts
  - test/ladybug-benchmark-gates.test.ts
  - test/ladybug-benchmark-report.test.ts
  - test/ladybug-benchmark-runner.test.ts
  - test/ladybug-benchmark-workflow.test.ts
  - test/ladybug-concurrency.test.ts
  - test/ladybug-package-qualification.test.ts
  - test/ladybug-qualification-evidence.test.ts
  - tsconfig.json
parent_task_id: LCLI-283.1
priority: high
type: task
ordinal: 390000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Prove that the persistent index improves repeated retrieval at acceptable startup, memory, disk, packaging, and concurrency cost across Lore supported platforms before M6 is accepted.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Benchmarks report cold build, warm open, graph, query, and context latency plus memory and disk usage on versioned small and large fixtures
- [ ] #2 M6 defines quantitative regression thresholds and demonstrates a material warm-query improvement without unacceptable small-repository regression
- [ ] #3 Node and Bun distribution paths pass supported native platform install, build, smoke, and clean-uninstall tests
- [ ] #4 Single-writer and multi-reader scenarios, CLI and future long-running-process contention, crash recovery, and file-lock diagnostics are tested
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Freeze benchmark fixture schema `ladybug-benchmark-fixture/1` under `benchmark/ladybug/fixtures/v1/` and add a deterministic seeded generator plus digest tests. `small.json` will generate exactly 64 concepts, 128 tasks, 512 authored edges, and 1 MiB of Markdown body bytes; `large.json` will generate 4,096 concepts, 4,096 tasks, 32,768 authored edges, and 64 MiB of body bytes. Both fixtures will cover chains, stars, cycles, duplicate and dangling edges, Unicode, additive fields, common/rare/tied/no-hit lexical queries, graph depths, and context budgets. The committed specs will include expected canonical export, source-inventory, and task-snapshot SHA-256 values; generated repositories and caches stay temporary and untracked. (AC #1)
2. Add a benchmark-only worker and orchestrator under `benchmark/ladybug/` without a public backend flag or lifecycle change. A fresh worker process will select the existing injected `indexed` or `reference` policy, verify identical result digests first, and then measure: projection-cold build with no generation; fresh-process warm generation validation/open/reconstruction with an existing immutable generation; warm graph, query, and context operations; `Bun.spawn` wall and CPU time; subprocess `resourceUsage().maxRSS` bytes; canonical input bytes; emitted bytes; and recursive logical bytes only below `.lore/cache/graph/ladybug/1`. Cold means projection-cold, not an unverifiable OS page-cache flush. Every run will hash docs, Backlog, profile, Git metadata, and non-cache `.lore` bytes before and after so any source write fails the run. (AC #1)
3. Emit one ordered `lore.ladybug-benchmark/1` JSON document containing exact Lore, Bun, Node, Ladybug package/runtime/storage, OS/arch, runner image, CPU/RAM, commit/dirty state, fixture version/digests/counts, seed, scenario definitions, warmups, repetitions, randomized AB/BA order, every raw sample, median, p95, MAD, paired ratios, one-sided 95% bootstrap intervals, resource totals, gate definitions, and pass/fail/inconclusive reasons. Cold build uses 1 discarded setup plus 7 fresh generated-repository repetitions; warm open and each command use 5 discarded warmups plus 30 measured repetitions in three interleaved batches. A calibration workload with coefficient of variation above 10% makes timing evidence `inconclusive` and requires a rerun; it never silently passes. Schema/golden/digest tests will make the report machine reproducible while host facts and raw measurements remain honest evidence rather than byte-identical fiction. (AC #1, #2)
4. Freeze thresholds before collecting the acceptance run: cold-build p95 at most 2 s small / 30 s large; warm-generation-open p95 at most 350 ms small / 3 s large; cold peak RSS at most 512 MiB small / 2 GiB large; warm command peak RSS at most 384 MiB small / 1 GiB large; index logical bytes at most 64 MiB small and, for large, both 512 MiB and `64 MiB + 4 * canonicalExportBytes`. The primary material-improvement gate is large warm query indexed/reference median ratio at most 0.60, p95 ratio at most 0.75, and upper one-sided 95% interval for the paired median ratio at most 0.75. Large graph/context may not regress beyond 1.10 median or 1.20 p95. For every small warm open/graph/query/context case, indexed median regression is capped at the larger of 15 ms or 15%, and p95 regression at the larger of 30 ms or 25%. These are product budgets, not values fitted to the first result; changing them requires a reviewed task-plan update with the failed evidence retained. (AC #2; approval required)
5. Keep pull-request CI non-timing-gated: fixture determinism, JSON schema, result parity, resource accounting, and one-repetition benchmark smoke run there. Add a fixed `ubuntu-24.04` x64 release-qualification job that runs the full paired methodology with no services or parallel benchmark jobs, uploads the JSON evidence, and gates the existing release package/publish chain only on conclusive threshold success. Relative paired gates, robust statistics, three interleaved batches, generous independent resource ceilings, and explicit `inconclusive` reruns prevent noisy shared-runner timing from becoming either a flaky failure or a false pass. Darwin-arm64 local results from pinned Bun 1.2.23 will be recorded separately as supplemental evidence; matching-host CI, not cross-compilation or local Darwin results, is required for Linux and Windows claims. (AC #1, #2)
6. Extend the existing five-package release matrix with matching-host qualification: `macos-latest` (Darwin arm64), `macos-15-intel` (Darwin x64), `ubuntu-24.04-arm` (Linux arm64), `ubuntu-24.04` (Linux x64), and `windows-latest` (Windows x64). On each host, run an isolated frozen install; assert the exact `@ladybugdb/core@0.18.2` optional package, OS/CPU metadata, integrity, and copied `lbugjs.node`; compile the matching Bun target on the same host; pack the root launcher and matching Lore platform package; install both tarballs into a new Node project; run Node-launcher version/help plus graph/query/context; move the Bun executable away from the checkout and repeat the smoke; uninstall root and platform packages; and assert the launcher, package directories, addon, install scratch, and global locations are clean. Uninstall will not delete repository-local user cache implicitly; explicit contained cache disposal and source preservation are tested separately. Preserve the six-package publication order and all five Lore distribution packages. (AC #3; uninstall policy approval required)
7. Qualify native indexing only where executable evidence passes. The initial candidate native-index set is Darwin arm64/x64 and Linux arm64/x64. For Windows x64, a sacrificial child process will probe the exact addon and record its exit/signal; the main test process must still prove import-safe CLI, install, compile, launcher, graph/query/context reference fallback, no database creation, clean uninstall, and stable output under the known Bun 1.2.23 addon segfault. The five-platform distribution contract remains unchanged, but Windows native-index support is not claimed. If approval instead requires native indexing on all five distribution targets, leave AC #3 and LCLI-283.1.4 open until Bun/Ladybug supplies executable Windows evidence; never convert package availability or cross-compilation into a support claim. (AC #3; platform decision approval required)
8. Add `test/ladybug-concurrency.test.ts` plus a benchmark/test-private subprocess helper with a strict sanitized event/result protocol. Reuse the frozen deterministic small fixture and source snapshots rather than adding another fixture contract. Cover at least eight simultaneous read-only processes against one immutable generation; multiple connections in one long-running read-only process; an old-generation reader held open while one writer builds and atomically publishes a new generation; two missing-generation writers racing with exactly one owner and safe reuse/fallback; reusable and stale-source behavior under a live Lore writer lock; native same-file read-only/read-write conflict classification; stable redacted lock/dispose diagnostics; and unchanged deterministic result digests. Map the existing lifecycle hooks to child pause points after database close, after the fully fsynced control manifest, and after atomic rename, and add only the missing asynchronous `beforeLockRelease` test hook so a real child can be killed while ownership is still live. After each kill, prove staging cleanup, same-host dead-process stale-lock recovery, immutable-generation reuse or rebuild according to publication state, no partial public output, exact cache-root containment, and byte-identical repository sources. Keep public Cypher, native IDs, addon failures, crash text, paths/secrets, and lock-owner secrets out of user-visible output. (AC #4)
9. Add a strict task-level lore.ladybug-qualification-evidence/1 manifest builder and an opt-in lore.ladybug-concurrency-evidence/1 test report. The manifest must hash every input and map it directly to AC #1-#4. Accept only a full small+large pinned-Bun 1.2.23 Linux-x64 benchmark artifact with conclusive gate success; the exact committed gate digest; a separate pinned-Bun full Darwin-local report whose supplemental outcome remains explicit; exactly five matching-host qualification reports with complete install/build/pack/Node-launcher/Bun-binary/uninstall cleanup, four executable native-index verdicts, and the explicit Windows reference-fallback-only verdict; and a complete concurrency report covering every reader/race/live-lock/kill-point scenario with before/after source hashes, cache containment, and scratch cleanup. Reject missing, duplicate, wrong-host, smoke, inconclusive primary, skipped, simulated, cross-compile-only, or metadata-only evidence. Preserve the required benchmark command and focused benchmark/concurrency/lifecycle/indexed/release tests, keep workflow artifacts and job logs downloadable, and leave the actual exact-pin benchmark, five-host workflow execution, acceptance checks, and final qualification for item 10. (AC #1-#4)
10. After implementation only, run exact Bun 1.2.23 frozen install, package dry-run, audit, all focused tests, full `bun test --isolate`, lint, typecheck, source and compiled builds/smokes, the five matching-host qualification matrix, the conclusive benchmark gate, `lore sync`, `lore validate --strict`, `lore check --strict`, `git diff --check`, and tracked/untracked/cache/source-write audits. Update architecture, tech stack, release publishing, roadmap, and campaign handover only outside Lore-managed regions; record raw evidence and any approved platform limitation; then follow task-finalization without advancing parent LCLI-283.1 until every child gate is genuinely complete. Implementation remains blocked on explicit approval of the thresholds, fixture scale, Windows fallback-only native policy, uninstall/cache policy, and release-gate placement above.

11. Approved benchmark strategy (2026-07-31): supersede the 30-pair bootstrap, 3,976-worker, and 240-360 minute release-gate model. First complete LCLI-283.1.5 to select and qualify the exact supported LadybugDB version. Then use a deterministic 100 MiB authored-repository fixture as the blocking scale gate: record fixture counts and digest, perform one cold projection build, and collect five warm samples for representative query, graph, and context operations. Apply conservative initial budgets of 30 seconds for the cold build, 3 seconds for warm open, 500 milliseconds for ordinary queries, 1 second for graph/context operations, and 2 GiB peak RSS, with a 15-minute hard cap for the entire benchmark job. Keep the broader functional scenario matrix in fast non-timing tests. Add an opt-in 1 GiB manual/nightly run with one cold build and representative warm operations, a 30-minute time box, and informational non-blocking results until evidence supports a separate decision. Keep concurrency/crash recovery and five-host native-package qualification as separate gates. Do not publish benchmark numbers from this acceptance harness. Ground the rationale and interpretation in docs/reference/ladybugdb-benchmark-and-scale-acceptance-strategy.md.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Completed plan item 1: added the versioned ladybug-benchmark-fixture/1 schema, deterministic seeded small/large repository generator, exact 1 MiB/64 MiB body budgets, 512/32,768 authored-edge topologies, lexical/graph/context coverage, and pinned canonical-export/source-inventory/task-snapshot digests. Focused suite passes 47 assertions; generated small tasks also parse through Backlog JSON v1. Qualification timing remains unclaimed because the host Bun is 1.3.14 rather than the acceptance pin 1.2.23.

Completed a coherent plan-item-2 boundary: added a strict benchmark-private worker protocol; fresh-process indexed/reference policy injection; projection-cold and existing-generation warm-open/reconstruction; preloaded warm graph/query/context command operations; parity-before-measure orchestration; canonical-input, emitted-output, wall/CPU/maxRSS, and exact Ladybug-cache logical-byte accounting; and per-worker docs/Backlog/Git/profile/non-cache-.lore byte guards. Canonicalized the disposable fixture Backlog config so read-only task parsing does not rewrite it. Runner smoke: 3 tests passed with 120 assertions; combined fixture/runner focus: 6 tests passed with 150 assertions; full isolated suite, repository lint, typecheck, and git diff check passed. No thresholds, Windows policy, uninstall policy, release gate, acceptance checks, or qualification claims were started. Tests used Bun 1.3.14 rather than the pinned Bun 1.2.23, so these results are functional evidence only.

Completed plan item 3: added the ordered lore.ladybug-benchmark/1 report contract and CLI; frozen qualification versus smoke counts; deterministic balanced AB/BA scheduling across three interleaved batches; raw setup/parity/warmup/measurement samples; median, p95, MAD, paired indexed/reference ratios, and deterministic one-sided 95% bootstrap upper bounds; exact host/toolchain/repository/fixture/resource metadata; and a SHA-256 calibration workload whose coefficient of variation above 10% is explicitly inconclusive. Normalized Bun subprocess CPU bigint counters to safe JSON integers at the orchestrator boundary. The real small-fixture smoke produced and re-parsed 94 samples across 23 scenarios, then removed every temporary repository and cache. Report/runner/fixture focus: 11 tests passed with 187 assertions; full isolated suite, lint across 137 files, typecheck, and git diff check passed. The report intentionally carries no quantitative gate definitions and evaluates gates as inconclusive until plan item 4 is approved and implemented. Tests and smoke used Bun 1.3.14, not the qualification pin 1.2.23, so no performance qualification claim is made.

Qualification mode now fails before calibration or fixture generation unless Bun.version is exactly 1.2.23; Bun 1.3.14 is accepted only with the explicit --smoke functional-evidence mode.

User explicitly approved the plan-item-4 quantitative thresholds exactly as recorded on 2026-07-31. Implementation may freeze and evaluate those values; any later threshold change still requires a reviewed plan update with failed evidence retained.

Completed approved plan item 4: froze the exact thresholds and measurement semantics in benchmark/ladybug/gates/v1.json (sha256:169cc001f5334988aacff1fa89b864b1724396aebe4865256db5635f84479a59), added strict gate-set loading, and expanded each full two-fixture qualification report into 175 concrete gates. The evaluator covers cold/warm-open wall p95, cold and warm-command peak RSS, absolute and canonical-input-scaled index bytes, all five large-query median/p95/bootstrap ratios, every large graph/context median/p95 ratio, and every small warm-open/graph/query/context dynamic absolute-or-relative regression allowance. Qualification can pass only with exact methodology counts, both fixtures, 7 cold repetitions, 30 pairs per scenario, and stable calibration; failures and inconclusive reasons are explicit, while smoke remains successful functional evidence with inconclusive gates. Focused fixture/runner/report/gate suite: 15 tests passed with 214 assertions. A real small smoke produced and re-parsed 94 samples plus 72 inconclusive gate results. Full isolated suite, lint across 139 files, typecheck, and git diff check passed. All runs used Bun 1.3.14, so no qualification evidence or acceptance criterion is claimed.

User explicitly approved plan item 5 on 2026-07-31: keep pull-request CI timing-non-gated with functional smoke evidence, and gate the existing release package/publish chain on a fixed Ubuntu 24.04 x64 conclusive full qualification job.

Plan item 5 implemented on 2026-07-31. Pull-request CI now runs a timing-non-gated two-fixture Ladybug smoke on ubuntu-24.04, records the hosted runner image, and uploads the smoke report plus pinned gate policy with 30-day retention. Release qualification now runs the full two-fixture suite on fixed ubuntu-24.04 x64 with exact Bun 1.2.23, serialized concurrency, a 240-minute timeout, and an explicit conclusive-pass artifact assertion; the existing build, package, and publish chain is transitively gated on that job, and qualification artifacts are retained for 90 days. Static workflow coverage passed: 13 tests and 71 assertions. The exact two-fixture CI-equivalent smoke completed locally in about 17 minutes with 224 samples across 55 scenarios, 175 expected inconclusive smoke gates, and a 1,275,652-byte canonical report. Full bun test --isolate, repository lint (140 files), typecheck, actionlint, and git diff --check passed. Local runtime remained Bun 1.3.14, so this is functional evidence only and no qualification or acceptance-criterion claim was made. The disposable smoke report was removed and no generated repositories or Ladybug caches remain.

User explicitly approved plan item 6's uninstall/cache policy on 2026-07-31: uninstall the root launcher and matching platform package and prove launcher/package/addon/install-scratch/global cleanup, while never implicitly deleting repository-local user cache; verify explicit contained-cache disposal and repository-source preservation separately.

Plan item 6 implementation completed on 2026-07-31 under the approved uninstall/cache policy. The existing five-distribution release matrix now carries matching hosts macos-latest Darwin arm64, macos-15-intel Darwin x64, ubuntu-24.04-arm Linux arm64, ubuntu-24.04 Linux x64, and windows-latest Windows x64, together with exact Bun targets and frozen @ladybugdb/core platform-package SRI values. A benchmark-private package qualification runner asserts Bun 1.2.23 in qualification mode, host OS/CPU, @ladybugdb/core@0.18.2, matching optional-package metadata and lock integrity, and a byte-identical copied lbugjs.node; compiles on the matching host; packs the root launcher and platform package; installs both tarballs into an isolated Node project; exercises version/help/graph/query/context through both the Node launcher and a relocated standalone Bun binary; uninstalls both Lore packages; and conclusively audits launcher, package, addon, install-scratch, isolated-global, repository-cache, explicit-disposal, and source-preservation cleanup. Read-only immutable Ladybug generations are made writable only inside the validated unique scratch root before final removal, and every subprocess has a hard deadline. The canonical package/publish chain now depends on the five-host qualification matrix; the six-package publication order and five distribution packages remain unchanged. Static workflow/runner focus passed 19 tests with 124 assertions. A real Darwin-arm64 functional smoke passed all ten launcher/standalone commands and all eight cleanup assertions in about 11 seconds; its report recorded Bun 1.3.14 mode smoke and was removed, so it is not matching-host qualification evidence. Full bun test --isolate passed 2,320 tests and 6,968 assertions across 60 files; lint checked 142 files; typecheck, actionlint, and git diff --check passed. All temporary tarballs, fixtures, reports, scratch roots, and Ladybug caches were removed. AC #3 remains unchecked until the actual five-host Bun 1.2.23 workflow runs, and no Windows native-index policy from plan item 7 is claimed.

User explicitly approved plan item 7's fallback-only Windows policy on 2026-07-31: qualify native indexing only on Darwin arm64/x64 and Linux arm64/x64 when executable evidence passes; run the exact Windows x64 addon only in a sacrificial child; prove stable import/install/compile/launcher and reference-fallback graph/query/context with no database creation and clean uninstall on Windows; preserve the five-platform distribution while making no Windows native-index support claim.

Plan item 7 implementation completed on 2026-07-31 under the approved fallback-only Windows policy. Added a sacrificial native-probe child contract. Darwin and Linux matrix hosts must use indexed mode to load the exact driver, build an isolated database from the pinned small fixture, verify all 64 concepts, 128 tasks, and 512 authored edges, read the graph back, and emit a sanitized digest; any nonzero exit or signal fails their native-index verdict. Windows uses import-only mode, so even an unexpected future addon success cannot create a database. The parent records exit code, signal, and SHA-256 digests of child streams without exposing crash text; it accepts only SIGSEGV/SIGABRT or the concrete segfault/access-violation statuses as the known crash, while an ordinary exit-1 failure remains a qualification failure. Packaged Windows graph/query/context still runs in the main processes through the import-safe reference boundary, must create no Ladybug cache, and must produce byte-stable stdout digests across the Node launcher and relocated standalone binary. Report and workflow assertions enforce native-index claims only for Darwin/Linux and reference-fallback-only with no database for Windows. Crash working directories are contained under the validated scratch root. The five Lore distribution packages and six-package publication order remain unchanged. Focused platform/native/release coverage passed 51 tests with 215 assertions. A real Darwin-arm64 functional run on Bun 1.3.14 built and verified the full native probe with graph digest sha256:3200e856f04b6a8d2a44981ead7cd53f682be2b8e54a193cec8719ad3af79af2; the complete package smoke then passed in about 12 seconds with a native-index verdict, identical launcher/standalone version/help/graph/query/context digests, and every cleanup assertion. Full bun test --isolate passed 2,321 tests and 6,991 assertions across 60 files; lint checked 143 files; typecheck, actionlint, and git diff --check passed. All temporary evidence was removed. These local Bun 1.3.14 results are functional evidence only; AC #3 remains unchecked until the actual five-host Bun 1.2.23 workflow records four native-index verdicts and the Windows fallback verdict.

User explicitly approved plan item 8's concurrency and crash-test scope on 2026-07-31: eight simultaneous read-only processes; multiple connections in a long-running read-only process; old-generation readers during atomic publication; missing-generation writer races; live Lore writer-lock behavior; native same-file conflict classification; redacted diagnostics; deterministic result parity; and real child-writer kill points after database close, manifest fsync, atomic rename, and before lock release, with cleanup, recovery, containment, and source-preservation proof.

Completed approved plan item 8 on 2026-07-31. Added a strict benchmark/test-private concurrency subprocess protocol and worker plus test/ladybug-concurrency.test.ts. Real-process evidence covers eight simultaneously open read-only processes against one immutable generation; four connections in one long-running reader; an old-generation reader surviving a replacement generation's atomic publication; two missing-generation writers with exactly one builder and safe reuse/fallback; exact-generation reuse and changed-source fallback under a real live Lore writer lock; stable redacted dispose/lock diagnostics and public graph parity; and deterministic result digests. The native same-file probe objectively classifies @ladybugdb/core 0.18.2 read-only/read-write opens as read-write-compatible on this Darwin runtime; a separate native lock-failure boundary proves stable locked/unavailable classification without exposing the raw owner secret. Added only the missing asynchronous beforeLockRelease lifecycle test hook. Real child writers were SIGKILLed after database close, after the fully fsynced control manifest, after atomic rename, and immediately before lock release. Every point proves no child result after the checkpoint, abandoned-staging cleanup, same-host dead-process stale-lock recovery, build before publication or reuse after publication, one complete immutable generation, public output parity with no Cypher/native IDs/addon/crash/path/owner leakage, exact cache containment, and byte-identical docs/Backlog/Git/profile/non-cache-.lore sources. The races also exposed and fixed two lifecycle defects: cache-directory creation now tolerates a concurrent EEXIST only after validating the resulting real directory, and a stale writer lock is recovered even when the published generation is already reusable. Focused concurrency/lifecycle/indexed/package/benchmark coverage passed (71 tests), including two repeated writer-race runs. Full bun test --isolate passed 2,324 tests with 7,112 assertions across 61 files. bun run lint checked 146 files; bun run typecheck and git diff --check passed. Repository and OS scratch audits found no generated repositories, databases, Ladybug caches, reports, logs, or test scratch. Branch remains feature/lcli-283-1-4-ladybug-qualification and HEAD remains baseline 7b81734764658048a9924b0ba64cd810e8c212b7; no commit and no Lore documentation change were made. All local execution used Bun 1.3.14, so this is functional evidence only, not pinned Bun 1.2.23 matching-host performance or package qualification. The task remains In Progress and all acceptance criteria remain unchecked pending the complete required evidence.

Completed approved plan item 9 on 2026-07-31. Added the strict lore.ladybug-qualification-evidence/1 task-level manifest builder and lore.ladybug-concurrency-evidence/1 test artifact. The final builder hashes every input and maps evidence directly to AC #1-#4, but produces a pass only for one clean shared commit with: the exact frozen gate digest; complete small+large pinned-Bun 1.2.23 Linux-x64 benchmark samples and a conclusive 175-gate pass; a separate complete pinned-Bun Darwin report whose supplemental gate outcome remains explicit; exactly five matching-host package manifests with root/platform/addon/binary hashes, stable Node-launcher and relocated Bun-binary version/help/graph/query/context outputs, all cleanup assertions, four executable native-index verdicts, and the Windows reference-fallback-only/no-database verdict; and a clean pinned-Bun concurrency artifact containing all six records, four ordered kill points, deterministic result digests, source before/after inventories, cache hashes/counts, containment, lock/staging absence, and scratch cleanup. It rejects wrong-host, smoke, incomplete/inconclusive primary, duplicate, wrong-commit, skipped/simulated/cross-compile-only, metadata-only, or policy-inconsistent evidence. Package reports now carry and validate repository commit provenance. The release workflow adds a pinned ubuntu-24.04 real-process concurrency/lifecycle/indexed job, uploads its sanitized artifact, asserts the active GitHub commit, and gates build plus all five matching-host package jobs on both benchmark and concurrency qualification; benchmark and package reports also must match github.sha. Native same-file classification remains objective across runtimes by accepting either actual compatible or conflict classification while preserving redaction. A real local opt-in concurrency artifact passed 3 tests and 121 assertions, contained six complete records and all four kill points, reported stable sources and removed scratch, and contained no absolute path or lock-owner material; that temporary artifact was deleted. Focused benchmark/concurrency/lifecycle/indexed/release/package/evidence coverage passed 86 tests with 677 assertions. Full bun test --isolate passed 2,328 tests with 7,148 assertions across 62 files. Lint checked 149 files; typecheck, actionlint, and git diff --check passed. Repository and /private/tmp audits found no generated repositories, databases, Ladybug caches, reports, logs, evidence artifacts, or scratch. Branch remains feature/lcli-283-1-4-ladybug-qualification and HEAD remains baseline 7b81734764658048a9924b0ba64cd810e8c212b7; no commit and no Lore documentation change were made. All local execution used Bun 1.3.14 and is functional evidence only. The exact Bun 1.2.23 Linux/Darwin benchmarks, five matching-host workflow jobs, final manifest, and any acceptance checks remain item 10; the task stays In Progress with every AC unchecked.

Item 10 partial exact-pin verification on 2026-07-31: the frozen isolated install completed with Bun 1.2.23 using the copyfile backend required by this external-volume worktree; bun.lock remained byte-identical at sha256:e294f5f925c2bf03071b08a001e1edb13cbdb6a23d6f7c3b2f730ec5c4450685, and bun audit reported no vulnerabilities. The exact-pin focused item-10 suite passed 86 tests, 677 assertions, 0 failures; the full isolated suite passed 2,328 tests, 7,148 assertions, 0 failures across 62 files. Lint checked 149 files; typecheck, actionlint, and git diff --check passed. Source and compiled CLI version/help smokes passed. npm pack --dry-run succeeded for the root and all five platform manifests. A real disposable Darwin-arm64 package qualification on Bun 1.2.23 passed native indexing, Node-launcher/relocated-binary output parity, uninstall, explicit cache disposal, isolated-global cleanup, and source preservation. That local package report and every build, tarball, npm cache, report, and scratch artifact were removed.

Item 10 cannot yet collect admissible final evidence under the standing no-commit constraint. HEAD is still baseline 7b81734764658048a9924b0ba64cd810e8c212b7, all item 1-9 implementation is uncommitted, and no remote feature branch exists. GitHub matching-host jobs therefore cannot execute this implementation, while the item-9 manifest correctly rejects artifacts that are dirty or do not share the workflow commit. The full Linux qualification benchmark, clean-commit Darwin supplemental benchmark, five matching-host package reports, concurrency workflow artifact, and final manifest remain pending. Lore sync --dry-run reports docs/log.md drift, but a real Lore sync would commit the dirty Backlog tree and therefore also requires explicit commit authority. No Lore documentation was changed, all ACs remain unchecked, and the task remains In Progress.

User explicitly authorized creating and pushing the implementation, evidence, and documentation commits required to complete item 10 on 2026-07-31. This supersedes the prior no-commit/baseline-HEAD blocker for item 10 only; package publication and parent-task advancement remain out of scope.

Item 10 workflow run 30654089886 on clean commit b888886d7422fed7d436a5e7e8d3a6a6709e214f exposed a benchmark hermeticity defect before measurement: the Linux job failed because the private worker attempted to resolve a host backlog executable. Concurrency qualification passed, and all downstream build/package/publish jobs were correctly skipped. Narrow refinement before retry: persist the fixture already-generated deterministic task snapshot inside the disposable Backlog tree, validate it strictly, and inject a benchmark-private read-only adapter into source loading in both the orchestrator and worker. Add a regression that poisons any accidental host backlog invocation. Public retrieval behavior, fixtures, thresholds, platform policy, and scope remain unchanged.

The same hermeticity audit found the matching-host packaged graph/query/context smoke would otherwise inherit the undeclared host-Backlog dependency. Include a matching-host compiled fixture-only Backlog JSON shim in package-qualification scratch and prepend it only to the two packaged smoke subprocess environments. The shim implements the exact capability-probe and task-list JSON contract from the deterministic snapshot; it is never shipped, never used by public runtime code, and does not simulate OS/native behavior.

Hermeticity refinement verified locally on exact Bun 1.2.23. The benchmark runner passed with a deliberately poisoned backlog command, proving no host subprocess was reached. The real Darwin-arm64 package qualification passed using the scratch-only compiled Backlog JSON shim, including native indexing, Node launcher and relocated Bun binary parity, uninstall, explicit cache disposal, isolated-global cleanup, and source preservation. Combined focused benchmark/concurrency/lifecycle/indexed/package/release/evidence coverage passed 86 tests with 685 assertions; full bun test --isolate passed 2,328 tests with 7,156 assertions across 62 files. Typecheck, lint across 150 files, and git diff --check passed. All package-fix scratch, tarballs, compiled shims, npm caches, and reports were removed.

Restore reconciliation on 2026-07-31 (GitHub run evidence): release workflow run 30654887987 executed clean commit fd52e05ba7940044a3b75b64b151756dd8a3c4b7. Matrix resolution and package-metadata verification passed. The pinned Bun 1.2.23 Linux-x64 concurrency/crash qualification passed and uploaded complete evidence. The full paired benchmark started at 18:23:07Z and was cancelled by its exact 240-minute job timeout at 22:23:08Z before producing a report; the uploaded benchmark artifact therefore contains only the committed gate policy. Matching-host package, compile, package-sanity, and publish jobs were skipped by the benchmark dependency. No PR exists for the feature branch.

Diagnosis before rerun: this is a structural workload/cap mismatch, not evidence of a threshold failure. The two-fixture smoke recorded in prior notes launches 224 samples and took about 17 minutes. The approved full configuration launches 3,976 samples: per fixture, 1 discarded cold setup + 7 cold measurements + 2 parity workers, 10 warmup workers, and 60 measured workers for every scenario; the small fixture has 23 scenarios and the large fixture has 32. That is 17.75 times the smoke's process count, before hosted-runner variance, and each worker currently hashes all docs/Backlog/Git/profile/non-cache-.lore bytes before and after. The 64 MiB large fixture amplifies this guard overhead. A blind retry under the same 240-minute cap is not justified. Continue only after a reviewed choice between increasing the job budget or refining the fresh-process methodology/guard placement without weakening the approved 30-pair statistics; retain this failed-run evidence.

2026-07-31: User approved replacing the multi-hour synthetic stress model with the bounded 100 MiB blocking gate and optional 1 GiB informational run documented in docs/reference/ladybugdb-benchmark-and-scale-acceptance-strategy.md. LCLI-283.1.5 now gates final acceptance so the exact supported LadybugDB version is qualified before benchmark evidence is treated as authoritative.

Wave 2 restore and bounded-strategy implementation (2026-07-31): merged origin/dev at eb5dc0ee2e2f743acbda35477c83cdaadc0f9750 into the existing feature branch as integration commit ece1da2f5b735d25f63186c1cf281a25b938cf6a. Resolved the completed LCLI-283.1.5 task and current Lore documentation from authoritative dev, retained LCLI-283.1.4 implementation history, and regenerated Lore's managed log. The merge exposed a stale worktree install and stale release-matrix integrities: frozen install refreshed @ladybugdb/core to 0.19.0, and release.yml now matches all five 0.19.0 bun.lock integrities.

Implemented the user-approved bounded benchmark strategy. The blocking fixture now contains exactly 100 MiB of authored Markdown with refreshed deterministic export/source digests. Qualification performs one cold projection build and five indexed/reference pairs for warm open, rare lexical query, depth-2 graph, and depth-2/16,384-token context. Eighteen frozen gates enforce the 30 s cold build, 3 s warm open, 500 ms query, 1 s graph/context, 2 GiB peak RSS, disk-growth bounds, material query improvement, and graph/context non-regression. CI keeps the full small-fixture scenario matrix as non-timing functional smoke. release.yml caps the blocking Linux-x64 job at 15 minutes and adds an explicit default-off, continue-on-error 1 GiB observation capped at 30 minutes. Package qualification and fixture fingerprints now consume the merged 0.19.0/storage-43 constants.

Local evidence on Bun 1.3.14 after bun install --frozen-lockfile: 81 focused benchmark/concurrency/lifecycle/indexed/workflow/package/evidence tests passed with 674 assertions and zero failures; typecheck, lint across 152 files, actionlint for ci.yml/release.yml, and git diff --check passed. A real small-fixture report run completed with 23 scenarios, 94 raw samples, strict schema parsing, and the expected inconclusive smoke gate status. No repository benchmark/database/report artifacts were retained; the smoke report is outside the repository under /private/tmp.

Remaining completion evidence is intentionally external and exact-pin: commit the integrated implementation, push it, run the exact Bun 1.2.23 Linux-x64 100 MiB blocking benchmark, pinned concurrency/crash job, five matching-host package matrix, and final qualification manifest on one clean commit. All four acceptance criteria remain unchecked and the task remains In Progress until those artifacts pass.

Final local integration verification before exact-host dispatch (2026-07-31): aligned lore.ladybug-qualification-evidence/1 with approved plan item 11 by requiring the conclusive pinned Linux-x64 100 MiB benchmark, approved gate artifact, pinned concurrency report, and exactly five matching-host package reports from one clean commit. Removed the obsolete duplicate Darwin timing-artifact requirement; Darwin executable evidence remains mandatory in the Darwin arm64/x64 package reports. release.yml now downloads every qualification artifact, builds and validates the strict AC 1-4 manifest, uploads it for 90 days, and prevents package/install-sanity and publish from bypassing that final evidence gate. Local verification on Bun 1.3.14: full isolated suite 2,337 tests / 7,219 assertions / 0 failures across 64 files; focused manifest/workflow/package suite 15 tests / 163 assertions; lint 152 files, typecheck, actionlint, Lore strict validation/check, and git diff check all pass. bun audit reports no vulnerabilities; source and compiled version/help smokes pass; npm pack --dry-run passes for the root and all five platform manifests using an isolated cache. Exact Bun 1.2.23 Linux benchmark, concurrency, matching-host matrix, and assembled manifest remain the only acceptance evidence outstanding.

Exact-host bounded run 30685082979 at commit 4f0b69cdb1e3f2ddc2fde4c1dcc474974b6e510a retained as failed evidence. Metadata/version checks passed; pinned Bun 1.2.23 concurrency/crash qualification passed in 1m10s and uploaded evidence. The 100 MiB benchmark command ran from 05:03:20Z until the exact 15-minute job cancellation at 05:18:25Z, emitted no report, and uploaded only the frozen gate policy, so this is not a threshold result. Downstream build, matching-host package, final-manifest, package, and publish jobs were correctly skipped. Diagnosis: plan item 11 reduced scenarios and repetitions but the runner still launched one fresh process per policy per scenario, producing 41 processes; every process reparsed and rehashed the entire 100 MiB authored repository. Refine without changing approved fixture, samples, gates, thresholds, or 15-minute cap: each of five randomized AB/BA repetitions will use one fresh indexed session and one fresh reference session, measure warm open once, then execute the representative query, graph, and context operations against that loaded session. This preserves five independent warm samples per policy and result parity while reducing 40 warm processes to 10 and keeping one separate cold-build process.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: @codex
created: 2026-08-01 00:24
---
2026-07-31 user review: pause the approved 3,976-worker timeout refinement before source edits. The multi-hour harness does not represent a Lore user query and should not be used to rediscover LadybugDB's maximum scale. Revise the acceptance plan around a conservative bounded Lore integration envelope, upstream published benchmark evidence, and a short optional larger-scale check; obtain explicit approval before replacing plan item 11 or changing benchmark methodology.
---
<!-- COMMENTS:END -->
