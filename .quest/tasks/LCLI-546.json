{"id":"LCLI-546","aliases":[],"title":"Implement ODOC-231: ground lore's version control in git (committed citations, derived index, per-document history, branch/merge safety)","status":"To Do","description":"opum-agent decided this architecture under OPAG-204 (Accepted); opum-doc recorded it as ODOC-231 at docs/adr/ground-lore-version-control-in-git-with-committed-citations-and-a-derived-index.md (opum-doc repo), amended seven times same-day (ODOC-232/233/234/236/237/238/239). Git becomes lore's sole store of record for document history; the .lore/cache/snapshots/1 retention cache (cap 16, gitignored, machine-local) is replaced by small committed citations at .lore/snapshots/1/<kind>/<scope>/<name>.json naming a commit + verified projection_digest; queries are served by a gitignored, rebuildable SQLite index at .lore/cache/index.sqlite; generated files are regenerated in CI rather than merged; verified provenance becomes a required object (subject_digest/as_of/recorded_at). Per the ADR's own Downstream records: implementation sequencing is lore-cli's to plan, and lore-cli authors the ADR-0013 5th amendment (new committed .lore/snapshots/ member) plus a new ADR retiring the snapshot-retention spec (docs/specs/snapshot-change-and-provenance-workflows.md). LCLI-540, LCLI-474 and LCLI-539 are Done and satisfy specific narrow defects the ADR cites (edgeValue relation qualifiers; log.md merge-vs-replace; schema-export drift) but none of them implement this design. Verified absent at HEAD c40fa811 (2026-09-19): no .lore/snapshots/ committed, no lore history/diff/show commands, SNAPSHOT_RETENTION_LIMIT is still 16 (src/core/snapshot.ts:19), commands/snapshot.ts:77 still hardcodes maximum: 16, no SQLite index anywhere in src/. This task exists because OPAG-204's handoff asserted implementation was 'downstream and tracked' in lore-cli, and it was not until this filing.","acceptanceCriteria":[{"index":0,"text":"Each of the five subtasks filed under this parent (per-document history/diff, committed citations + ADR-0013 amendment, derived SQLite index, branch/merge safety for generated files, provenance schema) is Done","checked":false},{"index":1,"text":"A new lore-cli ADR retires the snapshot-retention contract currently living in docs/specs/snapshot-change-and-provenance-workflows.md, per the ADR Downstream records","checked":false},{"index":2,"text":"lore check exits 0 and the full suite passes on the branch that lands the final subtask","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["SESSION HANDOFF, 2026-09-19 (recycled for budget by opum-agent). Three things to survive:\n\n(1) THIS TASK: landed via PR opum-ai/lore-cli#185 (commit ba403847 on dev), which filed this\nparent plus five subtasks (LCLI-547 per-document history/diff, LCLI-548 committed citations +\nADR-0013 5th amendment, LCLI-549 derived SQLite index, LCLI-550 branch/merge safety for\ngenerated files, LCLI-551 provenance schema). All six are still \"To Do\" - filing only, no\nimplementation done. This closes the \"tracked there\" gap OPAG-204's handoff had wrongly\nasserted; see this task's own description for the ADR citation (opum-doc docs/adr/\nground-lore-version-control-in-git-with-committed-citations-and-a-derived-index.md, ODOC-231).\nNext session: pick a subtask and plan it, or split further if any exceeds the ~2-day/400-line\nceiling once scoped.\n\n(2) OPUM-WORKFLOW GUARD FALSE-ACCEPT (opum-fleet's defect, not lore-cli's - already reported to\nopum-agent, recorded on OPAG-270, routed to opum-fleet). guard-trunk-writes.sh's tokenizer\n(opum_git_trunk_violation in hooks/_lib.sh) resets its \"start of command\" flag only on a token\ncontaining ; & | ( ) or a shell keyword - NEVER on a bare newline (word-splitting treats\nnewline as plain whitespace). So a multi-line Bash call like:\n  git status --short\n  git commit -m \"x\"\nhas its `git commit` become invisible to the guard even with no unrelated command first - two\nconsecutive git lines are enough. Confirmed live: a bare single-line `git commit` on dev IS\nrefused; the same commit as line 2+ of a multi-line call is NOT. This is how I committed\ndirectly to dev this session (caught before push, recovered via branch+PR #185 - see this\ntask's description for the original command). Reproduction script (path may not survive - /tmp\nis not durable - so the content is inlined here too):\n\n  #!/bin/sh\n  HOOKDIR=/Users/jdnewhouse/.claude/plugins/cache/opum-fleet/opum-workflow/0.9.0/hooks\n  . \"$HOOKDIR/_lib.sh\"\n  test_cmd() {\n    label=\"$1\"; cmd=\"$2\"\n    REASON=$(opum_git_trunk_violation \"$cmd\" \"dev\")\n    RC=$?\n    if [ $RC -eq 0 ] && [ -n \"$REASON\" ]; then echo \"$label: REFUSED ($REASON)\"\n    else echo \"$label: ALLOWED (miss)\"; fi\n  }\n  VERB=commit\n  test_cmd \"A: bare git commit alone\" \"git $VERB -m x\"\n  test_cmd \"E: git status newline git commit, no leading cd\" \"git status --short\n  git $VERB -m x\"\n\n  (originally at /tmp/test_guard2.sh on this machine, this session - may be gone by now)\n\nNothing further needed from lore-cli here; opum-fleet owns the fix.\n\n(3) OPAG-277 (lore Story -> Arc rename, execution task): no outstanding question from lore-cli.\nDecision is closed (OPAG-255): rename to Arc, ratified, collision-checked clean against lore's\nown vocabulary (CLI verbs, profile fields, schemas, OKF terms - zero hits). Migration surface\nfully measured by a lore-cli fork and is now recorded directly on OPAG-277 itself: profile.ts\none-line change, a schema-filename break needing an alias, two hardcoded strings that ship in\nthe published opum-lore plugin (agent-bridge.ts, codex-bridge.ts), a live ADR-0009 needing a\nsuperseding amendment not a silent edit, and a fleet-wide `type: Story` frontmatter count that\nis a FLOOR of 48 across 5 counted repos (5 more repos uncounted). OPAG-277 itself rules this\ndoes NOT ride the lore/quest pair release - it's sequenced to follow it, since strict_types is\nfalse everywhere (no correctness pressure) and the bulk-rewrite script doesn't exist yet. No\nlore-cli task should be filed for the actual rename until OPAG-277 is scheduled - filing one now\nwould be work ahead of its own dependency."],"comments":[],"labels":["design","versioning","architecture"],"documentation":[],"dependencies":[],"createdAt":"2026-09-19T02:46:48.119Z","updatedAt":"2026-09-19T03:05:32.289Z","blockers":[],"gates":[],"gateEvents":[]}
