{"id":"LCLI-430","aliases":[],"title":"docker e2e harness depends on a live public npm install, so a required check is non-deterministic","status":"Done","description":"Filed by the opum-agent orchestrator session from CI evidence.\n\n`docker e2e harness (real lore + backlog binaries)` failed on PR #544 with 375\nassertions passing and 3 failing. The three are one root cause, not three defects:\n\n  [FAIL] docusaurus npm install (real)      exit 1,   expected 0\n  [FAIL] docusaurus npm run build (real)    exit 127, expected 0\n  [FAIL] docusaurus produced build/index.html\n\nExit 127 is \"command not found\" -- the build could not run because the install that\nshould have provided the binary had already failed. The install ran for 28 seconds\n(22:19:39 -> 22:20:07) before giving up. `lore scaffold docusaurus` PASSED\nimmediately before it, so lore's own behavior was correct; only the third-party\ndependency fetch failed. The mkdocs pair, which does not npm-install, passed.\n\nTransient, established rather than assumed: the identical content passed this exact\ncheck on PR #543 minutes earlier, the change under test was a `.quest/tasks/*.json`\nrecord which cannot reach a docusaurus install, and re-running the SAME job on the\nSAME commit returned success.\n\nTHE DEFECT IS NOT THE FLAKE, IT IS THE DEPENDENCY.\n\nThis step performs a live `npm install` against the public registry from inside a\ncontainer, and it is one of the three checks REQUIRED on `dev` -- the ref this\nrepository gates, unlike the rest of the fleet. So the gate on every landing here\nis coupled to npm registry availability. Three consequences, in increasing order of\ncost:\n\n1. Landing is blocked by an outage nobody in this repository caused.\n2. Every such failure is indistinguishable from a real regression until someone\n   reads 378 assertion lines to find that the 3 reds share one external cause.\n3. The cheap response -- re-run until green -- becomes habit, and habitual re-running\n   is exactly how a genuine failure gets waved through. This is the second flaky\n   required check found in this repository within one hour; see LCLI-429, a webkit\n   timing race in the explorer qualification. Two independent sources of\n   nondeterminism in front of a blocking ruleset is a pattern, not bad luck.\n\nSuggested directions, cheapest first:\n- Pin and vendor the docusaurus dependency set into the image, so the harness tests\n  lore's scaffold output rather than the registry's uptime.\n- Failing that, retry the install with backoff INSIDE the step and report the\n  retries, so a network fault is visibly a network fault rather than a red check.\n- Capture npm's stderr into the harness output. The harness currently prints only\n  `[FAIL] docusaurus npm install (real) (exit 1, expected 0)`, which is why\n  diagnosing this needed the raw run log. A failure that does not say why costs the\n  next reader the same twenty minutes.\n\nDo NOT fix this by removing the assertion. Verifying that a scaffolded site really\nbuilds is the point of a \"real binaries\" harness.\n\nRun: https://github.com/opum-ai/lore-cli/actions/runs/33812287851","acceptanceCriteria":[{"index":0,"text":"Reworded 2026-09-04 per opum-agent-7d ruling (original: \"The docker e2e harness does not fetch third-party packages from a live public registry during a required check\" -- ticking that as literally written would misstate what shipped). Reworded: docker/e2e/.npm-cache reduces the required check's live-registry dependency to cache-miss only (first run, or a pinned-version bump to src/core/consumer-scaffold.ts) instead of every run; on a cache miss it falls back to network and self-heals rather than hard-failing.","checked":true},{"index":1,"text":"A failing install reports the underlying npm error in the harness output, not just an exit code","checked":true},{"index":2,"text":"The scaffolded-site build assertion is retained","checked":true}],"definitionOfDone":[],"plan":["1. Dockerfile: pre-warm an offline npm cache at image build time by running the REAL compiled lore binary's `lore scaffold docusaurus` in a scratch dir, then `npm install --cache /opt/npm-cache` there -- extracts the real, pinned package.json rather than duplicating version numbers.","2. run-e2e.sh Phase 19: change the runtime `npm install` to `npm install --offline --cache /opt/npm-cache` so the required check never touches the live registry; keep npm run build + the build/index.html assertion unchanged (AC3).","3. lib/steps.sh: on FAIL, step()/step_json()/step_fail() print a tail of captured stderr to the CI log (not just exit code) -- generalizes the fix past just the install step, matches selftest.sh (stdout/stderr-redirecting expect()) so it stays safe.","4. Verify Dockerfile syntax and shellcheck run-e2e.sh/steps.sh locally (no Docker daemon available in this sandbox); real validation happens on the PR's docker-e2e required check."],"implementationNotes":["Opened PR opum-ai/lore-cli#547 from fix/LCLI-430-docusaurus-npm-offline-vendor. Implementation: (1) docker-compose.yml/Dockerfile bind-mount docker/e2e/.npm-cache -> /opt/npm-cache, mirroring the existing ./results pattern; (2) ci.yml restores/saves that dir via actions/cache keyed on src/core/consumer-scaffold.ts; (3) run-e2e.sh installs with npm install --prefer-offline --cache /opt/npm-cache so a warm cache never touches the registry, cold cache falls back to network; (4) lib/steps.sh step()/step_json()/step_fail() now print a stderr tail to the CI log on FAIL (AC2). Note on AC1: this is a steady-state fix, not an absolute one -- a cache miss (first run or pinned-version bump) still does one live fetch by design, so it self-heals rather than hard-fails. Rejected an earlier design (vendoring inside the Dockerfile RUN layer) after realizing docker compose up --build has no layer caching configured (LCLI-251 deferred that), so a build-time fetch would still run inside every required-check job -- no actual improvement. Docker was unavailable in this sandbox; the PR'\\''s own docker-e2e required check is the real verification. Watching PR #547 checks now.","Verified with real CI evidence on PR #547, run 33825431174 (first push) then 33825869633 (second push): first run's docker-e2e job logged 'Cache not found for input keys: docusaurus-npm-cache-...', fell back to network, docusaurus npm install/build passed, then 'Cache saved with key: docusaurus-npm-cache-...' (110MB). The very next run (triggered by the AC-reword commit, no code change) logged 'Cache hit for: docusaurus-npm-cache-...' and installed fully offline via --prefer-offline -- exactly the steady-state behavior AC1 (reworded) describes. All required checks green on #547 including docker e2e harness. Ready to mark Done once PR #547 lands."],"comments":[],"labels":[],"documentation":[],"dependencies":[],"createdAt":"2026-09-03T22:31:43.761Z","updatedAt":"2026-09-04T01:37:04.034Z","finalSummary":"Landed: PR opum-ai/lore-cli#547 squash-merged into dev (02ee187e) after all required checks passed, including a real cold-cache-then-warm-cache round trip on the docker e2e harness itself. Fixed by: (1) bind-mounting docker/e2e/.npm-cache into the container, restored/saved across CI runs via actions/cache keyed on src/core/consumer-scaffold.ts; (2) run-e2e.sh installing docusaurus deps with npm install --prefer-offline so a warm cache never touches the registry and a cold one self-heals; (3) lib/steps.sh printing a stderr tail to the CI log on any step FAIL, not just this one. AC1 was reworded (with the orchestrator's ruling) from an absolute claim to the true steady-state claim, since a cache miss still does one live fetch by design.","blockers":[],"gates":[],"gateEvents":[]}
