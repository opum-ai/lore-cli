{"id":"LCLI-516","aliases":[],"title":"init-wizard tests still intermittently exceed their own bun-test --timeout (per-test shape, distinct from the whole-process hang LCLI-507 fixed)","status":"To Do","description":"LCLI-507 widened the ubuntu Bun-epoll retry guard in .github/workflows/ci.yml to also catch a whole-process HANG (killed by the outer 'timeout --kill-after=10s 6m' wrapper, exit 124), on top of the pre-existing EEXIST-string retry. That fix is correctly scoped to only ONE of two distinct failure shapes named in LCLI-507's own history, and does not address this one.\n\nTHIS TASK IS THE OTHER SHAPE: an INDIVIDUAL test inside test/init.test.ts's TTY-wizard-spawning suite exceeds bun test's own per-test --timeout (10000ms on ubuntu-latest, 60000ms on windows-latest) while the surrounding bun test PROCESS finishes normally and reports a plain pass/fail count. Three occurrences are on record, all in this shape, all predating LCLI-507's fix:\n\n- 2026-09-11, PR #6, windows-latest: 'cli - init dispatch > the router wires stdinIsTTY + stderrIsTTY + a fake prompter through to the wizard end-to-end (LORE-260)' timed out at 60000ms.\n- 2026-09-15, promotion PR #91, ubuntu-latest: THREE init.test.ts wizard cases (Hermes, Antigravity/Gemini, AGENTS.md-label) each timed out at exactly 10000ms.\n- 2026-09-15, PR #105, ubuntu-latest, run 34984200004 job 104431984958: 'the interactive wizard offers Hermes only when its executable is detected' timed out at 10002.65ms, '3056 pass, 1 fail'. Cleared on re-run.\n\nINDEPENDENTLY RE-VERIFIED against the real CI log for the PR #105 occurrence (not just re-read from the prior note): fetched job 104431984958's full log via 'gh api repos/opum-ai/lore-cli/actions/jobs/104431984958/logs --allow-escape-sequences'. It shows '(fail) ... the interactive wizard offers Hermes only when its executable is detected [10002.65ms]' / '^ this test timed out after 10000ms.', and the job's own final line is '##[error]Process completed with exit code 1.' -- exit 1, not 124, and no 'error: EEXIST: file already exists, epoll_ctl' or any epoll-related text anywhere in the ~3800-line log.\n\nWHY LCLI-507's FIX CANNOT CATCH THIS: its retry guard only fires on (a) the literal EEXIST string in the captured log, or (b) the outer bash wrapper's exit 124 from a bounded-timeout kill. Neither condition is met here -- bun's OWN internal per-test timeout mechanism catches the hang at the single-test level, reports it as a failure, and lets the process exit normally (status 1) well inside the 6-minute outer bound. A guard keyed on the OUTER script's exit status and log content structurally cannot see an internal per-test timeout that the process survives.\n\nOPEN QUESTION for whoever picks this up: is this the SAME underlying Bun 1.3.14 Linux/epoll stdio race as LCLI-507, just caught by bun's per-test timer before it can escalate to a hang or an EEXIST print -- or a genuinely separate mechanism that happens to affect the same TTY-spawning test family? LCLI-507's own implementation notes explicitly grouped the init-wizard TTY tests under 'EXPLAINED by the Bun epoll/stdio race' alongside the whole-process-hang case, but that grouping was a root-cause hypothesis, not a verified failure-shape match -- treat it as a hypothesis to re-check, not settled fact, per this repo's own house rule that a prior note is a hypothesis until re-checked.","priority":"medium","type":"bug","acceptanceCriteria":[{"index":0,"text":"The root cause is identified for why an individual init-wizard TTY test intermittently exceeds bun test's own per-test --timeout on a CI runner (windows and ubuntu) but not locally, and whether it shares LCLI-507's epoll/stdio race or is a distinct mechanism","checked":false},{"index":1,"text":"A guard or fix is added that prevents this per-test-timeout shape from failing a required CI job outright on a flake, without merely raising the per-test timeout number -- or any timeout increase is justified in writing as the correct model of the work the test does","checked":false},{"index":2,"text":"A re-run of the affected job is no longer part of the normal delivery path for an unrelated diff, with real test evidence for the fix (not an inferred claim) wherever the tracker's existing test machinery (e.g. test/ci-workflow.test.ts, or a targeted shell/unit test of the guard in isolation) can support it","checked":false},{"index":3,"text":"The relationship to LCLI-507 -- same suspected root cause, different observed failure shape (process-survives per-test timeout vs. whole-process hang) -- is recorded in the repository, not only in this task, so the two are not silently re-folded into one already-closed issue","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["Independently re-verified 2026-09-16 by an agent working LCLI-507: fetched job 104431984958's full log directly from GitHub (`gh api repos/opum-ai/lore-cli/actions/jobs/104431984958/logs --allow-escape-sequences`). Confirmed exit code 1 (not 124) and zero occurrences of the EEXIST/epoll string anywhere in the ~3800-line log -- hard evidence, not inference, that LCLI-507's widened retry guard structurally cannot catch this failure shape. The failing test line reads exactly: `(fail) lore init — flags run non-interactively with zero prompts (AC#2/AC#4) > the interactive wizard offers Hermes only when its executable is detected [10002.65ms]` followed by `^ this test timed out after 10000ms.`"],"comments":[],"labels":["ci","flake","init"],"documentation":[],"dependencies":[],"references":["LCLI-507"],"createdAt":"2026-09-17T00:02:46.972Z","updatedAt":"2026-09-17T00:02:46.972Z","blockers":[],"gates":[],"gateEvents":[]}
