{"id":"LCLI-586","aliases":[],"title":"publish-release.sh hashes the root launcher tarball up to ~30 min before npm publish reads it","status":"In Progress","description":"Found by the reviewer pass on opum-ai/lore-cli#269 (LCLI-578), finding 6, PLAUSIBLE, not reproduced. The receipt gate hashes every tarball in $ARTIFACTS once, before any publish. The root launcher is published last, only after the registry-visibility wait (REGISTRY_WINDOW_SECONDS, default 1800 s) plus a 20 s cushion. Nothing in the script writes to $ARTIFACTS in that window (the README gate uses tar -xzOf, read-only), so only an outside process could swap the file. That is a narrow TOCTOU between the digest check and the bytes npm reads. Options: publish from a private read-only copy made at check time, or re-hash the root tarball immediately before its npm publish and refuse on drift.","priority":"Low","type":"bug","acceptanceCriteria":[{"index":0,"text":"The root launcher tarball handed to npm publish is proven byte-identical to the one the receipt gate hashed, by a copy made at check time or a re-hash immediately before publish","checked":true},{"index":1,"text":"A test swaps the root tarball after the gate and before its publish, and the script refuses before publishing it","checked":true}],"definitionOfDone":[],"plan":["Keep the receipt the LCLI-578 gate read (script-level path, cleaned on EXIT via the existing trap pattern) instead of deleting it","In publish_one, immediately before npm publish (and before the dry-run would-publish line), re-hash the tarball with shasum -a 256 and compare with the receipt tarballs[<file>] (own-property node -e lookup); die before publishing on mismatch or unreadable value","Correct the ROOT LAUNCHER provenance prose in scripts/publish-release.sh header and docs/runbooks/release-publishing.md","Tests in test/publish-release-script.test.ts: root swap, later-platform swap (and dry-run if hookable); mutation-test the re-hash"],"implementationNotes":["Also in scope when this lands (from LCLI-568, 2026-09-25): scripts/publish-release.sh:77-79 and docs/runbooks/release-publishing.md:696 still say the root launcher's sha256 is 'recorded nowhere' and 'irreducibly a LOCAL SELF-SEAL'. Since LCLI-578 (#269), the qualification receipt from opum-cli-e2e records it and the gate refuses on mismatch, which I measured live on 0.9.3 with a re-gzipped root. Correct both to say what now binds the root launcher, and what still does not (the check-to-publish window this task is about).","Context pack (lore agent context implementation) omitted runbooks/release-publishing and scripts/publish-release.sh; both are named by the task, so read directly.","EVIDENCE (2026-09-25; bun 1.3.14, node v24.20.0, quest 0.10.0, lore check from source via bun run). AC1: publish_one now calls recheck_against_receipt as its last step before npm publish and before the --dry-run would-publish line. It re-hashes with shasum -a 256 and compares with the KEPT receipt tarballs[<file>], read by own-property lookup; the gate had already matched that same value to the same file. Positive control test: the real path publishes all seven and prints 7 rehash lines. Real-artifact dry-run on 0.9.3 / Release run 36088480940 against the real opum-cli-e2e receipt, with an npm shim answering view as absent so publish_one is reached (0.9.3 is on the registry, so the unshimmed dry-run SKIPS all seven and never reaches the re-hash; exit 0, 0 rehash lines): exit 0, 7/7 rehash lines pass, root c70fd8db7be8e241a9efb7a7a705c387d810184bd8b2a2d436315234d39e114d. AC2: the ROOT SWAP test re-gzips the root (same tar stream) during the first platform npm publish. That is after the receipt gate and the README gate, so all six platforms publish and the registry gate passes. Result: exit 1, root never published, and the message names the root and both digests. Also refused on real 0.9.3 bytes: dry-run with the re-gzipped real root swapped at the root own pre-publish npm view -> exit 1, receipt c70fd8db... vs e31ccb0c.... Mutations (each run on the full 51-test suite; predicted vs observed): M1 remove the call -> 6 new red / 45 green, as predicted. M2 comparison always passes -> ROOT, PLATFORM, DRY-RUN red, 48 green, as predicted. M3 skip the 64-hex read validation -> the 2 unreadable-value tests red, as predicted. M4 cleanup skips the receipt -> the same 2 red (existsSync), as predicted. Gates: bun test 51/51 exit 0; lint:shell 0; lint 0 (3 warnings and 1 info are in files this change does not touch); typecheck 0; bun run lore check 0 (82 files, 0 errors)."],"comments":[],"labels":["release"],"documentation":[],"dependencies":[],"assignees":["lore-cli-session"],"references":["opum-ai/lore-cli#269"],"modifiedFiles":["scripts/publish-release.sh","test/publish-release-script.test.ts","docs/runbooks/release-publishing.md"],"createdAt":"2026-09-25T06:03:08.197Z","updatedAt":"2026-09-25T06:52:31.966Z","blockers":[],"gates":[],"gateEvents":[]}
