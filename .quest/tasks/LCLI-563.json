{"id":"LCLI-563","aliases":[],"title":"Specify what the committed-schema generator stamp holds (OPAG-354 item 3 mechanism)","status":"In Progress","description":"OPAG-354 item 3 delegates the MECHANISM to this repository: \"What exactly the stamp holds is NOT decided here and is lore-cli's to specify with measurement, because lore-cli raised the one open objection and holds the generator.\" Read from opum-ai/opum-agent's committed .quest/tasks/OPAG-354.json at ref main, not from the routing message. The architectural commitment is fixed and not reopened here: committed schemas must carry enough generator provenance for the tool to distinguish \"type removed from profile\" (prune correct) from \"binary older than the tree\" (prune destroys unreleased work), and drift must gain a third outcome rather than collapsing cannot-judge into a destructive remedy.\n\nTHE OPEN TENSION AS OPAG-354 STATES IT: a per-release version stamp may churn every schema file on every release; a profile digest says the artifact came from a different profile but not in WHICH DIRECTION, while a version says direction and is the thing this fleet elsewhere refuses to trust for capability detection.\n\nTHE CHURN HALF IS NOW MEASURED, and it settles against the version stamp. Against this repository's own tags, reading the tree SHA of .lore/schemas at each:\n\n  v0.6.0  4bb5609ac74d76c1e51dac90959de4c1f323a549  7 files\n  v0.6.1  4bb5609ac74d76c1e51dac90959de4c1f323a549  7 files\n  v0.6.2  4bb5609ac74d76c1e51dac90959de4c1f323a549  7 files\n  v0.7.0  4bb5609ac74d76c1e51dac90959de4c1f323a549  7 files\n  v0.8.0  125934b1dab5ea0fe15a5c154b7f7269724bcbbf  7 files\n  dev     700f1ed407cde2bce68137ac5362b69dfb2a4a35  8 files  (Arc, LCLI-554)\n\nByte-identical across four consecutive releases, and only 3 commits have ever touched .lore/schemas/. So a per-release version stamp would have rewritten all 7 files at v0.6.1, v0.6.2 and v0.7.0 while nothing about the profile changed -- 21 file rewrites carrying no information, in a directory that genuinely changes about twice in five releases. The churn objection was real and is now quantified rather than asserted. A profile digest changes when the profile changes: 2 of 5 here, which is not churn, it is signal.\n\nTHE DIRECTION HALF DISSOLVES RATHER THAN BEING TRADED OFF, and this is the substantive proposal. Direction is not required for the safety property. The destructive action is PRUNE specifically, and prune is safe only when the tool can affirm the committed artifact came from a profile it actually owns. A digest answers exactly that question: digest matches a profile I generate -> I own this, prune is a real finding; digest does not match -> I cannot judge this from here, third outcome, never a prune. The tool never needs to know whether it is older or newer to AVOID the destructive act -- it only needs to know whether the artifact is its own. Direction is needed to WORD the message well, not to decide the action, which is why the fleet's refusal to trust version comparisons for capability detection does not bite here.\n\nPROPOSED SPECIFICATION, for review rather than as a settled answer:\n- key the DECISION on a profile digest, carried in each committed schema\n- carry the generating lore version ALONGSIDE it, used only for the human-readable message (\"generated by lore X, this is lore Y\"), never as the predicate\n- unknown digest => cannot-be-checked-from-here, exit distinguishable from both clean and drifted, and never a prune suggestion\n\nThis mirrors LCLI-548's verified/failed/cannot-be-checked-from-here exactly, which is why OPAG-354 cites it. Note the two existing errors are NOT equally dangerous and the fix must preserve that: \"no longer matches what this profile generates\" rewrites a file (recoverable from git); \"no type owns this schema -- prune it\" deletes one.\n\nNOT A SUBSTITUTE FOR LCLI-545, per OPAG-354 item 4: 545 improves the wording of advice that is still undecidable underneath. Neither closes the other. OPAG-353 remains the fleet operating rule for binaries already on disk that will never receive this fix.\n\nA cross-fleet consequence to state rather than discover: all 71 committed schema files across the ten repositories share one identical top-level key set and none carries provenance (measured by opum-agent on OPAG-354). So whatever shape this takes, every repository's committed schemas become stale on the release that introduces it, and the rollout needs to not look like the 0.8.0 event in LCLI-545.","acceptanceCriteria":[{"index":0,"text":"The stamp contents are specified and the version-vs-digest tension is resolved with the measurement that settles it, not restated as an open question","checked":true},{"index":1,"text":"The spec REFUTES the set-containment refinement rather than leaving it open: both the type-removed and the binary-older cases present identically, because the type vocabulary comes from the binary whenever a repository commits no profile (loadProfile falls through to defaultProfile; this repository has neither profile file)","checked":true},{"index":2,"text":"The decision table names all outcomes including the unattributable one, and states the load-bearing property: prune is advised only when the running binary can affirm the artifact came from a profile it itself generates","checked":true},{"index":3,"text":"The rewrite/delete asymmetry (OPAG-354 item 5) is carried as a stated requirement of the mechanism, not left to the implementation to rediscover","checked":true},{"index":4,"text":"The no-stamp case is specified as the COMMON case rather than an edge case, since all 71 committed schemas fleet-wide are unstamped on the release that introduces this","checked":true},{"index":5,"text":"The rollout hazard is named as an open question with its precedent (LCLI-545), and the exit-code choice is explicitly deferred to implementation as an ADR-0005 CLI-contract change","checked":true},{"index":6,"text":"Implementation is split into its own task with the spec as a dependency, rather than carried here past the SDLC size ceiling","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":["WHY THE TOOL-SIDE FIX MATTERS MOST HERE, measured 2026-09-20 after opum-cli-e2e proposed a structural shortcut for OPAG-353. Their check: if a repo's .lore/profile.toml is entirely commented out, the bundle uses lore's built-in types, so an older binary's export has nothing to prune and the destructive direction is unreachable -- greppable, falsified only by someone uncommenting the file. IT INVERTS HERE. This repository has NO .lore/profile.toml at all (.lore/ holds agents, cache, config.toml, schemas), yet arc.schema.json is committed. The type vocabulary lives in src/core/profile.ts:1074 (name: \"Arc\"). lore-cli does not CONFIGURE a profile, it IS the profile. So a file-based check reads absent-file as empty-therefore-safe, which is exactly backwards, and the fleet sweep would score this repo as safe. THE PREDICATE WANTS RESTATING, not the grep: 'can this repository's effective profile differ from the installed binary's?' For a consumer that reduces to their greppable check. For a generator it is PERMANENTLY TRUE, because the effective profile is source under development and dev is ahead of every released binary by construction -- there is no safe window except immediately after a release, before the next profile change lands. CONSEQUENCE FOR THIS TASK: OPAG-353's operating rule can help the nine consumer repositories and cannot help this one, because the unsafe condition here is structural rather than occasional. The stamp is the only remedy that reaches lore-cli's own bundle, which raises this task's priority relative to how it was filed. Reported to opum-cli-e2e so opum-agent gets the predicate rather than the grep before measuring the fleet.","SCOPE AMENDED: this task is now the SPECIFICATION, and the implementation is LCLI-565 with this task as its dependency. The original AC[1]-[5] (export writes the stamp, check keys the prune decision on it, the third outcome and its exit code, the post-change churn re-measurement, the rollout note) moved to LCLI-565 verbatim in substance and sharper in two places. Split endorsed by opum-agent 2026-09-20 on the SDLC size ceiling: five deliverables of that shape produce either a pull request nobody reviews or a ceiling everyone ignores.\n\nIT IMPLEMENTS OPAG-354, IT DOES NOT REOPEN IT. Checked explicitly because the orchestrator asked to be told if the spec turned out to reopen the commitment. The architectural half -- provenance in committed schemas, a third drift outcome, prune never on cannot-judge -- is untouched and is quoted rather than re-argued. Only the mechanism is decided here, which is the part OPAG-354 delegated.\n\nTHE VERSION-VS-DIGEST TENSION IS SETTLED, AND THE CHURN HALF WAS RE-MEASURED HERE rather than carried from the filing record. .lore/schemas tree SHA is 4bb5609a at v0.6.0, v0.6.1, v0.6.2 and v0.7.0 (byte-identical across four consecutive releases), 125934b1 at v0.8.0, 700f1ed4 on dev with 8 files after the Arc addition; 3 commits have ever touched the directory. A per-release version stamp would have rewritten all 7 files at three of those releases -- 21 rewrites carrying no information -- because schemaDriftFindings compares committed against regenerated bytes with a plain inequality (check.ts:794-829), so any differing byte is drift. Ruling from opum-agent, which I had argued for independently: the digest is the decision key, no version is committed, and the running binary supplies its own version for the message. That avoids both hazards -- the churn, and the committed-field-no-gate-checks that excluding it from the comparison would create.\n\nTHE SUBSTANTIVE ADDITION BEYOND THE FILING RECORD'S PROPOSED SPEC IS A REFUTATION, and it is worth stating because it is the refinement a later reader will reach for. Making the stamp carry the generating profile's declared type-slug set, so containment yields direction, DOES NOT WORK: type-removed and binary-older present identically, both as \"the stamp declares a slug I do not\". The reason is that the type vocabulary is a property of the BINARY, not the tree, whenever a repository commits no profile -- loadProfile (profile.ts:305-325) falls through to the built-in defaultProfile(), and this repository has neither .lore/profile.toml nor .lore/profile.json, which is exactly why PATH lore 0.8.0 could not see dev's Arc type in LCLI-546. There is nothing in the tree to break the tie, so set containment buys nothing the digest does not and costs a larger committed field.\n\nThat same fact resolves a case I expected to need a special one: A REPOSITORY THAT COMMITS A PROFILE IS NOT EXPOSED TO THIS FAILURE AT ALL, and needs no extra field to say so. Its digest derives from a file in the tree, so every binary computes the same value, every artifact is always attributable, and prune stays decidable. One mechanism covers both kinds of repository.\n\nONE CONSEQUENCE STATED IN THE SPEC BECAUSE IT LOOKS LIKE A DEFECT AND IS THE DESIGN WORKING: after a legitimate type removal, the removed type's file is orphaned with an unrecognised stamp and reports unattributable PERMANENTLY, until a human deletes it. The surviving files re-stamp on the next export. A leftover file costing a deliberate rm is a far cheaper failure than the one this exists to prevent, and keeping the destructive step dependent on judgement is the whole point.\n\nDELIBERATELY LEFT OPEN, not overlooked: which exit code the third outcome takes. lore's documented set (0 ok, 2 usage, 3 not_found, 4 denied, 5 conflict, 6 validation/drift) has no spare slot with the right meaning, so adding one is an ADR-0005 CLI-contract change and belongs with the implementation, not with the mechanism. Carried as LCLI-565 AC[4] rather than left as prose."],"comments":[],"labels":["architecture","design"],"documentation":["docs/specs/committed-schema-generator-stamp.md"],"dependencies":[],"createdAt":"2026-09-20T23:16:29.748Z","updatedAt":"2026-09-21T04:21:01.972Z","blockers":[],"gates":[],"gateEvents":[]}
