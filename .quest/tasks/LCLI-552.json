{"id":"LCLI-552","aliases":[],"title":"Execute lore's Story -> Arc rename: lore-cli's own migration surface","status":"To Do","summary":"lore-cli execution scope for the fleet-wide Story -> Arc rename decided under OPAG-255 and split for execution under OPAG-277; filed locally so the work exists in this repo's own tracker rather than only opum-agent's (Mechanism C, OPAG-244).","description":"Operator ruling, relayed by opum-agent: lore's \"Story\" type is renamed to Arc so quest can use \"story\" as a proper Scrum issue type. Arc was collision-checked clean by lore-cli (zero hits as a CLI verb, profile field, generated schema, or OKF term). Decision record: opum-agent OPAG-255 (closed). Fleet-wide execution split: opum-agent OPAG-277 (To Do).\n\nRULED SEQUENCING (from OPAG-277): this does NOT ride the lore/quest pair release. It is a separate coordinated change that follows it, because it touches published plugin content (needing its own version bump and eval pass), the fleet-wide frontmatter migration script does not exist yet, and profile.strict_types being false everywhere means there is no correctness pressure forcing a flag day.\n\nThis task tracks lore-cli's own execution surface only. opum-agent's OPAG-277 tracks fleet coordination and the other repositories' counts; don't treat that record alone as sufficient for this repo's work (Mechanism C, OPAG-244: a design decided by the orchestrator and recorded only in opum-agent/opum-doc doesn't exist from the building repo's own seat until it also lands there).\n\nVERIFIED against dev@928b595a on 2026-09-18 (line numbers are current, not copied from OPAG-277 unread):\n\n1. src/core/profile.ts:965 - the `name: \"Story\"` schema type entry (single line inside the types array).\n\n2. .lore/schemas/story.schema.json - the generated schema FILENAME changes, so every document's `$schema` frontmatter pointer breaks unless the old name is retained as an alias.\n\n3. src/core/agent-bridge.ts:108,135,156,220 and src/core/codex-bridge.ts:11,37,47,85 hardcode \"Story/Task coupling\" / \"Story <-> Task\" into generated skill text that SHIPS IN THE PUBLISHED opum-lore plugin - agent-bridge.ts feeds the Claude skill (`lore instructions`), codex-bridge.ts feeds the Codex skill and AGENTS.md block. Product surface, not internal docs.\n\n4. docs/adr/0009-story-task-coupling-reconciliation.md is LIVE for the Story<->Task naming/coupling decision (only its status-vocabulary section was already superseded by ADR-0022 under LCLI-506); this rename needs its own superseding amendment, not a silent edit of a live ADR.\n\n5. 12 documents under docs/ carry `type: Story` frontmatter and need migrating: docs/adr/0006-schema-types-templates.md, docs/stories/adopt-backlog-knowledge-through-lore.md, docs/stories/build-the-lore-cli-foundation.md, docs/stories/build-the-persistent-local-graph-platform.md, docs/stories/harden-lore-cli-correctness-and-safety.md, docs/stories/harden-post-0-2-lore-correctness.md, docs/stories/hold-deferred-lore-capabilities.md, docs/stories/maintain-lore-cli-documentation-authority.md, docs/stories/prepare-the-first-lore-cli-release.md, docs/stories/retrieve-task-scoped-context-with-agent-profiles.md, docs/stories/support-lore-agent-interoperability.md, docs/stories/track-lore-cli-tracker-backend-integration.md. (lore-spec.md and test/fixtures/okf-bundle/stories/adopt-lore.md also currently match `type: Story` but are spec prose and test fixture content respectively - scope those separately, don't fold them into this count.)\n\n6. `lore rename` alone doesn't perform this migration - it repoints concept ids and links, not a document's `type:` frontmatter value. The fleet-wide frontmatter migration script referenced in OPAG-277 doesn't exist yet; that's a fleet gap, not something specific to this repo to build alone.\n\nFLEET CONSTRAINT while this is open: don't set `profile.strict_types = true` in this repo (or any repo) until the migration completes. Checked 2026-09-18: this repo's .lore/config.toml has no `strict_types` key, i.e. it's already at the required false/unset value - this task only needs to keep it that way, not change anything, until the migration lands.","priority":"Medium","type":"chore","acceptanceCriteria":[{"index":0,"text":"src/core/profile.ts's name: \"Story\" entry is renamed to name: \"Arc\" (or whatever term the fleet-wide decision settles on beyond OPAG-255, if it changes before execution)","checked":false},{"index":1,"text":".lore/schemas/story.schema.json is renamed/regenerated under the new type name, and every existing document with an old $schema pointer still resolves (alias or rewrite - decide and record which)","checked":false},{"index":2,"text":"Generated skill text in src/core/agent-bridge.ts and src/core/codex-bridge.ts (all 8 occurrences at 108,135,156,220 and 11,37,47,85) says Arc/Task rather than Story/Task, and the regenerated Claude + Codex skill docs are byte-identical on a no-op re-run","checked":false},{"index":3,"text":"docs/adr/0009-story-task-coupling-reconciliation.md carries a superseding amendment recording the Arc rename, following this repo's ADR immutability convention - not a silent edit of the live text","checked":false},{"index":4,"text":"All 12 verified docs/ files carrying type: Story frontmatter are migrated to the new type value via the fleet-wide migration script once it exists","checked":false},{"index":5,"text":"lore check exits 0 on the fully migrated tree, with profile.strict_types left at its current unset/false value for the duration of the fleet-wide migration","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["SCOPE MEASURED ON DISPATCH (opum-agent OPAG-277, read by git archive from its origin/main d5502b2 into scratch, not its working tree). Measured against this branch cut from origin/dev @ 85174242, 2026-09-19.\n\nCORRECTION TO THIS RECORD AND TO OPAG-277 — THE POINTER IS NOT FRONTMATTER. Both records say the generated schema filename change breaks \"every document's $schema FRONTMATTER pointer\". It is not frontmatter. NO document carries a $schema frontmatter key at all: the frontmatter key set across the 74 docs/ files that have frontmatter is type(74) title(74) timestamp(74) summary(74) tags(69) description(36) status(14) tasks(11) supersedes(2) superseded_by(2) okf_version(1) — measured by key presence, not by value. The pointer is a LINE-1 EDITOR MODELINE comment, \"# yaml-language-server: $schema=../../.lore/schemas/<slug>.schema.json\" (ADR-0006 §3), emitted by schema.ts:644 and spliced on serialize by concept.ts:404. I wrote the wrong location into my own record; the substance (a pointer that dangles after the rename) survives, its blast radius does not — an editor modeline degrades editor autocomplete, it does not fail a gate.\n\nNOTHING CATCHES THE DANGLING MODELINE. schemaDriftFindings (check.ts) has exactly three conditions — stale, missing, orphaned — all about the SCHEMA FILES under .lore/schemas/, and a committed of null yields no findings at all. No code anywhere validates that a modeline TARGET resolves; the only yaml-language-server hits in src/ are the two emitters and prose. So after the rename, lore check correctly reports story.schema.json orphaned + arc.schema.json missing, and says nothing about the N documents now pointing at a deleted file. That gap is worth closing as part of this work rather than documenting.\n\nTHE DEGRADATION STORY IS VERIFIED IN THE CONSUMING CODE, NOT THE CONFIG COMMENT. validate.ts:151 — severity is \"error\" only when isUnknownType && effective.strictTypes, otherwise \"warning\". profile.ts:394 defaults strict_types to false and profile.ts:943 records that the built-in profile never opts in on a project's behalf. So an un-migrated \"type: Story\" document warns. That is what makes this incremental rather than a flag day, and it is now read from the reader rather than assumed from scaffold.ts's comment.\n\nTHE NAME IS NOT A BEHAVIOURAL KEY — WHICH DECIDES THE SHAPE. Literal \"Story\" in src/ excluding tests is THREE sites, and only three: template.ts:460 (STORY_TEMPLATE const), template.ts:501 (the Story key in BUILTIN_TEMPLATES), and profile.ts:965 (the type declaration). Critically, lore link does NOT key on the type name — link.ts:906 keys on \"a concept type whose profile schema declares tasks (the built-in Story type)\", a STRUCTURAL predicate; the parenthetical is prose. The renamed Arc type carries the same tasks field, so Story<->Task coupling is a property of the field, not the name. NOT YET MEASURED, and it is the next thing to measure because it decides per-repo migration urgency: whether link still resolves a document whose OWN type is unknown (un-migrated) — an unknown-type document has no profile type, so it plausibly has no declared tasks either.\n\nTHE POPULATION IS UNBOUNDED, SO \"WHICH DIRECTORIES ARE IN SCOPE\" IS THE WRONG FRAME. opum-agent reports lore initialised in 35 directories on this machine, 10 being the tracked fleet, and asks whether the other 25 are in scope. My own sweep of /Volumes/external + ~ (81 config.toml files READ, reported as a count so an empty result could not read as a clean one) finds 81 lore workspaces: 80 carry .lore/schemas/story.schema.json and 46 carry at least one \"type: Story\" document. But the number is not the point and neither is 35 vs 81 — @opum-ai/lore 0.8.0 is PUBLIC ON NPM (dist-tags.latest = 0.8.0, re-resolved today). Every external user has a story.schema.json and Story documents, and no script run over enumerated directories can ever reach them. The proof that enumeration fails is already on this disk: 15 opum-workflow plugin version caches and 3 opum-lore caches each carry story.schema.json and Story docs. Those are PUBLISHED PAYLOADS ALREADY DISTRIBUTED, frozen at their version — nobody would put them on a migration list and nothing should rewrite them.\n\nSO: the 25 are NOT in scope as migration targets, and neither are the other 45. What is in scope is making the CLI not break them. RECOMMENDED SHAPE, mine to decide per OPAG-277: alias-first, migrate-later — retain Story as a deprecated alias of Arc so existing documents keep validating and external users never see a break, then migrate the 10 fleet repos on their own schedule, and only then may a repo flip strict_types. THE HONEST PRICE, verified: there is NO alias concept in the type system today — zero hits for \"alias\" in profile.ts, type-vocabulary.ts and schema.ts. This is net-new capability, not a config line, and it is the part worth arguing before committing.\n\nOPAG-277 OPEN GAP CLOSED. It records five repos never counted and 48 as a floor that must not be quoted as a total. Counted here: lore-web 3, quest-web 3, opum-web 0, opum-marketplace 0, opum-fleet 1. OBJECT-IDENTITY CAVEAT, which is why these must be re-resolved before anyone quotes a fleet total: OPAG-277 counted on each repo's origin/dev, mine are WORKING TREES and whole-repo including fixtures and .repro-scratch — which is exactly why this repo reads 23 raw here against the 12 real docs this record already carries. Same basis for all five new numbers; different basis from OPAG-277's.\n\nFEDERATION CONSEQUENCE TO ROUTE. skills/lore/SKILL.md carries 3 Story mentions, and opum-marketplace federates skills/ BY TAG against a recorded tree baseline (pinned v0.7.0, skills/ baseline 2998f74d, per this repo's CLAUDE.md). The rename changes what the next tag ships, so the LCLI-469 handshake applies. And the SKILL.md description line is the skill TRIGGERING description — changing \"Story/Task coupling\" to \"Arc/Task coupling\" changes trigger wording, which is precisely what OPAG-277's required eval pass is testing. The 8 bridge sites this record already lists were re-verified unchanged: agent-bridge.ts 108,135,156,220 and codex-bridge.ts 11,37,47,85.","ALIAS COST REVISED DOWN, AND A HARD BREAK FOUND THAT THE WARNING STORY HIDES. Two measurements taken after the note above, both of which change the recommendation rather than decorate it.\n\n1. `lore link` HARD-FAILS ON AN UN-MIGRATED DOCUMENT. This is the one that matters, because OPAG-277's premise — strict_types false means \"no correctness pressure\", so this is incremental rather than a flag day — is true for VALIDATION and false for LINKING. link.ts:197 calls assertTaskCouplingSupported, which calls profileTypeDeclaresField(concept.type, \"tasks\", profile) on the DOCUMENT's own type. profile.ts:1021-1032: that resolves via canonicalProfileType, which is byLowerName.get(lower) ?? bySlug.get(lower) ?? trimmed, then profile.types.get(...)?.declaredFields.has(field) ?? false. Under an Arc-only profile, \"Story\" hits neither map, falls through to the literal \"Story\", finds no type, and returns false — so link throws a validation LoreError, not a warning. So a hard rename would silently convert every un-migrated Story document in every workspace, fleet and external alike, into one that can NEVER RECEIVE A NEW TASK LINK until it is migrated. That is the primary workflow this CLI exists to support, and it degrades to a hard error with no deprecation window. NOT affected, checked rather than assumed: assertTaskCouplingSupported is called at link.ts:197 ONLY — `lore unlink` does not call it and has its own removeUnsupportedEmptyField recovery path (link.ts:882-886), so existing links can still be read and removed. It is new coupling that breaks.\n\n2. THE ALIAS IS MUCH CHEAPER THAN I PRICED IT. The note above says \"net-new capability, not a config line\", which was right that no alias concept exists and wrong about what that costs. Type-token resolution is centralized in exactly TWO functions with byte-identical bodies — schema.ts:250 canonicalType and profile.ts:1032 canonicalProfileType, both `byLowerName.get(lower) ?? bySlug.get(lower) ?? trimmed` — and both maps are populated in ONE place, the compile loop at profile.ts:751-752. So a deprecated alias is an additional lookup seeded at that single site, which both resolvers then pick up for free. The comment at profile.ts:1029-1031 already records that the two resolvers MUST agree and were deliberately written to mirror each other, so the codebase has already paid the design cost of keeping this a single concept.\n\nTAKEN TOGETHER these point the same way and harden the alias-first recommendation from a preference into the safe option: the break is worse than the record implied AND the mitigation is cheaper than I implied. I had those backwards in the note above and am correcting both directions.\n\nONE THING TO VERIFY BEFORE BUILDING, not guessed at here: whether an aliased document's `type: Story` frontmatter gets canonicalized to Arc on serialize, or only its modeline does. schema.ts:644 builds the modeline from slugForTypeName(type) while serializeConcept writes concept.frontmatter.type. If the modeline follows the alias and the frontmatter does not, then every lore-driven rewrite self-heals the dangling pointer from measurement 1 of the previous note without touching the document's declared type — which would be an elegant incremental migration path, and is exactly the kind of convenient conclusion that deserves to be measured rather than assumed.","MODELINE SELF-HEALING HYPOTHESIS: FALSIFIED. The note above flagged, as something to verify rather than assume, whether an aliased document's modeline self-heals on serialize — and called it \"exactly the kind of convenient conclusion that deserves to be measured\". It does not self-heal. It is DESTROYED.\n\nMeasured: only TWO call sites ever emit a modeline — scaffold.ts:174 (`lore init`) and template.ts:248 (`lore new`). Every other rewrite path uses the plain serializeConcept, including link.ts:891, which is the exact path `lore link` takes to write a coupled document. And concept.ts:417-419 records the reason: \"Round-trip caveat (ADR-0011 §2): js-yaml drops the in-fence comment if the file is ever re-serialized, so a doc written this way is emitted once, not rewritten in place.\" ADR-0011:213-217 states it as a deliberate non-guarantee — \"No round-trip-perfect comment preservation... authors should not rely on in-frontmatter comments.\"\n\nSo the modeline is WRITE-ONCE BY DESIGN: stamped at creation, silently dropped by the first lore-driven rewrite the document ever receives. Three consequences for this migration, and they all point the same way:\n\n1. The dangling-pointer surface is SMALLER again than the previous note said. It can only affect documents that have never been re-serialized since creation. Any document that has been linked, unlinked, or otherwise rewritten by lore already has no modeline to dangle.\n2. A migration script MUST NOT rely on a lore rewrite to fix the pointer, which is what the falsified hypothesis would have had us do. A rewrite removes the pointer rather than repointing it. If we want migrated documents to keep a working modeline, the migration has to re-stamp it deliberately — schema.ts:644 schemaModeline is the function, and serializeConceptWithModeline the seam.\n3. It weakens the case for treating the modeline as a migration blocker AT ALL. It is an editor convenience that lore itself already discards as a documented non-guarantee, so \"every document's pointer breaks\" — the claim this record and OPAG-277 both opened with — overstates a surface that lore was never maintaining in the first place.\n\nWHAT DOES NOT CHANGE: the `lore link` hard failure in the previous note. That one is about the TYPE not resolving, not about the modeline, and it remains the real reason to prefer alias-first. Recording this explicitly because three consecutive notes have now shrunk the modeline problem, and it would be easy to read the trend as shrinking the whole migration. It does not — the link break is untouched by all of it.","TWO FINDINGS AFTER #189 LANDED. The first answers the open question; the second corrects this record's own site count, and it is the same error this record was created to correct in OPAG-277.\n\n=== 1. `lore link` ON AN UNKNOWN-TYPE DOCUMENT: MEASURED, NOT INFERRED. RESOLVED: IT HARD-FAILS. ===\n\nThe previous notes established this by reading the code path. Now run against the PUBLISHED binary — lore 0.8.0, the version on npm — in a disposable workspace, with a positive control so the result cannot be an artifact of a broken setup.\n\nSetup: a bundle whose .lore/profile.toml declares Arc (sections [\"Acceptance criteria\"], fields tasks+specs) and Reference, and NO Story — i.e. the exact post-rename state. `lore types` confirms the vocabulary is \"Arc, Reference\". Two documents: docs/stories/legacy-story.md with `type: Story` (un-migrated), and docs/arcs-ok.md with `type: Arc` (control).\n\n  A) lore link stories/legacy-story TASK-1\n     error: cannot link tasks to stories/legacy-story: type \"Story\" does not declare a `tasks` field in the active profile\n     hint: link the task to a concept type whose profile schema declares `tasks` (the built-in Story type), or update the active profile before retrying\n\n  B) lore link arcs-ok TASK-1\n     error: Quest workspace is not initialized\n     hint: run `quest init`\n\nTHE TWO FAIL AT DIFFERENT STAGES, and that is the whole proof. The control gets PAST the type gate and dies later, at the tracker. The un-migrated document never reaches the tracker at all — it is refused at the coupling gate. Had both failed at the tracker, the test would have shown nothing; had both failed at the type gate, the setup would have been broken. So the gate moved for its own reason.\n\nAlso captured empirically, from the same run, the warning-level degradation that was previously only read from validate.ts:151: `warning: unknown type \"Story\" in stories/legacy-story.md; validated on `type` only (known types: Arc, Reference)`. Validation warns. Linking refuses. Both confirmed against the shipped binary.\n\nANSWER TO WHAT OPAG-296 STILL LISTS AS UNMEASURED: `lore link` does NOT resolve a document whose own type is unknown. It throws before consulting the tracker.\n\n=== 2. THIS RECORD'S \"8 SITES\" IS ITSELF A FLOOR PRESENTED AS AN INVENTORY. THE REAL COUNT IS 25. ===\n\nThis record exists partly because OPAG-277 cited THREE hardcoded Story/Task sites and there were EIGHT — a floor presented as an inventory. I then presented eight as the inventory. Re-measured with a WORD-BOUNDARY grep rather than a quoted-literal one: 45 word-boundary \"Story\" occurrences in src/ excluding tests, of which 25 are non-comment lines.\n\n  BEHAVIOURAL (3, was 2):\n    profile.ts:965     the type declaration\n    template.ts:501    Story: STORY_TEMPLATE, the builtin-template lookup\n    schema.ts:333      Story: \"stories\" in TYPE_DIRECTORIES  <-- NEW, AND IT IS BEHAVIOUR\n      TYPE_DIRECTORIES decides the bundle sub-directory `lore new` scaffolds a concept into. Its\n      docstring records that these names are NOT derivable from the type by any rule, and that a\n      type outside the map falls back to its lower-kebab slug. So a renamed Arc absent from this\n      map scaffolds into docs/arc/, not docs/arcs/ — a silent convention break in the one command\n      authors use most.\n\n  PUBLISHED AGENT/SKILL TEXT (8, already recorded): agent-bridge.ts 108,135,156,220; codex-bridge.ts 11,37,47,85\n  PUBLISHED `lore instructions` OUTPUT (11, ENTIRELY UNCOUNTED): instructions.ts 29,30,31,32,34,37,39,61,69,102,361\n  PUBLISHED CLI HELP EXAMPLES (2, NEW): manifest.ts:567 `lore types --type Story`, manifest.ts:822 `lore query --type Story ...`\n  USER-FACING ERROR HINT (1, NEW): link.ts:906 \"(the built-in Story type)\" — the very hint finding 1 reproduced\n\nSo the PUBLISHED-SURFACE count goes from 8 to 22, and instructions.ts — `lore instructions`, the canonical agent loop this fleet's CLAUDE.md tells every session to run — was not in the surface at all.\n\nWHY THE FIRST COUNT MISSED THEM, because the mechanism matters more than the number: my pattern was `\"Story\"` / `'Story'` / `STORY` — quoted string literals only. Every site missed is either an OBJECT KEY (`Story: \"stories\"`, `Story: STORY_TEMPLATE`) or prose inside a TEMPLATE LITERAL, where the word is not wrapped in quotes of its own. The pattern encoded a CLASSIFICATION ASSUMPTION — that a site worth finding would be a quoted literal — and then returned a count that looked like an inventory. Grep for the WORD and classify afterwards; do not put the classification in the pattern, because a pattern cannot report what it was built not to see."],"comments":[],"labels":["rename","arc-migration","cross-repo"],"documentation":[],"dependencies":[],"createdAt":"2026-09-19T03:13:36.457Z","updatedAt":"2026-09-19T05:27:40.822Z","blockers":[],"gates":[],"gateEvents":[]}
