{"id":"LCLI-463","aliases":[],"title":"Decide whether lore scaffold docusaurus should pin its algolia search dependency versions","status":"To Do","description":"The docusaurus scaffold's algolia search-integration dependencies are resolved unpinned at scaffold time (npm install against whatever docusaurus's own published template currently references). On 2026-09-09 that transitive resolution broke CI twice in an afternoon -- two different algolia sub-packages, both at 5.59.0, both failing to resolve from the registry -- confirmed as an upstream registry issue, not a lore-cli defect, and confirmed RESOLVED the same day (see notes). The acute outage is over; what remains open is whether lore should pin/vendor a known-good algolia dependency set in the scaffolded docusaurus template to prevent a recurrence, or accept that an unpinned transitive dependency can take this smoke test down again with no guarantee of how long it takes upstream to recover next time.","acceptanceCriteria":[{"index":0,"text":"Decide: pin/vendor a known-good algolia search dependency set in the docusaurus scaffold template, or explicitly accept the unpinned-transitive-dependency risk as acceptable for a smoke test (not a released artifact)","checked":false},{"index":1,"text":"If pinning is chosen, implement it and verify the scaffold smoke test still passes against the pinned versions","checked":false},{"index":2,"text":"If accepting the risk is chosen, record that decision here so a future recurrence is recognized as an accepted, known risk rather than investigated fresh","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["OUTAGE CONFIRMED RESOLVED (2026-09-09, dated so this doesn't sit stale). This task originally described the acute failure directly; narrowed above once the outage itself resolved, per opum-agent: an open task describing a condition that no longer exists is the same stale-record class as everything else cleared today.\n\nConfirmation, not just relief: the diagnosis (external, upstream, unrelated to lore-cli's own changes) was made BEFORE the resolution, on plausibility -- nothing in the CLAUDE.md-prose-plus-ci.yml-comment PR that first hit this could touch docusaurus scaffolding. PR #636's promotion run (a later, unrelated SHA) ran the full matrix clean, including docusaurus scaffold smoke passing outright -- confirming the prediction rather than merely ending the problem. That is the evidence this note exists to record: run https://github.com/opum-ai/lore-cli/actions/runs/34372155086, job 'docusaurus scaffold smoke', pass, 1m26s.\n\nWhat's still open, and why this stays a task rather than closing: the underlying exposure (unpinned transitive algolia dependency, resolved fresh at scaffold time) is unchanged. Nothing prevents a recurrence; today's incident just happened to self-resolve within the hour. That's the pinning decision the narrowed title/ACs above now ask for."],"comments":[],"labels":["ci","external-dependency","docusaurus"],"documentation":[],"dependencies":[],"createdAt":"2026-09-09T15:38:19.205Z","updatedAt":"2026-09-09T15:49:11.039Z","blockers":[],"gates":[],"gateEvents":[]}
