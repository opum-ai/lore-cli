{"id":"LCLI-10","aliases":["backlog:/Volumes/external/repos/lore-cli:active:LCLI-10","LCLI-10"],"title":"Implement .lore config loader (native TOML + env overlay)","status":"Done","summary":"{\"backlog\":{\"sourcePath\":\"backlog/tasks/lcli-10 - Implement-.lore-config-loader-native-TOML-env-overlay.md\",\"sourceFolder\":\"active\",\"git\":{\"commit\":\"b18b7e5c0b42cb4bad9bb64dc06bcf0f6a473912\",\"blob\":\"a4ceced7a7a88ded371a49047c4681ce6bd1d73a\"},\"assignees\":[\"@claude\"],\"references\":[],\"modifiedFiles\":[],\"createdAt\":\"2026-07-28 20:13\",\"updatedAt\":\"2026-08-03 16:09\"}}","description":"---\nid: LCLI-10\ntitle: Implement .lore config loader (native TOML + env overlay)\nstatus: Done\nassignee:\n  - '@claude'\ncreated_date: '2026-07-28 20:13'\nupdated_date: '2026-08-03 16:09'\nlabels:\n  - core\n  - 'doc:stories/build-the-lore-cli-foundation'\nmilestone: m-1\ndependencies: []\ndocumentation:\n  - docs/adr/0013-lore-state-directory.md\n  - docs/stories/build-the-lore-cli-foundation.md\npriority: high\nordinal: 10000\n---\n\n## Description\n\n<!-- SECTION:DESCRIPTION:BEGIN -->\nParse .lore/config.toml with Bun native TOML; overlay env (LORE_CONFLUENCE_TOKEN never persisted).\n<!-- SECTION:DESCRIPTION:END -->\n\n## Acceptance Criteria\n<!-- AC:BEGIN -->\n- [x] #1 config.toml is committed; cache/ is gitignored\n- [x] #2 Reconcile rules and link options are configurable\n<!-- AC:END -->\n\n## Implementation Plan\n\n<!-- SECTION:PLAN:BEGIN -->\n1. src/config.ts — loadConfig({root?, env?}) → LoreConfig. Sync: existsSync+readFileSync + Bun.TOML.parse; missing file → defaults (zero-config). root/env are injectable seams (design §8).\n2. Types: LoreConfig{reconcile{mode,overrides}, validate{externalLinks,promotePortability}, confluence{baseUrl?,space?,parentPageId?,format,token?}}. Map TOML snake_case → TS camelCase at the boundary.\n3. Validation: strict type/enum on known keys → LoreError('validation'); malformed TOML → LoreError('validation'); unknown keys tolerated (forward-compat). A 'token' committed under [confluence] → LoreError('validation') (ADR-0013 fail-loud). Token read ONLY from env LORE_CONFLUENCE_TOKEN, never persisted/written back.\n4. AC#1: commit .lore/config.toml (cache/ already gitignored at repo root). AC#2: [reconcile] + [validate] are the configurable surface.\n5. test/config.test.ts — defaults, full+partial parse, malformed TOML, bad enum/type, env overlay, token-in-file error, unknown-key tolerance; determinism via injected env/root + temp dirs.\n6. Align design §2 module tree (add config.ts line; note state.ts consumes it). Update CHANGELOG (Unreleased).\n7. Gates: bun test, bun run lint, bun run typecheck. Feature branch → PR into dev.\nDecisions (user-approved this session): module = src/config.ts; token-committed-in-file = hard-error (validation).\n<!-- SECTION:PLAN:END -->\n\n## Implementation Notes\n\n<!-- SECTION:NOTES:BEGIN -->\nImplemented src/config.ts: loadConfig({root?, env?}) -> LoreConfig. Bun-native Bun.TOML.parse (no dependency; parses data not a module, so it survives bun build --compile). Sync via existsSync+readFileSync; a missing file -> zero-config defaults; snake_case TOML mapped to camelCase at the boundary. Hand-rolled type/enum validation (Zod deferred to LCLI-15) raises LoreError type=validation (exit 6) on malformed TOML or an out-of-contract value; unknown keys/sections are tolerated for forward-compat. The Confluence token is read ONLY from the LORE_CONFLUENCE_TOKEN env var, never persisted; a committed [confluence].token fails loud (validation) even when the env token is set. Decisions (user-approved this session): (1) module = src/config.ts, a focused loader the design's state.ts consumes (lore-design module tree section 2 + 2.4 updated to match); (2) committed token = hard-error per ADR-0013 fail-loud. Validation: bun test 70 pass / 0 fail (18 new config tests), bun run lint PASS, bun run typecheck PASS; e2e smoke: committed config -> defaults, env token overlays onto confluence, committed token -> --json validation envelope on stderr + exit 6.\n\nRound-1 /code-review max (35 agents, ~1M tokens): 11 findings, applied 9, deferred 1, skipped 1. Fixed: (1) __proto__/constructor override keys silently dropped in asStringMap -> Object.create(null) (matches errors.ts hardening); (2) committed-token guard now deep-scans the whole [confluence] subtree incl. nested subtables/arrays (was top-level only); (3) whitespace-only LORE_CONFLUENCE_TOKEN treated as absent and trimmed; (4) consolidated file read+parse - single readFileSync (ENOENT->defaults, removes existsSync TOCTOU), non-ENOENT failures surfaced with their OS reason not a blanket 'check permissions', and Bun's parser message surfaced not swallowed; (5) parent_page_id accepts an unquoted TOML integer (coerced to string; over-MAX_SAFE_INTEGER rejected with a quote-it hint); (6) softened overrides doc comment (value semantics deferred to reconcile.ts/LCLI-23); (7) removed the dead <root> branch; (8) single-sourced the confluence.format default; (9) exported defaultConfig + test imports it (was a hand-copied literal) and de-vacuoused the tolerance test. Deferred: (10) repo-root discovery when run from a subdir - belongs with bundle-root/CLI wiring, not yet triggerable (cli is a stub). Skipped: obscure [[confluence]] array-of-tables typo masking. +8 tests (78 total, 0 fail), lint+typecheck clean, e2e smoke re-verified. Running a 2nd max pass per the foundational-module >=2-passes convention.\n\nRound-2 /code-review max (41 agents, ~1.68M tokens): 9 findings, applied 8, rejected 1 false positive. Round-2 caught a SECURITY bug + 2 regressions from round-1 fixes, validating the >=2-passes convention. Fixed: (1) UTF-8 BOM bypass - a BOM-prefixed config.toml parsed as {} (Windows Notepad/PowerShell default), silently dropping all settings AND bypassing the committed-token guard; now strip a leading U+FEFF before parse. (2) [[confluence]] array-of-tables routed around the token scan (generic 'must be a table', no env-var pointer); now scan root.confluence for a token across table/array shapes BEFORE the shape check. (3) REGRESSION from round-1 fix #4: describeCause leaked Bun's internal 'BuildMessage:' class prefix (not instanceof Error); now extract .message via a causeMessage helper. (4) GAP in round-1 fix #5: asPageId accepted 0/negative integers and empty/whitespace strings; now require a positive integer / non-empty string. (5) Round-1 fix #1's Object.create(null) gave overrides a null prototype under a plain Record type (a future overrides.hasOwnProperty() would throw); switched to Object.defineProperty (normal prototype + own __proto__ key). (6) Added an AggregateError test (round-1 input swap left describeCause's aggregate-flatten branch uncovered; it IS reachable - 'this is = = not [' throws AggregateError w/ 2 sub-messages - so round-2's 'dead code' claim #9 was a false positive, rejected). (7) describeCause empty reason -> no dangling colon (withReason helper). (8) Relaxed the AC#1 test to assert well-formedness, not equality to defaults. +7 tests (85 total, 0 fail), lint+typecheck clean, e2e smoke re-verified all 8. Running round-3.\n\nRound-3 /code-review max (32 agents, ~1.14M tokens): 9 findings; closed all real ones at the CLASS level. Round-3 again caught a SECURITY regression + asymmetries from round-2 fixes. (1) SECURITY: round-2's single-BOM strip missed a DOUBLE BOM (some Windows/PowerShell paths emit >1) - re-parsed as {}, re-bypassing the token guard; now strip ALL leading BOMs via a charCodeAt loop. (2/3/9) asPageId asymmetry from round-2 fix #4: string branch skipped the positive/numeric checks the integer branch enforced (quoted '0'/'-5'/'not-a-number' slipped through); safe-integer check ran before sign check (negative-non-safe said 'too large' not 'must be positive', and its 'quote it' hint would smuggle the negative through as a string); message/hint disagreed on 'positive'. Rewrote asPageId to validate BOTH forms identically: string must match /^[1-9][0-9]*$/ (positive int, any length so quoting preserves precision); number must be a positive integer (sign before magnitude) then safe-or-quote. (8) overrides reserved-key class: replaced round-2's Object.defineProperty (which let a constructor/prototype key shadow Object.prototype) with explicit rejection of __proto__/constructor/prototype on a plain {} - closes the whole prototype-key class (round-1 drop, round-2 null-proto, round-3 shadow) loud+clean. (5) Reverted round-2 fix #8's AC#1 relaxation - committed config documents itself as a defaults no-op, so pin toEqual(DEFAULTS) again. (7) describeCause: filter empties at push time + fall back to top-level message so an all-empty AggregateError can't degrade to a bare diagnostic. (4) integral float accepted (can't distinguish from int in JS; benign). +2 tests (87 total, 0 fail), lint+typecheck clean, e2e re-verified all. Recurring classes now closed at the class level, not patched per-instance.\n\nRound-4 (convergence) /code-review max (24 agents, ~830K tokens): 6 findings, NO new shipped bug - all polish/pins/latent-trap. Applied: (1) added a double/triple-BOM regression test (the strip-all CODE was correct but only single-BOM was pinned; a revert to slice(1) would have passed green). (2) reserved-key fail() now echoes input.value like every sibling validator (JSON-contract consistency). (3) Broadened the override-key guard from a 3-key denylist to ALL Object.prototype members + 'prototype' via isUnsafeMapKey (key in Object.prototype): 'toString'/'hasOwnProperty'/'valueOf'/etc. override keys now rejected too - comprehensively closing the prototype-key class for string keys (TOML keys are always strings); comment no longer over-claims. (4) Fixed asPageId docstring's false 'validated identically' claim (number form value-constrained after Bun normalizes 0x10/1_000/1e3 to decimal; string form text-constrained; neither admits an invalid id). Left by-design: AC#1 toEqual(DEFAULTS) loud-drift (intended). Skipped (out of scope/pre-existing/refused prior rounds): exporting errors.ts singleLine. +1 test (88 total, 0 fail), lint+typecheck clean, e2e verified. CONVERGED: 4 independent 10-finder max passes; round-4 found zero shipped bugs.\n<!-- SECTION:NOTES:END -->\n\n## Final Summary\n\n<!-- SECTION:FINAL_SUMMARY:BEGIN -->\nAdded src/config.ts, the .lore/config.toml loader: Bun-native TOML parse (no dependency) into a typed, validated LoreConfig (reconcile/validate/confluence) with injectable root/env seams and zero-config defaults. The Confluence token is environment-only (LORE_CONFLUENCE_TOKEN); a committed token fails loud. Malformed or out-of-contract config raises a validation LoreError (exit 6); unknown keys are tolerated. Committed lore's own .lore/config.toml (AC#1; cache/ already gitignored at repo root) and added test/config.test.ts (18 tests). Aligned the design module tree (lore-design sections 2 and 2.4). Gates green: 70 tests pass, lint + typecheck clean, e2e smoke verified. Delivered as feature branch feat/lore-10-config-loader -> PR into dev; left In Progress pending review/merge.\n<!-- SECTION:FINAL_SUMMARY:END -->\n","priority":"high","ordinal":10000,"acceptanceCriteria":[{"index":0,"text":"config.toml is committed; cache/ is gitignored","checked":true},{"index":1,"text":"Reconcile rules and link options are configurable","checked":true}],"definitionOfDone":[],"plan":["1. src/config.ts — loadConfig({root?, env?}) → LoreConfig. Sync: existsSync+readFileSync + Bun.TOML.parse; missing file → defaults (zero-config). root/env are injectable seams (design §8).\n2. Types: LoreConfig{reconcile{mode,overrides}, validate{externalLinks,promotePortability}, confluence{baseUrl?,space?,parentPageId?,format,token?}}. Map TOML snake_case → TS camelCase at the boundary.\n3. Validation: strict type/enum on known keys → LoreError('validation'); malformed TOML → LoreError('validation'); unknown keys tolerated (forward-compat). A 'token' committed under [confluence] → LoreError('validation') (ADR-0013 fail-loud). Token read ONLY from env LORE_CONFLUENCE_TOKEN, never persisted/written back.\n4. AC#1: commit .lore/config.toml (cache/ already gitignored at repo root). AC#2: [reconcile] + [validate] are the configurable surface.\n5. test/config.test.ts — defaults, full+partial parse, malformed TOML, bad enum/type, env overlay, token-in-file error, unknown-key tolerance; determinism via injected env/root + temp dirs.\n6. Align design §2 module tree (add config.ts line; note state.ts consumes it). Update CHANGELOG (Unreleased).\n7. Gates: bun test, bun run lint, bun run typecheck. Feature branch → PR into dev.\nDecisions (user-approved this session): module = src/config.ts; token-committed-in-file = hard-error (validation)."],"implementationNotes":["Implemented src/config.ts: loadConfig({root?, env?}) -> LoreConfig. Bun-native Bun.TOML.parse (no dependency; parses data not a module, so it survives bun build --compile). Sync via existsSync+readFileSync; a missing file -> zero-config defaults; snake_case TOML mapped to camelCase at the boundary. Hand-rolled type/enum validation (Zod deferred to LCLI-15) raises LoreError type=validation (exit 6) on malformed TOML or an out-of-contract value; unknown keys/sections are tolerated for forward-compat. The Confluence token is read ONLY from the LORE_CONFLUENCE_TOKEN env var, never persisted; a committed [confluence].token fails loud (validation) even when the env token is set. Decisions (user-approved this session): (1) module = src/config.ts, a focused loader the design's state.ts consumes (lore-design module tree section 2 + 2.4 updated to match); (2) committed token = hard-error per ADR-0013 fail-loud. Validation: bun test 70 pass / 0 fail (18 new config tests), bun run lint PASS, bun run typecheck PASS; e2e smoke: committed config -> defaults, env token overlays onto confluence, committed token -> --json validation envelope on stderr + exit 6.\n\nRound-1 /code-review max (35 agents, ~1M tokens): 11 findings, applied 9, deferred 1, skipped 1. Fixed: (1) __proto__/constructor override keys silently dropped in asStringMap -> Object.create(null) (matches errors.ts hardening); (2) committed-token guard now deep-scans the whole [confluence] subtree incl. nested subtables/arrays (was top-level only); (3) whitespace-only LORE_CONFLUENCE_TOKEN treated as absent and trimmed; (4) consolidated file read+parse - single readFileSync (ENOENT->defaults, removes existsSync TOCTOU), non-ENOENT failures surfaced with their OS reason not a blanket 'check permissions', and Bun's parser message surfaced not swallowed; (5) parent_page_id accepts an unquoted TOML integer (coerced to string; over-MAX_SAFE_INTEGER rejected with a quote-it hint); (6) softened overrides doc comment (value semantics deferred to reconcile.ts/LCLI-23); (7) removed the dead <root> branch; (8) single-sourced the confluence.format default; (9) exported defaultConfig + test imports it (was a hand-copied literal) and de-vacuoused the tolerance test. Deferred: (10) repo-root discovery when run from a subdir - belongs with bundle-root/CLI wiring, not yet triggerable (cli is a stub). Skipped: obscure [[confluence]] array-of-tables typo masking. +8 tests (78 total, 0 fail), lint+typecheck clean, e2e smoke re-verified. Running a 2nd max pass per the foundational-module >=2-passes convention.\n\nRound-2 /code-review max (41 agents, ~1.68M tokens): 9 findings, applied 8, rejected 1 false positive. Round-2 caught a SECURITY bug + 2 regressions from round-1 fixes, validating the >=2-passes convention. Fixed: (1) UTF-8 BOM bypass - a BOM-prefixed config.toml parsed as {} (Windows Notepad/PowerShell default), silently dropping all settings AND bypassing the committed-token guard; now strip a leading U+FEFF before parse. (2) [[confluence]] array-of-tables routed around the token scan (generic 'must be a table', no env-var pointer); now scan root.confluence for a token across table/array shapes BEFORE the shape check. (3) REGRESSION from round-1 fix #4: describeCause leaked Bun's internal 'BuildMessage:' class prefix (not instanceof Error); now extract .message via a causeMessage helper. (4) GAP in round-1 fix #5: asPageId accepted 0/negative integers and empty/whitespace strings; now require a positive integer / non-empty string. (5) Round-1 fix #1's Object.create(null) gave overrides a null prototype under a plain Record type (a future overrides.hasOwnProperty() would throw); switched to Object.defineProperty (normal prototype + own __proto__ key). (6) Added an AggregateError test (round-1 input swap left describeCause's aggregate-flatten branch uncovered; it IS reachable - 'this is = = not [' throws AggregateError w/ 2 sub-messages - so round-2's 'dead code' claim #9 was a false positive, rejected). (7) describeCause empty reason -> no dangling colon (withReason helper). (8) Relaxed the AC#1 test to assert well-formedness, not equality to defaults. +7 tests (85 total, 0 fail), lint+typecheck clean, e2e smoke re-verified all 8. Running round-3.\n\nRound-3 /code-review max (32 agents, ~1.14M tokens): 9 findings; closed all real ones at the CLASS level. Round-3 again caught a SECURITY regression + asymmetries from round-2 fixes. (1) SECURITY: round-2's single-BOM strip missed a DOUBLE BOM (some Windows/PowerShell paths emit >1) - re-parsed as {}, re-bypassing the token guard; now strip ALL leading BOMs via a charCodeAt loop. (2/3/9) asPageId asymmetry from round-2 fix #4: string branch skipped the positive/numeric checks the integer branch enforced (quoted '0'/'-5'/'not-a-number' slipped through); safe-integer check ran before sign check (negative-non-safe said 'too large' not 'must be positive', and its 'quote it' hint would smuggle the negative through as a string); message/hint disagreed on 'positive'. Rewrote asPageId to validate BOTH forms identically: string must match /^[1-9][0-9]*$/ (positive int, any length so quoting preserves precision); number must be a positive integer (sign before magnitude) then safe-or-quote. (8) overrides reserved-key class: replaced round-2's Object.defineProperty (which let a constructor/prototype key shadow Object.prototype) with explicit rejection of __proto__/constructor/prototype on a plain {} - closes the whole prototype-key class (round-1 drop, round-2 null-proto, round-3 shadow) loud+clean. (5) Reverted round-2 fix #8's AC#1 relaxation - committed config documents itself as a defaults no-op, so pin toEqual(DEFAULTS) again. (7) describeCause: filter empties at push time + fall back to top-level message so an all-empty AggregateError can't degrade to a bare diagnostic. (4) integral float accepted (can't distinguish from int in JS; benign). +2 tests (87 total, 0 fail), lint+typecheck clean, e2e re-verified all. Recurring classes now closed at the class level, not patched per-instance.\n\nRound-4 (convergence) /code-review max (24 agents, ~830K tokens): 6 findings, NO new shipped bug - all polish/pins/latent-trap. Applied: (1) added a double/triple-BOM regression test (the strip-all CODE was correct but only single-BOM was pinned; a revert to slice(1) would have passed green). (2) reserved-key fail() now echoes input.value like every sibling validator (JSON-contract consistency). (3) Broadened the override-key guard from a 3-key denylist to ALL Object.prototype members + 'prototype' via isUnsafeMapKey (key in Object.prototype): 'toString'/'hasOwnProperty'/'valueOf'/etc. override keys now rejected too - comprehensively closing the prototype-key class for string keys (TOML keys are always strings); comment no longer over-claims. (4) Fixed asPageId docstring's false 'validated identically' claim (number form value-constrained after Bun normalizes 0x10/1_000/1e3 to decimal; string form text-constrained; neither admits an invalid id). Left by-design: AC#1 toEqual(DEFAULTS) loud-drift (intended). Skipped (out of scope/pre-existing/refused prior rounds): exporting errors.ts singleLine. +1 test (88 total, 0 fail), lint+typecheck clean, e2e verified. CONVERGED: 4 independent 10-finder max passes; round-4 found zero shipped bugs.","Added src/config.ts, the .lore/config.toml loader: Bun-native TOML parse (no dependency) into a typed, validated LoreConfig (reconcile/validate/confluence) with injectable root/env seams and zero-config defaults. The Confluence token is environment-only (LORE_CONFLUENCE_TOKEN); a committed token fails loud. Malformed or out-of-contract config raises a validation LoreError (exit 6); unknown keys are tolerated. Committed lore's own .lore/config.toml (AC#1; cache/ already gitignored at repo root) and added test/config.test.ts (18 tests). Aligned the design module tree (lore-design sections 2 and 2.4). Gates green: 70 tests pass, lint + typecheck clean, e2e smoke verified. Delivered as feature branch feat/lore-10-config-loader -> PR into dev; left In Progress pending review/merge."],"comments":[],"labels":["core","doc:stories/build-the-lore-cli-foundation"],"documentation":["docs/adr/0013-lore-state-directory.md","docs/stories/build-the-lore-cli-foundation.md"],"dependencies":[],"createdAt":"2026-07-28T20:13:00.000Z","updatedAt":"2026-08-03T16:09:00.000Z","milestoneId":"M-2","blockers":[],"gates":[],"gateEvents":[],"source":{"system":"backlog","reference":"LCLI-10"}}
