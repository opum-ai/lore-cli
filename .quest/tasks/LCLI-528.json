{"id":"LCLI-528","aliases":[],"title":"cli-contract's kind registry says lore init emits 'init'; the code emits 'init.result'","status":"To Do","description":"Found incidentally by an adversarial reviewer during PR #140 (LCLI-467), while checking that PR's envelope against the contract. Pre-existing, untouched by that PR, and it affected nothing there -- filed because the contract document is the normative machine-facing spec and a consumer branching on `kind` would be reading a wrong table.\n\nreference/cli-contract section 2.1's `kind` registry lists `init` for `lore init`. The code emits `init.result`:\n  - src/core/manifest.ts\n  - src/commands/init.ts\n  - test/init.test.ts asserts init.result\n\nSo the document and the implementation disagree about a value external consumers are explicitly invited to branch on.\n\nDECIDE WHICH IS WRONG rather than making them match blindly: if `init.result` is correct, the contract's registry needs the correction and the change is documentation-only; if `init` is correct, changing the emitted kind is a BREAKING change to a machine surface and needs the contract's own compatibility rules applied. Check whether any other kind in that registry has drifted the same way while in there -- one divergence in a hand-maintained table is rarely alone.","priority":"low","acceptanceCriteria":[],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["docs","cli-contract"],"documentation":[],"dependencies":[],"createdAt":"2026-09-17T01:45:33.473Z","updatedAt":"2026-09-17T01:45:33.473Z","blockers":[],"gates":[],"gateEvents":[]}
