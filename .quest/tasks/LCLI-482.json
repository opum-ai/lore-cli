{"id":"LCLI-482","aliases":[],"title":"OIDC trusted publishing is broken by GitHub immutable subject claims","status":"To Do","description":"release.yml's publish job cannot authenticate to npm. GitHub issues immutable-format OIDC subject claims for this repository (repo:opum-ai@311910387/lore-cli@1365013937:...), and npm Trusted Publishing matches the classic repo:opum-ai/lore-cli:... form, so npm treats the token as unauthenticated and returns E404 on PUT. Proven by decoding a real token minted with audience npm:registry.npmjs.org in probe run 34787200768. Cause is GitHub's 2026-04-23 policy that every repository created after 2026-07-15 uses the immutable format; lore-cli was recreated 2026-09-10, one day after 0.6.0 published successfully from CI under the old repo id. quest-cli shows the same E404 and has confirmed use_immutable_subject true on its own repository, so the same mechanism is very likely in play there -- but it was recreated the same day and has a competing NODE_AUTH_TOKEN theory of its own, so do NOT restate this as 'broken identically' until one of us reads the registry's actual refusal message. quest-cli's theory is disproven in npm's source; see the reconciliation comment. Repo-level opt-out does not work: PUT to actions/oidc/customization/sub with use_immutable_subject false is accepted and has no effect. GitHub documents no opt-out once adopted. Until this is resolved every release must use the manual scripts/publish-release.sh path, which itself requires a human because npm demands a browser OTP that cannot be completed in a non-TTY. Full evidence and two documented false leads are on LCLI-475.","acceptanceCriteria":[{"index":0,"text":"release.yml publish job successfully authenticates to npm via OIDC on a real publish:true dispatch, OR the job is removed/replaced with a documented manual-only path and release-publishing.md no longer presents OIDC as the working mechanism","checked":false},{"index":1,"text":"release-publishing.md explains the immutable subject claim incompatibility, including that re-registering Trusted Publishers does not fix it and that the GITHUB_TOKEN Permissions log group omits IdToken even when granted","checked":false},{"index":2,"text":"The npm-side limitation is reported upstream or a tracked upstream issue is linked, so this is not rediscovered from scratch","checked":false},{"index":3,"text":"quest-cli is told the outcome and the reconciliation is settled in both trackers: either the shared cause is confirmed by the registry's own refusal message, or the two repos' causes are recorded as distinct -- rather than either side asserting the other's mechanism from a shared symptom","checked":false},{"index":4,"text":"The publish step runs at NPM_CONFIG_LOGLEVEL=verbose (or --loglevel verbose) so npm's swallowed 'Failed token exchange request with body message' line is actually visible, since every diagnosis on this task so far is an inference from a downstream E404","checked":false},{"index":5,"text":"release.yml's 'Assert no token is shadowing OIDC' guard keeps its check but loses its false mechanism: npm calls oidc() before reading credentials and overwrites _authToken on success, so a configured token does not prevent OIDC -- it becomes the silent fallback when the exchange fails","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":["AMENDED 2026-09-18: HALF OF THIS RECORD IS STALE AND WAS ACTIVELY MISINFORMING SESSIONS. Read this note before acting on the description above.\n\nSTILL TRUE: OIDC trusted publishing cannot authenticate for this repository. GitHub issues immutable-format subject claims, npm Trusted Publishing matches the classic form, the token exchange is refused and npm returns E404 on PUT. Repo-level opt-out does not work. So release.yml with publish:true CANNOT publish, and every release from 0.6.1 onward carries NO provenance attestation. That half is unchanged.\n\nNO LONGER TRUE: the sentence 'requires a human because npm demands a browser OTP that cannot be completed in a non-TTY'. There is NO OTP. scripts/publish-release.sh reads a GRANULAR npm token from the macOS Keychain (service npm-opum-ai-publish), and a granular token bypasses npm's 2FA-on-write. The entry EXISTS -- verified 2026-09-18 by shape without printing it: 40 characters, npm_ prefix. Publishing is a one-command operation needing no human at a terminal.\n\nWHY THE STALE HALF SURVIVED, because the mechanism matters more than the correction. This record was ACCURATE when written. publish-release.sh's own header states that on 2026-09-14 the keychain entry did NOT exist and NPM_TOKEN was unset, so the publish fell through to ~/.npmrc with a web-login-shaped token subject to require-2FA-on-write -- 'that, and not a dead credential, is where the human-at-a-TTY constraint came from'. LCLI-488/489 then installed the granular token and removed the constraint. This task was never amended.\n\nTHE FAILURE IS STRUCTURAL, NOT FORGETFULNESS (operator report, filed by opum-agent as OPAG-265: 'every time lore-cli and quest-cli go to npm publish they forgot OIDC is broken and want to do OTP... but they forget this every time'). A session asking 'why can't I publish' finds this TASK ID first; the accurate script header is one level deeper. So a stale record outranks an accurate source. And HALF-stale is worse than wrong: the true half (OIDC is broken) lends the false half its credibility, so the whole record reads as verified. At least two sessions have reached the same wrong conclusion and escalated to the operator, and on 2026-09-18 this session relayed the stale half to opum-agent, opum-cli-e2e and opum-marketplace, one of which re-planned around it.\n\nTHE GENERAL RULE, worth more than this instance: A TASK ID PINS WHAT WAS TRUE WHEN IT WAS WRITTEN, NEVER THAT IT REMAINS TRUE. Same shape as a correctly-cited sha256 over a dated report -- the identifier is right and the tense is wrong.\n\nFOLLOW-UPS FILED SEPARATELY: make publish-release.sh fail loudly and specifically when the keychain entry is missing, instead of silently degrading into the ~/.npmrc path that manufactured this belief; and put the live fact where a session looks at publish time."],"comments":[{"id":"lcli-482-npm-oidc-source-reconciliation","authorId":"@claude","body":"RECONCILED WITH quest-cli, 2026-09-13. This task's sentence \"quest-cli is broken identically\" was asserted without quest-cli's evidence, and quest-cli has since published a COMPETING root cause. I went and read npm's source rather than let either claim stand on authority. The immutable-subject diagnosis below SURVIVES; the \"identically\" wording does not, and is corrected in the description.\n\nQUEST-CLI'S COMPETING CLAIM (QCLI-271 implementation note, from actions/setup-node@v6.0.0's source): setup-node with `registry-url` and no token unconditionally runs\n    core.exportVariable('NODE_AUTH_TOKEN', process.env.NODE_AUTH_TOKEN || 'XXXXX-XXXXX-XXXXX-XXXXX')\nso npm's .npmrc resolves `_authToken=${NODE_AUTH_TOKEN}` to that placeholder and -- they concluded -- \"authenticates with a fake token instead of ever attempting OIDC trusted publishing\". Their fix (`unset NODE_AUTH_TOKEN` before npm publish) is merged to their dev as PR #39.\n\nTHAT CONCLUSION IS WRONG, and it is wrong in npm's code, not as a matter of opinion:\n  - lib/commands/publish.js:152 calls `await oidc({...})` UNCONDITIONALLY, and does it BEFORE reading credentials at all -- getCredentialsByURI is line 154, after.\n  - lib/utils/oidc.js contains NO early return on an existing _authToken. Its only bail-outs are: not a recognised CI vendor; ACTIONS_ID_TOKEN_REQUEST_URL/TOKEN absent; no id_token; the exchange request threw; the exchange response carried no `.token`.\n  - On success it OVERWRITES whatever .npmrc held:\n        opts[authTokenKey] = response.token\n        config.set(authTokenKey, response.token, 'user')\n  - Verified identical in npm 12.0.2 (local) and npm 11.19.1 (packed fresh from the registry). The `^11` major is what this repo's publish job installs, so the version that matters is checked, not assumed.\n\nEMPIRICAL CORROBORATION: this repo's release.yml has carried actions/setup-node with registry-url -- and therefore that exact placeholder -- since before v0.6.0. `git show v0.6.0:.github/workflows/release.yml` and HEAD have a byte-identical setup-node step. 0.6.0 PUBLISHED SUCCESSFULLY FROM CI with the placeholder present, and carries real SLSA provenance (opum-cli-e2e re-derived that from the registry independently). A cause that was present during a success is not the cause of the later failure.\n\nWHAT QUEST-CLI ACTUALLY FOUND, which is real and worth keeping: the placeholder is a MASKING layer, not a cause. When the exchange fails, oidc() swallows it (log.verbose + return undefined) and npm falls back to .npmrc. With the placeholder that is a PUT with a fake token -> E404. Without it -> ENEEDAUTH. Same failure, better error. Their `unset` is a genuine diagnostic improvement; it will not make a publish succeed.\n\nCONSEQUENCE FOR THIS REPO'S OWN WORKFLOW, and it is a real defect: the guard at release.yml \"Assert no token is shadowing OIDC\" states the same wrong mechanism -- \"If NODE_AUTH_TOKEN holds a REAL token, npm authenticates with it and NEVER ATTEMPTS OIDC\". Per the source above, npm attempts OIDC first regardless and overwrites the token on success. The guard is still WORTH KEEPING (a real token silently becomes the fallback when the exchange fails, so a publish can succeed under the wrong identity and without provenance, which is worse than failing) but its stated reasoning is false and its error text tells the reader something untrue. Fix the comment and the ::error:: wording; do not delete the check.\n\nHIGHEST-VALUE UNTAKEN STEP, cheap: nobody has read the registry's own refusal. oidc.js logs it at verbose only --\n    log.verbose('oidc', `Failed token exchange request with body message: ${error?.body?.message || 'Unknown error'}`)\nSet NPM_CONFIG_LOGLEVEL=verbose (or --loglevel verbose on the publish step) and npm will print why the exchange was refused, in the registry's words, instead of us inferring it from an E404 three layers downstream. Every theory in this task so far -- mine and quest-cli's -- is an inference from that E404. Do this before the next dispatch.\n\nMETHOD NOTE worth more than the bug: both wrong claims here were produced the same way -- a mechanism inferred from a symptom, then asserted about a SECOND repository whose evidence the author had not seen. quest-cli inferred from setup-node's source without checking whether npm consults .npmrc before OIDC; this task inferred \"identically\" about quest-cli from a shared symptom. The check that settled it was reading the consuming code, not the producing code.","createdAt":"2026-09-13T23:19:47.000Z"},{"id":"lcli-482-ac4-blocked-no-dispatch","authorId":"@claude","body":"AC#4 IS BLOCKED, AND ON A TECHNICALITY WORTH RECORDING, 2026-09-13.\n\nThe verbose instrumentation landed in #18, but there is no dispatch coming to exercise it. I had proposed to quest-cli that whichever repo dispatched first would answer the question for both. They checked with their user and correctly walked it back: quest-cli is publishing 0.6.2 MANUALLY, not through release.yml, precisely because the CI OIDC path is known dead.\n\nTHEIR CORRECTION IS RIGHT AND IS THE USEFUL PART: a manual local publish authenticates with the human's own npm credentials. It never calls oidc() with a GitHub-minted token at all -- oidc.js returns early (not a recognised CI vendor, and no ACTIONS_ID_TOKEN_REQUEST_URL), so the 'Failed token exchange request with body message' line CANNOT be produced by that path no matter what loglevel it runs at. NPM_CONFIG_LOGLEVEL=verbose only pays out on a CI dispatch. My coordination proposal was wrong on its own terms and they caught it.\n\nSo AC#4 is gated on lore-cli's OWN next release.yml dispatch, whenever one happens for an independent reason. It should NOT become a reason to manufacture a dispatch: a publish is in the fleet's irreversible set and needs the user's direct authority, and 'read one log line' does not justify asking for that on its own.\n\nONE THING TO HOLD THE LINE ON, since tonight was a lesson in exactly this. quest-cli described the mechanism as 'already independently confirmed on both repos via the customization/sub API (not inferred)'. That API confirms GITHUB ISSUES IMMUTABLE SUBJECTS. It does not confirm THAT IS WHY NPM REFUSED. The second step is still an inference -- a strong one, with the 0.6.0-published-one-day-before-recreation timing behind it, but an inference. Every wrong claim on this task and on LCLI-484 tonight was a strong inference that nobody had closed the last step on. Keep writing it as 'confirmed subject format, inferred refusal cause' until someone reads the refusal, and the file stays honest whether or not the inference turns out right.\n\nNET: AC#0-#3 unchanged. AC#4 blocked, not abandoned, with the payout condition recorded. AC#5 delivered in #18.","createdAt":"2026-09-13T23:26:21.000Z"},{"id":"lcli-482-registration-alternative-excluded","authorId":"@claude","body":"REGISTRATION ALTERNATIVE IS EXPERIMENTALLY EXCLUDED FOR THIS REPO -- and the check that excludes it is already in the record. 2026-09-13.\n\nquest-cli, tightening their own overclaim, raised a simpler alternative neither of us had ruled out: whether Trusted Publisher is even registered on npmjs.com for all 7 package names after the recreation. Unregistered packages would produce a refused exchange and the identical E404, and it is mundane and fixable. They cited lore-cli as having found \"only 1 of 7 registered\".\n\nThat reads a STALE SNAPSHOT of our own evidence. The sequence on LCLI-475 is:\n  1. Initial state, from the user directly: darwin-arm64 only; the other six unregistered (@opum-ai/lore, lore-darwin-x64, lore-linux-arm64, lore-linux-x64, lore-win32-arm64, lore-win32-x64).\n  2. The user then registered all seven.\n  3. The publish was re-attempted. LCLI-475, verbatim: \"Re-registering Trusted Publishers. The user registered all seven; the E404 is unchanged.\"\n\nStep 3 is an EXPERIMENT, not an inference, and it excludes the registration hypothesis for lore-cli specifically. That materially strengthens the immutable-subject inference HERE -- the leading simple alternative was tested and the symptom did not move.\n\nIT DOES NOT TRANSFER TO quest-cli, and this is the same mistake in the same place as before: their registration state is unknown to both of us because nobody has looked. Their check is free -- read it off npmjs.com -- and if theirs is unregistered they have a DIFFERENT and fixable cause, and \"identical\" would be wrong in the opposite direction from the way this task originally had it. Told them.\n\nWHAT THIS DOES NOT CHANGE: the refusal itself is still unread. Excluding one alternative raises the posterior; it does not close the last step. Keep the wording at \"confirmed subject format, inferred refusal cause\" -- see AC#4. What it does change is the honest strength of the inference for THIS repo: there is now a tested alternative behind it, not just timing.\n\nAC#1's wording was right all along -- it already requires release-publishing.md to state that re-registering Trusted Publishers does not fix it -- and that is precisely because this experiment was run. Do not weaken it.","createdAt":"2026-09-13T23:27:37.000Z"}],"labels":["release","npm","oidc","blocked"],"documentation":[],"dependencies":[],"createdAt":"2026-09-13T22:40:29.998Z","updatedAt":"2026-09-19T00:45:30.787Z","blockers":[],"gates":[],"gateEvents":[]}
