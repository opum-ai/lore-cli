{"id":"LCLI-494","aliases":[],"title":"Projection task records claim sourceAdapterVersion backlog-json/1 regardless of the backend that produced them","status":"To Do","description":"Every projection task record is stamped `sourceAdapterVersion: \"backlog-json/1\"` as a HARD-CODED LITERAL (src/core/projection.ts, in the task-record loop), no matter which tracker backend actually produced it. A Quest-backed repository -- which is lore's DEFAULT for new bundles since commit 3c4c239 -- exports records claiming they came from Backlog.md.\n\nFound while implementing LCLI-476 (dependency edges). Not fixed there deliberately: LCLI-476 was already changing the projection shape and its schema version, and folding an unrelated provenance correction into that change would have made both harder to review and harder to revert independently.\n\nWHY IT MATTERS RATHER THAN BEING COSMETIC. The field exists to tell a consumer WHERE a record came from, which is the one question it is unable to answer. It sits in the same record as the `dependencies` field LCLI-476 just added -- and that field's emptiness is backend-dependent (Quest's list carries dependencies, Backlog.md's and Jira's do not). So a consumer trying to distinguish \"this task genuinely has no prerequisites\" from \"this backend does not report them\" would reach for exactly this field, and be told Backlog.md in every case. The two defects compound: one field is silent about the backend, and another field's meaning depends on the backend.\n\nThat is this release's own theme pointed at our own projection: the record cannot express something a consumer needs, and unlike a missing field this one is worse -- it does not omit the answer, it states a wrong one. A silent contract is recoverable; a confidently incorrect one is not.\n\nSHAPE OF A FIX, not prescribed: the adapter already knows what it is (`resolveSelectedBackend`, and each adapter has its own identity), so the value should be threaded from the producing adapter rather than literal at the projection site. Whoever takes it should decide whether the existing \"backlog-json/1\" string stays the value for the Backlog backend (probably yes -- retained projections carry it and provenance reads them) and what the Quest and Jira spellings are.\n\nCHECK FOR THE CLASS, NOT THE INSTANCE. LCLI-476 found TWO other hard-coded literals of exactly this kind -- a projection schema version compared as \"1.0\" in ladybug-lifecycle, and a synthesized workspace manifest announcing \"1.0\" while emitting the current shape -- and added a source-scanning invariant test (test/projection.test.ts) that fails if a schema-version literal reappears outside projection.ts. Consider whether that invariant should be widened to cover adapter-identity literals too, rather than fixing this one site.","priority":"high","type":"bug","acceptanceCriteria":[{"index":0,"text":"A projection produced against a Quest-backed repository stamps a value naming Quest, not `backlog-json/1`, and the same for Jira.","checked":false},{"index":1,"text":"The value is threaded from the producing adapter rather than written as a literal at the projection site, so a new backend cannot be added without supplying one.","checked":false},{"index":2,"text":"Whether `backlog-json/1` remains the Backlog backend`s value is decided explicitly and recorded, since retained projections already carry it and `lore provenance` reads those.","checked":false},{"index":3,"text":"Retained projections produced before this change still load — the tolerant-read property LCLI-476 established must not regress.","checked":false},{"index":4,"text":"Consideration is given to widening LCLI-476`s source-scanning invariant test to adapter-identity literals, and the decision either way is recorded rather than left implicit.","checked":false},{"index":5,"text":"`bun run lint`, `typecheck` and the test suite pass, and `lore check` exits 0.","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["SIZED 2026-09-14, at the orchestrator's request ('belongs in 0.7.0 if it is small'). VERDICT: small\nenough -- roughly an afternoon -- BUT it carries one design decision and one hard deadline, and the\ndeadline is the reason this is now HIGH rather than medium.\n\nTHE WINDOW CLOSES AT THE 0.7.0 TAG, and this is the load-bearing fact.\n\nLCLI-476 moved the projection export schema from 1.0 to 1.1. That bump is on dev and main but\n0.7.0 is NOT YET TAGGED, so schema 1.1 has never been published. Fixing sourceAdapterVersion BEFORE\nthe tag means the corrected value ships as part of 1.1's definition and no consumer ever observes the\nintermediate state. Fixing it AFTER means 1.1 has already been published with the field lying, so the\ncorrection needs its own 1.2 -- and worse, it needs a compatibility story for records emitted by\n0.7.0 itself, which a consumer cannot distinguish from correct ones without a version to test.\n\nThat asymmetry is large and it is purely a scheduling artifact. Same work, materially cheaper before\nthe tag. Whoever plans the tag should know that landing this first is not a preference.\n\nBLAST RADIUS, measured rather than estimated:\n  - TWO literal sites: src/core/projection.ts:142 and src/core/ladybug-source.ts:404.\n  - TWO callers of buildProjection (src/commands/export.ts:37, src/core/ladybug-source.ts:215) that\n    would need to supply the backend identity.\n  - The value flows into the Ladybug SQLite column (ladybug-driver.ts:125) and into retained\n    snapshot records (snapshot.ts:163 schema, :486 read-back), so it is persisted, not derived.\n  - Changing the emitted value changes every projection digest AGAIN: benchmark fixture goldens,\n    docker/e2e's pinned row, and test/explorer.test.ts.\n\nA THIRD SPELLING ALREADY EXISTS IN THE TREE, found while sizing this and not previously recorded:\ntest/fixtures/snapshot/v1.json carries \"backlog-cli/1\" while all three code sites emit\n\"backlog-json/1\". So the field is not merely wrong about the backend -- its value set is already\ninconsistent between the fixtures and the producer, and nothing catches that because nothing asserts\nacross the two. Whoever takes this should settle the vocabulary once and pin it, not just thread it.\n\nTHE DESIGN DECISION, which is why this is not purely mechanical: RETAINED PROJECTIONS ALREADY CARRY\nTHE WRONG VALUE and cannot be rewritten -- history cannot be re-exported (the same constraint that\nmade LCLI-476's reader tolerant). So a consumer reading an old snapshot sees \"backlog-json/1\" on\nrecords a Quest backend produced. The fix must decide what that means going forward: whether the\nschema version is the discriminator (records at 1.0 make no backend claim worth trusting), or whether\na distinct 'unknown' value is introduced for the un-answerable case. Do not skip this by only fixing\nthe emitter -- that leaves every historical record silently mis-attributed with no way to tell."],"comments":[],"labels":["projection","provenance","cli-contract","0.7.0"],"documentation":[],"dependencies":[],"createdAt":"2026-09-14T21:34:30.729Z","updatedAt":"2026-09-14T22:31:49.907Z","blockers":[],"gates":[],"gateEvents":[]}
