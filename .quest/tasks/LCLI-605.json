{"id":"LCLI-605","aliases":[],"title":"Move the push-only 'main is fast-forward of dev' guard into its own unfiltered workflow and pin that it has no path filter","status":"In Progress","description":"Binding record: opum-doc main 7818aca8b, docs/adr/keep-push-path-filters-off-main-guard-release-publish-and-federation-jobs.md (ODOC-287, commit 0e96eaf3f, opum-ai/opum-doc#459; opum-agent OPAG-444). Read it there before implementing. Defect: paths-ignore applies to the whole workflow, so a Markdown-only push to main (a non-fast-forward rewind included) skips ci.yml's 'main is fast-forward of dev' job. That job runs only on the push to main and main has no ruleset, so it is the one guard that cannot afford to be skipped. LCLI-602 (opum-ai/lore-cli#293) narrowed the filter (CLAUDE.md and README.md no longer ignored) but did not remove this hole. Ruling: a push path filter on main is cost control and is permitted, but it must never cover a job that runs only on that push. (1) Move the job into its own workflow file with NO paths/paths-ignore at all, keeping its rendered job name 'main is fast-forward of dev' exactly, so anything referencing it by name (and CLAUDE.md's promotion prose) stays true. (2) Pin with a test that this workflow carries no path filter and fires on push to main. (3) docs/** and skills/** STAY ignored on ci.yml's main push (the pull_request run already tested the identical tree on ubuntu and windows; macOS coverage of a Markdown-only change is deferred to the next code push, a deliberate OPAG-429 cost choice). Also audit release.yml and upstream-backlog-watch.yml, and any other workflow, for a push-only, release, publish or federation job sharing a filtered workflow; the ADR names the pattern fleet-wide. Update CLAUDE.md's repository profile, which says a docs-only push does not trigger the guard; that sentence becomes false. Proving it needs a real Markdown-only push to main, which the next docs-only promotion supplies (the same shape as LCLI-602 AC1).","priority":"High","type":"chore","acceptanceCriteria":[{"index":0,"text":"The main is fast-forward of dev job lives in its own workflow with no paths or paths-ignore, keeping its exact rendered name and its LCLI-514 assertions (new tip on dev, old tip an ancestor), and ci.yml no longer carries it","checked":true},{"index":1,"text":"test/ci-workflow.test.ts (or equivalent) pins that the guard workflow has no path filter and triggers on push to main; a mutation adding a paths-ignore turns it red","checked":true},{"index":2,"text":"docs/** and skills/** remain in ci.yml push paths-ignore; every other workflow is audited for a push-only/release/publish/federation job behind a path filter, with the result recorded here","checked":true},{"index":3,"text":"CLAUDE.md repository profile no longer claims a docs-only push skips the guard; bun test and bun run lore check exit 0","checked":true},{"index":4,"text":"A real Markdown-only push to main runs the guard, proven by its run id (the next docs-only promotion)","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["IMPLEMENTED 2026-09-26 on chore/LCLI-605-unfiltered-main-guard. The guard job moved to .github/workflows/main-fast-forward-guard.yml: on push branches [main] only, no paths/paths-ignore, key main-is-fast-forward-of-dev, rendered name \"main is fast-forward of dev\", same checkout pin, fetch-depth 0, BEFORE/AFTER/DEV_REF env, git fetch origin dev then scripts/assert-main-fast-forward.sh (both LCLI-514 assertions unchanged). Per-run concurrency group with cancel-in-progress false. ci.yml no longer carries the job. AUDIT (AC3), 2026-09-26: .github/workflows holds ci.yml, release.yml and upstream-backlog-watch.yml. release.yml is workflow_dispatch only; upstream-backlog-watch.yml is schedule + workflow_dispatch; neither has a path filter. ci.yml is the only filtered workflow, and after the move it holds no push-only, release, publish or federation job. A test now pins \"ci.yml is the only path-filtered workflow\" as a tripwire. SKILLS/** (AC3 wording): the criterion says docs/** and skills/** \"remain\" in ci.yml push paths-ignore, but skills/** was never listed there. The ADR says \"stay ignored\", which holds: skills/ contains exactly one file, a .md, covered by */**/*.md (pinned by the exact-list test). skills/** was deliberately NOT added, because it would start ignoring any future non-Markdown skill file, which errs toward running less. AC3 is checked on the ADR reading, and this note records the gap in the criterion wording. TESTS: test/ci-workflow.test.ts \"the main fast-forward guard has its own unfiltered workflow (LCLI-605)\" (4 tests), plus test/assert-main-fast-forward.test.ts wiring now reads the guard workflow. MUTATIONS: paths-ignore on the guard gave exactly 2 red (no-filter, audit) and 22 green. The reviewer independently ran 12 mutations, each reddening only its named case. continue-on-error: true and cancel-in-progress: true each give 1 red (pinned after the review). REVIEW: an opum-workflow reviewer found nothing blocking. Fixed: a stale \"skipped on every PR\" sentence in CLAUDE.md and a test comment; \"EVERY push\" qualified with the [skip ci] and pre-LCLI-605-tip exceptions; a stale OPAG-444 comment. AC5 is post-merge by nature: the next docs-only promotion supplies the run id, so this task stays In Progress after the PR lands.","AC4 evidence: CLAUDE.md profile no longer claims a docs-only push skips the guard (reviewer re-grep; remaining mentions are dated history). On branch head: bun test 3645 pass / 1 skip / 0 fail, bun run lore check 0 errors, typecheck clean."],"comments":[],"labels":["ci"],"documentation":[],"dependencies":[],"references":["opum-doc 0e96eaf3f","opum-ai/lore-cli#293"],"createdAt":"2026-09-26T19:40:05.281Z","updatedAt":"2026-09-26T20:46:50.508Z","blockers":[],"gates":[],"gateEvents":[]}
