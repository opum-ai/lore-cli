{"id":"LCLI-479","aliases":[],"title":"Required flags are unmarked in usage and inexpressible in the manifest (lore impact --direction, and four more)","status":"Done","description":"Usage is 'lore impact <id>' and the flag list does not identify --direction as required. 'lore impact PGF-3 --kind task --json' exits 2 with '--direction is required'. Adding --direction outbound succeeds, and the documented example does carry one -- so the requirement exists and is correct, it's just invisible until the command fails.\n\nHelp usability, not a graph correctness bug -- but the machine-manifest half matters more than it looks, since that is what an agent reads to construct a valid invocation without trial-and-error.\n\nFix: show the requirement in usage, and expose required flags in the machine manifest (lore's --json manifest / help output), or give --direction a documented default.\n\nSource: 2026-09-13 proof-graph inspection (Lore 0.6.0, Quest 0.6.0), read-only against production, recorded as OPAG-120 on opum-agent.","priority":"high","type":"bug","acceptanceCriteria":[{"index":0,"text":"`lore help impact` distinguishes its REQUIRED flags (`--kind`, `--direction`) from its optional ones, and `lore help path` does the same for `--from-kind`, `--to-kind` and `--direction` -- all five, not only the one that was reported.","checked":true},{"index":1,"text":"`lore manifest --json` expresses requiredness machine-readably, so an agent enumerating the contract can tell a mandatory flag from an optional one. The field is ADDITIVE, so a consumer that does not know it is unaffected -- the same property that made the LCLI-366 `kind` field safe.","checked":true},{"index":2,"text":"A regression test ties the manifest's required markers to the ACTUAL parser, so a flag that becomes required, or stops being, cannot silently drift from what the manifest claims. Verified mutation-sensitive: flip one marker and the test must fail.","checked":true},{"index":3,"text":"Every command is audited for the same defect, not only the two reached by the reporter's tooling. Conditionally-required flags enforced by ad-hoc `throw usage(...)` -- `--force requires --out`, `--repository requires --workspace`, `--from`/`--to` together, `--workspace requires retained snapshot selectors` -- are either covered or EXPLICITLY ruled out of scope with a reason on this record.","checked":true},{"index":4,"text":"If the `--direction`-gets-a-default alternative is taken instead, it is argued on the record against the three objections already raised (it resolves one flag of five, does nothing for `lore path`, and leaves the manifest still unable to express requiredness for anything) -- not chosen by default.","checked":true},{"index":5,"text":"If the manifest carries a schema version this change affects, it is bumped and its contract test updated; if it does not, that is stated rather than left implicit.","checked":true},{"index":6,"text":"`bun run lint`, `typecheck` and the test suite pass, and `lore check` exits 0.","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":["WIDENED 2026-09-14 after mbpm2 RE-REPORTED this against the published 0.6.2. It was already filed\nhere (from their 2026-09-13 proof-graph inspection of 0.6.0, OPAG-120), and a second session filed\nit AGAIN as LCLI-493 before finding this record. LCLI-493 is archived as the duplicate; everything\nit established is folded in below. The two facts it added are the reason this record is retitled and\nre-scoped rather than merely annotated.\n\nONE INSTANCE WAS REPORTED. THERE ARE FIVE. Measured on dev at 661529cf, not inferred.\n`requiredChoice` (src/commands/traversal.ts:29,45) governs:\n\n    lore impact  --kind        --direction\n    lore path    --from-kind   --to-kind    --direction\n\nAll five are mandatory. None is marked. `lore help impact` renders every flag in one\nundifferentiated list, so `--direction` looks exactly as optional as `--limit`. Fixing only the\nreported flag would leave four identical defects behind, and would leave `lore path` -- which needs\nTHREE of them -- completely unaddressed.\n\nTHE ROOT CAUSE IS IN THE CONTRACT, NOT THE RENDERER, and this is the half worth the most.\n`ManifestFlag` (src/core/manifest.ts:52) carries `name`, `alias`, `takesValue`, `repeatable`,\n`summary` and `kind` -- and NO WAY WHATSOEVER to express that a flag is required. So the help\nrenderer is not withholding information it has; it has none. Neither does anything reading\n`lore manifest --json`.\n\nThat is what makes this more than a help-text nicety. The manifest exists so an agent can enumerate\nwhat the CLI accepts WITHOUT reading its source. An agent doing that today is told `--direction` is\nan ordinary optional flag, constructs `lore impact <id> --kind task`, and gets exit 2. The CLI is\nmachine-describing a command that does not exist. The contract was SILENT rather than wrong, and\nsilence parses as \"optional\".\n\nSAME SHAPE AS LCLI-366, which is also where the fix shape comes from: a flag that terminates with\nits own envelope (`--version`) was undeclared through 0.3.5, and was fixed by ADDING an optional\nfield to the manifest rather than restructuring it. An optional `required?: boolean` is additive in\nexactly the same way -- a consumer that does not know the field is unaffected.\n\nRESONANCE WITH QUEST, noted by the orchestrator and worth carrying into the 0.7.0 pair: quest moved\n`data.task` to `data` while both shapes still reported `schemaVersion: 1`, so a consumer could not\nselect a decoder. Two sibling products, two machine-readable contracts, both silent about something\na consumer needed. The shared principle, if we adopt it for 0.7.0: ANYTHING AN AGENT PARSES MUST BE\nABLE TO EXPRESS WHAT IT DOES NOT CURRENTLY SAY.\n\nAUDIT THE WHOLE SURFACE, NOT THE FIVE. Other commands enforce requirements through ad-hoc\n`throw usage(...)` rather than `requiredChoice`, and several are CONDITIONAL, which a plain boolean\ncannot express: `--force requires --out` (src/commands/agent.ts:428), `--repository requires\n--workspace` (src/commands/args.ts:181), `--from and --to must be supplied together` and `--workspace\nrequires retained snapshot selectors` (src/commands/explorer.ts:169,172). Decide deliberately whether\nthey are in scope; if they are not, SAY SO on the record rather than leaving the next reader to\nwonder why some mandatory flags are marked and others are not.\n\nThe original AC#3 offered \"or --direction gets a documented default and the required-flag error is\nremoved\" as an alternative. That alternative is now WEAK and should not be taken without argument:\nit resolves one flag of five, does nothing for `lore path`, and leaves the manifest still unable to\nsay \"required\" for anything -- which is the defect an agent consumer actually hits. A default also\nsilently changes traversal semantics for anyone who omitted the flag expecting an error."],"comments":[],"labels":["help","manifest","cli-contract","downstream-feedback","0.7.0"],"documentation":[],"dependencies":[],"references":["OPAG-120 (opum-agent, 2026-09-13 proof-graph inspection)"],"createdAt":"2026-09-13T21:09:15.848Z","updatedAt":"2026-09-14T21:05:24.546Z","finalSummary":"DELIVERED in PR #62. The code was qualified by CI run 34896218696 on head e8a1527a, where all FOUR\ncontexts dev requires were green BY NAME -- four, not three: `lint · typecheck · test\n(ubuntu-latest)` was added to the ruleset on 2026-09-14. (This closing commit rides the same PR and\nre-runs those contexts on its own head; it changes no source.)\n\nWHAT WAS WRONG. `lore impact <id>` rendered as a complete command and always exited 2. `--kind` and\n`--direction` are mandatory and nothing said so. The reported symptom was one flag. The cause was\nthat `ManifestFlag` had NO WAY TO EXPRESS REQUIREDNESS AT ALL, so every flag rendered as optional in\nboth the help text and `lore manifest --json`.\n\nTHE MANIFEST HALF IS THE ONE THAT MATTERED. It exists so an agent can construct a valid invocation\nWITHOUT reading lore's source. With no way to say \"required\" it described a command that does not\nexist, and an agent following it faithfully got exit 2. The contract was not wrong -- it was SILENT,\nand silence parses as \"optional\". Fixed the way LCLI-366 fixed the undeclared `--version` envelope:\nan optional `required?: boolean`, ABSENT rather than `false` on an ordinary flag, so `schemaVersion`\nis unchanged and a consumer that does not know the field reads exactly what it read before.\n\nSEVEN FLAGS, NOT ONE. Five came from `requiredChoice`: `impact --kind/--direction` and\n`path --from-kind/--to-kind/--direction`. The audit found TWO MORE that no string search would have\nreached: `provenance` required BOTH `--kind` and `--snapshot` while marking neither, and its\nrefusals did not even use the same words. The audit worked because it RAN every command in an empty\nworkspace and read the exit codes, rather than grepping for a message -- the grep-shaped version of\nthis audit would have stopped at five and reported success.\n\nA REAL MESSAGE DEFECT FELL OUT OF IT. Omitting `--kind` from `lore provenance` answered\n`--kind must be concept, task, or edge`: a complaint about a value the caller never supplied.\n`requiredOptionValue`/`requiredChoice` now live in `commands/args.ts` as the SINGLE definition of\nthat refusal, used by both `traversal.ts` (which owned a private copy) and `provenance.ts` (which\nhand-rolled its own). Absent and invalid are now reported distinctly. The five instances were one\nfunction; consolidating it is what makes \"all of them\" true rather than \"the ones I remembered\".\n\nTHE GUARD IS WHAT TURNS THIS FROM A DOCUMENTATION CHANGE INTO A CONTRACT, and it is bidirectional.\nForward: every flag marked required must actually be refused when absent. Reverse: with every marked\nflag supplied, no command may still refuse for a missing one -- so a flag that silently BECOMES\nrequired fails the suite. The reverse sweep covers EVERY command, not only those already declaring\none. Scoping it to the latter is how I first wrote it, and that blind spot was exactly `provenance`'s\nshape: the guard could not see the unfixed case. Verified mutation-sensitive five ways, each caught\nby the intended assertion:\n\n  1. unmark impact's --direction (the original defect)        -> reverse fails\n  2. drop required flags from the usage line                  -> usage-line test fails\n  3. mark --limit required when it is not                     -> forward + coverage fail\n  4. unmark BOTH provenance flags (the blind-spot shape)      -> reverse + coverage fail\n  5. add a NEW requirement to a previously unmarked command   -> reverse fails\n\n(5) is the one worth keeping: a future absolute requirement lands in the guard by default, because\n`requiredOptionValue` is now the single producer of that refusal.\n\nDELIBERATELY NOT MARKED, and recorded as a decision rather than left to look like an oversight:\nconditional requirements -- `--force requires --out`, `--repository requires --workspace`,\n`--from`/`--to` together, `--workspace` needing a snapshot selector. None is expressible as a\nboolean. A field that cannot express a thing is honest; a field that half-expresses it is not.\n\nTHE `--direction`-GETS-A-DEFAULT ALTERNATIVE WAS REFUSED, not skipped: it resolves one flag of\nseven, does nothing for `path` or `provenance`, leaves the manifest still unable to express\nrequiredness for anything -- which is the defect an agent actually hits -- and would silently change\ntraversal semantics for anyone who omitted the flag expecting an error.\n\nONE THING WORTH KEEPING. The specs already said \"requires\" for all three commands\n(docs/specs/bounded-path-and-impact.md, docs/specs/snapshot-change-and-provenance-workflows.md).\nThe human-readable documentation was right the whole time. Only the contract a MACHINE reads was\nsilent -- which is why a docs review would never have found this, and why the fix had to be a test\nthat executes the parser rather than a sentence.\n\nDOWNSTREAM COVERAGE GAP, carried deliberately and NOT closed by this task: opum-cli-e2e reads\nnothing from `.flags` or `.parameters` on a manifest command entry -- zero hits across their tree\n(reported by opum-agent, 2026-09-14). So this field lands unverified downstream and their pair suite\ngoing green does NOT mean it works. They are adding shape-locked reads for exactly this; they have\nbeen told the field is live so they can lock onto it. Green there is not evidence for this change\nuntil that read exists.\n\nbun run lint 0, typecheck 0, 2990 tests pass / 1 skip / 0 fail, lore check 0 errors 0 warnings.","blockers":[],"gates":[],"gateEvents":[]}
