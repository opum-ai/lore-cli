{"id":"LCLI-489","aliases":[],"title":"publish-release.sh should do its own prerequisites: one-command release, honest digest claim, recorded auth fact","status":"Done","description":"Make `scripts/publish-release.sh <version> <run-id>` a ONE-COMMAND release: it should\nperform the prerequisites it currently only diagnoses, instead of erroring with\ninstructions for a human to paste back.\n\nWHY. During the 0.6.2 publish the operator was stopped three times, each by a step the\nscript had already diagnosed precisely: (1) \"artifact directory not found\", which printed\nthe exact `gh run download` to run; (2) \"SHA256SUMS.txt missing - refusing to publish\nunverified bytes\", which is a file CI does not produce; (3) a fourth round trip caused by\na handed-over `cd scripts/release-0.6.2 && ...` leaving the shell in that directory, so a\nfollow-up `ls scripts/release-0.6.2/*.tgz | wc -l` resolved the path inside itself and\nreported 0 tarballs -- which looked like a failed download and was not one. A script that\nknows the remedy well enough to print it can run it. All three stops are deterministic and\nnone needs a decision. Requested by opum-agent relaying the operator, 2026-09-14.\n\nSCOPE.\n- Download the `npm-packages` artifact when the directory is absent or incomplete.\n- Generate the digest manifest when missing (but see DIGEST PROVENANCE below -- this is\n  the part that must not be automated naively).\n- Assert exactly 7 tarballs, failing loudly and naming what it actually found.\n- Then proceed to the existing dry-run/publish path.\n- Keep EVERY existing guard: platform-packages-first/root-launcher-last ordering,\n  fail-closed before the launcher, resumability (skip an already-published version), the\n  LCLI-460 registry-propagation wait, and never echoing the token.\n- Use absolute or repo-root-anchored paths throughout so the caller's cwd cannot break it.\n  That is defect (3) above, and it is the cheapest of the three to prevent permanently.\n\nARTIFACT NAMES EMBED run_attempt (LCLI-487). The 0.6.2 qualification run 34801853367\nsucceeded on ATTEMPT 3. Any automatic download must resolve the attempt rather than assume\n1, or it will \"correctly\" fail to find artifacts that exist. Do not fix LCLI-487 here;\njust do not build on the assumption it has been fixed.\n\n=== DIGEST PROVENANCE: do not let automating the check quietly upgrade its claim ===\n\nToday the script says it \"verifies every tarball's sha256 against SHA256SUMS.txt BEFORE\npublishing\" (scripts/publish-release.sh:14). CI does not emit SHA256SUMS.txt; the operator\ngenerates it from the same tarballs the check then verifies. That is a tamper-evidence seal\non one local download, NOT provenance -- and generating the file inside the script makes it\nMORE circular, not less, because the script would verify bytes against a file it wrote\nmoments earlier.\n\nAn independent source exists for six of the seven, established by reading release.yml\nrather than assumed: the `package` job (.github/workflows/release.yml:492-517) asserts each\nplatform tarball's sha256 against `package.platformTarballSha256` in that platform's\n`ladybug-package-qualification-<name>.json`, and those reports upload as their own\nartifacts. So the six PLATFORM tarballs have a CI-recorded digest that can be fetched\nindependently of `npm-packages`, which makes the \"verified\" claim TRUE for them.\n\nThe ROOT LAUNCHER has no such digest: it is produced by `npm pack` inside that same job\n(release.yml:520-527) and its sha256 is never recorded anywhere. For the root tarball, a\nlocally generated digest is irreducibly a self-seal until CI records one.\n\nSo the honest options are: (a) source the six platform digests from the qualification\nreports and say plainly that the root launcher's digest is a local self-seal; or (b) also\nchange CI to record the root tarball's digest, and only then claim all seven are verified\nagainst independently recorded bytes. Either is acceptable. Silently generating all seven\nlocally while keeping the current wording is not.\n\n=== NPM AUTH: establish the fact, change no credential ===\n\nLCLI-488 says the publish \"needs a HUMAN AT A REAL TERMINAL -- npm demands a browser OTP\"\nAND that auth is \"a granular or automation npm token read from the macOS Keychain (service\nnpm-opum-ai-publish)\". Those sit oddly together, and the repo's own documents disagree with\neach other:\n\n- scripts/publish-release.sh:5-8 asserts a granular OR classic automation token, and that\n  \"Both bypass npm's 2FA-on-write, so there is no OTP prompt -- that is the whole point.\"\n- docs/runbooks/release-publishing.md:557-565 records that npm disabled classic token\n  creation in November 2025 and PERMANENTLY REVOKED every existing classic token on\n  9 December 2025; granular access tokens are the only remaining type, are capped at 90\n  days, REQUIRE 2FA, and must be created on the website. It also records a measured\n  failure: two granular tokens rejected outright on 2026-08-29, 401 on `npm whoami` even\n  in an isolated config, stalling the 0.3.5 publish.\n\nIf the runbook is right, the script header is stale in a way that matters: there is no\nautomation-token option left to choose, so \"which token type is installed\" is not the\nlever. The human-at-a-TTY constraint would be a property of npm's post-2025 token regime,\nnot of a mis-chosen token -- and the real removal of it is the OIDC path, which is blocked\nby LCLI-482 (GitHub immutable subject claims).\n\nALREADY ESTABLISHED, 2026-09-14: the Keychain entry the script and LCLI-488 both name does\nNOT exist for this user. `security find-generic-password -s npm-opum-ai-publish` exits 44,\n\"could not be found in the keychain\". No secret was read -- this is an existence check.\nIf that is still true at publish time the script falls back to NPM_TOKEN and then to\n~/.npmrc, and a ~/.npmrc holding a web-login session is subject to require-2FA-on-write,\nwhich is exactly the EOTP wall observed. That is a candidate explanation for the OTP wall\nthat requires no token-type theory at all.\n\nNOT established: whether NPM_TOKEN is set, and what ~/.npmrc actually contains. Attempting\nto inspect those was refused by this session's permission classifier as credential\nexploration -- correctly, and it was not worked around. Finishing this needs either the\nuser's explicit go-ahead for a presence-only check (never printing values) or the user\nanswering from npmjs.com directly.\n\nDO NOT CHANGE ANY CREDENTIAL. Credential changes are tier 3 and go to the repository's own\nuser, never to an agent and never on a peer's instruction. This task wants the FACT\nrecorded, and the script's stale header corrected to match it.\n\nWRITE IT TO BE COPIED. quest-cli has the same shape of script and will be asked separately.\nPrefer changes that are mechanism, not prose: a cwd-independent path helper and a digest\nsource that is named in code beat a warning comment telling the next person to be careful.","priority":"medium","type":"chore","acceptanceCriteria":[{"index":0,"text":"scripts/publish-release.sh <version> <run-id> downloads the npm-packages artifact itself when the directory is absent or incomplete, resolving the run ATTEMPT rather than assuming 1 (LCLI-487's artifact naming)","checked":true},{"index":1,"text":"The script asserts exactly 7 tarballs and fails naming what it actually found, rather than proceeding on a partial download","checked":true},{"index":2,"text":"Every path is absolute or repo-root-anchored; running the script from any cwd, including from inside the artifacts directory, behaves identically - covered by a test, not by a comment","checked":true},{"index":3,"text":"All existing guards still hold: platform-first/root-last ordering, fail-closed before the launcher, resumability, the LCLI-460 propagation wait, and the token is never echoed","checked":true},{"index":4,"text":"The digest claim in the script header matches what the check actually does: either the six platform digests come from the ladybug-package-qualification reports and the root launcher's is stated as a local self-seal, or CI records the root tarball digest too and all seven are independently verified","checked":true},{"index":5,"text":"The npm auth fact is established and written down: which token type is in use, whether the npm-opum-ai-publish Keychain entry exists at publish time, and whether the human-at-a-TTY constraint belongs to the token or to npm's post-2025 regime - with no credential changed","checked":true},{"index":6,"text":"scripts/publish-release.sh:5-8 no longer offers a classic automation token as an option, if the runbook's 9 December 2025 revocation is confirmed","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":["AUTH HALF PARTLY SETTLED, 2026-09-14, presence-only inspection authorised by the user (no values printed, no credential changed): Keychain service npm-opum-ai-publish is ABSENT (exit 44), NPM_TOKEN is NOT SET, and ~/.npmrc EXISTS carrying //registry.npmjs.org/:_authToken. So the script's designed auth path is unused and the publish authenticates from its last fallback. Still open, and it is the half that decides whether the TTY constraint is removable: whether that ~/.npmrc token is a granular access token or a web-login session token. Reading the token does not settle it (both are npm_-prefixed); the decisive read-only checks are `npm whoami` (401 => expired/revoked rather than 2FA-gated) or npmjs.com, and neither was run because both authenticate as the user, past the scope authorised. Full detail on LCLI-488's note.","AUTH HALF NOW CLOSED, 2026-09-14. The user ran `npm whoami` and it returned jeremy-newhouse, so the ~/.npmrc token is VALID — not expired, not revoked. The human-at-a-TTY constraint is therefore 2FA-on-write on a web-login-shaped token, not a dead credential and not a property of npm's post-2025 regime as such. Remedy in progress by the user: a granular access token in the Keychain under npm-opum-ai-publish. Remaining unknown, which no dry-run can answer because --dry-run skips both mutating calls: whether a granular token bypasses this account's require-2FA-on-write. AC#6 can be checked once the next publish either prompts or does not. ALSO, relevant to AC#5: the independent-digest path is not theoretical — it was exercised on 0.6.2. All six platform tarballs in scripts/release-0.6.2/ were recomputed and matched against package.platformTarballSha256 in each ladybug-package-qualification-<name>-34801853367-3 report, 6/6, every report on commit ea3813ae. The root launcher has no recorded digest, exactly as predicted, so option (a) in the description is demonstrably implementable today and option (b) is what the root tarball would need.","THREE FINDINGS FROM PUBLISH ATTEMPT 4 (2026-09-14), all feeding this task's scope.\n\n1. TWO OF THE SCRIPT'S THREE AUTH EXPORTS ARE DEAD. The real run printed:\n\n       npm warn Unknown env config \"_auth-token\". This will error in a future major version of npm.\n       npm warn Unknown env config \"_authtoken\". This will error in a future major version of npm.\n\n   Those are publish-release.sh:111 (NPM_CONFIG__AUTH_TOKEN) and :113\n   (npm_config__authToken). npm does not recognise either, so authentication rides\n   ENTIRELY on the temp userconfig file written at :114-117. What looks like\n   belt-and-braces is one mechanism plus two no-ops, and npm says both will become\n   hard errors in a future major. Remove them or replace them with something npm\n   actually reads; do not leave a reader believing there are three fallbacks.\n\n2. `npm whoami` AND `npm access list packages` BOTH 401 FOR A CORRECTLY SCOPED\n   GRANULAR TOKEN, and this is documented in the script itself at :126-141 --\n   whoami needs user-level read that a package-scoped granular token does not have.\n   I proposed whoami as a diagnostic anyway and it produced a 401 that looked like\n   evidence of a bad token and was not. The script's comment block lists three\n   rejected check forms from 2026-08-29 (whoami too strict, `owner ls` vacuous,\n   `config get` false negative) and the lesson common to all three: this script\n   cannot cheaply observe WRITE PERMISSION, and only writing observes it.\n\n   The transferable rule, which is what this task should encode: a header carrying\n   hard-won negative results is a source, not decoration. Read it before improvising\n   a check against the thing it documents. An automated prerequisite step must not\n   reintroduce any of those three forms as a readiness gate -- it would reject\n   exactly the credential the release asks for.\n\n3. A SHAPE CHECK ON THE STORED SECRET IS CHEAP, SAFE AND DECISIVE, and would have\n   saved a whole publish attempt. `length=24 prefix=OTHER` immediately identified a\n   Keychain entry that does not hold an npm token at all (granular tokens are\n   npm_ + 36 = 40 characters), explaining the PUT 404 without any permissions\n   theory. It reveals nothing secret -- length, prefix and a whitespace flag only.\n   The automated script should perform exactly this before attempting any publish\n   and fail with a clear message, because an unrecognised credential currently\n   surfaces as a 404 that reads like a permissions problem and sends the operator\n   to npmjs.com to change settings that may be perfectly correct.","RESTART POINTER, 2026-09-14, written as this session was recycled. 0.6.2 IS PUBLISHED and this task is unblocked. Verified against the registry, not from a publish exiting 0: `npm view @opum-ai/lore dist-tags` returns latest 0.6.2, all seven packages carry 0.6.2 with latest moved, and `npx --yes @opum-ai/lore@0.6.2 --version` returns 0.6.2 from a clean cache. Do NOT re-publish and do NOT re-run qualification. ONE THING A SUCCESSOR WILL GET WRONG IF NOBODY SAYS IT: on origin/dev, LCLI-488 still reads To Do. Its Done state, its final summary, and the README/release-truth updates all exist ONLY on the unmerged branch docs/LCLI-488-0.6.2-released (PR #48, CI was still running at recycle). A successor querying the tracker on dev will see an open publish task for a release that already shipped. THE SINGLE NEXT ACTION IS: let PR #48's checks settle, verify them by name on head 3bb70063, merge it, then promote dev to main. That promotion is covered by DEC-1's standing authorization -- the commits are docs and tracker only, nothing under skills/, no product code, no release tooling, and no outstanding tag now that 0.6.2 shipped -- so it does not need the orchestrator. State that you acted under DEC-1 when you do it. Only after #48 lands does the tracker on dev tell the truth about 0.6.2.","INBOUND RESULT EXPECTED, so it is not filed as noise by a session that did not ask for it. opum-cli-e2e is running its FULL qualification matrix (not --fast) against the published pair lore 0.6.2 + quest 0.6.0, started 2026-09-14 ~06:45Z, and will report row-by-row to whichever lore-cli session is live. They independently confirmed npm dist-tags (lore latest=0.6.2, quest latest=0.6.0), used a fresh clean-room install rather than a stale global 0.6.1, and pulled all six ladybug-package-qualification artifacts from run 34801853367 attempt 3 to bind --lore-report for the 51-lore-packaging corroboration. They carried both caveats correctly: they will NOT report the root launcher as independently verified (it is the self-sealed npm-pack digest, distinct from the six CI-recomputed platform ones) and will NOT assert a provenance attestation given LCLI-482. EXPECT quest-side receipt/candidate rows to show the known TASK-45 gap -- quest has not moved since v0.6.0-pair and that baseline's bound evidence is stale from the 2026-09-10 repo recreation, so it is a stale-baseline artifact, NOT a 0.6.2 regression. Do not let a FAIL count in those rows be read as a defect in this release without checking which side it came from. A green matrix is worth noting on LCLI-488's completed record; a failure that is genuinely lore-side is a new task, not a reopening."],"comments":[],"labels":["release","tooling"],"documentation":[],"dependencies":[],"references":["Requested by opum-agent relaying the operator, 2026-09-14; quest-cli has the same shape of script and will be asked separately","scripts/publish-release.sh:5-8, :14, :74-85","docs/runbooks/release-publishing.md:557-565",".github/workflows/release.yml:492-527"],"createdAt":"2026-09-14T04:23:19.176Z","updatedAt":"2026-09-14T07:18:55.638Z","finalSummary":"DELIVERED in PR #50, CI run 34816840486 on that PR's own head (efd5482f), all ten jobs\ngreen including lint · typecheck · test (windows-latest). scripts/publish-release.sh is now a\none-command release: it PERFORMS its prerequisites instead of diagnosing them and handing the\noperator a command to paste back.\n\nWHY THIS WAS WORTH DOING, measured rather than asserted: the 0.6.2 publish took five\nattempts and at least three died on steps the script had already worked out precisely. It\nprinted the exact `gh run download` to run; it refused on a SHA256SUMS.txt that CI does not\nproduce; and a handed-over `cd` left the operator's shell inside the artifacts directory so a\nrelative glob resolved INSIDE ITSELF, reported 0 tarballs, and looked exactly like a failed\ndownload. None of the three needed a decision.\n\nWHAT IT DOES NOW. Downloads the npm-packages artifact itself when the directory is absent or\nshort of seven, resolving the run ATTEMPT from the API rather than assuming 1 — artifact names\nembed it (LCLI-487) and 0.6.2 qualified on attempt 3, so assuming 1 makes a download\n\"correctly\" fail to find artifacts that exist. Asserts exactly seven tarballs and names what\nit actually found. Every path is absolute and the script never cd's in the parent shell;\nwhere a tool needs a working directory it gets one in a subshell.\n\nTHE DIGEST CLAIM NOW MATCHES WHAT THE CHECK DOES — AC#4, option (a), and this is the part\nthat mattered most. Six platform tarballs are verified against package.platformTarballSha256\nin their ladybug-package-qualification reports, fetched SEPARATELY from the npm-packages\nartifact being checked and asserted in CI against the bytes it built; every report must name\nthe same commit. The root launcher has no CI-recorded digest, so its digest is stated as a\nLOCAL SELF-SEAL, and SHA256SUMS.txt is described as tamper-evidence on one download rather\nthan as provenance. Header, runtime output and runbook all say SIX OF SEVEN and refuse to\nround up. opum-cli-e2e independently corroborated the same six from the opposite direction\nand honoured the same caveat.\n\nTWO DEAD AUTH EXPORTS REMOVED. The real 0.6.2 run printed `Unknown env config \"_auth-token\"`\nand `\"_authtoken\"` for NPM_CONFIG__AUTH_TOKEN and npm_config__authToken. npm recognised\nneither, so authentication rode ENTIRELY on the temp userconfig while appearing to have two\nfallbacks behind it, and npm says both will become hard errors. One mechanism, now described\nas one.\n\nTOKEN SHAPE CHECK — length, prefix, whitespace flag, never a value. It would have saved two\npublish attempts: `length=24 prefix=OTHER` identifies a stored secret that is not an npm token\nat all, where npm's answer to an unauthorised PUT is 404, which reads exactly like a\npermissions problem and sends the operator to change settings that are fine. The script also\nnames which of the three auth paths it is on. The three auth-check forms the header documents\nas REJECTED — whoami (too strict for a package-scoped granular token), owner ls (vacuous,\nsucceeds with no token), config get (false negative, npm redacts) — are deliberately NOT\nreintroduced as readiness gates.\n\nAN ADVERSARIAL REVIEWER PASS FOUND TEN ISSUES AND ALL WERE ADDRESSED. It is required by\nCLAUDE.md for anything touching scripts/, and it earned its place twice over:\n\n  F1, the most serious, and reproduced before being fixed: a leftover SHA256SUMS.txt\n  silently verified almost nothing while the script printed that it had sealed the launcher.\n  `shasum -c` checks only the lines a manifest contains, so a manifest covering 1 of 7 exits\n  0 — and the tarball most likely to go unchecked that way is the ROOT LAUNCHER, the only\n  tarball the local seal exists for. Reachable with no attacker: the directory is per-version\n  and reused across attempts, and the re-download does not remove a manifest sealed against a\n  partial state.\n\n  F2: `die` inside $( ) exits the SUBSHELL only. With `set -uo pipefail` and no -e the parent\n  carried on with an empty attempt and died again somewhere less useful. The safety argument\n  for this whole script is \"die stops it\", and here it did not.\n\n  F3: the new test joined PATH with \":\" on every platform, which destroys the inherited PATH\n  on windows-latest — a REQUIRED check. The reviewer predicted this before CI confirmed it.\n\n  Also fixed: an absent repository.commit satisfying the cross-report check vacuously (F5);\n  --verify-only widened to need gh, network and a live artifact, when it is the command the\n  propagation-timeout message recommends seconds after the irreversible step (F7); a\n  digest-mismatch remedy that loops if the cause is an attempt mismatch (F4); bash -x tracing\n  the token and a failed mktemp silently falling back to ~/.npmrc (F6); stale header text,\n  decaying line-number citations, \"8 of 7 tarballs\", tag-shaped versions dying late, ssh://\n  remotes, and usage() printing all 85 header lines (F8/F10).\n\nONE REVIEW CLAIM WAS NOT ADOPTED, deliberately. The review reported that an EXIT trap does\nnot fire on SIGINT on bash 3.2. A direct test could not reproduce it — EXIT alone cleaned up\nunder both SIGINT and SIGTERM. INT and TERM are still named on the trap because it costs\nnothing and removes the question, but the comment says that, not a measured leak. A finding\nis a hypothesis until it is reproduced.\n\nTESTS: test/publish-release-script.test.ts, 17 tests driving the REAL script end to end with\ngh/npm/security stubbed — no network, no registry. Verified MUTATION-SENSITIVE rather than\nassumed: hardcoding the attempt, dropping the shape check, reverting path resolution, and\nreverting the F1, F5 and F7 fixes each break the suite. That check earned its keep — the\nfirst cwd mutation did NOT fail the suite, because the `cd` was inside a subshell and so was\nnot the real bug, and the tests were tightened until they caught the actual regression shape.\nThe gh stub now honours -p, which it previously ignored entirely. Full suite 2983 pass / 0\nfail; lint, typecheck and `lore check` (77 files, 0 errors, 0 warnings) clean.\n\nAC#6 was already satisfied before this branch: the classic-automation-token option was\nremoved from the header on 2026-09-14 after the runbook's 9 December 2025 revocation was\nconfirmed.\n\nWHAT THIS TASK CANNOT SETTLE, and no amount of work here will: whether a granular token\nbypasses THIS account's require-2FA-on-write. --dry-run skips both mutating calls, so only\nthe next real publish answers it. That is the remaining unknown behind LCLI-482.\n\nFINDING FOR QUEST-CLI, produced while closing this task: `quest task edit\n--check-acceptance-criteria <n>` fails with \"task edit received invalid arguments\" and never\nnames the offending flag or the expected value shape, across every form tried (space, =,\n0-based, 1-based, comma list, JSON array, criterion text, bool). The same patch through\n`task edit-batch` answers precisely — \"must be a list of 1-based positive integers\" — which\nis what actually unblocked it. This is a live second instance of quest-cli's own finding #2,\nalready fixed on their dev under QCLI-270 but not yet published.","blockers":[],"gates":[],"gateEvents":[]}
