{
  "id": "LCLI-497",
  "aliases": [],
  "title": "Indexed BundleGraph reads reject task-dependency edges, disabling the ladybug backend",
  "status": "Done",
  "description": "LCLI-476 added task->task `dependency` edges to the projection. `readIndexedBundleGraph` (src/core/ladybug-driver.ts) selects authored edges with `WHERE n.kind <> 'task'`, which keeps `dependency` rows, then resolves both endpoints against the CONCEPT record-key map. A task record key is absent from that map, so every read corrupts with 'indexed edge is invalid'.\n\nThe sibling reader (readIndexedTraversalRecords, same file) filters on `edgeSourceKind(record) === 'concept' && edgeTargetKind(record) === 'concept'` before validating, which is why traversal survived and only the BundleGraph path broke -- `lore path`/`impact` stayed healthy while `graph`/`query`/`context` stopped using the index.\n\nMEASURED, not inferred, on this repository 2026-09-14 (dev at 3d36293d, local CLI from the working tree):\n\n  loadRetrievalGraph({ policy: 'indexed' }) -> throws\n  'Ladybug projection verification failed: indexed edge is invalid'\n\n  loadRetrievalGraph({}) -> backend 'reference', with\n  'warning: native indexed retrieval failed; using the in-memory reference backend' on stderr\n\nThis repo's own Quest tracker carries 94 tasks with a non-empty `dependencies` array, so the trigger is ordinary, not exotic -- after LCLI-476 it is the normal shape of a Quest-backed workspace.\n\nTHE DEGRADATION IS WARNED, NOT SILENT. An earlier revision of this description claimed the opposite; see the correction note below for how that got in. The default `auto` policy catches the failure, prints the fallback warning, and returns a correct reference graph. What is wrong is that a shipped performance feature never engages, and that the warning names no cause (LCLI-498).\n\nFound while implementing LCLI-477, which touches the same function.",
  "priority": "high",
  "type": "bug",
  "acceptanceCriteria": [
    {
      "index": 0,
      "text": "readIndexedBundleGraph excludes non-concept-endpoint edges by the same declared predicate the traversal reader uses, not by a kind denylist",
      "checked": true
    },
    {
      "index": 1,
      "text": "loadRetrievalGraph with policy indexed succeeds on a bundle whose tracker carries task dependencies",
      "checked": true
    },
    {
      "index": 2,
      "text": "A regression test fails without the fix and is verified mutation-sensitive against it",
      "checked": true
    },
    {
      "index": 3,
      "text": "The indexed and reference backends are asserted to agree on the edge set for a bundle carrying dependency edges",
      "checked": true
    }
  ],
  "definitionOfDone": [],
  "plan": [],
  "implementationNotes": [
    "CORRECTION, 2026-09-14, before any of this landed: THE FALLBACK IS NOT SILENT. The description above says the failure is invisible. It is not. `loadRetrievalGraph`'s catch path already calls `warnReferenceFallback(..., \"failed\")`, so every affected command prints on stderr:\n\n  warning: native indexed retrieval failed; using the in-memory reference backend\n\nVerified by running `bun run src/cli.ts graph --plain` against this repo with the defect in place; the line is there, first on stderr, on every invocation.\n\nHOW THE WRONG CLAIM GOT IN, because it is the more useful half: retrieval.ts's own docstring says \"Failed indexed warnings and data are discarded\" -- which is true of `indexedWarnings`, the warnings collected DURING the indexed attempt, and says nothing about the fallback notice. I read one sentence about one collector as a statement about the whole path and did not run the auto path before writing it down. The first command I ran in this session did print that warning; I piped it through `tail -5` and scrolled past my own evidence.\n\nWHAT IS STILL TRUE, AND WHY THIS IS STILL A RELEASE BLOCKER: the indexed backend is dead on any Quest-backed workspace whose tracker carries a prerequisite, which after LCLI-476 is the ordinary case rather than an exotic one. This repository: 94 tasks with a non-empty `dependencies` array. A shipped performance feature that never engages is the defect, with or without a warning.\n\nWHAT THE WARNING DOES NOT DO: it names no cause. \"native indexed retrieval failed\" is the same sentence for a corrupt edge, an unreadable generation and a version mismatch, so it cannot be acted on without a debugger. Filed separately as LCLI-498 rather than fixed here, because the obvious repair -- interpolating the cause -- would carry internal vocabulary (\"Ladybug projection verification failed\") into public output, which an existing test in indexed-retrieval.test.ts explicitly forbids. That needs a sanitised diagnosis, not a string concatenation.\n\nFOUND BY ACCIDENT, WORTH RECORDING: nothing alerted anyone. It surfaced because LCLI-477 needed a working indexed path to verify its own indexed/reference fidelity, three hours after LCLI-476 introduced it. A reader who assumes a detector existed will not build one.\n\nTHE DIAGNOSTIC THAT MATTERS: `readIndexedTraversalRecords` filters on a declared endpoint-kind predicate before validating; `readIndexedBundleGraph` filtered on `kind <> 'task'`, a proxy for the same question. Two readers of the same table, one defensive by construction and one not -- so `lore path`/`impact` stayed healthy while `graph`/`query`/`context` stopped using the index. The fix adopts the traversal reader's predicate rather than extending the proxy."
  ],
  "comments": [],
  "labels": [
    "graph",
    "0.7.0"
  ],
  "documentation": [],
  "dependencies": [],
  "references": [
    "LCLI-476 (introduced the dependency edge kind)"
  ],
  "createdAt": "2026-09-14T23:16:56.114Z",
  "updatedAt": "2026-09-14T23:23:24.637Z",
  "finalSummary": "Fixed in PR #75, CI run 34908691071 on head 03f28341 -- 12 checks, all SUCCESS or SKIPPED, none failed.\n\nA CORRECTION TO THIS CITATION, kept rather than silently repaired because it exposes something about the rule itself: this summary first cited run 34908593471 on head c6710918, which was CANCELLED. It was cancelled because pushing this very closing commit superseded it. A run id written inside the PR that closes a task can therefore only ever name a run that predates the citing commit -- the last commit's own run does not exist yet when its message is written. The rule asks for evidence knowable before the merge, and that is satisfied; but \"cite the run id\" has a fixed point nobody can reach, and a session that pushes twice will cite a cancelled run unless it looks. Worth fixing in the process doc rather than rediscovering.\n\n`readIndexedBundleGraph` now filters authored-edge rows by the declared endpoint kinds of each stored record -- `edgeSourceKind(record) === \"concept\" && edgeTargetKind(record) === \"concept\"`, the same predicate `readIndexedTraversalRecords` already used -- instead of by `kind <> 'task'`, a proxy that answered a different question. The query selects `sourceRecordJson` so the predicate has something to read.\n\nEVIDENCE FOR EACH CRITERION:\n\nAC1 -- the predicate is the traversal reader's own, not a denylist. Adding `dependency` to a list of excluded kinds would have fixed this instance and re-armed the trap for the next edge kind whose endpoints are not concepts; asking each record what its endpoints ARE cannot go stale that way.\n\nAC2 -- measured on this repository (94 tasks with non-empty `dependencies`), before and after:\n  before: loadRetrievalGraph({ policy: \"indexed\" }) -> throws \"indexed edge is invalid\"\n  after:  backend \"indexed\", 800 edges, 71 concepts\n\nAC3 -- mutation-tested. Reverting the filter and re-running test/indexed-retrieval.test.ts fails 20 of the conformance cases plus both new ones; restoring it returns 36 pass / 0 fail. The fixture change is what makes that true: TASK-1 now depends on TASK-2, so every existing case carries a task->task dependency edge. The suite could always SEE this defect -- it runs both backends over every affected command and compares -- and simply never produced the trigger.\n\nAC4 -- asserted two ways. \"a task-dependency edge does not corrupt the indexed bundle graph\" compares the indexed and reference edge sets element-by-element and additionally asserts no `dependency` edge is present in either; measured on this repo, the two backends agree on all 800 edges. \"the automatic policy keeps the indexed backend when the tracker carries dependencies\" asserts the BACKEND, which is the only assertion that can fail on a fallback -- a fallback returns correct output, so nothing about output can detect it.\n\nONE CLAIM IN THE ORIGINAL FILING WAS WRONG and is corrected in the record rather than quietly dropped: the fallback is warned, not silent. `auto` prints \"native indexed retrieval failed; using the in-memory reference backend\" on every affected command. The implementation note carries how the error got in -- a docstring sentence about a different collector, and a `tail -5` that scrolled past the warning in my own first command of the session.\n\nCarried forward as LCLI-498: that warning names no cause, and the obvious repair would leak internal vocabulary into public output, which an existing test forbids.\n\nValidation: bun test 3002 pass / 1 skip / 0 fail; typecheck clean; lore check 77 files, 0 errors, 0 warnings.",
  "blockers": [],
  "gates": [],
  "gateEvents": []
}
