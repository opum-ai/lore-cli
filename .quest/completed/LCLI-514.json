{"id":"LCLI-514","aliases":[],"title":"main-is-fast-forward-of-dev checks CONTAINMENT, not fast-forwardness — a rewind of main passes","status":"Done","description":"MEASURED 2026-09-15 in a local scratch repo, with the constructed git state asserted BEFORE\nthe verdict was read. Nothing was pushed to origin.\n\nci.yml:295 `main-is-fast-forward-of-dev` runs on every push to main and asserts exactly one\nthing:\n\n    git merge-base --is-ancestor \"${{ github.sha }}\" origin/dev\n\nThat is CONTAINMENT: \"main's new HEAD is a commit dev already held\". The job is NAMED\n\"main is fast-forward of dev\", which is a strictly stronger property, and the job never\nlooks at where main WAS. `github.event.before` is not read.\n\nTHE HOLE. Rewind main to an older commit that is still on dev. The new HEAD is an ancestor\nof origin/dev, so the guard is GREEN, while commits come OFF main:\n\n    dev tip (c4)        = bbf1c8cefb827b7940aa0d44a06af41a9f6fbeba\n    rewind target (c2)  = 2fbcd7973916f1817e167c11677f348443a0caca\n    main after rewind   = 2fbcd7973916f1817e167c11677f348443a0caca   (asserted, not assumed)\n\n    guard's check   `merge-base --is-ancestor c2 dev`  -> GREEN\n    a real ff check `merge-base --is-ancestor c4 c2`   -> RED (c4 is not an ancestor of c2)\n\nBoth halves of prove-it-rejects and prove-it-accepts are already satisfied by this job: a\ngenuine promotion is green, the merge button is red. The case that finds it is the third\nquestion -- does the NAME claim more than the BODY measures -- which neither half asks.\n\nWHY IT MATTERS HERE SPECIFICALLY. The job's own comment says it is \"the guard that matters\nmost for this repo specifically: main carries zero branch rules here, so nothing else stands\nbetween a bad push and main\". A rewind is precisely the bad push it is the last line against,\nand it is the one shape it cannot see.\n\nTHE CLAIM IS REPEATED OUTSIDE THE BODY, WITHOUT THE CAVEAT. CLAUDE.md's repo profile names\nthe job as \"main is fast-forward of dev\" when explaining why the two promotion guards are\ndeliberately not required contexts. A reader takes the guarantee from the name.\n\nFIX SHAPE. Also assert the OLD main is an ancestor of the new one -- `git merge-base\n--is-ancestor \"$BEFORE\" \"$AFTER\"` -- skipping when before is all-zeros (branch creation).\nKeep the containment check; it catches a different thing (a foreign commit). Two assertions,\ntwo messages.\n\nAND EXTRACT IT. The job is inline shell, so it is untestable by construction and has never\nhad a test; that is why this survived. Extract to scripts/ and drive both branches against\nlocal fixture repos, the way test/readme-readback.test.ts now does for A4 (LCLI-510).\n\nMETHODOLOGY NOTE, from opum-marketplace, and it cost them two false verdicts: zsh reads the\n`:r` in \"$SHA:refs/heads/main\" as a variable modifier and pushes a garbage refspec, and\ndouble quotes do NOT protect it. Use ${SHA}:refs/heads/main or run under bash, and assert the\nresulting git state before trusting any verdict -- otherwise a proof that a gate reported on\nan object it never measured is itself a measurement of an object that was never built.\n\nPROVENANCE. Found by generalising LCLI-510's review: opum-marketplace reported the identical\ndefect in their promotion-guards.yml (OMARK-58) and named this repo's guard as a likely second\nhost. Verified here rather than inherited. opum-cli-e2e independently hit the same class\n(TASK-74): a required check named \"qualification matrix vs the committed baseline\" that has\nnot compared against a baseline since 2026-09-07.","priority":"High","type":"bug","acceptanceCriteria":[{"index":0,"text":"A rewind of main to an older commit that is still on dev makes the guard RED, proved against a constructed local repo whose state is asserted before the verdict is read","checked":true},{"index":1,"text":"A genuine fast-forward promotion is still GREEN, proved separately — the accepting half, not inferred from the rejecting one","checked":true},{"index":2,"text":"A foreign commit on main (the merge-button case) is still RED, so the existing containment check is kept rather than replaced","checked":true},{"index":3,"text":"The assertion lives in scripts/ with a test that drives every branch against local fixture repos, because inline workflow shell is untestable by construction and that is why this survived","checked":true},{"index":4,"text":"The job name and CLAUDE.md's repo-profile reference both describe what the body actually measures, or the body is widened to match the name","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":["SEVERITY RESOLVED FOR THIS REPO, measured 2026-09-15 rather than cited from CLAUDE.md.\n\nopum-marketplace found this same assertion in EIGHT repositories (opum-agent\npromotion-guards.yml:52, opum-doc :73, quest-cli promotion-guardrails.yml:59,\nopum-cli-e2e :59, lore-web ci.yml:207, quest-web check.yml:206, opum-marketplace,\nand here), byte-identical in the six they read, and none of the eight mentions\ngithub.event.before or github.event.forced. They flagged severity as UNEVEN rather\nthan ruling on it, because theirs sits behind a tier-3 rail and is therefore\ndefense-in-depth failing open, and they had not checked anyone else's branch rules.\n\nChecked ours. Three independent reads, all agreeing:\n\n    gh api repos/opum-ai/lore-cli/rules/branches/main        -> []\n    gh api repos/opum-ai/lore-cli/branches/main/protection   -> 404 Branch not protected\n    gh api repos/opum-ai/lore-cli/branches/main --jq .protected -> false\n\n(dev, by contrast, carries exactly one rule: required_status_checks.)\n\nSo main here has NO ruleset, NO classic protection, and force-push is not blocked at\nthe platform level. This guard is not defense-in-depth in lore-cli -- it is the only\nautomated thing that was supposed to notice, and the one shape it cannot see is the\nrewind. That makes this repo the severe instance of the eight, and it is why the job's\nown comment calls it \"the guard that matters most for this repo specifically\".\n\nRecorded so the next reader does not re-derive it, and so nobody downgrades this by\nanalogy to a sibling whose main IS protected. Severity is per-repo here; the assertion\nis identical, the consequence is not.\n\nCROSS-REPO HALF IS NOT OURS. opum-marketplace routed the fleet-wide fix to opum-agent,\nbecause the shape is documented in opum-doc's promote-dev-to-main runbook and patching\ncopies without fixing the source reproduces it at the next repo. LCLI-514 is the lore-cli\nhalf only. Do not edit a sibling's workflow.","CORRECTION TO THE NOTE ABOVE: it is the severe THREE, not the severe one.\n\nThe previous note ended \"that makes this repo the severe instance of the eight\",\ncorrectly bounded at the time -- I had measured lore-cli and opum-marketplace had\nmeasured opum-marketplace, and I said so. The bound was the honest part; the\nsingular was still wrong, and a reader who skipped the caveat would have carried\naway an exclusivity nobody had measured.\n\nopum-agent ran the per-repo survey at 2026-09-15 23:28Z, `rules/branches/main`\nlength and `branches/main.protected`:\n\n    opum-agent        rules=1  protected=true\n    opum-doc          rules=1  protected=true\n    quest-cli         rules=1  protected=true\n    opum-cli-e2e      rules=1  protected=true\n    opum-marketplace  rules=1  protected=true\n    lore-web          rules=0  protected=FALSE\n    quest-web         rules=0  protected=FALSE\n    lore-cli          rules=0  protected=FALSE\n\nTHREE of the eight have an unprotected main. In each of those three this guard is\nthe only automated thing that was supposed to notice, and the shape it cannot see\nis the rewind.\n\nPROVENANCE, because a relayed measurement is still a hypothesis until someone says\nwho resolved it. The lore-cli row is MINE, read first-hand three ways (rules ->\n[], protection -> 404, .protected -> false). The other seven rows are opum-agent's,\nnot re-resolved here. What raises confidence in theirs is that their lore-cli row\nagrees exactly with my independent read, so the method matches on the one row where\nboth exist -- that is evidence about their method, not proof of the other six rows.\nAnyone acting on the lore-web or quest-web rows should re-resolve them.\n\nWHAT DOES NOT CHANGE: this is still a per-repo severity fact, the assertion is still\nidentical across all eight, and lore-cli's exposure is unchanged by having company.","THIRD CORRECTION, and this one invalidates a COLUMN rather than a row.\n\nThe note above reproduced opum-agent's survey as \"the other five carry rules=1,\nprotected=true\", which reads as \"those five are safe from this\". It does not\nsupport that. opum-cli-e2e measured their own ruleset after being reported as\nprotected on the strength of rules=1, and found `required_status_checks` ONLY --\nno `non_fast_forward` rule. opum-marketplace had made the same inference and\ncorrected it.\n\nThe mechanism, which is the part that generalises: a rewind pushes a commit that\nALREADY carries green required contexts, because the earlier promotion is what put\nthem there. So a required-status-checks rule cannot refuse a rewind. The rule that\nrefuses one is `non_fast_forward`, and neither `rules/branches/<b>` length nor the\n`protected` boolean tells you whether it is present.\n\nMeasured here 2026-09-15, since the question is now \"which rule\", not \"how many\":\n\n    gh api repos/opum-ai/lore-cli/rules/branches/dev --jq '[.[].type]'\n        -> [\"required_status_checks\"]\n    gh api repos/opum-ai/lore-cli/rulesets\n        -> exactly one: 22838594 require-ci-on-dev, active, target branch\n    ruleset 22838594 refs -> [\"refs/heads/dev\"], rules -> [\"required_status_checks\"]\n\nSo lore-cli has NO non_fast_forward rule anywhere: not on main, which has no rules\nat all, and not on dev, whose single ruleset carries only required_status_checks.\n\nTWO CONSEQUENCES.\n\n1. lore-cli's own severity is UNCHANGED -- main was already rules=0. Everything in\n   the first note stands. This correction is about the comparison, not about us.\n2. The eight-repo split into \"three exposed, five protected\" is NOT ESTABLISHED.\n   The five were classified by a metric that does not measure the property. They\n   may be protected; nobody has measured `non_fast_forward` on them. Do not cite\n   that split as a reason this is narrow.\n\nThis is the third time tonight a correctly-bounded summary of this survey carried a\nstronger claim than its measurement: severe-one -> severe-three -> and now the\nfive-protected column itself. The recurring error is not sloppiness about caveats,\nit is reaching for a number that is nearby and available (rule COUNT) instead of\nthe property actually in question (which RULE).","HANDOFF, session recycled for context. Implemented and pushed; NOT landed.\n\nSTATE, verified rather than remembered:\n  branch  fix/LCLI-514-fast-forward-guard-measures-containment\n  head    16eee03b45a0c4e9e3b3471a6b926ae27931f4c4\n          (git rev-parse HEAD == git ls-remote origin <branch>; working tree clean)\n  PR      #119, OPEN, base dev\n  CI      run 35024959874 on 16eee03b -- completed/success, zero failed jobs, all\n          five require-ci-on-dev contexts green including windows-latest.\n\nNEXT ACTION IS ONE STEP: land PR #119 (`gh pr merge 119 --squash --delete-branch`).\nIt is green and mergeable. Cite the run id and the PR, NEVER the squash SHA -- the\nsquash commit is a different SHA from the one CI ran on and carries no run of its\nown. Then `quest task complete LCLI-514`.\n\nAC 1-4 CHECKED, each proved by a test that was mutation-checked to fail without the\nfix (matrix in the commit message and PR body). AC 1 is additionally proved outside\nthe suite, in a scratch repo with the constructed git state asserted before the\nverdict was read.\n\nAC 5 DELIBERATELY LEFT OPEN, and this is the one thing a successor must not assume.\nHalf of it is met: the body was widened, so the job name \"main is fast-forward of\ndev\" now describes what it measures. The other half is NOT, and I found a second\ndefect while checking it rather than assuming it --\n\n  CLAUDE.md:163-165 says the two promotion guards \"only run on PRs into `main`\".\n  Measured against ci.yml:\n      promotion is manual         if: pull_request && base_ref == 'main'   TRUE\n      main is fast-forward of dev if: push                                 FALSE\n\n  The CONCLUSION is still right -- neither should be a required context -- but the\n  stated REASON is wrong for one of the two. The fast-forward guard fires FROM the\n  push it would need to gate, so requiring it could only ever block after the fact;\n  that is a different reason from \"it only runs on PRs into main\", and the sentence\n  gives one reason for both jobs.\n\n  This is exactly this evening's pattern -- a stated reason that does not match the\n  measurement -- sitting in our own governing file. NOT fixed here: I was recycled\n  under an explicit \"nothing else is being asked of you\", and editing CLAUDE.md at\n  recycle time is scope I should not take unasked. It is a one-paragraph correction\n  and it belongs either in this task before AC 5 is checked, or in its own.\n\nDO NOT check AC 5 until that paragraph is corrected. A criterion checked on faith\nstops anyone else from looking, which is how the original defect survived.\n\nTWO THINGS OWED OUTWARD, neither blocking:\n  - opum-marketplace is drafting the fleet-wide fix and asked for this working\n    implementation once proved both ways. It is proved; send them\n    scripts/assert-main-fast-forward.sh + test/assert-main-fast-forward.test.ts\n    after #119 lands. One reference beats nine restatements, which is why opum-doc\n    deliberately wrote no implementation of its own (their source fix: PR #296).\n  - Scope rail already agreed with the orchestrator: lore-cli's guard ONLY. Not\n    lore-web's, not quest-web's, not opum-doc's runbook.\n\nSTILL OPEN AND UNSTARTED IN THIS REPO: LCLI-511 (reconcile the README lockstep\nclaim, which clause 3 cannot see by design), LCLI-513. Two standing user rulings\nunchanged: WAIT on any 0.7.1 until LCLI-502/507 land (510 has now landed), and the\nnpm token rotation stays DEFERRED (LCLI-500).","Session write at 2026-09-15T21:26:57Z. Branch fix/LCLI-514-fast-forward-guard-measures-containment, 1 uncommitted file(s). Written by the opum-workflow write hook (0.8.1 (/Users/jdnewhouse/.claude/plugins/cache/opum-fleet/opum-workflow/0.8.1)), not by the model.","HANDOFF MACHINERY GAP, found by verifying the flush instead of assuming it worked.\nNot about LCLI-514's subject; recorded here because this is the live In Progress\nrecord and a successor reads it first.\n\n`opum-handoff write` ran. The cursor was written and stamped correctly:\n\n    .claude/handovers/cursor.md\n    \"Written by the opum-workflow write hook, running from 0.8.1 (...)\"\n    -- which matches this session's own bound plugin root, so no cross-version write.\n\nThe TRACKER NOTE half of the flush wrote nothing, and the reason is structural:\n\n    .claude/settings.json                     DOES NOT EXIST in this repository\n    => OPUM_HOOK_ACTOR / OPUM_HOOK_ACCOUNTABLE_HUMAN are unset\n    => flush-state.sh cannot declare an actor, so it declines to write a note\n\nThe skill documents that failure as swallowed on purpose, so a broken write can\nnever wedge a session. The consequence for lore-cli specifically:\n\n  - The automatic PreCompact and SessionEnd hooks can NEVER write a tracker note\n    here. Not \"did not this time\" -- cannot, until that file exists.\n  - `.claude/handovers/` is gitignored (.gitignore:74), so the cursor is\n    machine-local. It does not survive a fresh clone or a rebuilt workstation.\n  - So the DURABLE half of every unattended handoff in this repo is missing, and\n    the surviving half is the one the skill itself ranks as never authoritative.\n\nNothing was lost this time only because the reasoning note above was written by\nhand with an explicit actor. That is not a mechanism, it is a session remembering.\n\nNOT FIXED HERE, deliberately. Creating .claude/settings.json is a governing-file\nchange that also commits an actor identity into reviewable config, and I was\nrecycled under an explicit \"nothing else is being asked of you\". It is the repo\nowner's call, it is small, and it should be its own task rather than a rider on\nthis one.\n\nWORTH CHECKING FLEET-WIDE before anyone concludes it is ours alone: the same two\nenv vars are required in every Opum repository, and the same silent-degrade\napplies. A sibling with no .claude/settings.json has the identical gap and the\nidentical absence of any signal about it.","AC 5 closed 2026-09-15 by the restarted session (6b2d22b6): CLAUDE.md:163-174 now gives each guard its own trigger and reason — promotion-is-manual on PRs into main, main-is-fast-forward-of-dev on the push to main (skipped on PRs, paths-ignore for docs-only pushes) — and names the two assertions. Also ran the ODOC-211 mutant relayed by the orchestrator: this script has no refspec, so the nearest equivalents were (A) delete the workflow's 'git fetch origin dev' line, which SURVIVED the suite with 0 red, and (B) delete the script's resolve of DEV_REF, which died on the unresolvable-ref test with exit 1 instead of 2. One wiring assertion added for (A); re-run kills it. Clean suite 7 pass / 25 expects, biome and tsc clean, lore check 0 errors. Not landed yet: pushed for CI on the new head."],"comments":[],"labels":[],"documentation":[],"dependencies":[],"createdAt":"2026-09-15T20:50:18.300Z","updatedAt":"2026-09-15T21:43:14.989Z","finalSummary":"Delivered in PR #119 (squash 6e91ad78 on dev), gating run 35026408941 on head 3d313da6: all five require-ci-on-dev contexts success, the two by-design guards skipped. The guard main-is-fast-forward-of-dev now makes two assertions from scripts/assert-main-fast-forward.sh — fast-forward (old main is an ancestor of new main, reading github.event.before, which the old inline job never read) and containment (new main is an ancestor of origin/dev) — with exit 2 for an unresolvable ref instead of a silent pass. test/assert-main-fast-forward.test.ts drives every branch against local fixture repos and pins the ci.yml wiring, including the explicit dev fetch, which a mutation check showed was otherwise unpinned. Each of the five acceptance criteria is held by a test mutation-checked to fail without the fix; AC 1 was additionally proved in a scratch repo with the git state asserted before the verdict was read. AC 5 closed by correcting CLAUDE.md's repo profile, which gave one reason for two guard jobs where only one was true. Fleet-wide fix is opum-agent/opum-marketplace's (OMARK-58); this script and test were handed to opum-marketplace as the working reference.","blockers":[],"gates":[],"gateEvents":[]}
