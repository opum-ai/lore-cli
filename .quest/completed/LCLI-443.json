{"id":"LCLI-443","aliases":[],"title":"Opt .claude/skills/lore/SKILL.md distribution into the opum-lore marketplace plugin","status":"Done","description":"Remaining upstream slice split from LCLI-442 (its finalSummary explicitly scopes this out: lore init/agents still write .claude/skills/lore/SKILL.md per repo rather than pointing at the opum-lore marketplace plugin, LCLI-441). Adds an explicit opt-in, [agents].skill_source in .lore/config.toml (repo|plugin, default repo unchanged), and a lore init --skill-source <repo|plugin> flag to persist it. Under skill_source=plugin, a bare lore agents/lore agents --check stops writing or proposing .claude/skills/lore/SKILL.md and instead flags a leftover one as drift (orphaned, exit 6 under --check), removable only via --force and only when its bytes exactly match lore's own generated content. An explicit scoped lore init --claude/--agents request always still materializes the file regardless of this config (explicit beats config).","acceptanceCriteria":[{"index":0,"text":"lore init --skill-source <repo|plugin> persists [agents].skill_source in .lore/config.toml without prompting, rejecting an unsupported value","checked":true},{"index":1,"text":"with no skill_source configured (or skill_source=repo), lore agents/lore init behavior is byte-for-byte unchanged from before this task","checked":true},{"index":2,"text":"under skill_source=plugin, a bare lore agents never creates SKILL.md, and lore agents --check on a leftover SKILL.md reports it orphaned and exits 6 naming the file and the remedy","checked":true},{"index":3,"text":"lore agents --force removes an orphaned SKILL.md only when its bytes exactly match the generated doc; a hand-edited/differing leftover is left in place even under --force","checked":true},{"index":4,"text":"an explicit lore init --claude/--agents request still materializes SKILL.md under skill_source=plugin (explicit beats config)","checked":true},{"index":5,"text":"CHANGELOG.md documents both the new config key and the new drift/removal behavior","checked":true},{"index":6,"text":"lore check exits 0","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["cutover","claude-code"],"documentation":[],"dependencies":[],"createdAt":"2026-09-05T12:59:15.880Z","updatedAt":"2026-09-05T13:06:33.929Z","finalSummary":"Added [agents].skill_source (\"repo\"|\"plugin\", default \"repo\") to .lore/config.toml and lore init --skill-source <repo|plugin> to persist it non-interactively (src/config.ts, src/commands/init.ts). Under skill_source=plugin, planSkill (src/core/agent-bridge.ts) stops planning SKILL.md creation/update and instead reports a leftover as orphaned (exit 6 under lore agents --check, naming the file and remedy) or removed (only when --force is given on a non-check run AND the file's bytes exactly match lore's own generated content -- a hand-edited/differing leftover stays orphaned even under --force). A scoped, explicit lore init --claude/--agents request always still materializes SKILL.md regardless of this config (applyAgentsBridge's includeCodex:false path never reads it), mirroring the existing includeCodex/hasCodexBridge gating. New tests cover: unchanged default (skillSource absent/repo), the fresh-repo/leftover/--check/--force/--check+--force matrix, trailer and color rendering for orphaned/removed, config parse+validation for [agents].skill_source (valid and rejected values), and lore init --skill-source persistence/validation plus an end-to-end explicit-beats-config proof through lore init --claude. CHANGELOG.md [Unreleased] documents both the config key and the drift/removal behavior. Full suite 2838 pass/1 skip/0 fail, typecheck clean, lint clean (one pre-existing unrelated warning on dev, unchanged), lore check 77 files/0 errors. Landed via PR #579 (all CI green including the 3 required checks: docker e2e harness, windows-latest, operating block digest), now on dev at 88e1fdb. Filed as LCLI-443 since LCLI-442 was already Done and its own finalSummary explicitly scoped this slice out.","blockers":[],"gates":[],"gateEvents":[]}
