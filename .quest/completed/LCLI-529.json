{"id":"LCLI-529","aliases":[],"title":"opum-cli-e2e reports lore refusing quest's declared pausedStatus; it does not reproduce on either pair here","status":"Done","description":"REPORTED by opum-cli-e2e 2026-09-17 (their TASK-70) as a reproducible defect: \"lore sync/lore check refuse quest's own declared pausedStatus, and lore 0.7.0 is green only because quest renamed to a string lore happens to allow.\" Their report is well-formed and carries a real exit-6 transcript.\n\nIT DOES NOT REPRODUCE HERE, ON EITHER PAIR. Filed open rather than closed, because a defect someone measured and I could not is a difference between two environments, not an absence.\n\nWHAT I MEASURED, all on throwaway git dirs, binaries resolved by path:\n 1. quest 0.6.2 DOES declare the paused status: `task status-flow --json` -> pausedStatus \"Blocked\". Run from the exact tree opum-cli-e2e named, /Volumes/external/.opum-candidates/0.6.2-full-pair.\n 2. quest 0.7.1 declares pausedStatus \"Paused\".\n 3. Lore reads the DECLARED value rather than a literal: src/commands/reconcile-shared.ts:120 calls adapter.pausedStatus?.() and passes it to reconcileStatus (line ~210); src/adapters/quest.ts:397 reads it off `task status-flow`. Present IDENTICALLY in the v0.6.2 and v0.7.0 tags -- 8 refs in quest.ts and 5 in reconcile-shared.ts in both.\n 4. quest 0.6.2 + lore 0.7.0, story linked, task paused to \"Blocked\": `lore sync --json` EXIT 0.\n 5. quest 0.6.2 + lore 0.6.2 -- their exact registry-installed pair, both from the path they named -- same fixture: `lore sync --json` EXIT 0.\nIn 4 and 5 the link was confirmed exit 0 and the task confirmed at status \"Blocked\" before sync ran.\n\nSO THE PROPOSED FIX SHAPE IS ALREADY IMPLEMENTED. Their acceptance example was \"reconcile reads quest task status-flow and admits the declared pausedStatus, so the allowed set comes from the tracker rather than a literal on lore's side\". That is what the code does, and it is why 4 passes. On this evidence the current pair's agreement is NOT a coincidence of two independent choices.\n\nTHE STRONGEST LEAD, AND IT POINTS AT WHICH BINARY RAN. Their quoted error carries the BACKLOG-flavoured hint: \"the task's status must match one of `backlog/config.yml`'s `statuses:` exactly\". Backend-conditional hints landed in LCLI-503: the v0.6.2 tag has ZERO refs to statusFlowHints, and 0.7.0/dev supplies QUEST_STATUS_FLOW_HINTS (src/adapters/quest.ts:392). A quest-backed project on a post-LCLI-503 lore should NOT be told to edit backlog/config.yml. So a \"byte-identical message\" on lore 0.7.0 is the signature of a PRE-LCLI-503 lore, which is the \"verify the binary actually running\" shape: their 0.7.0 fixture may have resolved to a 0.6.2 lore via PATH or a node_modules/.bin shadow.\n\nSTATED AS A LEAD, NOT A VERDICT, because two things stop me concluding it:\n - I could not reproduce even the 0.6.2+0.6.2 failure, which their report measures directly and which the lead does not explain.\n - I could NOT inspect the shipped bundle: @opum-ai/lore/bin/lore.cjs is a launcher shim (0 refs to `backlog/config.yml`, which certainly exists in the product), so the real code is the platform binary and a grep of the .cjs proves nothing either way. I nearly drew a conclusion from that zero before noticing.\n\nONE CONCRETE DIFFERENCE IN THE PASTED REPRO, worth resolving first: as written it omits the actor context, and `lore link` then exits 6 with quest.actor-context-required (this is LCLI-495's reported case). I had to export LORE_QUEST_ACTOR / LORE_QUEST_ACTOR_KIND to get a link at all. Their harness sets those at process level, so their real fixture differs from the pasted minimal repro in at least that way -- and since reconcile only runs for LINKED tasks, an unlinked fixture would pass rather than fail. So the pasted commands are not the thing that produced their transcript.\n\nWHAT WOULD CLOSE THIS, asked of opum-cli-e2e: from inside the failing fixture, the resolved binary path (`node -e 'console.log(require(\"fs\").realpathSync(process.execPath))'` is not it -- what is wanted is which `lore` ran) and that binary's own `--version`; whether a `backlog/` directory or a `[reconcile]` section existed; and the full `quest task status-flow --json` output from that same fixture. If their lore turns out to be pre-LCLI-503, this closes as already-fixed and their suite rows should pin the version. If it is genuinely 0.7.0, the difference is environmental and worth finding, because then I have a passing case and they have a failing one on the same declared inputs.\n\nNOT GATING ANYTHING: their CI runs the 0.7.0 pair and is green; this repository runs quest 0.7.1 + lore 0.7.0 with nothing parked outside the three-status flow.\n\nSEPARATE AND VALID REGARDLESS OF THE ABOVE -- their own closing point, which they are fixing on their side: reconcile rows that pass for ANY value lore happens to allow do not establish what they are read as establishing. The lore-side equivalent is worth checking: is there a test that a NON-DEFAULT declared pausedStatus is admitted? If every test uses whatever quest currently declares, the suite proves lore tolerates today's string, not that it reads the declaration.","priority":"medium","acceptanceCriteria":[],"definitionOfDone":[],"plan":[],"implementationNotes":["RESOLVED 2026-09-17 by opum-cli-e2e, and it corrects BOTH sides. The cause was a PATH-shadowed quest, and the one piece that looked like a live 0.7.0 defect turns out to be already fixed on dev.\n\nTHEIR DECISIVE MEASUREMENT. Same fixture, same lore binary, same task parked at \"Blocked\"; only PATH changed:\n  lore sync --json                                  -> exit 6 (ambient quest 0.7.1 first on PATH)\n  PATH=\"<0.6.2-pair>/node_modules/.bin:$PATH\" ...   -> exit 0\nTheir ambient quest is 0.7.1, which declares pausedStatus \"Paused\". The fixture's records were written by quest 0.6.2, which declares \"Blocked\". So adapter.pausedStatus?.() asked 0.7.1, got \"Paused\", saw a record at \"Blocked\", and correctly refused. Lore did exactly what it says it does; it was handed the wrong quest.\n\nSO THE HARDCODE HALF OF THIS REPORT IS WITHDRAWN BY ITS AUTHOR and is not a lore defect. reconcile-shared.ts:120 -> adapter.pausedStatus?.() -> quest.ts:397 is correct, and correct in both tags. That also explains why it would not reproduce here: this workstation's ambient quest matches its fixtures, theirs cannot, because quest 0.7.1 is installed globally for their own tracker.\n\nMY OWN LEAD WAS RIGHT ABOUT THE MECHANISM AND WRONG ABOUT THE OBJECT, which is worth recording because it is the same error this task is otherwise about. I said the backlog-flavoured hint is the signature of a pre-LCLI-503 lore, and therefore their \"0.7.0\" might not really be 0.7.0. The first half was right; the inference was wrong. Their binary genuinely IS 0.7.0 (resolved path and its own package.json both checked by them). The truth is that 0.7.0 ITSELF is pre-LCLI-503: measured here, `git show v0.7.0:src/core/reconcile.ts` has ZERO refs to QUEST_STATUS_FLOW_HINTS and v0.7.0's quest adapter has zero refs to statusFlowHints, while dev has both. I reached for \"the binary is not what you think\" when the available explanation was \"the release is older than the fix\". Two objects again -- the version STRING was never in doubt; what the version CONTAINS was.\n\nSO THE HINT DEFECT IS FIXED ON DEV AND UNRELEASED, not open. LCLI-503 is Done and landed after the v0.7.0 tag. Reproduced here first-hand before concluding: with a 0.6.2-written record and ambient quest 0.7.1, installed lore 0.7.0 exits 6 and emits \"the task's status must match one of `backlog/config.yml`'s `statuses:` exactly\" on a quest-backed project with no backlog/ directory at all. That is correct behaviour for 0.7.0 and will stop happening at the next release. Nothing to fix; worth a release-note line, since consumers qualifying the 0.7.0 pair will keep seeing it.\n\nWHAT SURVIVES AND IS GENUINELY OURS: a missing capability, filed separately -- there is no way to tell lore which quest binary to use, and nothing in lore's output names the tracker binary it talked to. That is what made this mismatch undetectable from the outside and cost a cross-repo round trip. See the follow-up task.\n\nWHAT THEY ARE FIXING ON THEIR SIDE, recorded because it is the more consequential half and it is theirs: their harness takes explicit --quest and --lore paths but does not govern what lore invokes internally, so every lore->quest row has been qualifying lore against the ambient quest rather than the candidate. A green result about the wrong object, in the repository whose purpose is catching those.\n\nDISPOSITION: this task stays only as the record of the resolution. Nothing here needs code."],"comments":[],"labels":["tracker","reconcile"],"documentation":[],"dependencies":[],"createdAt":"2026-09-17T02:15:29.070Z","updatedAt":"2026-09-17T18:45:30.985Z","finalSummary":"RESOLVED-AS-NON-DEFECT 2026-09-17, jointly with opum-cli-e2e. Root cause: their harness's ambient quest 0.7.1 shadowed the intended quest 0.6.2 binary in the failing fixture (PATH-shadowing) -- pausedStatus \"Paused\" from the wrong quest was compared against a record quest 0.6.2 had written as \"Blocked\", and lore correctly refused. reconcile-shared.ts:120 -> quest.ts:397 (adapter.pausedStatus?.()) is correct in both v0.6.2 and v0.7.0 tags; no lore code changed. A secondary lead (backlog-flavored hint suggesting a pre-LCLI-503 lore) was half-right: the mechanism was correct but the object was wrong -- lore 0.7.0 genuinely IS pre-LCLI-503 (git show v0.7.0:src/core/reconcile.ts has zero refs to QUEST_STATUS_FLOW_HINTS), so the stale hint is expected 0.7.0 behavior, already fixed on dev, unreleased. Surviving actionable item -- lore has no way to pin or report which quest binary it consulted -- filed separately as LCLI-530, which now also carries the release-note follow-up. Full detail in this task's implementationNotes.","blockers":[],"gates":[],"gateEvents":[]}
